# Risk Register

Status snapshot: 2026-07-12.

This register captures the risks that should drive the next roadmap decisions. Severity is based on
impact to editor correctness, user trust, or ability to enter Phase 2 safely.

| ID | Risk | Severity | Current signal | Mitigation / next action | Owner surface |
|---|---|---:|---|---|---|
| R1 | M5 acceptance evidence can drift after acceptance | Low | `docs/m5-checklist.md` passes after PR #33 | Keep README, `agent.md`, and planning docs synchronized with checklist evidence; reopen only on a real regression | Docs, PerformanceTests |
| R2 | Visible-range highlighting regresses after PR #20 | Medium | PR #20 measured Markdown 17.918 ms max and MDX 22.670 ms max and closed #14 | Keep PerformanceTests and scheduling regression coverage in place before Phase 2 folding work | EditorKit, PerformanceTests |
| R3 | Memory gate scope is misunderstood after PR #21/#22 | Medium | PR #21 records 149.8 MB host RSS; helper RSS is diagnostic | Keep M5 scoped to host RSS; create a separate system-footprint budget only if needed | App, PreviewKit, PerformanceTests |
| R4 | MDX sanitizer schema can regress toward active/spoofing HTML | Medium | Mitigated by PR #24/#27 | Keep malicious snapshot coverage; require a new Decision Log entry before allowing user-authored inline CSS or SVG | preview-src |
| R5 | Local asset and image import paths can read large or active files into memory | Medium | Mitigated by PR #24 with bounded raster-only policy | Keep size/type/path rejection tests; require a separate design before SVG or larger managed assets | PreviewKit, WorkspaceKit |
| R6 | Settings/themes can regress after manual validation | Medium | PR #26 landed settings; PR #30 manually confirmed workflows | Keep Settings/theme checks as regression evidence; keep custom theme JSON and user CSS deferred | App, EditorKit, PreviewKit |
| R7 | Preview render cost can grow with large DOM/code/Mermaid content | Medium | Preview still rewrites assets, highlights code, renders Mermaid, and scans line anchors | Cache code highlighting/Mermaid by source hash; measure code-heavy and Mermaid-heavy fixtures | preview-src, PerformanceTests |
| R8 | Documentation drift causes Codex/agents to work from stale milestone assumptions | Medium | Several milestone PRs needed follow-up synchronization | Keep README, `agent.md`, `docs/m5-plan.md`, and this file synchronized per PR | Docs |
| R9 | Actual macOS IME (Zhuyin + Pinyin) event-stream coverage can regress | Medium | Actual Zhuyin and Pinyin pass at heading, bold, italic, and inline-code boundaries. The 2026-07-06 owner run passed both input methods at inline-link boundaries (84.114 seconds, 0 failures); the 2026-07-12 owner run passed ready-thumbnail and loading-placeholder composition immediately before/after an image for Traditional Pinyin and Zhuyin (10 tests, 0 failures, 106.508 seconds). The harness selects only composition-capable input methods, never same-named keyboard layouts. | Keep the opt-in `WYSIWYGActualIMEEventGateTests` harness, including link- and image-boundary scenarios, runnable and rerun it before stable/default promotion or whenever the fold/image presentation changes; re-run once if the IME server is cold. | EditorKit, manual QA |
| R10 | Native arrow, reverse shift-selection, and mouse click-to-caret behavior can strand users near hidden delimiters or folded link destinations | Medium | Delimiter edge-snapping keeps collapsed carets out of hidden marker interiors while raw selections remain unchanged. Link gates L3/L6 extend this to the complete hidden `](url)` run, including arbitrarily long destinations, and keep keyboard/pointer selections on raw UTF-16 ranges. | Keep `WYSIWYGEdgeSnappingGateTests`, `WYSIWYGLinkNativeGateTests` L3, and `WYSIWYGLinkNativePointerGateTests` L6 as regression coverage; rerun them if the fold mechanism or link chrome changes. Stable/default promotion remains gated by §D.4. | EditorKit |
| R11 | Partial folded-span copy policy is misunderstood | Medium | Attribute-only presentation copies the exact raw selection, not synthesized rendered text. Link gate L4 also proves whole-link, visible-text-only, and partial hidden-URL selections plus normal source mutation when pasting into folded/revealed links. | Keep the exact-raw policy documented and covered by native interaction tests plus `WYSIWYGLinkNativeGateTests` L4. | EditorKit, docs |
| R12 | WYSIWYG is promoted to stable/default before explicit approval | High | Native input/selection and §D integration gates are complete. Inline-link L1-L9 and image-thumbnail I0-I10 are green. Experimental WYSIWYG uses `.inlineFoldRevealWithLinkFolding` behind the off-by-default kill switch and supplies folder-scoped thumbnails only under those same gates; reference links, autolinks, ineligible/single-file images, and other deferred constructs remain raw/deferred. | Keep WYSIWYG Experimental/off by default until §D.4 is explicitly checked with a Decision Log entry. Do not promote it to stable/default without that approval, or expand remaining deferred constructs without their own gate pass. | App, EditorKit, docs |
| R13 | CI misses TypeScript type errors | Low | PR #22 added explicit preview typecheck | Keep `npm test` and `npm run typecheck` separate in CI | CI, preview-src |
| R14 (closed 2026-07-05) | Public alpha starts without release hardening | Closed | The P5 alpha checklist is fully checked: P0 decisions + MIT LICENSE landed, `PLAINSONG_UNSIGNED=1 make release` reproduced end-to-end (build 56 DMG), the DMG was owner-verified on a second Mac via the documented Gatekeeper bypass (workspace/edit/save/offline preview, WYSIWYG off by default), and all §12 budgets passed on the Release configuration (perf-log 2026-07-05). Accepted residual: unsigned launch friction until P1/P2 resume | Keep the unsigned path clearly labeled; buy Apple Developer Program membership and resume P1/P2 before beta/wider distribution | Release/docs |
| R15 | Hosted CI runner variance can fail wall-clock perf budgets despite local M5 evidence | Medium | Shared-runner scheduling spikes hit a different budget each run (preview render 133.9 ms on PR #53's first run, a single typing-pair 16.87 ms sample on PR #59); xcodebuild does not forward the shell env, so CI detection needed the PR #53 `TEST_RUNNER_CI` Makefile forwarding | All wall-clock budgets (typing, highlight, preview render, file open) assert through `assertPerformanceBudget(isInformationalOnCI: true)`: hard locally, informational prints on CI. Memory RSS stays hard-asserted (stable, 2.6x headroom). Local/result-bundle evidence remains the only perf acceptance evidence | PerformanceTests, docs |
| R16 | Helper-inclusive WebKit memory can exceed the M5 host RSS budget | Medium | Helper aggregate RSS is diagnostic, not an M5 gate | Open a separate system-footprint budget only if Activity Monitor-style memory becomes a release requirement | PerformanceTests, PreviewKit |
| R17 | Restoring the native adjustable sidebar can reintroduce launch instability | Medium | PR #30 replaced `NavigationSplitView` after AppKit constraint-loop crash | Keep fixed-width `HStack` during Phase 2; restore adjustable sidebar only as post-M5 polish | App |
| R18 (closed 2026-06-26) | The `baselineOffset(-1000)` zero-width fold mechanism distorts multi-line layout/viewport | Closed | The baseline-offset/tiny-font hiding path was removed and replaced by a TextKit 2 content-storage paragraph projection behind WYSIWYG presentation. Checklist §A invariants are green, B1-B13 reran against the replacement mechanism, and `WYSIWYGNativePointerGateTests.testFoldedLineGeometryMatchesUnfoldedLineAndKeepsSiblingLinesInViewport` proves folded-line geometry no longer inflates to ~1013 pt or displaces sibling lines. | Keep B13 plus the IME/selection/pointer/accessibility/performance gates as regression coverage. WYSIWYG remains off by default behind the Experimental kill switch until the stable-promotion gate is explicitly approved. | EditorKit |

## Immediate risk burn-down order

1. Keep WYSIWYG Experimental/off by default; stable/default promotion remains blocked by the explicit promotion checkbox and requires a Decision Log entry.
2. Keep the opt-in actual IME (Zhuyin + Pinyin) harness, including the completed link- and image-boundary scenarios, and the pointer/selection/B13 gates runnable before any WYSIWYG promotion.
3. Keep partial folded-span copy/paste policy, including partial hidden-URL selections, documented and covered by EditorKit tests.
4. Keep inline-link folding and eligible folder-workspace image thumbnails confined to off-by-default Experimental WYSIWYG; keep reference links, autolinks, ineligible/single-file images, and other deferred constructs raw until their own native gates pass.
5. Keep R17 as post-M5 polish; do not fold adjustable/native sidebar work into WYSIWYG gates.
6. Keep release hardening separate from Phase 2 feature work.
