# Phase 3 editor-find F2 retained evidence

This compact pack retains every raw text record and a cryptographic inventory. It omits large build and xcresult bundles; compact-only audit is therefore PARTIAL/OPEN. Full PASS additionally requires the owner-local, read-only artifact root documented in docs/perf-log.md. Loss of that root invalidates the full baseline and requires six fresh runs. The full root is not replicated off this Mac, so the overall F2 retention gate remains open.
