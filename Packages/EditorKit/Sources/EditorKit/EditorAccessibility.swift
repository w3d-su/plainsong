/// Stable accessibility identifiers exported by EditorKit for app-level focus routing and UI
/// automation. These identifiers expose native state; they do not mutate editor behavior.
public enum EditorAccessibility {
    public static let textViewIdentifier = "plainsong.editor.textView"
}
