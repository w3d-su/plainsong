# agent.md — Plainsong (Native macOS Markdown/MDX Editor)

> **Read this file fully before writing any code.** It is the single source of truth for
> architecture, conventions, and roadmap. If you (an LLM agent or human) make a decision
> that contradicts or extends this document, update the **Decision Log** at the bottom in
> the same commit.

---

## 1. Project Overview

**Plainsong** is a native macOS Markdown editor written in Swift, in the spirit of Typora,
optimized for blog authoring workflows (`.md` and `.mdx` files, YAML frontmatter, folder-based
projects such as Astro/Next.js content directories).

### Goals

- Native macOS app (Swift + SwiftUI shell + AppKit text engine). No Electron, no Catalyst.
- First-class editing of `.md`, `.markdown`, `.mdx` files.
- Live preview rendered side-by-side with synchronized scrolling (Phase 1), evolving into
  Typora-style in-place WYSIWYG rendering (Phase 2, see §13).
- Syntax highlighting in the editor (markdown structure + embedded code blocks + JSX in MDX).
- Context-aware autocompletion (snippets, link paths, fence languages, frontmatter keys).
- Open a single file *or* a folder workspace with a file-tree sidebar.
- Fast: instant typing latency on documents up to ~1 MB; preview updates < 150 ms debounced.

### Non-Goals (for now)

- iOS/iPadOS version (keep core packages platform-portable, but do not build UI for it).
- Cloud sync, accounts, collaboration.
- Full MDX component *execution* with user project bundling (placeholder rendering instead; see §9).
- Plugin system for third parties.

### Locked Product Decisions

| Decision | Choice |
|---|---|
| Editing model | Phase 1: two-pane source + live preview. Phase 2: Typora-style WYSIWYG. |
| File handling | Both single-file open and folder workspace with sidebar. |
| Preview technology | Native AppKit/TextKit 2 editor + `WKWebView` preview running a local JS pipeline. |
| Documentation language | English (this file). |
| Min deployment target | macOS 14 (Sonoma). |
| UI framework | SwiftUI app shell; AppKit (`NSViewRepresentable`) for the text editor. |

---

## 2. Tech Stack

| Layer | Technology | Notes |
|---|---|---|
| Language | Swift 5.10+ (enable strict concurrency; migrate to Swift 6 language mode when all deps allow) | |
| UI shell | SwiftUI (windows, sidebar, settings, toolbars) | |
| Text editor | [STTextView](https://github.com/krzyzanowskim/STTextView) (TextKit 2, AppKit) wrapped in `NSViewRepresentable` | Actively maintained; provides line numbers, plugins, completion window. Keep it behind our own `MarkdownEditorView` abstraction so it can be swapped for raw `NSTextView`+TextKit 2 if needed. |
| Syntax highlighting | [Neon](https://github.com/ChimeHQ/Neon) + [SwiftTreeSitter](https://github.com/tree-sitter/swift-tree-sitter) | Incremental, async highlighting. |
| Grammars | `tree-sitter-markdown` (block) + `tree-sitter-markdown-inline`, with injections: yaml (frontmatter), html, tsx (MDX/JSX regions), plus common fence languages (swift, js, ts, tsx, python, bash, json, css, html, yaml, go, rust) | Grammars vendored as SPM targets (each grammar repo ships an SPM package, or we wrap the C source). |
| Structure parsing (outline, etc.) | Reuse the tree-sitter syntax tree. Optionally [swift-markdown](https://github.com/swiftlang/swift-markdown) for export tasks. | Avoid two parsers driving the same UI. |
| Preview | `WKWebView` + local JS bundle built with **esbuild** from `preview-src/` | No remote network access by default (strict CSP). |
| Preview JS pipeline | unified: `remark-parse`, `remark-gfm`, `remark-frontmatter`, `remark-math`, `remark-mdx` (mdx only) → `rehype`, `rehype-katex`, `highlight.js` for fences; `mermaid` for ```mermaid fences; `morphdom` for incremental DOM patching | unified/remark is the standard 2026 pipeline and the only realistic path to MDX support. |
| Persistence of prefs | `UserDefaults` + built-in theme IDs; custom theme/user CSS imports deferred | |
| Project generation | **XcodeGen** (`project.yml` committed; `.xcodeproj` is generated, never hand-edited) | pbxproj diffs are hostile to LLM collaboration. |
| Lint/format | SwiftFormat + SwiftLint (configs committed) | |
| JS tooling | Node 20+, npm, esbuild, vitest (preview pipeline unit tests) | |

**Dependency policy:** keep the dependency list short. Adding any new Swift package or npm
package requires a Decision Log entry explaining why.

---

## 3. Repository Layout

```
blogeditor/
├── agent.md                  # ← this file
├── project.yml               # XcodeGen manifest (source of truth for the Xcode project)
├── Makefile                  # bootstrap / generate / build / test / preview-bundle
├── App/                      # Thin app target (SwiftUI)
│   ├── PlainsongApp.swift   # @main, WindowGroup, Settings scene
│   ├── AppState.swift        # open workspaces, recent items
│   ├── Views/                # SwiftUI views: WorkspaceWindow, Sidebar, EditorSplit, StatusBar, FrontmatterPanel
│   └── Resources/            # Assets, preview dist bundle (generated), themes
├── Packages/
│   ├── MarkdownCore/         # Pure logic: document model, markdown utilities, completion engine, scroll-sync mapping. No AppKit/SwiftUI imports. Testable via `swift test`.
│   ├── EditorKit/            # STTextView wrapper, Neon/tree-sitter setup, editing behaviors (AppKit)
│   ├── PreviewKit/           # WKWebView controller, JS bridge, asset URL scheme handler
│   └── WorkspaceKit/         # File tree model, FS watching, security-scoped bookmarks, atomic save
├── preview-src/              # JS/TS source for the preview pipeline (npm workspace)
│   ├── package.json
│   ├── src/index.ts          # bridge protocol impl, render(), morphdom patching, scroll sync
│   ├── src/pipeline.ts       # unified pipeline (md and mdx variants)
│   ├── src/styles/           # preview CSS themes (github-light/dark, etc.)
│   └── test/                 # vitest specs
└── .github/workflows/ci.yml  # macOS runner: swiftformat --lint, swiftlint, swift test, xcodebuild test, npm test
```

Build artifacts: `preview-src` builds to `App/Resources/preview/` (`index.html`, `bundle.js`,
`bundle.css`). The dist output **is committed** so Swift-only agents can build the app without
Node installed; regenerate with `make preview-bundle` whenever `preview-src/` changes.

---

## 4. Architecture

```
┌────────────────────────── SwiftUI App Shell ──────────────────────────┐
│  WorkspaceWindow                                                      │
│  ┌──────────┐  ┌─────────────────────────┐  ┌──────────────────────┐  │
│  │ Sidebar  │  │  EditorPane (AppKit)    │  │  PreviewPane         │  │
│  │ FileTree │  │  STTextView + Neon      │  │  WKWebView + bridge  │  │
│  └────┬─────┘  └───────────┬─────────────┘  └──────────┬───────────┘  │
│       │                    │                           │              │
│  WorkspaceKit         EditorKit                   PreviewKit          │
│       └────────────┬───────┴───────────────┬──────────┘               │
│                    ▼                       ▼                          │
│              MarkdownCore (document model, completion, sync map)      │
└───────────────────────────────────────────────────────────────────────┘
```

### Data flow (Phase 1)

1. `DocumentSession` (MarkdownCore) owns the canonical text (`String` + version counter)
   for one open file.
2. Editor edits → `DocumentSession.apply(edit)` → publishes change (Combine/AsyncStream).
3. Neon receives incremental edit ranges → tree-sitter re-parse → highlight attributes
   applied asynchronously to visible range.
4. A debounced (150 ms) subscriber sends `{text, version, fileKind, baseURL}` to the
   preview via the JS bridge. JS renders HTML, patches DOM with morphdom, reports back
   `renderComplete(version)`.
5. Scroll sync uses line ↔ DOM node mapping (§8).
6. Autosave: debounced 1 s after last edit + on window resign; atomic write via
   `Data.write(.atomic)`; file watcher suppresses self-triggered events by comparing
   content hash.

### Key types (initial sketch — keep names)

- `DocumentSession` — open file: text, version, undo coordination, dirty state, encoding.
- `Workspace` — root folder URL, file tree snapshot, watcher, bookmark data.
- `EditorController` (EditorKit) — owns STTextView, applies behaviors, exposes
  `onTextChange`, `onSelectionChange`, `visibleLineRange`.
- `PreviewController` (PreviewKit) — owns WKWebView, queues render requests, drops stale
  versions, handles bridge messages.
- `CompletionEngine` (MarkdownCore) — pure function: `(text, cursorOffset, context) -> [Completion]`.
- `SyncMap` (MarkdownCore) — source line → preview anchor id mapping helpers.

### Concurrency rules

- UI mutation on `@MainActor` only.
- tree-sitter parsing and highlight query execution off main thread (Neon handles this; do
  not block its callbacks).
- Preview render requests are coalesced: only the latest version may be in flight; stale
  `renderComplete` responses are ignored.
- No `DispatchQueue.global` ad-hoc usage in new code; use structured concurrency (`Task`,
  actors) except where AppKit delegates force otherwise.

---

## 5. File & Workspace Handling

- **App Sandbox: ON.** All file access via user-selected URLs; persist
  security-scoped bookmarks for recents/workspaces; always wrap access in
  `startAccessingSecurityScopedResource()` pairs (helper in WorkspaceKit).
- Open single file: standard `NSOpenPanel` / drag onto Dock / Finder "Open With"
  (import-declare `net.daringfireball.markdown`, and export a UTI for `.mdx`:
  `app.plainsong.mdx` conforming to `public.plain-text` — the `public.*` namespace
  is reserved for Apple).
- Open folder: sidebar shows the tree; filter to show only markdown-related files by
  default (`.md`, `.markdown`, `.mdx`), toggle "Show all files". Images shown so they can
  be drag-inserted.
- File tree operations: create/rename/delete/move file & folder (Finder-style, with
  trash not hard delete). Watch root recursively via `FSEventStream`; debounce 300 ms;
  reconcile tree diff instead of full reload to preserve expansion state.
- External change to the open file: if editor is clean → silently reload; if dirty →
  non-modal banner "File changed on disk: Reload / Keep mine".
- Multiple workspace windows are structurally allowed, one preview per editor pane, but
  Phase 1 currently shares one App-scoped `AppState` across the `WindowGroup`. Separate
  windows mirror the same workspace/current document until window-scoped state is built.
- Tabs: native window tabs (`NSWindow.tabbingMode`) deferred; Phase 1 uses sidebar
  selection to switch the single editor pane (like Typora). Multiple open
  `DocumentSession`s kept warm in an LRU (max 8) so switching files is instant.

---

## 6. Editor (EditorKit)

### 6.1 Base component

`STTextView` (AppKit, TextKit 2) hosted in `NSViewRepresentable` (`MarkdownEditorView`).
Configure: line numbers gutter (toggleable), invisible characters off, wraps lines,
variable-width font allowed (default: SF Mono 13 for source mode).

**Abstraction rule:** App/ and MarkdownCore/ must never import STTextView types. Only
EditorKit knows the concrete editor. This keeps the Phase-2 WYSIWYG swap and any library
replacement local to EditorKit.

### 6.2 Syntax highlighting

- Neon `TextViewHighlighter` (or its TextKit-2 system interface) drives incremental
  highlighting from tree-sitter.
- Two-layer markdown grammar: `markdown` (block structure) with `markdown-inline`
  injected, then language injections inside fenced code blocks keyed by the info string.
- Frontmatter: the markdown grammar exposes the YAML frontmatter block → inject
  `tree-sitter-yaml`.
- `.mdx` files: tree-sitter has no mature MDX grammar. Strategy: parse with the markdown
  grammar; add custom injection queries that route `html_block` / `html_inline` nodes and
  top-level `import`/`export` lines to the `tsx` grammar. This yields good-enough JSX
  highlighting. Known limitation: multiline JSX expressions containing blank lines may
  mis-highlight — acceptable for Phase 1; revisit with a dedicated MDX grammar later.
- Theme: semantic capture names (`@markup.heading`, `@markup.bold`, `@string`,
  `@keyword`, …) mapped to a `EditorTheme` struct (colors + font traits). Ship
  `default-light` and `default-dark`, follow system appearance.
- Style targets in source mode (Typora-like source styling): headings bold & scaled
  slightly, bold/italic spans rendered with the actual trait, inline code with background
  pill, links colored, fence blocks with subtle background band.

### 6.3 Editing behaviors (all in EditorKit, unit-test the pure parts in MarkdownCore)

- **List continuation:** Enter inside `-`/`*`/`1.`/`- [ ]` list inserts next marker
  (renumbering ordered lists); Enter on an empty item removes the marker (exits list).
  Tab / Shift-Tab indents/outdents list items, including every list item overlapped by
  a multi-line selection.
- **Auto-pairing:** `_`, `` ` ``, `(`, `[`, `{`, `"`, `<` (mdx) auto-close at the
  caret. `*` wraps a selection as italic (`*…*`) but does not auto-close at a bare
  caret so bullets and manually typed italics stay natural. Wrapping: with a selection,
  typing a pair character wraps the selection. Skip-over on closing char except for `*`.
- **Code fence helper:** typing ``` ``` ``` then Enter auto-inserts closing fence and places
  cursor inside; language autocomplete fires after the opening fence.
- **Smart paste:** pasting a URL over selected text creates `[selection](url)`. Pasting an
  image from clipboard saves it to `assets/` (configurable, relative to the file) and
  inserts `![](path)`. Drag-in image files insert relative-path links (copy into
  `assets/` if outside the workspace).
- **Table helper:** Tab/Shift-Tab navigate cells; Enter adds a row; `Format Table`
  command (⌥⌘F) realigns pipes. Pure formatting function lives in MarkdownCore.
- **Checkbox toggle:** ⌘L toggles `- [ ]`/`- [x]` on current line(s).

### 6.4 Formatting commands & shortcuts

| Command | Shortcut | Behavior (toggle, selection-aware) |
|---|---|---|
| Bold | ⌘B | `**…**` |
| Italic | ⌘I | `*…*` |
| Strikethrough | ⌃⌘X | `~~…~~` |
| Inline code | (Format menu; ⌘E is Use Selection for Find) | `` `…` `` |
| Use Selection for Find | ⌘E | set find pattern from selection; does not show bar |
| Link | ⌘K | `[sel](cursor)` |
| Heading 1–6 | ⌘1…⌘6 | replace line prefix |
| Paragraph | ⌘0 | strip heading prefix |
| Quote | ⌘⇧Q | toggle `> ` |
| Code fence | ⌘⇧K | wrap selection in fences |
| Toggle preview pane | ⌘⇧P | show/hide right pane |
| Toggle sidebar | ⌘⇧S (or native ⌃⌘S) | |
| Format table | ⌥⌘F | |
| Toggle checkbox | ⌘L | |
| New file | ⌘N | workspace: dedup `Untitled.md` at root; single-file: save panel |

All commands appear in the menu bar (Format menu) — menu first, shortcut second, so they
are discoverable and scriptable. Format menu actions route through the AppKit responder
chain so commands apply to the focused editor in the key window; if focus is in the
sidebar or preview, the formatting command no-ops instead of falling back to another
editor.

### 6.5 Completion engine

UI: STTextView's completion window (or custom NSPanel if insufficient). Trigger: typing
the trigger characters below, or ⌃Space manually. Engine is pure Swift in MarkdownCore:
`complete(text:cursor:workspace:) -> [Completion]` — fully unit-tested.

| Context | Trigger | Completions |
|---|---|---|
| Line start | typing `#`, `>`, `-` … | snippet list: headings, table skeleton, fenced block, task item, frontmatter block (only at file top) |
| After ```` ``` ```` | fence info | language ids (`swift`, `ts`, `python`, `mermaid`, …) |
| Inside `](` or `](./` | `(`, `/` | workspace-relative file paths (md files and images), anchors `#heading` within current file |
| Inside `![](` | `(` | image files in workspace |
| `:` + 2 chars | `:` | emoji shortcodes → insert unicode |
| Frontmatter block | line start | keys: `title`, `description`, `date`, `tags`, `draft`, `slug` + keys learned from sibling files in workspace |
| `.mdx` after `<` | `<` | component names found via regex-scan of `import` lines in the current file (Phase 1 scope) |

Ranking: prefix match > fuzzy; recently used boosted. Max 50 items. Never block typing —
compute async, cancel stale requests.

---

## 7. Preview (PreviewKit + preview-src)

### 7.1 Web view setup

- One `WKWebView` per editor pane, non-opaque, loads the bundled `index.html` once and
  stays alive (re-render via JS, never reload the page per keystroke).
- **Security:** strict CSP (`default-src 'none'; script-src 'self'; style-src 'self'
  'unsafe-inline'; img-src asset: data: https:`), `isElementFullscreenEnabled` off, and
  no remote loads by default. The preview JS removes remote image `src` values unless
  the user enables the Allow Remote Images preference; that preference permits only
  `https:` images and does not relax script, style, navigation, or non-image network
  behavior.
- **Local assets:** custom scheme `asset://` via `WKURLSchemeHandler`. JS rewrites
  relative image/link `src` to `asset://<workspace-relative-path>`; the handler resolves
  against the current file's directory / workspace root, enforces path containment
  (reject `..` and symlink escapes outside the workspace), and serves only PNG, JPEG,
  GIF, or WebP assets up to 10 MiB. SVG and other active/ambiguous formats are rejected
  until a separate sanitization policy exists.
- Link clicks: intercepted in Swift. `http(s)` → `NSWorkspace.open` (external browser).
  Relative `.md`/`.mdx` → open that file in the editor. `#anchor` → scroll preview.

### 7.2 JS pipeline (preview-src)

Two unified pipelines selected by `fileKind`:

- `md`: `remark-parse` → `remark-gfm` → `remark-frontmatter` → `remark-math` →
  `remark-rehype` → `rehype-katex` → custom rehype plugin `rehype-source-lines` (adds
  `data-line` from AST `position` to block elements) → `rehype-stringify`.
- `mdx`: same + `remark-mdx`; a custom remark plugin transforms `mdxJsxFlowElement` /
  `mdxJsxTextElement` / `mdxjsEsm` nodes into placeholder HTML (§9) instead of compiling.

Post-render in browser: `highlight.js` handles non-Mermaid code fences. Before the live
DOM patch, Mermaid fences become `div.mermaid-rendered` placeholders carrying the exact
fence source, its Mermaid-fence ordinal, and the current Mermaid-theme generation.
`morphdom` keeps a matching successful live wrapper. A changed wrapper receives the new
descriptor and `.mermaid-pending` state while retaining its previous SVG children only
as a stale visual until rendering settles; pending wrappers are never authoritative,
and success or `.mermaid-error` replaces those children. Remaining placeholders use a
64-entry success-only LRU keyed by the exact JavaScript source string + ordinal tuple,
and misses call `mermaid.render`. Exact-string comparison, not the removed 32-bit djb2
hash, authorizes reuse. The ordinal is part of the key because Mermaid output embeds its
render ID in SVG IDs and references; reusing one SVG string for identical fences at two
positions would duplicate IDs. Inserting or removing a fence therefore deliberately
invalidates every shifted following ordinal. Concurrent identical misses coalesce in a
separate in-flight table with the same 64-entry bound. Failed renders remain
`.mermaid-error`, are not cached, and retry on the next render. A preview-theme change
reinitializes Mermaid, clears both tables, and re-renders every visible diagram because
the theme is baked into its SVG. Checkbox inputs remain enabled and clickable.

DOM update: render to HTML string → prepare Mermaid placeholders → `morphdom` patch
against live DOM (retaining a changed diagram's prior SVG while pending) → resolve
pending Mermaid wrappers. This preserves unchanged Mermaid SVG DOM, unchanged
highlighted-code DOM, scroll position, and image loads.

Frontmatter is **not** rendered as a table in the preview body; it is stripped (shown in
the Frontmatter panel, §10). Optional toggle to show it as a styled block.

### 7.3 Swift ↔ JS bridge protocol

`window.webkit.messageHandlers.bridge.postMessage(...)` (JS→Swift) and
`webView.evaluateJavaScript("render(payloadJSON)")` (Swift→JS). Keep the protocol in one
Swift file (`BridgeMessage.swift`, Codable) and one TS file (`bridge.ts`) — **these two
must be kept in sync; both list message names in the same order with a
`PROTOCOL_VERSION` constant checked at `ready`.**

| Direction | Message | Payload |
|---|---|---|
| JS→Swift | `ready` | `{protocolVersion}` |
| Swift→JS | `render` | `{renderID, version, fileKind, text, baseDir, theme}`; `renderID` is a controller-assigned globally-monotonic stale-drop key (ordered across document switches); `version` is the per-document `DocumentSession.version` used only for `checkboxToggled` round-tripping; `baseDir` is the workspace-root-relative parent directory for the rendered file, or `null` for single-file/root renders |
| JS→Swift | `renderComplete` | `{renderID, version, blockCount}` |
| Swift→JS | `scrollToLine` | `{line, animated}` |
| JS→Swift | `previewScrolled` | `{topVisibleLine}` (only while preview owns scroll) |
| JS→Swift | `linkClicked` | `{href}` |
| JS→Swift | `checkboxToggled` | `{line, checked, version}` → Swift edits source text only when `version` matches the current document |
| Swift→JS | `setTheme` | `{theme}` |

### 7.4 Build

`make preview-bundle`: `npm ci && esbuild src/index.ts --bundle --minify` →
`App/Resources/preview/`. KaTeX fonts/CSS, highlight.js theme CSS, mermaid bundled
locally. No CDN, app must work fully offline. Commit the dist output.

---

## 8. Scroll Sync

Mapping basis: `data-line` attributes emitted by `rehype-source-lines` on every
block-level element.

- **Editor → preview (primary direction):** on editor scroll, compute first visible
  source line (TextKit 2 `textLayoutManager` viewport) → `scrollToLine`. JS finds the
  nearest `[data-line]` ≤ line and the next one > line, interpolates between their
  `offsetTop`s proportionally to line distance → smooth, accurate positioning.
- **Preview → editor:** symmetric, using `previewScrolled`.
- **Loop prevention:** a `scrollOwner` token (`editor` | `preview` | `none`) with 100 ms
  decay; messages from the non-owner are dropped.
- Cursor-follow option: "typewriter sync" — on text edit, preview scrolls to the edited
  line (on by default, like Typora).

---

## 9. MDX Support — Scope and Phasing

`.mdx` = Markdown + ESM imports/exports + JSX. Full fidelity requires a JS bundler with
the user's project dependencies — out of scope. Defined behavior:

**Phase 1 (required):**

- Editor: highlight JSX/ESM regions via tsx injection (§6.2). Completion for component
  names from `import` lines.
- Preview: `remark-mdx` parses; custom plugin renders:
  - `mdxjsEsm` (import/export) → collapsed chip row, e.g. `⟨import Button from
    '../components'⟩`, monospace, dimmed.
  - JSX elements → a "component card": bordered box, header = component name + key props
    (stringified, truncated), body = rendered markdown children if any. Lowercase HTML
    elements (`<div>`, `<img>`) render as real HTML through the sanitizer, but inline
    `style`, event-handler attributes, scripts, `srcdoc`, and user-authored SVG are
    stripped or dropped.
  - `{expression}` inline → rendered as code chip with the raw expression.
- Parse errors (MDX is stricter than MD): show inline error banner in preview with line
  number; editor keeps last good render below the banner. Never blank the preview while
  typing through transient syntax errors — keep last good DOM and show a subtle
  "stale/error" indicator instead.

**Phase 3+ (optional, not scheduled):** real component rendering via `@mdx-js/mdx`
in-browser compile + user-supplied component map. Requires design for sandboxing — do not
attempt without a Decision Log entry.

---

## 10. Frontmatter Panel

- Detect YAML frontmatter (`---` fences at byte 0). Parse with `Yams` (SPM).
- Sidebar-attached collapsible form panel: key/value editing with type-aware controls
  (string, date picker for `date`, tag token field for `tags`, toggle for `draft`).
- Edits write back into the source text (panel ↔ text always derived from the document;
  the text is the single source of truth).
- Malformed YAML → panel shows raw text + error, never crashes, never rewrites what it
  cannot parse.

---

## 11. Theming & Settings

- **Editor themes:** two built-ins wired through Settings and applied live. Custom JSON
  theme files in Application Support are deferred until a separate import/validation
  design exists.
- **Preview themes:** bundled CSS variables for system/light/dark preview themes, paired
  with the editor settings bridge. User CSS overrides are deferred until a separate
  sanitizer/design exists.
- **Settings window (SwiftUI `Settings` scene):** General (default folder, autosave
  interval), Editor (font, size, line numbers, typewriter sync), Preview (theme, allow
  remote images), Files (image-paste asset folder pattern, default extension `.md`/`.mdx`).

---

## 12. Performance Requirements

| Metric | Budget |
|---|---|
| Keystroke → screen (typing latency) | < 16 ms (never block main thread on parse) |
| Highlight update after edit | < 50 ms visible range, async |
| Preview update (debounced) | render < 100 ms for 100 KB doc; debounce 150 ms |
| File open (500 KB md) | < 300 ms to first paint, highlight may stream in |
| Memory (app host RSS) | < 400 MB in the Plainsong host process with 8 warm sessions + 2 settled live WKWebViews |

Techniques: tree-sitter incremental edits (`InputEdit`), Neon visible-range-first
highlighting, morphdom patching, render-version dropping, LRU sessions, and bounded
success-only Mermaid memoization by exact fence source + Mermaid-fence ordinal within a
theme generation. The ordinal prevents duplicate Mermaid-generated SVG IDs when equal
source appears more than once; inserting or removing a fence intentionally invalidates
shifted following ordinals. While a changed diagram renders, morphdom keeps its prior
SVG children only as non-authoritative pending content, avoiding a temporary collapse.
**Any PR that regresses typing latency is rejected regardless of features.** Add a
`PerformanceTests` target with a large fixture (`Fixtures/large-1mb.md`).

M5 planning note: the highlight budget is accepted only from visible-range-first
highlighting instrumentation, not from the historical 250 KB full-document inline
parsing cutoff. The memory budget is scoped to deterministic app host-process RSS.
OS-managed WebKit helper processes should be recorded as diagnostic/informational
data because helper reuse and process-pool ownership vary across runs and CI hosts.

---

## 13. Phase 2 — Typora-Style WYSIWYG (design sketch, do not build during Phase 1)

Approach: stay on TextKit 2 (no contentEditable). The source text remains the model; the
editor *folds* markdown tokens via rendering, not text mutation:

- Parse → for each inline/block node, when the selection/cursor is **outside** the node,
  apply "rendered" presentation: hide delimiter tokens (zero-width via TextKit 2 layout
  fragment customization or attribute folding), apply real styles (heading size, bold,
  italic), replace image links with `NSTextAttachment` thumbnails, render fences with a
  framed code block (custom `NSTextLayoutFragment`).
- When the cursor enters a node's range → reveal raw markdown for that node only
  (Typora's behavior).
- Tables and mermaid in WYSIWYG: custom layout fragments embedding views — hardest part;
  tables may initially stay raw with the table helper (§6.3).
- Undo, IME composition (Chinese input!), and selection across folded tokens are the risk
  areas — write exploratory tests early. **IME correctness is non-negotiable: test with
  Traditional Chinese (Zhuyin/Pinyin) marked text at every change.**
- The two-pane mode remains available behind a toggle forever (⌘⇧P cycles: source+preview
  / source only / WYSIWYG once it ships).

Phase 2 implementation begins only when Milestones M1-M5 are accepted and a dedicated
design doc (`docs/wysiwyg-design.md`) is approved.

---

## 14. Roadmap & Milestones (Phase 1)

Work strictly in order; each milestone has acceptance criteria that must pass before the
next begins.

### M0 — Scaffold
- XcodeGen `project.yml`, app target + 4 local SPM packages, Makefile, CI workflow,
  SwiftFormat/SwiftLint configs, this file.
- ✅ Accept: `make generate && make build` succeeds; empty window launches; `swift test`
  runs (even with 0 tests); CI green.

### M1 — Editor core
- STTextView wrapped, open/save single `.md` (sandbox + UTIs), autosave, dirty indicator,
  undo/redo, line numbers, word/char count in status bar.
- Temporary regex-based source styling is allowed only as a bridge to parser-backed
  highlighting; it must be debounced and must not synchronously re-highlight every
  keystroke. Documents above `MarkdownEditorView.maxComputedHighlightLength`
  (~200 KB UTF-8) enter large-document mode: bridge styling and the line-number
  gutter are disabled to keep typing latency flat; M1.5 removes this limit.
- ✅ Accept: open/save `Fixtures/kitchen-sink.md`; type at top of `large-1mb.md` with
  no visible lag from the fallback styling path; quit & relaunch restores last file.

### M1.5 — Parser-backed highlighting
- Neon + tree-sitter markdown highlighting incl. frontmatter-yaml and fence injections.
- ✅ Accept: open `Fixtures/kitchen-sink.md`, all constructs highlighted from the parser
  tree; type at top of `large-1mb.md` with no visible lag; fallback regex highlighter is
  removed or left only as an explicitly disabled emergency path.

### M2 — Live preview
- WKWebView + bundled pipeline (md only), bridge protocol, debounced incremental render,
  asset:// images, link handling, checkbox writeback, scroll sync both directions,
  KaTeX + mermaid + highlight.js working offline.
- Preview pane toggle: ⌘⇧P (and a toolbar button) switches between source-only and a
  side-by-side fully rendered pane ("Warp-style" final-render view, owner-requested).
  The chosen layout persists across relaunch.
- ✅ Accept: kitchen-sink renders correctly offline; ⌘⇧P shows/hides the rendered pane
  with the layout restored on relaunch; scroll sync drift < 1 viewport on a 10k-line
  doc; toggling a checkbox in preview edits the source line.
- Known M2 limitation: in single-file mode under the app sandbox, the app only receives
  a security-scoped grant for the selected Markdown file. Sibling `asset://` images may
  fail until M3's folder workspace grants directory scope.

### M3 — Workspace
- Folder open, sidebar file tree (create/rename/delete/move, drag), FSEvents reconcile,
  LRU sessions, external-change banner, recents menu, security-scoped bookmark restore.
- ✅ Accept: edit files in Finder/git while app open — tree and open file stay correct;
  no bookmark prompts on relaunch.

### M4 — Authoring features
- Completion engine (all contexts in §6.5), editing behaviors (§6.3), formatting
  commands + menu (§6.4), smart paste/drag of images, frontmatter panel, table helper.
- M4 landed across review-sized PRs: editing behaviors, formatting commands, completion
  engine, frontmatter panel, smart paste, drag-in image handling, and table helper.
- ✅ Accept: unit tests cover every completion context and list/table behavior; manual
  script `docs/m4-checklist.md` passes.

### M5 — MDX + polish
- `.mdx` end-to-end: UTI, tsx-injection highlighting, mdx pipeline with placeholder
  components, error banner; themes + settings window; app icon; performance pass
  against §12 budgets.
- Current status: MDX preview, TSX highlighting, icon/accent, §12 performance
  measurements, PR #24/#27 security hardening, and PR #26 Settings/themes have landed.
  The 2026-06-25 final sweeps fixed scroll sync, launch/Open Recent, MDX error liveness,
  and live MDX completion-popup checklist blockers; M5 is **accepted** because
  `docs/m5-checklist.md` now passes.
- ✅ Accept: open a real Astro/Next.js content folder; every `.mdx` post renders without
  blanking; all §12 budgets measured and recorded in `docs/perf-log.md`.

**Phase 2 = WYSIWYG (§13). Phase 3 candidates:** export (HTML/PDF via preview print),
window tabs, search across workspace (ripgrep-style), publish integrations, real MDX
component rendering.

---

## 15. Build, Run, Tooling

```sh
make bootstrap        # brew install xcodegen swiftformat swiftlint node; npm ci (preview-src)
make generate         # xcodegen generate  (run after editing project.yml)
make preview-bundle   # build preview-src → App/Resources/preview/ (commit the output)
make build            # xcodebuild -scheme Plainsong build
make test             # swift test (packages) + xcodebuild test + (cd preview-src && npm test)
make release          # DMG pipeline (Scripts/release.sh); PLAINSONG_UNSIGNED=1 for the alpha path
cd preview-src && npm run typecheck  # preview TypeScript check; CI runs this separately
make format           # swiftformat . && swiftlint --fix
```

- Xcode 16+. `.xcodeproj` is generated — **never hand-edit, never commit pbxproj
  conflicts; edit `project.yml`.**
- Code signing: "Sign to Run Locally" for dev; hardened runtime + notarization scripted
  later (Phase 3, direct distribution first, App Store optional).

---

## 16. Testing Strategy

- **MarkdownCore (highest coverage, pure Swift):** completion engine (table-driven tests
  per context), list continuation/renumber, table formatter, sync-map math, frontmatter
  parse/writeback. Target ≥ 90% on this package.
- **EditorKit:** behavior tests via programmatic `NSTextView` interaction where feasible;
  IME marked-text regression tests (insert Zhuyin composition, assert no corruption).
- **PreviewKit:** bridge protocol round-trip tests; path-containment tests for
  `asset://` handler (security-critical: `../../` must be rejected).
- **preview-src (vitest):** pipeline snapshot tests for kitchen-sink.md and mdx fixtures;
  `data-line` presence; placeholder rendering of MDX nodes.
- **Fixtures:** `Fixtures/kitchen-sink.md` (every GFM construct + math + mermaid),
  `Fixtures/kitchen-sink.mdx`, `Fixtures/large-1mb.md` (generated, committed),
  `Fixtures/broken-frontmatter.md`, `Fixtures/mdx-syntax-error.mdx`,
  `Fixtures/product-page.mdx`, `Fixtures/perf-100kb.md`,
  `Fixtures/perf-500kb.md`.
- UI tests: minimal smoke (launch, open fixture, type, preview updates). Don't invest in
  brittle full UI automation.

---

## 17. Collaboration Rules for LLM Agents

1. **Read agent.md first.** If your task conflicts with it, stop and surface the conflict
   instead of silently diverging.
2. **Scope:** one milestone-task per session/PR. Do not start M(n+1) work inside M(n).
3. **Layering is law:** App → {EditorKit, PreviewKit, WorkspaceKit} → MarkdownCore.
   Lower layers never import higher ones; MarkdownCore imports no UI frameworks.
4. **Edit `project.yml`, never `.xcodeproj`.** Run `make generate` after.
5. **Bridge changes:** any change to `BridgeMessage.swift` requires the mirrored change in
   `bridge.ts`, a `PROTOCOL_VERSION` bump, and `make preview-bundle` in the same commit.
6. **Tests accompany logic.** New MarkdownCore/pipeline logic without tests is incomplete.
   Run `make test` and `make format` before declaring done.
7. **No new dependencies** (Swift or npm) without a Decision Log entry with rationale and
   alternatives considered.
8. **Performance budgets (§12) are gates**, not suggestions. When touching the edit path,
   state how you verified typing latency.
9. **Main-thread discipline:** parsing/rendering off main; UI on `@MainActor`. No new
   `DispatchQueue.global` calls.
10. **Naming/style:** Swift API Design Guidelines; SwiftFormat config is authoritative;
    no abbreviations in type names; files ≤ ~400 lines, split by extension/feature.
11. **Commits:** imperative subject, body explains *why*; reference milestone (e.g.
    `M2: add scroll owner arbitration`).
12. **When uncertain about UX,** match Typora's behavior first, macOS HIG second, then ask.
13. **Update documentation:** behavior changes → update relevant § here; architectural
    choices → Decision Log entry (date, decision, why, alternatives).
14. **Branch + PR workflow (adopted after M2):** `main` is protected. Do all work on a
    branch named `m<N>-<slug>` (e.g. `m3-workspace`) and open a PR against `main`.
    State the branch name when you start. Never push to `main` directly, never create
    branches with other naming schemes, never force-push, and never merge your own PR —
    the maintainer merges (squash) after review and green CI. One milestone (or one
    review-fix batch) per PR.

---

## 18. Decision Log

| Date | Decision | Rationale / Alternatives |
|---|---|---|
| 2026-06-12 | Two-pane first, WYSIWYG as Phase 2 | Ship value early; WYSIWYG on TextKit 2 is the highest-risk component. Alt: WYSIWYG-first (rejected: months before usable). |
| 2026-06-12 | STTextView over raw NSTextView | Line numbers, completion window, plugin points, active maintenance; M1 pins STTextView 2.3.10 exactly for deterministic builds. Wrapped behind EditorKit abstraction to keep swap cost low; alt raw NSTextView remains fallback if dependency risk grows. |
| 2026-06-12 | Neon + SwiftTreeSitter for highlighting | Incremental + async, proven in Chime/CodeEdit ecosystem. Alt: regex highlighting (rejected: wrong for nested fences), Highlightr (rejected: full-document re-highlight, no structure). |
| 2026-06-12 | Regex styling is a temporary M1 bridge, not final highlighting | Used to unblock editor-core integration while Neon/tree-sitter dependency shape is resolved. It must stay behind `MarkdownEditorView`/`MarkdownSyntaxHighlighter`, use cached regexes, and debounce full-document work. M1.5 owns replacing it with Neon + SwiftTreeSitter before preview/workspace milestones proceed. |
| 2026-06-12 | M1.5 uses a direct SwiftTreeSitter highlighter façade first | The parser-backed highlighter now consumes tree-sitter markdown + markdown-inline nodes and a wrapped YAML grammar target, removing the M1 regex fallback and large-document skip while keeping parsing off the main actor. Neon 0.6.0 currently depends on ChimeHQ/SwiftTreeSitter `main`, which conflicts with the official grammar packages in this repo; keep the EditorKit façade narrow so Neon/TextViewHighlighter can replace the mapper once the dependency graph is deterministic. |
| 2026-06-12 | WKWebView + unified/remark preview | Only realistic MDX path; KaTeX/mermaid free. Alt: full native rendering (rejected: MDX/math/diagrams cost). Editor stays native — app is not a web app. |
| 2026-06-12 | MDX components render as placeholder cards in Phase 1 | Real execution needs user project bundling + sandbox design. |
| 2026-06-12 | XcodeGen + committed preview dist | pbxproj and node_modules are hostile to LLM diff-based collaboration. |
| 2026-06-12 | App Sandbox on from day 1 | Retrofitting sandbox is painful; keeps App Store option open. |
| 2026-06-12 | Keep network-client entitlement for sandboxed WKWebView startup | App-hosted `PreviewController()` smoke test reproduced WebContent process launch failure without `com.apple.security.network.client` (`Application does not have permission to communicate with network resources`), leaving the pane blank before any remote request. Preview remains offline by CSP and bundled resources. |
| 2026-06-12 | macOS 14+ | TextKit 2 mature, modern SwiftUI APIs; Typora-class audience updates quickly. |
| 2026-06-12 | M0 ships with zero external Swift dependencies | First build must be deterministic; STTextView/Neon/grammars land in M1 with pinned versions. Packages use swift-tools 5.10 + StrictConcurrency experimental flag. |
| 2026-06-12 | Editor typing hot path moves plain `String` only | Per-keystroke String⇄NSAttributedString⇄AttributedString bridging of the whole document caused visible lag on 1 MB files. The editor binding carries `String`; highlight output flows separately as a revisioned `HighlightedText` (Equatable by revision), is computed on a detached task, and is applied via in-place `setAttributes`. Statistics are likewise computed off-main. This shape is also what M1.5's Neon integration needs. |
| 2026-06-12 | Keystrokes must not publish through the document model | Time Profiler showed per-key SwiftUI re-renders of the whole window (DynamicBody under `keyDown`) plus foreign-string traffic (`_StringGuts.foreign*`, CFStorage). `DocumentSession.text`/`version` are non-`@Published`; `isDirty` assignments are deduped; the coordinator eagerly `makeContiguousUTF8()`s the bridged string so downstream compares/counts run native. M2's preview must subscribe to text via its own debounced channel, not `objectWillChange`. |
| 2026-06-12 | Product named **Plainsong**; bundle id namespace `app.plainsong.*` | Owner decision. Verified no editor-category collision (App Store / GitHub; "plainsong" in software is liturgical-chant apps only). `plainsong.app` domain is registered by a third party — marketing site will need a variant; bundle id does not depend on it. Exported MDX UTI is `app.plainsong.mdx`; UserDefaults keys and JS bridge globals renamed in the same commit (saved window/preview prefs reset once). Repo folder name stays `blogeditor`. |
| 2026-06-12 | M2 scroll sync gets a narrow EditorKit scroll proxy exception | STTextView is an `NSView`, not `NSTextView`, so App-level hierarchy probing could never attach and retried forever. The exception is limited to `EditorScrollSupport.swift`, one optional `MarkdownEditorView(scrollProxy:)` parameter, and `MarkdownTextView` make/update wiring; `textViewDidChangeText` and the typing hot path remain unchanged. |
| 2026-06-12 | M3 keeps workspace ownership in WorkspaceKit and uses bookmark-backed Open Recent | File-tree reconciliation, directory scanning, FSEvents, file operations, bookmark recents, and LRU eviction stay below App so App composes rather than owns workspace rules. Preview assets resolve from the workspace root when a folder is open, while single-file mode keeps file-parent resolution. A simple File > Open Recent submenu backed by app-scope bookmarks was chosen over adopting NSDocumentController because Plainsong still owns custom folder workspaces and warm sessions. |
| 2026-06-13 | M4 editing behaviors get a narrow EditorKit command/interception exception | STTextView owns key input and undo, so editing behaviors must intercept `shouldChangeTextIn` and apply accepted transforms through STTextView insertion APIs. The exception is limited to `EditingBehaviorsSupport.swift`, one optional command-proxy parameter on `MarkdownEditorView`/`MarkdownTextView`, and a thin Coordinator delegate call; all transform logic stays pure in MarkdownCore, no textStorage writes are used for behavior edits, and the EditorKit boundary no-ops while IME marked text exists. Alt: App-layer text mutation was rejected because it would lose live selection and native one-step undo. |
| 2026-06-13 | M4 review fixes keep behavior edits pure and selection-aware | Ordinary non-trigger typing returns from EditorKit before whole-document materialization, menu commands share the Coordinator reentrancy guard, ordered task-list continuation increments and preserves checkbox markers, and Tab/Shift-Tab over selected list lines indents/outdents the selected block. Alt: deferring multi-line list indentation to M4 part 2 was rejected because the pure transform is small and covered by MarkdownCore tests. |
| 2026-06-13 | M4 reentrancy guard is a reference flag; `*` is wrap-selection-only | STTextView synchronously re-enters `shouldChangeText` during programmatic inserts, so EditorKit must never hold an `inout` or other mutating access to the editing-behavior guard across `insertText` or similar delegate-reentrant calls. The guard is a Coordinator-owned reference object read by reentrant calls. Maintainer decision: bare `*` inserts literally, selected text wraps as `*…*`, and `*` has no skip-over behavior; `_`, backticks, brackets, quotes, and MDX angle pairs keep existing auto-close behavior. Alt: keeping bold-first `*` auto-pairing was rejected because it blocks bullets and Typora-like italics. |
| 2026-06-14 | Preview stale-drop keyed on a controller `renderID`, not document `version` (bridge protocol v4) | The long-lived preview WebView dropped any render whose `version` was below the highest it had seen, but `DocumentSession.version` resets to 0 per file. After editing one file then selecting another freshly opened (lower-version) file, the new file's render was discarded and the preview stranded on the previous document. Fix decouples ordering from document version: `PreviewController` assigns a globally-monotonic `renderID` per render request used as the only drop key on both sides; `version` is retained solely for `checkboxToggled` writeback matching. Protocol bumped 3→4 with mirrored `BridgeMessage.swift`/`bridge.ts` and regenerated preview bundle. Alt: resetting the JS watermark on `fileURL` change was rejected because `baseDir` is not a reliable document identity (siblings share a directory) and it still needs a bridge signal. |
| 2026-06-15 | M4 review follow-ups route format commands by focused editor and preserve line endings | Format menu commands now dispatch through the AppKit responder chain to the key window's first-responder editor instead of a shared App-level proxy. MarkdownCore multi-line transforms operate on `MarkdownLine` content ranges and re-stitch original line terminators, and the code-fence helper only auto-inserts a closing fence for opening fences. Alt: per-window shared proxies were rejected because AppKit already owns the correct focused-editor routing. |
| 2026-06-15 | Defer independent multi-window document state | Phase 1 keeps one App-scoped `AppState` for `WindowGroup`, so multiple windows mirror the same current workspace/document even though Format commands route by first responder. Window-scoped `AppState` is deferred to a dedicated workspace/windowing change because it needs autosave, warm-session LRU, recents, and external-change behavior reviewed together. |
| 2026-06-15 | M4 completion uses a pure `CompletionWorkspace` value boundary | MarkdownCore owns ranking and context detection but receives only plain workspace-relative paths, heading anchors, learned frontmatter keys, and recency IDs. WorkspaceKit builds that value under security-scoped root access and enforces root containment; EditorKit asks STTextView's async completion delegate and applies selected replacements through STTextView insertion APIs. Alt: letting MarkdownCore read files or import WorkspaceKit was rejected by the layering rule; App-layer completion panels were rejected because STTextView already provides a native async completion window. |
| 2026-06-15 | MDX import parsing is shared in MarkdownCore | Component-name completion stays fresh by allowing the engine to rescan current text, while WorkspaceKit also precomputes names for the plain `CompletionWorkspace` value. A shared `MDXImportParser` in MarkdownCore removes duplicate import parsing without violating layering because WorkspaceKit already depends on MarkdownCore. Alt: relying only on WorkspaceKit was rejected because its debounced refresh can lag the current keystroke; keeping duplicate parsers was rejected as review-risky drift. |
| 2026-06-16 | Yams handles frontmatter YAML validation and typed loading | Frontmatter remains source-text-first: MarkdownCore uses Yams to validate and load the YAML mapping, then performs localized writeback so unknown keys, comments, body text, and line endings are preserved. Alt: hand-rolled YAML parsing was rejected because malformed YAML diagnostics and scalar/list semantics would be fragile; using Yams to dump whole mappings was rejected because it would reorder or normalize user-authored frontmatter. |
| 2026-06-17 | M5 TSX highlighting vendors tree-sitter-typescript TSX C sources | EditorKit now injects MDX ESM/JSX regions into a vendored `TreeSitterTSXFixed` target from `tree-sitter-typescript` v0.23.2 (`f975a621f4e7f532fe322e13c4f79495e0a7b2e7`) and maps TSX capture names into the existing MarkdownSyntaxToken/theme facade. Vendoring avoids the upstream Swift package's ChimeHQ/SwiftTreeSitter dependency, preserving the exact `tree-sitter/swift-tree-sitter` 0.10.0 pin and avoiding the Neon dependency-shape conflict. Alt: adding the upstream Swift package was rejected because it would reintroduce a conflicting SwiftTreeSitter graph; keeping `.mdxSource` coarse styling was rejected by §6.2/M5 acceptance. |
| 2026-06-17 | M5 MDX preview uses remark-mdx placeholders without bridge v5 | The preview pipeline adds `remark-mdx`, `mdast-util-mdx` node typing, and `rehype-sanitize` so `.mdx` files render Markdown normally while ESM, JSX components, and expressions become non-executed placeholders. MDX parse errors are handled entirely in preview JS with an inline banner and stale last-good content, so bridge protocol v4 remains sufficient. Alt: `@mdx-js/mdx` runtime compilation/component execution was rejected as Phase 3+ sandboxing work; native bridge diagnostics/protocol v5 are deferred until Swift-owned error chrome is required. |
| 2026-06-24 | M5 memory budget uses host-process RSS | The §12 memory gate is app host-process RSS with 8 warm sessions and 2 settled live preview webviews. PR #21 measured 149.8 MB host RSS and prints OS-managed WebKit helper memory as diagnostics only; helper reuse and process-pool ownership are too host-dependent to assert in CI. Alt: aggregating WebKit helper RSS was rejected for the M5 gate because the local diagnostic aggregate was 648.3 MB and not stable enough to compare across runners. Issue #13 is closed under this host-process RSS scope. |
| 2026-06-24 | PR #24/#27 M5 preview security rejects active HTML/SVG and uses bounded raster assets | Sanitized MDX/lowercase HTML strips inline `style` instead of CSS-sanitizing because Phase 1 has no user-authored CSS policy and style spoofing can cover the app with fixed or giant layout boxes. Script-like elements are dropped before sanitize so payload text does not leak into preview output. `asset://` preview serving and image file imports accept only PNG, JPEG, GIF, and WebP up to 10 MiB per file; inline user-authored SVG/path is rejected as active content until a dedicated sanitizer/design exists, and larger files should be inserted by link/reference outside the preview asset path. Alt: allowing all `UTType.image` files was rejected because it includes scriptable or memory-heavy formats. |
| 2026-06-24 | M5 settings use UserDefaults and opt-in HTTPS-only remote images | Settings persist through `UserDefaults` with the default folder stored as a security-scoped bookmark and no new persistence dependency. Editor settings apply by updating the existing STTextView font, gutter, and highlight theme instead of reloading document text; preview settings travel over bridge protocol v5. Remote images remain disabled by default; enabling them permits only `https:` image `src` values while keeping script, style, navigation, asset containment, and SVG rejection policies unchanged. Custom editor-theme JSON and user CSS overrides are deferred until separate import/sanitizer designs exist. Alt: broad WebView network allowance or arbitrary CSS/theme file loading in M5 was rejected as unnecessary release risk. |
| 2026-06-25 | M5 editor-to-preview scroll sync emits selection and visible-range source lines | The M5 checklist found that editor selection/Page Down movement could leave the preview near the previous top anchor even though source-line anchors existed. EditorKit now emits the source line containing the current selection and reported visible range through the existing scroll proxy, deduping repeated line sends and staying within the narrow M2 scroll-proxy exception. Alt: changing preview anchoring or adding a new bridge message was rejected because the failure was in when the existing editor line signal was emitted, not in the preview protocol. |
| 2026-06-26 | Phase 2 WYSIWYG design gate approves an inline-first v1 | Phase 2 v1 starts with source-range fold/reveal for headings, emphasis/strike, and inline code. Links are included only after the selection/copy spike keeps full `[text](url)` source ranges sane. Images, fenced-code custom fragments, tables, Mermaid, math, and real MDX rendering are deferred. Spike A/B/C (IME, undo, selection/copy) must pass before production implementation; WYSIWYG must not enter the user-facing ⌘⇧P cycle until a dedicated production PR proves the real mechanism. Alt: broad block rendering and attachment-heavy WYSIWYG were rejected until inline safety is proven. |
| 2026-06-26 | Phase 2 production fold/reveal core rides the visible highlighter parser and stays dev-only | Production fold/reveal is attached to `MarkdownHighlightService`/`MarkdownSyntaxParser` so syntax tokens and fold candidates share one visible block parse under the existing debounce, avoiding parser-per-keystroke and the initial 60.700 ms double-parse path. A default-off `_developmentPresentation: .inlineFoldReveal` hook exercises headings, emphasis/strike, inline code, and list/quote styling without changing App layout modes or the user-facing `⌘⇧P` cycle. Links remain in the range model but are not visually folded until native selection/copy edge semantics are specified. Alt: a standalone fold parser per update was rejected as a §12 performance regression; at this point, exposing WYSIWYG in layout prefs was rejected until actual IME streams and native selection gates passed (they later did in the PR #41-#49 sequence). |
| 2026-06-26 | Phase 2 native gates use exact raw-copy policy and keep WYSIWYG blocked | For the attribute-only development hook, copy/paste/accessibility use the raw Markdown backing string: entire folded spans include delimiters, content-only selections copy content only, boundary selections copy exactly the selected `NSRange`, and paste mutates source text normally. Native arrow and shift-selection may enter delimiter offsets, but touched regions reveal on the next presentation pass rather than trapping the caret. Link visual folding remains deferred because link chrome and destination-edge selection need equivalent native coverage. Actual macOS Zhuyin/Pinyin event streams were not fully proven in this PR, so the App still must not expose or persist WYSIWYG. Alt: synthesizing rendered copy text or enabling link folding now was rejected as ambiguous source-range behavior. |
| 2026-06-26 | Actual Zhuyin IME commit keys stay owned by the input context while marked text is active | The opt-in actual TCIM Zhuyin event-stream harness found that candidate/commit keys can update marked text and still fall through to normal STTextView space/Return handling, corrupting source with whitespace/newlines. `MarkdownSTTextView` now reserves space, Return, and keypad Enter for `inputContext` while marked text exists. At this point Pinyin was still blocked by local input-source enablement (superseded later by the actual Pinyin gate), so WYSIWYG stayed non-user-facing. Alt: treating the newline as expected Return behavior was rejected because it couples composition commit to source mutation. |
| 2026-06-26 | Actual Pinyin IME event stream passes the inline fold/reveal gate; the actual-IME harness selects only composition-capable input methods | Enabling `com.apple.inputmethod.TCIM.Pinyin` with `TISEnableInputSource` made a real Pinyin IME selectable (direct selection had returned `-50` while it was disabled), and the opt-in harness drove physical `t/a/i → space` Pinyin composition (committing `太`) through the production `_developmentPresentation: .inlineFoldReveal` hook at heading, bold, italic, and inline-code fold boundaries with no source corruption, no caret escape from the marked range, no premature commit, fold attributes skipped during marked text, and presentation reapplied after commit. `ActualIMEInputSource.enabled(matching:)` now requires `kTISPropertyInputSourceType` to be a keyboard input method/mode so the gate never selects a same-named `TISTypeKeyboardLayout` (e.g. `com.apple.keylayout.PinyinKeyboard`) that produces no marked text; Zhuyin still selects its IME and stays green. IME is no longer a Phase 2 blocker. At that point, user-facing WYSIWYG still needed native pointer hit-testing and a release checklist; those follow-up gates later passed in the PR #41-#49 sequence. Link visual folding stays deferred. Alt: selecting the first name-matched source was rejected because it can pick a non-composing keyboard layout and yield false IME evidence. |
| 2026-06-26 | Native pointer hit-testing passes with reveal-on-touch; delimiter edge-snapping is deferred to the user-facing WYSIWYG PR | `WYSIWYGNativePointerGateTests` dispatches real `NSEvent` left-mouse-downs at the laid-out screen position of folded heading/bold/strike/inline-code delimiters (target from `firstRect(forCharacterRange:)`, caret from STTextView's own `mouseDown → caretLocation` hit-test) and proves caret placement is sane, the touched span reveals on the next presentation pass, pointer-extend (shift-click) drag selection across folded spans copies exact raw Markdown with delimiters included, and no click traps the caret inside a hidden delimiter. Decision: for the attribute-only development hook, reveal-on-touch is sufficient and no delimiter edge-snapping is added at this layer, because any caret that touches a span reveals its delimiters before the next frame, so there is no trap and selection/copy stay on raw source offsets. Edge-snapping is a UX refinement owned by the user-facing PR. The `baselineOffset(-1000)` zero-width fold mechanism in use at this point laid a single folded line out ~1013 pt tall and distorted multi-line viewport (it did not trap the caret), so the follow-up user-facing path needed to replace it with a cleaner zero-width mechanism and rerun the IME/selection/pointer gates against that mechanism. That replacement and rerun later passed; link folding stays deferred. Alt: adding edge-snapping to the dev hook now was rejected as redundant work that would complicate the raw-offset selection model the other gates depend on. |
| 2026-06-26 | User-facing WYSIWYG release checklist specified; mechanism + mode policy fixed before any user-facing exposure | `docs/wysiwyg-release-checklist.md` is the blocking gate list for exposing WYSIWYG to users (groups A mechanism, B gate-rerun matrix, C UX policy, D mode integration, E scope, F sign-off). Mechanism decision: the user-facing zero-width fold must replace `baselineOffset(-1000)` (R18) with **`NSTextLayoutFragment` customization** as the primary TextKit 2-native mechanism because it collapses delimiter geometry while keeping the backing `String` canonical (so copy and `AXValue` stay exact raw Markdown); **attachment-based hiding** is the fallback only if it proves no U+FFFC leakage into copy/accessibility; tiny-font/`kern`-only, TextKit 1 glyph suppression, and any source-text deletion are rejected. Every native gate already proven against the dev-hook mechanism (IME Zhuyin/Pinyin, keyboard arrow/shift-selection, pointer click/drag, copy, paste, accessibility, large-doc performance, undo/redo, source-only/source+preview regression) must rerun against the replacement mechanism, plus a new layout-geometry sanity gate that closes R18. Mode policy: `⌘⇧P` becomes a three-state cycle (source+preview → source-only → WYSIWYG); the persisted layout value becomes a three-state enum migrated from the legacy `Plainsong.preview.isVisible` boolean; a kill switch + deterministic fallback to source-only handles mechanism failure/disable; WYSIWYG ships behind an off-by-default Experimental label until the checklist is fully green. Reveal-on-touch is retained and caret edge-snapping near hidden delimiters becomes required for the user-facing mode; copy stays exact raw Markdown; link visual folding stays deferred behind its own sub-gate. This is a spec-only PR: no user-facing mode, no persisted WYSIWYG layout value, no link folding, no construct-scope change. Alt: shipping WYSIWYG on the `baselineOffset(-1000)` mechanism, or promoting it without rerunning gates against the replacement, was rejected because the throwaway geometry distorts multi-line viewports (R18) and a different layout path invalidates the prior gate evidence. |
| 2026-06-26 | Phase 2 zero-width fold uses TextKit 2 content-storage projection; R18 is closed and WYSIWYG stays dev-only | The replacement mechanism for folded Markdown delimiters is a dev-hook-only `NSTextContentStorageDelegate` paragraph projection: when folded delimiter marker attributes appear in a paragraph, EditorKit returns an `NSTextParagraph` copy where only those delimiter characters are replaced by equal-length U+200B runs for layout. The backing `NSTextStorage.string` remains exact raw Markdown; copy, paste, undo/redo, and `AXValue` stay source-based; no `NSTextAttachment` or U+FFFC is introduced; no TextKit 1 path is added. The original `NSTextLayoutFragment` primary was attempted but not shipped because STTextView 2.3.10 owns `textLayoutManager.delegate` and supplies `STTextLayoutFragment`; taking that delegate directly would break STTextView's chain, and the custom line-fragment prototype did not safely drive TextKit's segment / `firstRect` hit-testing path for native pointer gates. `MarkdownSTTextView` therefore keeps the projection narrow and adds WYSIWYG-only raw pointer/keyboard adapters while the delegate is installed. The old `baselineOffset(-1000)` + 0.1 pt clear-font hiding is removed. Checklist A and B1-B13 pass against this mechanism, including actual Zhuyin/Pinyin IME, pointer click/drag, exact raw copy/paste/accessibility, undo/redo, source-only/source+preview regression, and B13 folded-line geometry; large-doc WYSIWYG fold/highlight/apply measured 26.964 ms. R18 is closed. User-facing WYSIWYG remains blocked: no `⌘⇧P` exposure, no persisted WYSIWYG layout mode, no link visual folding, and no deferred constructs were added. Alt: attachment hiding was rejected because the content-storage projection avoids ORC risk; shipping the layout-fragment delegate takeover was rejected because it would fight STTextView's TextKit 2 ownership and did not pass native hit-testing safely. |
| 2026-06-27 | Phase 2 delimiter edge-snapping adjusts caret rest only, never selections; WYSIWYG stays dev-only | Behind the existing `_developmentPresentation: .inlineFoldReveal` hook, a collapsed caret that would rest strictly inside a folded (zero-width) delimiter now snaps to the delimiter-inner boundary (checklist §C.2). `WYSIWYGCaretSnap.snap` is a pure offset function; `MarkdownSTTextView.wysiwygFoldedDelimiterRange(containingInterior:)` reads the live `foldedDelimiterAttribute` runs via a bounded `longestEffectiveRange` scan so snapping reflects exactly what is currently hidden and stays O(1) on the caret path. Keyboard `moveLeft`/`moveRight` snap by direction of travel; non-shift `mouseDown` snaps the hit-test result to the nearer edge. The extending keyboard branch and the shift/pointer-extend `mouseDown` branch are deliberately left raw, so selections still span delimiter offsets and copy stays exact raw Markdown (§C.3/§C.4); source text is never mutated. Because reveal-on-touch reveals a span at its leading boundary, opening-edge snapping mainly fires when fold state lags the caret, while closing-edge snapping fires on natural leftward traversal — both are covered by `WYSIWYGEdgeSnappingGateTests` (pure function, keyboard bold/strike/heading, inline-code no-trap, pointer snap + real clicks, shift-selection raw copy, emoji/CJK movement). The B3 gate test was renamed `testNativeArrowIntoFoldedDelimiterSnapsToInnerEdgeAndReveals` to reflect that caret rest is upgraded from reveal-only to edge-snapping; existing native/pointer gates stay green and large-doc fold/highlight/apply stayed at 25.348 ms. User-facing WYSIWYG remains blocked on §D: no `⌘⇧P` exposure, no persisted WYSIWYG layout mode, no link visual folding, no construct-scope change. Alt: clamping selections out of delimiters was rejected because it would break exact-raw-Markdown copy (B7); snapping on post-move (destination) fold state was rejected because reveal-on-touch always reveals the entered span, which would make snapping a no-op and reintroduce the one-frame interior-rest flicker. |
| 2026-06-27 | Phase 2 WYSIWYG mode plumbing ships behind an off-by-default Experimental kill switch | App layout state is now the three-case `EditorLayoutMode` (`sourcePreview`, `sourceOnly`, `wysiwyg`) persisted under `Plainsong.layout.mode`, with one-time migration from the legacy `Plainsong.preview.isVisible` boolean (`true → sourcePreview`, `false → sourceOnly`). `⌘⇧P` remains a two-state source+preview/source-only cycle while `Plainsong.settings.experimentalWYSIWYGEnabled` is false; when the flag is enabled and the editor mechanism is healthy, the cycle includes WYSIWYG and the App passes `_developmentPresentation: .inlineFoldReveal` only for that mode. A persisted WYSIWYG value read while disabled, or a mechanism-install failure reported from EditorKit, falls back deterministically to source-only, records/logs the recovery, and leaves source text untouched. WYSIWYG remains Experimental and off by default; source+preview and source-only remain stable; link visual folding, image thumbnails, fenced-code custom fragments, tables, Mermaid/math widgets, and real MDX rendering stay deferred. Alt: promoting WYSIWYG into the default cycle without a kill switch was rejected by R12; silently ignoring mechanism failure was rejected because users could be stranded in a broken pane; enabling links/deferred constructs was rejected because their native selection/copy gates are not complete. |
| 2026-07-02 | CI builds run on `pull_request` and `workflow_dispatch` only; push-to-main builds removed | Protected `main` only receives squash merges of PR heads that already passed the identical workflow, so the push build duplicated every PR build at the private-repo 10x macOS minute multiplier. That duplication helped exhaust the June 2026 Actions quota, which took runner allocation offline from 06-24 to 07-01 and let PRs #42-#49 merge unlinted (cleaned up in PR #52). `workflow_dispatch` remains for on-demand verification of `main`. Alt: keeping both triggers with path filters was rejected because the duplication is inherent, not path-dependent; if stacked merges ever land an untested combination on `main`, add branch-protection "require branches to be up to date" rather than restoring the push build. |
| 2026-07-02 | Link folding and release engineering get spec/plan docs before any implementation | `docs/link-folding-gates.md` defines the checklist-§C.5 sub-gate (L1-L9: asymmetric hidden-URL fold model, chrome/pointer policy, destination-edge selection, partial-URL raw copy, IME/pointer/AX/perf/undo reruns) that must be green before `[text](url)` folds visually; reference links, autolinks, and images stay raw. `docs/release-engineering-plan.md` sequences R14 (P0 owner decisions incl. the missing LICENSE, then Developer ID signing, hardened-runtime audit, notarization, hdiutil DMG packaging, optional tag-only release CI) with a clean-VM P5 exit gate; alpha default is no updater and no telemetry, and any Sparkle/crash-reporter adoption needs its own Decision Log entry. Both docs are spec/plan-only (PR #45 precedent): no behavior, dependency, or gate-status change. Alt: implementing link folding or signing scripts directly was rejected because both need owner decisions and macOS-side evidence this session cannot produce. |
| 2026-07-02 | Release P0 decisions: MIT license, direct-download alpha, no updater, no telemetry, 0.x + build number | Owner decisions closing `docs/release-engineering-plan.md` P0: Plainsong is open source under **MIT** (copyright `w3d-su`; `LICENSE` committed, README updated); alpha distribution is **direct download** with App Store deliberately deferred as a future decision (sandbox-on preserves the option); **no auto-updater** and **no telemetry** in alpha, feedback via GitHub Issues; versioning is `0.x` marketing + monotonically increasing build number stamped at release. Adopting Sparkle or a crash reporter later requires its own Decision Log entry and dependency review. P1 (Developer ID signing) is unblocked. Alt: GPL/Apache were declined in favor of MIT's simplicity; shipping unlicensed was rejected because it blocks any public artifact. |
| 2026-07-02 | Release pipeline scaffolding is env-driven shell over plain Apple tooling | `make release` runs `Scripts/release.sh`: xcodegen → Release `xcodebuild` with per-invocation overrides (`CODE_SIGN_STYLE=Manual`, Developer ID identity from `PLAINSONG_SIGNING_IDENTITY`, `ENABLE_HARDENED_RUNTIME=YES`, `--timestamp`, `MARKETING_VERSION`/`CURRENT_PROJECT_VERSION` stamped with build number defaulting to `git rev-list --count HEAD`) → `codesign --verify` → `notarytool`/`stapler`/`spctl` via App Store Connect API-key env vars (`PLAINSONG_SKIP_NOTARIZE=1` allows pre-P2 smoke runs marked non-distributable) → `Scripts/make-dmg.sh` (`hdiutil` UDZO, app + /Applications symlink) → SHA-256. Debug signing (`Sign to Run Locally`, hardened runtime off) is untouched because overrides live only in the release invocation, not `project.yml`. Scripts were authored on Linux and syntax-checked; the P3 reproducibility gate stays open until a first Mac run with P1 credentials. Alt: committing a Release signing config into `project.yml` was rejected to keep secrets/identity out of the manifest; `create-dmg` was rejected as an unnecessary dependency. |
| 2026-07-02 | Alpha ships unsigned; Apple Developer Program membership deferred | Owner decision: do not purchase ADP (US$99/yr) for the alpha. `PLAINSONG_UNSIGNED=1 make release` produces an ad-hoc-signed, "-unsigned"-suffixed DMG with no notarization; README "Installing (alpha)" documents the Gatekeeper bypass (Open Anyway / `xattr -d com.apple.quarantine`) and the build-from-source path, which MIT licensing makes first-class. Rationale: the alpha audience is technical (Astro/Next.js authors) and tolerates the bypass; the $99 buys polish that matters at beta/1.0, and `release.sh` already supports the signed path so resuming P1/P2 is credential-only. Plan P1/P2 are marked DEFERRED and the P5 DMG gate is restated for the unsigned path. Alt: buying membership now was declined as premature before product validation; distributing without any documented bypass was rejected as a support trap. |
| 2026-07-05 | Unsigned releases are published by tag-triggered CI as draft prereleases | `.github/workflows/release.yml` runs on `v*` tags and `workflow_dispatch` only: macos-15 runner, full-history checkout for the commit-count build number, `PLAINSONG_UNSIGNED=1 make release`, then the DMG + SHA-256 are uploaded to the tag's GitHub Release — created as a draft prerelease when absent so the owner still authors notes and presses publish; dispatch runs attach workflow artifacts instead. No secrets exist on the unsigned path and public-repo macOS minutes are free, so the June-quota concern does not apply; the signed path stays manual until P1/P2 resume. Alt: publishing releases non-draft was rejected to keep notes owner-authored; per-push release builds were rejected per the 2026-07-02 CI-trigger decision. |
| 2026-07-06 | Inline-link visual folding joins only the off-by-default Experimental WYSIWYG mode after L1-L9 pass | PR A (#65), PR B (#67), and PR C (#68) complete `docs/link-folding-gates.md`: exact `[text](url)` chrome folding/styling, destination-edge collapsed-caret snapping, selection and exact-raw-copy/paste, real-pointer, accessibility, performance, undo/redo, and owner-run real macOS Zhuyin/Pinyin composition all pass. The 2026-07-06 owner run passed `testActualZhuyinEventStreamAtLinkBoundaries` and `testActualPinyinEventStreamAtLinkBoundariesWhenEnabled` at the start/end of link text and immediately after `](url)` (84.114 seconds, 0 failures). Pointer policy is Typora-style: plain click places the caret and reveals the raw link; v1 adds no ⌘-click URL opening. PR B supersedes the 2026-06-27 bounded-`longestEffectiveRange`/O(1) wording: caret lookup now asks for the complete live folded attribute run across storage so arbitrarily long destinations cannot strand the caret; it stays cheap because it is a single existing-attribute-run query, not a source scan or parse. The App selects `.inlineFoldRevealWithLinkFolding` only when the Experimental flag, WYSIWYG layout, and mechanism-health gates already permit WYSIWYG; source-only/source+preview and kill-switch fallback are unchanged. Reference links, autolinks, and images remain raw, all presentation stays source-preserving, and stable/default promotion is unaffected and still governed by checklist §F/D.4. Alt: enabling before owner IME evidence, clamping selections, synthetic rendered copy, opening URLs on plain/⌘-click in v1, or expanding other constructs were rejected. |
| 2026-07-06 | Image thumbnails get a spec-first sub-gate; mechanism spike required before implementation | `docs/image-thumbnail-gates.md` defines gates I0-I10 for rendering `![alt](src)` thumbnails inside Experimental WYSIWYG only: local in-workspace raster files under the existing PR #24/#27 allowlist (PNG/JPEG/GIF/WebP ≤ 10 MiB, containment), no editor network I/O, remote/SVG/reference images stay raw. I0 is a blocking mechanism spike because `NSTextAttachment` implies U+FFFC while the backing string must stay canonical: the primary candidate places the attachment only in the projected `NSTextParagraph` (backing raw), with an overlay adornment view as fallback and the layout-fragment delegate rejected per the 2026-06-26 precedent. Async downsampled decode with a bounded cache must keep §12 typing latency intact. Spec/plan only: images remain raw text, no behavior or dependency change. Alt: implementing directly on attachments without the spike was rejected because U+FFFC leakage into copy/AX would violate B7/raw-AX invariants discovered only after the fact. |
| 2026-07-10 | Phase 3 workspace search uses an in-process Swift MarkdownCore matcher, not external ripgrep | Workspace content search’s pure text core is `TextSearchEngine` / `TextSearchQuery` / `TextSearchMatch` in MarkdownCore: synchronous literal matching with smart/sensitive/insensitive case, Unicode whole-word boundaries, UTF-16 `NSRange`s, one-based line numbers, and bounded grapheme-aware snippets. Named limits keep synchronous work predictable: queries are capped at 256 UTF-16 units, preview context at 1,024 UTF-16 units per side, and the later WorkspaceKit layer initially admits files up to 512 KiB because dense 1 MiB release probes crossed one second. If a match occupies only part of a source grapheme and reaching that side's grapheme boundary would exceed the context cap, the snippet preserves the exact match with an ellipsis on that side instead of copying the full pathological context. An external `rg` process was rejected for App Sandbox compatibility, deterministic CI without bundling an executable, and no new dependency; regex remains a later Phase 3 gate. This entry records only the MarkdownCore matcher architecture — WorkspaceKit orchestration and the Search sidebar are not claimed complete. |
| 2026-07-10 | Image-thumbnail I0 chooses a projection-only `NSTextAttachment`; Option A GO, Option B NO-GO | The internal-only I0 hook projects the one literal `![alt](fixture.png)` as one U+FFFC attachment plus equal-length U+200B padding inside an ephemeral `NSTextParagraph`; the backing string and raw UTF-16 offsets never change. STTextView gives the attachment run its ordinary 16 pt height, so a font metric matching the 48 pt image is applied only to the projected attachment character; this produces 16/48/16 pt sibling/image/sibling lines without imposing paragraph-wide height on wrapped visual lines, and preserves a nonzero viewport origin. All ten `WYSIWYGImageAttachmentMechanismSpikeTests` pass against live layout: projected shape, byte-identical backing, plain+RTF whole/partial/boundary copy, raw paste, raw `AXValue`/selected/range/attributed AX output, nested undo isolation plus reveal/edit restoration, standalone and wrapped geometry/viewport, bidirectional arrow snapping, and real-`NSEvent` attachment click/reveal. A projected-substring leak in `accessibilitySelectedText()` was fixed with a spike-only raw-backing adapter; no U+FFFC reaches storage, copy, or AX. The App and public presentation cases are untouched; images remain raw outside this hook and I1-I10 still block production. Alt: B overlay adornment is NO-GO as the chosen mechanism but its full matrix was not run because A passed; an overlay reserves zero TextKit height and would overlap the next line without a separate line-height fake. C layout-fragment ownership remains rejected because STTextView owns that delegate; D backing mutation remains rejected by source canonicality. |
| 2026-07-10 | Image-thumbnail I1 lands pure MarkdownCore image regions + eligibility; images stay raw in the editor | I1 model-only: `MarkdownInlineImageRegion` carries exact UTF-16 whole/alt/path/title/chrome ranges for supported `![alt](path)` forms (empty alt allowed; double-quoted titles include their quotes); reference-style images, angle-bracket destinations, empty `()`, and malformed/single-quoted titles return `nil` so they stay raw. `MarkdownImageThumbnailPolicy` evaluates a pure `MarkdownImageWorkspaceSource` value (no Core file I/O) against the shared `MarkdownImageAssetPolicy` (PNG/JPEG/GIF/WebP ≤ 10 MiB) and deterministic stay-raw reasons (remote/data/file schemes, no directory scope, outside workspace, unresolved path, unsupported extension, missing/oversize bytes). EditorKit’s existing visible-range Tree-sitter traversal (`image` / `image_description` / `link_destination` / optional `link_title`; rejects `link_label`) appends metadata-only `imageRegions` on `WYSIWYGFoldPlan` without a second parse pass and without any image fold kind or presentation attributes — images remain raw until I2+. PreviewKit `AssetURLPolicy` and WorkspaceKit `WorkspaceImageAssetStore` now read the Core allowlist/size constants so the later thumbnail load path cannot drift from the PR #24/#27 preview policy; layering requires the constants in MarkdownCore because PreviewKit/WorkspaceKit are higher layers. Alt: re-parsing images in WorkspaceKit/App was rejected (layering + drift); promoting images into fold candidates or presentation in I1 was rejected by the sub-gate order. |
| 2026-07-10 | Image-thumbnail loader lands in WorkspaceKit with shared containment, CGImageSource downsample, and LRU cache | `WorkspaceImageThumbnailProvider` (actor) resolves document-relative image sources under security-scoped root access, builds `MarkdownImageWorkspaceSource`, evaluates I1 eligibility, and returns a downsampled PNG bitmap + metadata for a later presentation layer (`WorkspaceImageThumbnailLoading` protocol keeps EditorKit free of WorkspaceKit imports). Containment is extracted to public `WorkspaceRootContainment` (symlink-resolving root prefix + document-relative `..` only while the final URL stays inside the root) and reused by `CompletionWorkspaceProvider` / `WorkspaceImageAssetStore` instead of a third copy. Decode uses ImageIO `CGImageSource` thumbnailing (`kCGImageSourceCreateThumbnailFromImageAlways` + max-pixel bound; GIF index 0 only) off the main actor; no `DispatchQueue.global`, no new packages. Cache is a byte-budgeted LRU keyed by resolved path + mtime + maxPixelSize with hit/miss/eviction/coalesce counters; mtime changes invalidate for a future I2 refresh path. I10 is checked with named security tests; I2 stays open (decode/cache only partial). Images remain raw in the editor. Alt: App-layer loading was rejected (sandbox + policy live in WorkspaceKit); full-size decode was rejected by the I2/I8 discipline; process `rg`/network was rejected by sandbox and I10. |
| 2026-07-10 | Phase 3 WS2 workspace search: exact content identity and bounded deterministic orchestration | Workspace search orchestration stays in WorkspaceKit (`WorkspaceSearchService` + pipeline). Every result carries `WorkspaceSearchContentFingerprint`: the 64-character lowercase hexadecimal SHA-256 of the exact searched `String`'s UTF-8 bytes plus its byte count, computed identically for disk and dirty-overlay text; the public pure `init(text:)` lets WS3 fingerprint an activated session, while snapshot metadata and caller session versions are not retained as content identity. Dirty overlays use a throwing validated collection that canonicalizes relative paths and rejects empty/absolute/traversing paths, dictionary key/path mismatches, and normalized collisions instead of selecting by iteration order. Plan-index reads publish in path order under `inFlightReadCount + bufferedCompletedReadCount <= maximumConcurrentReads`, with maximum concurrent/buffered/outstanding instrumentation and no text cache. Expected file problems remain capped typed skip details with exact skipped totals; unexpected producer faults emit exactly one `.failed(context, .unexpectedProducerFailure)`, success emits one `.completed`, and cancellation emits neither while releasing security scope and child tasks. The lossless `.unbounded` stream is request-finite: with `N` candidates, effective nonnegative global match limit `G = max(0, maximumMatchesPerQuery)`, skipped-detail cap `S`, and progress cap `M`, at most `min(N, G) + min(N, S) + min(max(N, 1), M) + 1` events are produced; defaults retain/emit the first 100 path-ordered skipped details and coalesce monotonic, deduplicated progress to at most 100 events using stride `ceil(N / M)` with a final value. App retains the raw snapshot plus monotonic `workspaceGeneration`, rejecting cancelled/stale reloads before apply. Ignore handling loads bounded root/nested `.gitignore` then `.ignore` rules with the documented subset (`*`, `?`, `**`, rooted/directory-only patterns, and negation), not full Git compatibility. WS2 adds no dependency and remains non-user-facing until WS3; replace and navigation remain deferred. Alt: snapshot/session metadata as content proof, collision winner rules, silently swallowed producer faults, replacement reads beyond the ordered window, lossy stream buffering, external `rg`, a text cache, and premature WS3 integration were rejected. |
| 2026-07-11 | Image-thumbnail I2 presentation uses an EditorKit loader seam, fixed placeholder canvas, and path refresh hook | EditorKit defines its own `EditorImageThumbnailLoading` Sendable protocol and PNG/pixel-size/mtime outcome values, so it imports no WorkspaceKit; a later App adapter will map the real `WorkspaceImageThumbnailProvider`. The explicit internal development hook turns each untouched eligible image region into one projection-only U+FFFC attachment plus equal-length U+200B padding, with run-local font metrics and a fixed editor-width-bounded canvas capped below 300 pt; loading, missing, failed, and corrupt outcomes use the alt-text placeholder in that same canvas, so ready swaps preserve unrelated line geometry, selection, and scroll. Requests coalesce by source/pixel bound, text/document generations cancel and stale-drop completions, and `EditorImageThumbnailRefreshProxy` invalidates workspace-relative paths and reprojects only affected paragraphs so WorkspaceKit's path+mtime cache can decode changed files. The ten I0 invariants now run on parsed regions, including multiple images beside a folded link; raw backing/copy/AX remain canonical and the attachment image carries the alt accessibility description. App, the public `.inlineFoldReveal` / `.inlineFoldRevealWithLinkFolding` cases, dependencies, and user-facing behavior are untouched; I2 is checked, while I3-I9 remain open (I7/I8 evidence is partial). Alt: importing WorkspaceKit into EditorKit, changing attachment bounds after load, awaiting I/O on the typing path, retaining the hardcoded spike marker, or wiring FSEvents/App enablement in this PR were rejected by layering, geometry, latency, and scope. |
| 2026-07-11 | WS2 global truncation preserves terminal accounting; cancellation is Task-state based | Once a readable file proves the global match limit exceeded, `WorkspaceSearchService` enters an explicit accounting-only phase: it makes no further MarkdownCore match calls or file-result events but drains every remaining path-ordered plan item through the unchanged bounded read window, preserving exact readable/ignored/skipped counts, capped skip details, read instrumentation, final `N / N` progress, event ordering, and the existing event bound. `CancellationError` is silent only when the producer Task is actually cancelled; a reader-thrown instance while the producer remains active emits exactly one unexpected-failure terminal, and private pipeline invariant failures no longer impersonate cancellation. The existing `AsyncStream` API remains: early termination requires explicitly cancelling the consuming Task, and future WS3 App lifecycle work must retain and cancel that Task rather than relying on `break` or abandonment to invoke `onTermination`. No WS3 gate is closed. Alt: cancelling the read group at the cap was rejected because it loses terminal accounting; treating the error type alone as cancellation was rejected because readers can throw it independently; a new public operation/sequence type was deferred outside this focused fix. |
| 2026-07-11 | WS3A editor navigation is document-aware, monotonic, cancellable, and exact-range only | EditorKit owns `EditorDocumentIdentity`, `EditorNavigationRequest`, `EditorNavigationCommand`, and a coordinator-local pending state machine. Navigation and cancellation share one monotonic `UInt64` high-water mark: a newer navigation supersedes older work, a newer cancellation clears pending navigation plus retry/deferral tasks without effects, and repeated/older commands are idempotently ignored. Optional document identity equality is not installation proof. Each representable update prepares a candidate generation without changing an already-installed binding; only after marked text ends and the candidate's literal UTF-16 code-unit sequence is installed are its model binding, optional identity, and installed generation replaced together. Candidate-specific selection, styling, focus, and navigation stay disabled until that install succeeds, so two default/nil-identity representables cannot redirect an in-flight IME commit. “Exact” means literal UTF-16 code-unit identity without Unicode normalization or Swift `String` canonical-equivalence equality. Raw UTF-16 ranges are validated with overflow-safe subtraction and rejected rather than clamped, and navigation selection flows through the existing WYSIWYG reveal binding without source or undo mutation. Existing `Int` focus-request tokens remain independent, and ordinary typing retains its String-only path with eager contiguous conversion only when a genuinely different raw source is published. App lifecycle, fingerprints, sidebar, tree sync, shortcuts, and refresh remain open WS3 work. Alt: optional-identity equality as a binding-swap gate, early rebinding during IME, sticky pending navigation without an ordered cancel command, reusing the clamping external-selection path or focus token, normalization, and byte-array comparison on the update path were rejected because they can corrupt another document, resurrect stale effects, silently change a stale range, conflate request lifecycles, or add avoidable allocation. |
| 2026-07-11 | WS3B makes workspace-search lifecycle and safe activation App-owned while UI stays deferred | `ExactSourceText` compares literal UTF-16 without normalization and gates every `DocumentSession`/App text transition. AppState owns the injected search stream, debounce, exact consumer Task, monotonic context, and immutable dirty overlays; replacement, generation invalidation, close/switch, empty clear, and teardown cancel that Task, while context plus task-token checks reject stale events and cleanup. Editor publication carries the exact binding/installation pair, App-owned base revision/raw source, and proposed source; the ordinary `Binding<String>` setter has no App mutation authority. App owns one writer installation per session, and install/activate/release/revoke/pending events stay installation-scoped and idempotent. Writer activation now includes the coordinator's exact `EditorDocumentSourceSnapshot`: for an authorized exact installation, an equal App-owned monotonic revision is the constant-time current-source proof, so the ordinary native mutation path scans neither App source nor native buffer; literal comparisons remain only on stale/rekey/rejection recovery paths. A stale coordinator is synchronized before retry without transferring writer authority. If only session metadata advanced the revision while the raw source/view remain exact (including same-session Save Copy rekey), EditorKit synchronizes and reacquires with the returned snapshot inside the same pre-mutation request; content-stale views still restore current source and reject that native event. Current-base publication keeps the allocation-light path without eager whole-buffer UTF-8 transcoding; the real App/source-contract/coordinator/native-view 1 MiB gate covers ordinary and re-entrant edits with zero current-path full comparisons and a local hard `<16 ms` budget (hosted timing remains informational under R15). Legitimately older marked-source publication uses literal UTF-16 three-way reconciliation so non-overlapping edits survive once, while unsafe overlap restores the coherent current view/installed snapshot/model, clears pending state, and releases writer ownership. EditorKit retains the newest complete IME-deferred transition candidate (generation, source snapshot, selection, identity, binding/lifecycle, and navigation), automatically retries after the old document's marked/pending source commits, and installs destination state once. Newer candidates or teardown supersede it; `EditorNavigationCommand.cancel` cancels only obsolete range navigation and never document installation. Contract-free candidates refresh source and the appropriate non-navigation selection from their live Bindings before retry. Installation-scoped pending source blocks save/autosave and makes clean-looking disk changes retained conflicts. Reload / Keep Mine intent can survive composition, retirement, and reactivation. A structured-concurrency Reload runs physical work off-main while one security-scoped lease covers no-follow preflight/open/read/postflight/retry; one stable descriptor supplies exact bytes, SHA-256, device/inode, size, and high-resolution mtime/ctime, and a race (including same-size restored-mtime writes) retries the entire operation. Exact session, retained location/proof, lifecycle, intent, and request tokens stale-drop obsolete results; missing/unreadable/invalid UTF-8 enters recovery without discarding local source or authority. Reload remains in-flight and save/autosave/writer eligibility stays fenced until every live exact coordinator has synchronized its installed snapshot and native buffer or revoked, after which immediate input applies once to the disk source and every mirror remains snapshot-fenced. Active and retired sessions share one session-owned exact retained-location/expected-identity registry: deferred conflicts, prompts, autosave, save, retirement, and recovery never dynamically re-resolve `session.fileURL`; validated Save Copy alone atomically rekeys it. Every write performs no-follow regular-file/expected-identity/other-session validation, so in-workspace and escaping symlink substitutions cannot overwrite their target. Retirement shares old-workspace authority until the final dependent resolves and preserves exact registrations/tasks/prompts. Missing/reappeared reconciliation, session-scoped `pendingExternalTexts`, authority-bound contained workspace identity, and WorkspaceKit physical-target preflight remain as previously specified. Accepted search activation cancels older range navigation before fallible work without cancelling a deferred document transition, validates destination/fingerprint/range before switching, transfers old autosave/statistics work only on success, then emits newer exact navigation; termination flush covers dirty warm/retired sessions. `WorkspaceWindow` stays headless integration only; WS3C, sidebar modes, shortcuts/focus, rendering/accessibility, ordinary edit refresh, and FSEvent refresh remain open. Alt: installation-only activation, whole-source activation scans, using navigation cancel as transition cancel, frozen Binding candidates, dynamically re-resolving owned paths, following symlinks for writes, main-actor/multi-lease Reload, metadata-only byte-stability claims, clearing Reload before native convergence, accepting stale AppKit mutation then parking it pending, manual second-render transition retry, cached deferred-Reload text paired with newer metadata, stale whole-string overwrite, eager authority release, trusting overlays before physical preflight, range clamping, abandoned stream iteration, and premature UI work were rejected. |
| 2026-07-11 | Image-thumbnail native gates: empty-alt AX fallback, marker-preserving highlight apply, O(1) source identity | Closing I3/I4/I6/I7/I8/I9 behind the existing internal `_developmentImageThumbnails` hook (no App wiring). Empty alt text exposes the deterministic accessibility description `"Image"` (same label as the visual placeholder chip). `applyHighlightedText` preserves `WYSIWYGImagePresentationMarker` attributes across `setAttributes` so a visible-range recompute does not force a full image-marker rewrite; the styled-text path uses `forceReapply: false`. Image presentation source identity uses UTF-16 length plus endpoint samples instead of `ExactUTF16Text`'s full UTF-16 walk (~35 ms on large-1mb.md), which was blowing the I8 ≤ 50 ms recompute budget. I5 (owner-run actual IME) and user-facing enablement remain open. Alt: shipping empty-string AX for empty alt, wiping markers on every highlight apply, and full-document UTF-16 equality on the apply hot path were rejected. |
| 2026-07-12 | Image thumbnails join only the off-by-default Experimental WYSIWYG mode after I0-I10 pass | PR #83 completes `docs/image-thumbnail-gates.md`: the owner-run real macOS gate selected `com.apple.inputmethod.TCIM.Pinyin` (`Pinyin – Traditional`) and `com.apple.inputmethod.TCIM.Zhuyin` (`Zhuyin – Traditional`) as composition-capable `TISTypeKeyboardInputMode` sources, then passed ready-thumbnail and loading-placeholder composition immediately before and after the image span for both input methods. The complete `WYSIWYGActualIMEEventGateTests` run executed 10 tests with 0 failures in 106.508 seconds, proving exact source preservation, marked-range caret containment, no premature commit, raw image source text during marked text, and presentation reapply after commit. App maps `WorkspaceImageThumbnailProvider` through the EditorKit loader seam and supplies `_developmentImageThumbnails` only when the Experimental flag, WYSIWYG layout, mechanism health, `.inlineFoldRevealWithLinkFolding`, and a contained folder-workspace document all agree; source-only/source+preview, kill-switch fallback, and single-file mode stay raw. The existing FSEvents/external-reload path invalidates changed allowlisted raster paths through `EditorImageThumbnailRefreshProxy`. Remote/SVG/reference/outside-root/oversized images remain raw; GIF is first-frame-only with no playback; no drag-resize, open-in-Preview action, dependency, `.xcodeproj` edit, or source mutation was added. Stable/default promotion is unaffected and remains governed by checklist §F/D.4. Alt: enabling before owner IME evidence, importing WorkspaceKit into EditorKit, granting sibling scope in single-file mode, remote/SVG/reference rendering, animation playback, resize/open-in-Preview controls, backing-string U+FFFC, or a second loader/dependency were rejected by the gate order, layering, sandbox, v1 scope, and canonical-source rules. |
| 2026-07-13 | WS3B hardening makes filesystem, Reload convergence, and search activation transactional | This entry supersedes the earlier WS3B wording that cancellation precedes fallible search activation, a fingerprint mismatch implicitly restarts search, or Reload / Keep Mine intent survives composition unconditionally. Each production search result carries the exact immutable `WorkspaceSearchFileAuthority` that supplied its content: retained canonical root authority and normalized relative path plus descriptor-sampled physical identity. App consumes this token, anchored-loads the still-expected identity, and prepares the destination's exact retained location/physical identity, source fingerprint, and exact half-open UTF-16 range before changing navigation, document, tree, prompts, or search/autosave/statistics/completion task ownership; failure preserves all prior state, while success alone cancels obsolete range navigation, commits, and emits the newer exact request. Choosing Reload or Keep Mine captures the exact source revision and disk-event generation at selection time. Native input already authorized before that click may settle; if it advances the source revision, the resolution stale-drops and restores the conflict prompt without losing accepted input. The choice crosses retirement/reactivation only while its captured source revision, disk-event generation, retained-location, and lifecycle tokens remain valid. Reload, Keep Mine, and watcher inspection use one session-owned immutable filesystem location. One retained root authority and one security-scoped lease cover the root-anchored component-by-component `openat(..., O_NOFOLLOW)` walk, one descriptor's filesystem I/O, postflight, and retry; the lease and descriptor are released before off-main UTF-8 decoding, cancellation-checked content proof and 64 KiB SHA-256 chunks, equality, and `TextStatistics` preparation. Search disk reads share descriptor validation and bounded reads, overlays require the same physical validation, and save/autosave/Save Copy write relative to an anchored verified parent without `Data.write(.atomic)` path reopening. Same-inode/same-size/restored-mtime rewrites are detected through content and high-resolution ctime evidence. An outside-workspace Save Copy uses a standalone immutable parent/leaf authority and fails closed if it cannot establish or revalidate that authority; atomic writes depend on macOS `renameatx_np` `RENAME_SWAP`/`RENAME_EXCL`, and unsupported filesystem semantics fail closed rather than using check-then-reopen. Reload application carries immutable source, hash, exact-equality result, and statistics; writer activation, editor publication, save/autosave, and Save Copy stay fenced through all-live-mirror convergence, so native input is rejected before mutation rather than accepted then reset. EditorKit treats replacement ranges as half-open, merges exact lower/upper-boundary insertions without Unicode normalization, and rejects only strict-interior conflict. Selection-only closing-delimiter skip changes only the caret. The 1 MiB production gate hosts public `MarkdownEditorView` through scheduled SwiftUI updates for ordinary input, re-entrant pairing, and multiple marked-text updates, requiring zero App/native full scans and the existing local `<16 ms` budget. Editor/tree identities use captured retained location and physical identity rather than re-resolving mutable paths. WS3C, sidebar modes, shortcuts/focus, rendering/accessibility, ordinary-edit search refresh, and FSEvent search refresh remain open. Alt: URL-reconstructed result authority, final-component-only no-follow, check-then-reopen or nonexclusive writes, mtime-only unchanged, mutable-session Reload finalization, main-actor full-source work, accepting then discarding AppKit input, selection-only publication, pre-validation navigation cancellation, implicit mismatch refresh, boundary-inclusive overlap, and internal-representable-only performance evidence were rejected. |
| 2026-07-13 | WS3B watcher, write, and lifecycle fences bind to retained authority and exact disk content | A workspace watcher event starts coherent inspection for every unique current, warm, and retired session whose retained location belongs to its immutable root authority before the tree scan, without claiming the still-open active-search FSEvent refresh. Reload / Keep Mine selection supersedes an older initial inspection; retirement, reactivation, and lifecycle invalidation restart an otherwise-current initial or partially converged resolution so the session cannot remain stranded behind a stale fence. Ordinary save and autosave now require both expected device/inode identity and the last coherent SHA-256 baseline, hash the same no-follow target descriptor, resample metadata immediately before swap, then capture the actual displaced no-follow entry and verify it plus the held descriptor after `RENAME_SWAP`; a same-inode rewrite or atomic regular, directory, or symlink replacement in that final window restores the displaced object through a verified reverse swap before refusal, while earlier changed content enters coherent inspection. Search activation rejects duplicate physical ownership, including hard-link collisions. Metadata cleanup consumes the already-retained exact state key and recovery retains captured authority even if an intermediate path becomes a symlink. Marked-text commits distinguish the initial selected span from the delegate-confirmed explicit replacement location; rejected/selection-only mutations and direct unmark cannot leak that provenance into another composition. Alt: waiting for the tree scan before session inspection, identity-only atomic replacement, unchecked post-validation swap, raw restart without lifecycle revalidation, mutable-path key reconstruction, and selection-derived explicit replacement were rejected because they can overwrite external bytes, strand autosave, duplicate session ownership, clear the wrong metadata, or commit IME text at an unrelated caret. |

| 2026-07-13 | Phase 3 WS3B filesystem authority and write outcomes stay in WorkspaceKit | `WorkspaceFileSystemRootAuthority` binds one captured physical root identity to lexical relative locations. Reads and writes retain the complete no-follow descriptor chain from `/` through the root and final parent, validate every parent/name identity across the final postflight boundary, and reject a moved/replaced root or ancestor instead of returning detached bytes. Existing writes use same-directory prepared files plus `RENAME_SWAP`; missing creates use `RENAME_EXCL`; file and directory durability, rollback, and cleanup are deterministic fault boundaries. `WorkspaceFileWriteOutcome` distinguishes proven non-commit, committed-and-durable, and committed-but-indeterminate so a caller never receives an ordinary failure when new bytes may be visible. Search results carry exact descriptor-derived location/identity authority for a later App consumer. No AppState, EditorKit/MarkdownCore ownership, sidebar/UI, rename/move/Trash, session relocation, or dependency change is included. Alt: resolving mutable URLs after containment, closing ancestor descriptors, blind atomic-write errors, overwriting a racing create, deleting rollback material before directory durability, and treating rollback uncertainty as failure were rejected. |
| 2026-07-14 | Phase 3 WS3B binds search and writes to one exact, retained filesystem authority | This entry **supersedes** the 2026-07-13 row's "No AppState" scope statement. App reload captures and generation-fences a proven snapshot/authority pair off-main. Optional auto-open derives selected location, anchored loading, and cache/session validation through `capture.rootAuthority` before activation plus snapshot, authority, installed generation, and tree are committed without a main-actor suspension; a post-proof replacement or activation mismatch installs none of capture A. This reload lifecycle activation is distinct from still-deferred workspace-search-result activation. Root capture opens the selected directory once under its security-scoped lease, derives identity and canonical spelling from that descriptor, retains it in a reference-backed Sendable lifetime token, checks cancellation before and after construction, and normalizes post-capture binding loss/replacement to `namespaceChanged`; an old capture is never labeled with a newer workspace generation. Prepared writes start `0600` on `O_RDWR`, truncate, fsync, and repeatedly verify exact byte count plus SHA-256 through postcommit. Every rollback path, including `.afterRenameSwap`, first proves the displaced entry is the writer-owned original; an observed mismatch is not reverse-swapped and returns `committedButIndeterminate` with indeterminate artifact state rather than `.retained`. Cleanup uses a random same-directory staging name, not private isolation, never restores that mutable name into the destination, and exposes hooks at the actual final validation-to-rollback-swap, staging-rename, and staging-unlink boundaries. macOS provides no expected-identity condition for `renameatx_np`/`unlinkat` and no regular-file `funlinkat`, so residual validate-to-commit-rename, rollback-swap, cleanup-rename, and cleanup-unlink name races are documented; observed mismatches fail closed and report indeterminate state rather than being described as atomic safety. `WorkspaceSearchRequest` removes the legacy independent `rootURL` initializer as an intentional Phase 3 public API break; callers migrate to a pre-captured `rootAuthority`. Headless WS3B remains open until workspace-search-result activation is integrated and PR #82 is restacked; the existing `m3-` branch name is a historical exception. No WS3C, sidebar, shortcuts, rename/move/Trash, PR #84, dependency, or ordinary-edit/FSEvent search-refresh work is included. Alt: independent request roots, mutable-URL recapture after proof, partial capture installation, rollback through an observed unexpected displaced entry, describing a random sibling as private isolation, restoring a mutable cleanup name, claiming name-based operations identity-atomic, labeling old captures with new generations, and claiming WS3B complete before search-result activation/restack were rejected. |
| 2026-07-14 | WS3B retains exact filesystem authority through activation, completion, and writes, and proves the destination before non-commit | This entry supersedes the preceding row's deferred workspace-search-result activation statement. Reload loads selected bytes plus descriptor identity and exact-byte digest through `capture.rootAuthority`, reconciles cached current/warm sessions from that activation file, then repeats selected-root proof after the suspending load immediately before the uninterrupted session/snapshot/authority/generation/tree commit. A current-session/state-location change cancels that publication, and cache/retirement arbitration is repeated after the final proof so a stale source cannot be resurrected or trip a commit precondition; independently exercised cached and retired reuse also requires any retained binding or exact re-homed quarantine location to equal the prepared activation location, so URL equality cannot rebind A to replacement B. Search-result activation consumes retained `WorkspaceSearchFileAuthority` and postchecks the installed binding location; file-tree/sidebar open, completion sibling reads, external reconciliation, and Cmd-S/autosave likewise retain the installed location/authority rather than resolving a mutable post-install URL. Dirty overlay collection excludes an anchored cached session unless its retained root authority equals the installed request authority, so dirty A cannot enter B after A moves and B takes its spelling; completion under the same binding or context-only authority mismatch falls back to current-document-only metadata and reads no B siblings. Reload rejects a previous-authority location that lexically collides inside B, but treats an outgoing retained A location outside unrelated B as unrelated so an ordinary workspace switch still installs B's first editable file. These boundaries close A-to-B redirection paths without claiming identity atomicity: an external namespace change can still race after the final proof, and later anchored operations must detect it and fail closed. Save Copy inside the installed workspace uses typed anchored `existingOrMissing` after inspecting the target from its anchored parent descriptor. The inspection derives descriptor-canonical parent/leaf spelling and entry identity without content-read permission; App rejects noncanonical spelling, physical-identity collisions, and canonically equivalent locations owned by current, cached, retired, editor-bound, or context-only quarantined sessions. Parent `fpathconf(_PC_CASE_SENSITIVE)` controls case folding, so case aliases are blocked on insensitive filesystems while distinct case spellings remain valid on sensitive volumes. Only the source session is cleared after durable success. Any anchored `committedButIndeterminate`, including Save Copy, retains the exact destination/digest in a per-session quarantine: readable destinations use Reload/Keep Mine, observed-missing destinations retry exclusively with `.missing`, and the same destination always reuses that exact context across workspace close or A/B replacement; while quarantine exists, any different spelling requires reconciliation so case/normalization aliases or an outside-workspace symlink cannot fall through to a broad anchored or legacy URL write. The re-homed context is also the exact session-state key, so cleanup cannot follow a replacement symlink into another session. Cmd-S/autosave or another blind `.existingOrMissing` cannot proceed. Every path that cleans an artifact before `notCommitted` now re-proves the original existing destination or expected absence plus its retained namespace; proof or inspection uncertainty becomes `committedButIndeterminate` with truthful prepared metadata/recovery state. Artifact observation is tri-state, `.none` requires post-unlink sync and namespace-bounded observation, and cleanup borrows an existing writer descriptor or falls back to `O_EVTONLY` so write-only/no-access modes remain inspectable. App preserves every non-`none` durable cleanup or noncommit artifact as an exact authority-backed notice: proven commits still adopt metadata and finish clean/re-home before presenting “File Saved; Cleanup Required,” while noncommits remain dirty. The legacy URL facade maps those states to explicit committed-cleanup/noncommit-cleanup errors, and both App legacy paths consume committed-cleanup as success rather than quarantine or unwritable failure; automatic later deletion is rejected because artifact state carries no expected identity. `committedAndDurable` means the required host `fsync` calls and validations completed, not `F_FULLFSYNC` or a physical sudden-power persistence guarantee. Alt: cache-hit early return, URL-only cached/retired arbitration, stale post-suspension sources, cross-authority dirty overlays or completion reads, blocking an unrelated workspace switch, cached-only exact-URL Save Copy ownership, unconditional case folding, context-free or differently spelled quarantine retry, mutable-URL post-install handlers, location-free activation postchecks, blind save or Save Copy retry, destination-unproven non-commit, discarded cleanup telemetry, unsafe artifact auto-delete, `try?`-collapsed cleanup absence, `O_RDONLY` inspection, and physical-media durability claims were rejected as authority, recovery, or contract violations. |
| 2026-07-14 | PR #85 closes only the WS3B filesystem-authority and write sub-gate; PR #82 remains the Draft headless lifecycle workline | Initial Save Copy reuses one no-follow inspection and writes only with exact `.existing(identity)` or `.missing`; outside final-leaf symlinks cannot fall through to legacy saves, and descriptor identity rejects hard-link ownership across anchored and unanchored sessions. Standalone ordinary save and all Save Copy paths consume typed outcomes through one retained location. Any indeterminate commit quarantines the exact location, result, and prepared digest and blocks all later writers until reconciliation. Retirement authority follows the retained binding/context plus installed authority rather than mutable root containment; real initial/final root-proof failures remain typed errors. Cleanup notices survive workspace close and require explicit acknowledgement, which releases retained authority without deleting artifacts. This correction does not claim headless WS3B complete: PR #82 is still the Draft multi-window lifecycle hardening PR and must be restacked onto #85. Visible sidebar, shortcuts, refresh, WS3C, WS4, and overall DoD remain open. Alt: broad first-attempt expectations, symlink-following URL fallback, inode-blind ownership, quarantined retries, mutable-root lease decisions, cancellation-masked proof errors, automatic notice deletion, calling #82 a sidebar branch, or declaring WS3B complete were rejected. |
| 2026-07-14 | PR #85 retains unanchored ownership proof and gives every indeterminate destination one exact reconciliation route | This entry **supersedes** the preceding row's clause that quarantine blocks all later writers and tightens the two preceding rows' unanchored-ownership wording: when an unanchored session becomes App-managed, App retains its descriptor-derived location/identity and never reconstructs ownership from mutable `session.fileURL`; absent proof, deletion, replacement, or inspection failure fails closed. Save Copy exempts only the source session's self-collision when its byte-for-byte original path is proven missing, so recovery can create there with exact `.missing` while another session, an existing alias, or a hard link remains protected. Every `committedButIndeterminate` session is re-homed to its retained location and authority. A readable regular file exposes Reload/Keep Mine; proven absence exposes only an exact-spelling `.missing` recovery; symlink, non-regular, unreadable, and inspection-failure states stay quarantined but expose Check Again, which reclassifies only that retained location. Cmd-S/autosave, broad or differently spelled retries, authority substitution, and legacy URL fallback remain blocked. Deterministic App regressions cover anchored and unanchored original-path recovery, unlinked/replaced unanchored hard-link ownership, unavailable proof, exact missing retry refusal for a different destination, and every readable/missing/invalid quarantine state. PR #85 stays Draft; PR #82, WS3C/sidebar, stable/default promotion, and all other deferred gates are unchanged. Alt: source-wide ownership exemption, mutable-URL `try?` recovery, fail-open proof loss, all-writer dead-end quarantine, any-spelling retry, invalid-state overwrite, and unrelated scope expansion were rejected. |
| 2026-07-14 | PR #85 exact quarantine spelling is retained, not re-inferred from the current leaf kind | Hosted CI exposed Foundation adding a trailing `/` when the retained invalid leaf became a directory, even though the location was constructed as a file. `WorkspaceFileSystemLocation` now captures its file URL spelling once when the authority plus normalized relative path are installed, while equality and hashing remain authority/location based; the same retained location therefore cannot change its session key, prompt spelling, or later Reload/Keep Mine binding as the leaf moves between missing, directory, and regular-file states. A WorkspaceKit regression creates a directory at a retained file location and proves the URL value and absolute spelling remain unchanged; the App non-regular quarantine regression exercises the end-to-end state. This entry supplements, and does not weaken, the preceding row's exact-spelling retry rule. Alt: trimming slashes only in the test, treating directory spelling as equivalent during retry, or re-deriving the URL after repair were rejected because each hides authority-key drift. |
| 2026-07-15 | PR #85 makes retained standalone proof, Save Copy ownership, and indeterminate retention authority-exact end to end | A managed standalone session now retains its lexical location, descriptor identity, exact loaded SHA-256 digest, and installed-workspace membership when present. Cmd-S/autosave writes only through that proof with `existingContent`; deletion, replacement, changed content, or unavailable proof fails closed. `WorkspaceFileSystemLocation.fileURL` is built lexically without consulting leaf kind, and equality/hashing plus exact App spelling gates compare UTF-8 bytes so equal locations have one slash-free URL while NFC/NFD remain distinct. Save Copy exempts its source only for a proven-missing destination with the same retained authority/location, and rejects canonically equivalent App-visible paths across different authorities for cached, retired, editor-bound, and quarantined owners. `committedButIndeterminate` protects the exact session/context independently of dirty state across LRU eviction, editor retirement/metadata cleanup, missing close, workspace close, and switch. Retirement transfers security scope only from captured binding/context or standalone installed-workspace membership, never by reopening an unlinked or replaced current path. Deterministic regressions cover replacement inode and same-inode content change, unavailable proof, A-to-B same spelling, NFC/NFD, cross-authority ownership, directory-at-construction location spelling, stable session state, every clean-quarantine lifecycle exit, and unlinked/replaced retirement. PR #85 remains Draft; PR #82 and WS3C/sidebar scope are unchanged. Alt: identity-only standalone writes, Swift canonical-equivalent `String ==` for exact spelling, URL-only cross-authority ownership, dirty-only quarantine retention, and retirement-time path inspection were rejected because each can redirect, overwrite, or discard state outside retained authority. |
| 2026-07-15 | PR #85 external-change arbitration precedes proof adoption and literal spelling stays byte-exact | Every anchored or standalone external notification now takes one coherent retained-authority read and compares literal location, descriptor identity, and SHA-256 before a proof can change; mtime/FNV, mutable URL recapture, and cache/retirement reuse cannot authorize adoption. A dirty unaccepted same-inode rewrite or replacement inode records a session conflict, cancels autosave, retains the prior proof, and blocks Cmd-S until explicit Reload or Keep Mine obtains a fresh accepted observation. A pending/detached/indeterminate retained A also blocks a same-spelling replacement-parent B before it can inherit or clear A's URL-keyed state, including an awaiting retired binding. Root capture, root-relative recovery, standalone capture, and LRU keys no longer round-trip NFC to NFD; non-file/NUL raw paths are rejected before C-string APIs while containment remains descriptor-bound. Descriptor-parent aliases share a cache key without changing an NFC leaf, so exact missing-source Save Copy reuses NFC authority/location and refuses NFD. Deterministic anchored/standalone, cached/retired, true-mtime-preserved, NFC/NFD, and alias regressions cover these rules. PR #85 stays Draft and scoped to WS3B; PR #82 and WS3C/sidebar remain untouched. Alt: adopting before arbitration, identity-only/mtime acceptance, a dirty conflict that updates its proof, standardized URL cache keys, or broad Unicode recovery were rejected because each can overwrite external bytes or weaken retained authority. |
| 2026-07-15 | PR #85 uses UTF-8 path identity throughout search/tree and makes Keep Mine a complete reconciliation | `WorkspacePathByteKey` now backs candidate deduplication/order, dirty-overlay lookup, ignore-ancestor enumeration, file-tree grouping/visibility, and namespaced fallback node IDs, so canonically equivalent NFC/NFD paths remain independent even when a snapshot has no resource identity. A missing scanner identity remains absent instead of silently reintroducing a canonically equivalent String fallback. Root-relative extraction chooses the longest exact-byte matching canonical/original prefix, preserving an original symlink nested beneath the canonical root. Keep Mine adopts the newest fresh observation, removes matching detached/missing fences, and restores anchored or standalone delete/recreate saving; when a clean quarantined session's local source differs from observed disk bytes, its session/LRU becomes dirty before autosave. Deterministic synthetic search/overlay/ignore, tree, root-spelling, and App regressions cover all boundaries. PR #85 remains Draft; PR #82, sidebar/WS3C, and stable/default scope remain unchanged. Alt: Swift `String` path keys, canonical-root-first extraction, raw-path scanner fallback identity, clearing only the visible conflict, or scheduling autosave while a byte-different local session remains clean were rejected because they can omit files, cross-apply overlays/children, reject valid original-spelling operations, or leave local and disk content falsely reconciled. |
| 2026-07-15 | PR #85 makes fresh conflict baselines and every bounded sibling path decision byte-exact | Keep Mine now atomically adopts fresh disk C as `DocumentSession`'s saved-text baseline while preserving the installed local source, version, and text-change stream; exact C becomes clean, while old byte-distinct A remains dirty even when Swift considers A/C canonically equivalent. Anchored and standalone cached or reusable retired sessions remain detached when switched away, but reopening a recreated leaf now passes retained authority/location checks and reaches external arbitration instead of failing before Reload/Keep Mine can appear; detachment forces that explicit resolution even if A's exact identity/digest is restored, because its saved baseline was invalidated. Ignore glob literals compare UTF-8 bytes while retaining existing wildcard semantics, and completion excludes the current file plus orders the bounded 50 sibling reads through `WorkspacePathByteKey`. Deterministic regressions cover A/local/C baseline transitions, cached cross-file delete/restore and retired same-proof recovery for both authority modes, NFC/NFD ignore literals, and a canonical-equivalent completion sibling at the read cap. PR #85 remains Draft; PR #82, sidebar/WS3C, shortcuts, dependencies, and stable/default scope remain unchanged. Alt: transient `markSaved(C)`/restore-local publication, keeping baseline A, clearing or ignoring detached during activation, Swift `Character ==` for literal glob text, or canonical-equivalent completion filtering/sorting were rejected because they can publish false editor state, misreport dirty status, strand recoverable sessions, or suppress byte-distinct paths. |
| 2026-07-15 | PR #85 gives ignore-glob wildcard tokens precedence over identical filename bytes | `WorkspaceSearchGlob.matchesComponent` now recognizes pattern `*` before literal UTF-8 equality. A filename byte that is itself `*` is therefore consumed by wildcard backtracking rather than consuming the pattern token as a literal; `*.md` correctly matches `*draft.md`. The existing exact-byte literal rule and grapheme-based `?`/`*` behavior remain unchanged, with a synthetic regression covering the leading-literal-star filename. PR #85 remains Draft and scoped to WS3B; PR #82, WS3C/sidebar, shortcuts, dependencies, and stable/default scope remain unchanged. Alt: literal-first matching or merely special-casing the reported filename were rejected because either loses the wildcard backtracking anchor or fails to enforce token precedence generally. |
| 2026-07-15 | PR #82 review hardening closes activation and publication arbitration without expanding headless WS3B | Search-result activation now sends a cached or retired destination's coherent retained-file observation through external-change reconciliation before adopting its identity/digest proof: dirty local L over accepted A encountering a same-inode disk B retains A's proof, records B as the conflict, cancels autosave, and blocks save until explicit resolution. Toolbar commands, completion, smart paste, and image insertion share one writer-activation preflight before any native text mutation, while selection-only operations bypass writer acquisition. Stale exact-source three-way reconciliation accepts only an unambiguous literal UTF-16 alignment and rejects repeated-source alignment ambiguity instead of silently relocating an edit. Authorized `DocumentSession` publication treats an exact no-op as no-op and recomputes dirty state against the persisted baseline, so undoing to that baseline becomes clean. `EditorDocumentSourcePublication` equality is explicitly literal rather than Swift canonical `String` equality, preserving NFC/NFD-distinct raw source. This entry **supersedes** the 2026-07-11 and 2026-07-13 wording that the ordinary/current editor path performs zero full source comparisons or scans: writer activation remains revision-proven and its App/native comparison counters remain zero, but authorized session publication intentionally uses optimized Foundation literal equality for the exact no-op and persisted-baseline checks; the production-representative 1 MiB gate measures those paths and retains the local hard `<16 ms` budget. PR #85 is merged, Draft PR #82 is restacked directly on that baseline, and `Packages/WorkspaceKit` has no diff; the plan and PR wording are synchronized without adding WS3C, visible sidebar/UI, shortcuts/focus, ordinary-edit/FSEvent search refresh, dependencies, or stable/default promotion. Alt: proof adoption before arbitration, mutate-then-restore programmatic edits, greedy repeated-text alignment, unconditional version/dirty publication, canonical-equivalent source equality, preserving an inaccurate zero-scan claim, or absorbing deferred UI/refresh work were rejected because they can overwrite external bytes, mutate from a stale writer, corrupt exact source, misreport persistence state, or blur the gate boundary. |
| 2026-07-15 | PR #82 follow-up binds pending source, search observations, and async image side effects to exact lifecycle authority | This entry **extends and supersedes the narrower arbitration/preflight wording in the preceding PR #82 row**. Search-result activation treats exact pending editor/native source as local authority even when `DocumentSession` still appears clean, so a differing disk observation enters conflict arbitration instead of replacing retained proof or unpublished source. Every coherent search activation observation advances the session's external disk-event generation, whether its identity/digest matches or differs, and thereby supersedes stale initial inspection, Reload, and Keep Mine work; a still-valid explicit resolution intent is preserved or restarted only from a fresh coherent read under the new generation, never finalized by an older asynchronous result. Async image insertion acquires writer authority before any asset side effect and retains the exact binding/installation-scoped authorization across its suspension, so an initially rejected window creates no asset and a later supersession cannot publish through another installation. App placement uses exclusive staging/publish plus descriptor-bound identity, size, and digest receipts; rollback moves acquired leaves out of their publish names and surfaces retained recovery data without touching an unproven namespace occupant. PR #85 remains merged, Draft PR #82 remains directly restacked on that baseline, and `Packages/WorkspaceKit` has no diff. This follow-up adds no WS3C, visible sidebar/UI, shortcuts/focus, ordinary-edit/FSEvent search refresh, dependency, or stable/default work; CI and timing evidence are intentionally not claimed by this row. Alt: using only the dirty bit as local-authority evidence, leaving equal observations generation-neutral, allowing stale resolution work to consume intent, performing asset effects before installation-scoped authorization, overwriting a race winner, or path-only cleanup were rejected because they can discard unpublished input, adopt stale proof, resolve against old bytes, leak files from a non-writer window, or delete unrelated image bytes. |
| 2026-07-16 | PR #82 makes rejected observations and image placement authority-complete | This entry **extends the preceding PR #82 follow-up**. Once search activation has coherently read a reusable cached or retired session's retained location, it first supersedes older disk work and then either reconciles and adopts the exact observed identity/digest or records a conflict while preserving the prior proof; later fingerprint/range rejection can still block navigation but cannot erase disk C, leave writable proof A, or strand retirement cleanup. Image placement captures the managed session's retained document location and identity plus the descriptor identities of every workspace-root-to-document-parent component before suspension, and it rejects a moved/replaced parent even when a replacement path hard-links the old document inode. The asset-directory lease likewise proves its complete component chain still names the leased terminal descriptor immediately before and after exclusive publication; relocation fails closed and a post-publication failure rolls back into a surfaced recovery name without returning a false Markdown path. File-based image insertion keeps the supplied literal URL through containment and no-follow validation instead of letting Foundation rewrite descriptor-canonical `/private/var` through the `/var` symlink. EditorKit compares image context IDs with literal exact-source equality at both generation and commit fences, so canonically equivalent NFC/NFD paths supersede delayed work instead of sharing authority. Draft PR #82 remains headless and directly based on merged #85 with no `Packages/WorkspaceKit` diff; WS3C, visible sidebar/UI, shortcuts/focus, ordinary-edit/FSEvent refresh, dependencies, and stable/default promotion remain deferred. Alt: cancelling the only inspection before discarding C, treating navigation rejection as observation rollback, revalidating mutable `currentFileURL`, trusting only the document inode across a replaced parent, trusting an open directory descriptor after namespace relocation, resolving an exact source URL back through a symlink before `O_NOFOLLOW_ANY`, or Swift canonical `String ==` for context identity were rejected because they can hide external bytes, overwrite a newer file, publish assets into a displaced namespace, reject a valid retained spelling, or commit delayed Markdown in the wrong exact source. |
| 2026-07-16 | PR #82 orders physical collisions before observation retirement and revalidates image placement at Markdown commit | This entry **extends the preceding PR #82 image/search authority row**. A coherent search read showing that reusable cached or retired B now names A's already-owned inode first detaches and save-blocks B, then advances B's disk-event generation and cancels its old inspection; cancellation propagates through detached capture/read/preparation work and a late B replay cannot restore stale proof/source, while a newly loaded candidate remains side-effect-free. Image insertion now returns a retained placement receipt covering the exact document authority, each created asset's directory/leaf/content proof, and each existing in-workspace file reference. EditorKit keeps the installation-scoped writer lease across asynchronous placement and commit validation, rechecks literal context/source after validation, and performs the Markdown mutation without another suspension; invalid authority discards the transaction without publishing a path. The document authority is captured before a coherent read, post-validated, and carried into binding adoption without reopening the path; a reusable session can advance only through the same retained parent lineage, and a cache miss stays fail-closed. Durable atomic save rebinds the new leaf through the retained parent descriptor, while Save Copy captures the destination parent before the writer and binds/installs the durable leaf before adoption. SwiftUI document-binding and image-inserter reads consume only retained proof/cache state and never perform security-scope/open/fstat work from body. Workspace-local file assets capture descriptor authority before reading or hashing and validate it through publication. Commit validation runs off-main and performs a final all-namespace pass after hashing; cleanup proves the published leaf identity before rename, then fsyncs and surfaces every acquired entry under a visible `Plainsong-preserved-...` recovery name because Darwin cannot atomically unlink a prior identity proof. Draft PR #82 remains headless with `Packages/WorkspaceKit` unchanged; WS3C, visible sidebar/UI, shortcuts/focus, ordinary-edit/FSEvent refresh, dependencies, and stable/default promotion remain deferred. Alt: cancelling B's only observation before collision accounting, path-only placement results, validating only created assets, rebuilding authority from a body-time URL or at adoption, capturing file authority after reading, committing after an unvalidated await, or hiding/moving an unproven replacement without recoverable evidence were rejected because they can strand B, publish a stale Markdown reference, block the UI, or lose unrelated bytes. |
| 2026-07-16 | PR #82 closes the final cleanup and durable-adoption authority windows | Image rollback never performs check-then-unlink: after descriptor-relative exclusive rename it opens and validates the acquired entry, fsyncs the retained directory, keeps the entry under a visible recovery name, and reports the descriptor-current path after directory moves; an unavailable path is reported with retained identity and leaf hint rather than a mutable captured URL. A deterministic post-validation replacement race proves neither the exact created bytes nor the replacement is deleted. Save Copy treats a durable write whose retained-parent leaf bind fails as committed-but-unadoptable: the exact session is re-homed dirty into reconciliation, autosave and further writes stay fenced, durable metadata plus cleanup authority remain retained, and any cleanup notice survives Reload/Keep Mine. Draft PR #82 remains headless with `Packages/WorkspaceKit` unchanged and no WS3C, visible UI, shortcut, refresh, dependency, or stable/default expansion. Alt: preserving the final `fstatat`/`unlinkat` window, reporting the captured assets URL after its directory moved, treating a durable commit as uncommitted, or dropping cleanup state on bind failure were rejected because they can delete unrelated bytes, direct recovery to the wrong namespace, or lose committed/recovery evidence. |
| 2026-07-16 | PR #82 fails closed on cleanup racers and surfaces every retained artifact | This entry **extends and supersedes the cleanup ordering in the preceding row**. Cleanup fsyncs the descriptor-relative recovery rename before opening or validating it. Because Darwin cannot atomically bind that rename to a returned descriptor, every successful recovery rename reports an unknown/unavailable rename-acquired artifact and separately reports any current occupant—including a later hard link to the known created inode—from its own retained snapshot/descriptor; the same split applies after an `fsync` failure. It never attempts a second rename: `RENAME_EXCL` protects only the destination, so a check-then-restore could move a replacement source into the original name. The retained created-asset descriptor is reported separately whenever its inode remains linked elsewhere, including when the published leaf is already missing. Symlink/directory/current-racer paths are reported only while the mutable leaf or retained descriptor proves that observed identity; a later occupant never supplies the unknown acquired artifact's path. Namespace and descriptor-link inspection use explicit present/missing/indeterminate states: only `ENOENT` or a successful `st_nlink == 0` proves absence, every other inspection error remains visible, and every successful recovery rename triggers a workspace refresh even if placement fails or the artifact then disappears. A failed placement that preserves an original or preflight-discovered artifact also carries refresh provenance even when cleanup itself made no further namespace mutation, because the normal successful-placement refresh was never reached. Committed-but-unadoptable Save Copy errors include retained and removal-indeterminate artifact paths in the alert in addition to retaining the durable notice. Draft PR #82 remains headless with `Packages/WorkspaceKit` unchanged and no WS3C, visible feature UI, shortcut, refresh, dependency, or stable/default expansion. Alt: claiming a post-rename lookup as atomic provenance, a non-atomic racer restore, opening before the recovery rename is durable, collapsing filesystem errors into missing, silently treating a moved created inode as missing, dropping rollback refresh state, reporting only one of two retained artifacts, adopting a replacement leaf's path, or relying on an unconsumed internal notice were rejected because they can move unrelated bytes, hide recovery evidence, skip necessary refresh, or misdirect the user. |
| 2026-07-16 | PR #84 rebuilds workspace creation, relocation, Trash, and durable recovery on retained authority | Draft PR #84 is rebuilt from current `main` instead of carrying forward its superseded URL-save, external-change, mtime, or hash authorization. Creation persists a planned operation before its first syscall. Empty files use exact-leaf anchored `openat(O_CREAT | O_EXCL | O_NOFOLLOW)` with no unjournaled writer temporary; folders use one caller-planned hidden `0700` `mkdirat` staging leaf and durably advance the same operation with its captured descriptor identity before publication. Planned restart auto-releases only when every recorded candidate is proven missing. Darwin has no create-and-return-directory-descriptor primitive, so a same-user watcher that predicts and replaces the staging leaf between `mkdirat` and the first open remains a documented platform residual; failures never name-based-unlink an unproven directory. Rename and move consume exact snapshot expectations, persist write-ahead source/destination/parent/session state, and return typed indeterminate state after any committed rename failure rather than attempting an unsafe name-based reverse rollback. Trash persists initial text plus one exact caller-planned root staging location before mutation, captures a verified CF file-reference URL, and invokes `FileManager.trashItem` with that identity reference; the opaque Foundation reference-to-mutation interval remains explicitly non-atomic. A reported Trash URL is display-only after restart: only a bookmark that resolves to the expected device/inode/kind can prove completion, while bookmark failure keeps recovery actionable. A durable App relocation commits current, cached, retired, and editor-bound ownership together: session/LRU keys, retained bindings, autosave and external-work generations, prompts and coherent observations, writer activation, and destination image authority move as one prepared transaction. Trash rejects dirty or pending-source owners, fences disk/image side effects without blocking native input, and preserves input accepted during the handoff as detached recovery. Operation/text records use temp-file fsync, atomic rename, directory fsync, and durable Recovery → Plainsong → Application Support directory-edge publication; remove and quarantine retries also fsync when the prior unlink/rename already took effect, and any remove error immediately rewrites the exact current record before returning. Standalone text-write failure synchronously advances its operation bundle, bundle removal is last, and promotion chooses the newest bundled, pending, restored, or live logical revision across every session. Corrupt stores globally fence file access and termination while leaving editor input in memory; explicit Stop Tracking atomically quarantines the unreadable directory without deleting its bytes. Global Check Again, Show/Keep Editor Copy, deterministic multi-copy promotion, and zero-session Stop Tracking prevent permanent quarantine. Quit is vetoed until pending editor source, in-flight mutation, persistence failure, or unresolved operation is safe; restored copies have unavailable ownership proof and cannot overwrite a replacement original. Validation is tracked against the exact Draft PR tip; review and merge remain maintainer-owned. Alt: transplanting PR #84's old URL logic, check-then-create, creating before a durable plan, deleting or reverse-moving after a mutable-name proof, treating a display URL as Trash authority, comparing canonical-equivalent strings, reopening mutable URLs, partially rekeying App maps, clearing the operation bundle before durable removal, accepting a corrupt store as empty, claiming name-based APIs are identity-atomic, or keeping recovery only in memory were rejected because they can escape the workspace, mutate a replacement namespace, overwrite external bytes, resurrect stale journals, strand lifecycle state, or lose typed source. |
| 2026-07-16 | PR #84 contains creation at the retained root and gives failed text cleanup an explicit per-record release | This entry **supersedes** the preceding PR #84 row's folder-staging clause and extends its Stop Tracking contract. Empty-file creation resolves the complete normalized path from the retained root descriptor with `O_CREAT | O_EXCL | O_NOFOLLOW_ANY`; a parent moved before the syscall is re-resolved rather than receiving a create through the stale child descriptor, existing destinations are preserved, and every symlink component is rejected. Darwin has no equivalently flag-bearing `mkdirat`, so folder creation obtains one process-lifetime `.itemReplacementDirectory` source per destination filesystem while security-scoped access is active. Before every atomic `fclonefileat`, the named path must reopen to the retained descriptor identity and the source must be same-device, current-user-owned, exactly mode `0700`, flags-zero, empty, and limited to normal `com.apple.macl`/`com.apple.provenance` xattrs; the destination uses `CLONE_NOFOLLOW_ANY | CLONE_NOOWNERCOPY` and must repeatedly pass the same empty-directory policy. The source remains named rather than risking deletion of a racer's replacement, and no staging name exists inside the workspace. Cross-device or non-clone-capable volumes fail closed without a workspace mutation; there is no publication rename or name-based rollback. macOS 14/15 exposes no public `RESOLVE_BENEATH`, and folder clone cannot atomically return the new directory descriptor, so syscall-internal ancestor rename and clone-to-first-open replacement remain documented platform residuals. Exact parent/destination postflight never republishes, unlinks, or treats an observed replacement as removable; uncertainty stays indeterminate. If Save Copy succeeds but durable text-record removal fails, the exact clean anchored session remains visible through a `.textRecovery` Stop Tracking prompt, later quit attempts do not silently repeat the unlink, and termination stays fenced. Stop Tracking first upserts the latest exact source, then descriptor-relatively renames only that record to a unique non-`.plist` quarantine sibling with `RENAME_EXCL | RENAME_NOFOLLOW_ANY` and fsyncs the recovery directory; only success clears the runtime context, while failure preserves the bytes, prompt, and fence. Alt: child-fd creation after a parent-containment check, predictable workspace staging, deleting a mutable named clone source, publishing a post-create replacement, unsafe fallback on unsupported volumes, hidden cleanup contexts, infinite removal retry, deleting recovery bytes, or clearing the quit fence before durable quarantine were rejected because they can cross the workspace boundary, mutate unrelated namespace entries, or strand or lossily release recoverable editor text. |
| 2026-07-18 | PR #84 removes the clone-source name and validates sandbox system metadata exactly | This entry **supersedes the preceding PR #84 clone-source wording**. Folder creation opens and twice proves a same-device OS-random `.itemReplacementDirectory`, then removes only that non-workspace name and retains the descriptor in a process-lifetime per-device registry. APFS may materialize system xattrs on first access, so WorkspaceKit warms that state before choosing a ctime baseline. Only `com.apple.macl`, `com.apple.provenance`, and sandbox-observed `com.apple.quarantine` are allowed: readable values remain exact bytes, while `EPERM`/`EACCES` values remain explicitly access-controlled and are guarded by the exact name set plus ctime. The retained descriptor must stay current-user-owned, mode `0700`, flags-zero, empty, and equal to its post-removal ctime/xattr and kernel-reported-path baseline before and after every `fclonefileat`; the destination captures and repeatedly validates its own first-open baseline. A deterministic final-proof-to-`rmdir` move/replacement race proves a moved original is rejected and never cloned. macOS 14/15 still has no conditional descriptor-bound `rmdir`, so a synchronized same-user swap can delete an empty replacement at the OS temporary name; this residual never targets a workspace name or editor data and is not claimed identity-atomic. Signed App Sandbox creation plus package races cover the production xattr and path behavior. Zero-session operation recovery is rendered globally when no document can host the editor banner, keeping Check Again and Stop Tracking reachable after restart. Alt: retaining a mutable named source, treating access-denied xattrs as readable bytes, accepting arbitrary system metadata, relying on APFS directory `st_nlink` as unlink proof, or hiding recovery behind a document-only banner were rejected because they can leak temp state, clone changed policy, make folder creation fail only in the shipped sandbox, or permanently fence a documentless recovery. |
| 2026-07-18 | PR #84 closes final relocation confinement and destination-publication gaps | This entry **extends the preceding PR #84 rows**. Final relocation now resolves both complete literal paths from one retained workspace-root descriptor and calls `renameatx_np(RENAME_EXCL | RENAME_NOFOLLOW_ANY)`, so a destination-parent descriptor moved after preflight cannot publish outside the workspace. Exact directory enumeration distinguishes a requested literal entry from an equivalent lookup; a case-only or NFC/NFD-only destination is admitted only when it resolves to the authorized source in the same physical parent, and postflight proves the new literal bytes present and the old bytes absent. App performs destination-ownership validation independently of affected source-session records for both relocation and creation, covering current/cached/retired/detached/editor-bound sessions plus LRU, prompt, observation, indeterminate-write, and installed, pending, bundled, or unavailable operation/text-recovery state; display-root recovery candidates reserve ownership but never become filesystem authority, and literal source-state exclusions allow spelling-only relocation without hiding a distinct destination owner. Single-component validation preserves leading, trailing, and all-whitespace UTF-8 instead of trimming. Image placement retains its namespace lease through commit validation and synchronous Markdown publication, with one shared exactly-once commit/discard terminal that releases only after publication or completed cleanup; termination also checks that lease, so the App cannot exit during cleanup rename/fsync after EditorKit releases its pending-source fence. The Trash typing regression now waits beyond the 500 ms production autosave minimum and proves detached recovery remains reachable without recreating the trashed path. Package and App tests cover the moved-parent race, literal whitespace, case/NFC spelling publication, unopened-source/create ownership including pending multi-record recovery, equivalent-spelling detached collision, lease terminal ordering, blocked-cleanup termination, and delayed autosave. Draft status remains unchanged pending fresh exact-tip review. Alt: final rename through a preflight child descriptor, source-record-driven collision checks, alias-wide source exclusions, ignoring unloaded recovery records, trimming names, rejecting every filesystem-equivalent spelling, releasing the image counter after placement, allowing termination during terminal cleanup, or a 50 ms autosave assertion were rejected because they can escape authority, overwrite App-owned paths, hide a second owner, strand recovery, rewrite valid names, block legitimate renames, publish stale Markdown paths, abandon cleanup, or pass without exercising production timing. |
| 2026-07-19 | PR #84 retains relocation recovery authority and restores the whitespace and subtree-ownership contracts | This entry **supersedes the preceding PR #84 row's relocation-recovery, all-whitespace-name, and destination-ownership wording**. Before a forward rename, App durably records a security-scoped bookmark for the exact destination parent together with its no-follow directory identity, and mutation proceeds only after that authority resolves and validates. If the parent moves after the rename and both workspace paths disappear, Check Again may locate the moved leaf only through that retained parent authority, must match the captured moved-entry identity, and may only perform a recovery-only exclusive no-follow anchored move back to a proven-missing source under its still-valid workspace parent; it never adopts an escaped destination or treats a display URL as authority, and every unresolved, mismatched, occupied, cross-device, failed, or indeterminate result remains quarantined and actionable. App destination validation treats every requested entry, including an ordinary file, as reserving its byte-component-bounded subtree, so `archive` cannot shadow recovery-owned `archive/post.md` while prefix-only siblings remain independent. Filename validation trims whitespace and newlines only to reject an all-whitespace spelling; every accepted leading/trailing whitespace and other literal UTF-8 spelling is returned unchanged. Tests cover post-rename parent escape and authority-backed recovery, restart persistence, subtree shadowing with a prefix-sibling control, all-whitespace rejection, and literal valid spelling. Draft status remains unchanged pending fresh exact-tip review. Alt: retaining only old relative paths and inode, reopening a display URL, adopting an outside destination, releasing quarantine on failed recovery, checking descendants only for directory destinations, or changing the product contract to permit invisible all-whitespace names were rejected because they can strand the moved identity, recover through mutable namespace, bury owned recovery, or create unusable filenames. |
| 2026-07-19 | PR #84 makes every namespace publication retain an item locator before authority can move | This entry **supersedes the preceding PR #84 creation and destination-parent-only recovery wording**. File and folder creation first publishes a caller-planned OS-random `.plainsong-create-<uuid>` entry at the retained workspace root, fsyncs the artifact and root, then invokes App before any destination rename. App has already persisted the exact destination-parent bookmark plus literal leaf; the callback captures and resolves the exact staged-item bookmark and durably advances `planned` to `prepared` before returning. WorkspaceKit then performs one exclusive no-follow root-relative publication. App advances to `committed` before activation only when destination-parent authority, item authority, logical destination, expected identity, and missing staging agree, and repeats that proof before durable journal release. Legacy/absent phase is `unknown` and fail-closed. Rename/move similarly retain both parent-entry locators plus a mandatory selected-item bookmark; Trash retains source parent-entry plus expected-item authority in pending phase and an exact reported-Trash locator before committed cleanup. Check Again re-resolves bookmarks, never substitutes display URLs, never automatically reverse-moves an unexpected identity, and cannot treat `.none` alone as recovered. Combined destination-parent replacement plus post-rename escape, staging substitution, stale/unresolved locators, callback/store failure, remove-before-throw repair, and restart tests prove the only locator is not discarded. Draft status remains unchanged pending full exact-tip validation. Alt: direct destination creation, callback after publication, optional item locators, inferring phase from `isPlanned`, path-only `.none` release, or name-based unexpected rollback were rejected because each can strand A outside the workspace, adopt B, or erase the last recovery authority. |
| 2026-07-19 | PR #84 reserves every recovery authority and makes relocation text write-ahead dual-store | This entry **extends the preceding PR #84 recovery wording**. Creation, relocation, and Trash ownership inventories include every live parent-entry, item, staging, and reported location in that locator's own retained-root coordinates, plus display-only restart reservations. Save Copy and namespace publication reject symmetric component-bounded ancestor/descendant overlap under the retained filesystem's case and canonical-equivalence policy, so an escaped bookmark target, `archive` shadowing `archive/post.md`, or a missing case/NFC alias cannot overwrite the only recovery authority. Creation activation may exclude only its own recovery ID after repeated destination-parent/item/staging joint proof; all other recovery, URL-keyed, session, and indeterminate owners remain fenced. Dirty rename/move attempts the bundled operation journal and every standalone text record synchronously before mutation. Either store can preserve all editor snapshots; an operation-journal failure quarantines only after every standalone record is durable, while dual failure aborts before the namespace syscall without suppressing resumed autosave. Tests cover escaped locators across all three operations, alias and ancestor ownership, both store-failure directions, sibling-session continuation, and delayed production-interval autosave execution. Alt: comparing every recovery path in the original workspace root, byte-exact missing-name checks, descendant-only ownership, fencing after both stores fail, or stopping at the first standalone error were rejected because they can overwrite the last locator, hide a recoverable subtree, or leave dirty text only in memory. |
| 2026-07-19 | PR #84 releases terminal runtime recovery only with durable journal removal | This entry **extends the preceding PR #84 recovery lifecycle wording**. Reconciliation proof may reinstall a freshly resolved operation context while validating an ordinary success path. Durable operation-record removal and runtime release therefore form one transition: only after removal succeeds does App clear the exact installed context, matching per-session recovery ownership, indeterminate-session fences, and prompt state. Any removal failure reinstalls the context and keeps termination blocked. Tests prove a completed relocation or Trash operation permits a later mutation and successful termination instead of leaving a hidden permanent fence. Alt: assuming a terminal proof is read-only, clearing runtime state before durable removal, or leaving a proof-installed context behind were rejected because they can either lose the only actionable recovery record or permanently disable save, mutation, and quit after a successful filesystem transaction. |
| 2026-07-19 | PR #84 keeps the rekeyed relocation journal when standalone rekey fails | This entry **extends the preceding PR #84 dual-store recovery wording**. After a durable rename/move commits sessions to the destination, live text recovery is rekeyed to the new path. That rekey may fail on the standalone store while still advancing only the operation journal. Clearing the unpromoted-text marker and deleting the journal after session commit would discard the only durable recovery locator that points at the new path, leaving standalone stuck on the pre-move URL. `finishWorkspaceRelocation` therefore re-ensures standalone durability after session commit and destination proof; only then does it mark bundled text committed and remove the journal. If the post-commit rekey remains non-durable, App keeps the committed journal, reinserts the unpromoted marker, installs cleanup recovery, and returns indeterminate. A regression proves a dirty rename whose standalone store accepts only the old path keeps the journal at `renamed.md` through restart. Alt: unconditionally clearing the promotion marker after commit, promoting only when the marker happened to be set, or deleting the journal because pre-move standalone was durable were rejected because they can erase the only correct post-move recovery locator. |
| 2026-07-19 | Phase 3 WS3C PR A: Files/Search sidebar modes stay on the fixed HStack shell | App owns `WorkspaceSearchUIState` (mode, query text, match-case/whole-word, monotonic focus request + applied IDs, generation-scoped `pendingResumeGeneration`). Search query chrome uses an **owned AppKit `NSTextField`** (`WorkspaceSearchQueryField`) stamped with `plainsong.workspaceSearch.queryField` — **not** SwiftUI `FocusState` / `TextField`. Because Phase 1 shares one AppState across `WindowGroup` windows, focus apply is **key-window only** via window-local `WindowKeyStateTracker` that re-reads the host `NSWindow` (or an AppState test override) after every suspension — never a Bool captured before `await` — and tracks field mount via `searchFieldMountEpoch` when the owned field binds. The key window marks `focusAppliedID` only after that identifier-matched field is the real first responder, using a cancelable request-scoped retry loop so the first `⌘⇧F` `.files → .search` mount race can still succeed. Identity is accessibility-identifier-only; adjacent unlabeled decoys must not be stamped or focused. Hosted dual-window and Files-start AppTests assert first-responder routing. Picker re-entry and workspace reset still cannot replay a spent token. Search chrome only publishes through existing `setWorkspaceSearchQuery` / `clearWorkspaceSearch` / `restartActiveWorkspaceSearchWithFreshOverlays` / `activateWorkspaceSearchResult`; no URL rebuild, file-tree open, or WS3A/WS3B contract change. Case UI defaults to smart case; Aa forces sensitive; whole-word is independent; explicit insensitive is model-only. A non-empty query typed while search is not ready arms `pendingResumeGeneration` for the current workspace generation only; `resumePendingWorkspaceSearchFromUIIfNeeded` after install resumes that generation (and generation advance rebinds an already-pending request, never creates pending from a bare non-empty field). Ordinary FSEvent/filter reloads therefore do not silently implement refresh. Switching to Files keeps valid results; workspace close/switch resets chrome and marks focus tokens applied. Sidebar width is 280 pt shared by both modes. Owner focus sign-off, grouped results, keyboard activation, and full refresh remain later WS3C items. Alt: `NavigationSplitView` was rejected by R17; SwiftUI `FocusState` + frozen `controlActiveState` across `await` was rejected because delayed tasks mis-read key status and SwiftUI does not reliably expose a concrete AppKit field identity; process-wide AppKit key monitors and new dependencies remain rejected in favor of **window-local** `NSWindow` observation plus an owned Search `NSTextField`; sibling-hierarchy field discovery that could stamp unlabeled controls was rejected; global apply-from-any-window was rejected because a background window can consume the token; re-publishing every non-empty query after every reload was rejected as accidental refresh scope creep; removing `⌘⇧F` from PR A was deferred in favor of key-window routing while keeping the focus checklist open. |
| 2026-07-20 | Phase 3 WS3C PR B: pure presentation layer for grouped search results | Search results render through pure `WorkspaceSearchResultsPresentation` / `WorkspaceSearchResultsPresenter` (no phase/banner logic in SwiftUI `body`). Row/section IDs use `Data(relativePath.utf8)` plus query generation and match range/ordinal so NFC/NFD paths stay distinct. Snippet highlights use UTF-16 `NSRange` via `NSString` and are built only for visible lazy-`List` rows (`WorkspaceSearchSnippetHighlight`), never precomputed for the full result set. Activation re-resolves the live `WorkspaceSearchContext` + `WorkspaceSearchFileResult` + `TextSearchMatch` through `activationLookup` and calls `activateWorkspaceSearchResult`; `fileAuthority == nil` compatibility rows are non-activatable with no URL fallback. Searching and service-failure phases retain partial `fileResults` rather than presenting as No Results. Presentation rebuilds are memoized in the Search sidebar by exact `Equatable` comparison of the presenter inputs (`WorkspaceSearchResultsPresentationInputs`: search state, query text, readiness; copy-on-write keeps unchanged arrays cheap), so in-place same-count element rewrites — the App replace branches today, ordinary-edit/FSEvent refresh later — can never serve a stale presentation; path UTF-8 bytes are shared once per file section. Keyboard result navigation, owner focus sign-off, ordinary-edit/FSEvent refresh, and WS4 perf budgets remain open. Alt: rebuilding attributed snippets for every match up front was rejected by the 10k-match bound; a generation/phase/count memo key was rejected because a same-count in-place rewrite would present stale rows while activation fails closed into silent no-op clicks; Swift String identity for row IDs was rejected by NFC/NFD collisions; URL-based open for nil-authority rows was rejected by WS3B retained-authority rules; scattering status switches across view builders was rejected in favor of a pure presentation snapshot. |
| 2026-07-20 | Phase 3 WS3C PR C: keyboard selection and accessibility for search results | Search keyboard routing is a pure `WorkspaceSearchSelectionNavigation` reducer over ordered row IDs (path then source-range order, no wrap): query-field ↓ selects first and focuses the results list; results ↑/↓ move; Return activates only through `activationLookup` + `activateWorkspaceSearchResult` when the current state still holds the exact authority-backed match; Escape from results returns to the owned Search `NSTextField`, Escape from the field calls `requestEditorFocus()` without clearing query/results. Query-field focus remains AppKit first-responder identity (`WorkspaceSearchFieldFocus`); results list uses SwiftUI `FocusState` only as a secondary surface. Stable accessibility identifiers live in `WorkspaceSearchAccessibility` (mode/case/whole-word/status/banners/rows with UTF-8 path tokens); row labels always include relative path, line, and snippet; truncated/skipped/error expose text labels not color alone; selected/disabled are traits + spoken value. Generation change and empty clear drop selection; rapid query replace invalidates stale IDs. ⌘F / editor focus tokens stay independent of search focus. Ordinary-edit/FSEvent refresh and WS4 remain open; overall Workspace Search stays IN PROGRESS. Follow-up: list/button selection **claims results keyboard focus** so click-then-↑/↓ uses the pure reducer; query-field ↓/Escape only swallow keys when callbacks are installed; focus handoff clears the field editor, then re-asserts results `FocusState` on the next main turn behind a monotonic focus-intent token so a newer Escape/⌘⇧F/generation intent cannot be overridden by the queued re-assert. **Owner keyboard smoke** is a merge gate and is signed off by hosted `testHostedOwnerKeyboardSmokeFiveScenarios`, which drives **real `NSEvent`s** through `window.sendEvent`: field ↓/Escape through the field editor's `doCommandBy`, no-wrap ↑/↓/Return/Escape through the focused List's `onKeyPress` (SwiftUI genuinely claims key delivery after the handoff), and a real click on a backing `NSTableView` row that claims selection, activates through retained authority, and then routes click-then-↑ through the reducer; the Debug probe is observability only and injects nothing. In-process SwiftUI accessibility-identifier lookup was rejected for click targeting because those elements do not materialize without an assistive client; backing-table row rects are used instead, and WS4 XCUITest re-covers the same keys out of process with identifier-based queries. Alt: relying on native List wrap-around was rejected by the no-cycle contract; FocusState for the query field was already rejected in PR A; URL open on Return was rejected by WS3B authority rules. |

| 2026-07-20 | Anchored filesystem walks start at the retained root authority, never at "/" | This entry **supersedes** the 2026-07-13/14 WS3B wording that described walks "from / through the root". `WorkspaceAnchoredFileSystem.withAnchoredParent` now dups the location's `rootAuthority` retained descriptor (after `validateCanonicalBinding()`) and walks only `relativePath` components with the existing `O_NOFOLLOW` openat flags. The `DirectoryDescriptorChain` covers root→parent; ancestor-of-root identity is the root authority's own binding proof (reopening the granted root URL, which the sandbox allows). Trigger: Sev-1 — every real workspace/file open failed with `WorkspaceAnchoredFileSystemError.unreadable` (error 3) because App Sandbox denies `openat(slashFd, "Users", …)` for Powerbox-selected folders; only the granted subtree is readable, not absolute ancestors. Hosted tests create fixtures inside the app container whose ancestors the sandbox still allows, so the old "/"-walk stayed green under test and masked the shipped failure. Mid-op `DirectoryDescriptorChain.validateNamespace` re-opens the granted `canonicalRootURL` (sandbox-allowed) so lexical root rename/replace still fails closed — open root fds follow moved inodes and cannot detect that alone. Regression coverage asserts hook-observed `componentOpened` events are only relativePath intermediates (never absolute root/ancestor names) and that EPERM/EACCES open failures map to `.unreadable`. Residual: a true Powerbox grant outside the container cannot be exercised in-process without a user-selected security-scoped URL. No-follow, identity validation, `RENAME_SWAP`/`RENAME_EXCL`, fail-closed uncertainty, and standalone parent-authority locations are unchanged. Alt: keeping the "/"-walk with a special-case skip of unreadable ancestors was rejected because it weakens identity proof and still depends on sandbox-permitted intermediate opens; dropping no-follow or reopening mutable absolute paths was rejected by #84/#85 authority rules. |


| 2026-07-20 | ⇧⌘F Find-in-Workspace is delivered by swizzling `NSApplication.sendEvent` + shared AppState | Live key log under Zhuyin showed ⇧⌘F arrives with `option=true` (matcher must not require option-free flags) and that SwiftUI's `@NSApplicationDelegateAdaptor` does not expose the concrete delegate as `NSApp.delegate` (cast to `PlainsongApplicationDelegate` failed; menu click still worked). Install a one-time `sendEvent` method exchange; match Command+Shift+F (allow Option, reject Control); perform `focusWorkspaceSearch` via `PlainsongAppServices.appState` assigned from `PlainsongApp` init/onAppear. Menu item retained for discovery/mouse. Covered by `PlainsongMenuKeyBindingTests`. Alt: NSPrincipalClass subclass rejected (SwiftUI ignores it); local-monitor-only and delegate-cast paths rejected by live logs. |

| 2026-07-22 | ⇧⌘F uses an app-active Carbon hot key and toggles Files/Search | This entry **supersedes the preceding 2026-07-20 `NSApplication.sendEvent` delivery decision**. Built-in-keyboard traces proved physical ⇧⌘F at HID and session taps but not at annotated-session/AppKit, while the menu action and ⇧⌘P worked; local monitors, `performKeyEquivalent`, responder overrides, and both base/concrete `sendEvent` hooks were therefore downstream of the loss, and synthetic `NSApp.sendEvent`/automation results were false-green. The root cause is pre-AppKit key-equivalent/global-hot-key resolution consuming the physical event; the event pipeline does not expose the exact owner. No matching symbolic-hot-key, user-key-equivalent, Karabiner, or input-source configuration was found; cross-process Carbon registrations are non-exclusive, so a third-party owner cannot be excluded solely by successful probe registration. Plainsong now registers the exact physical `kVK_ANSI_F` + `cmdKey|shiftKey` combination on the application Carbon event target only while active, validates its namespaced `EventHotKeyID`, and unregisters on resign/termination. The shared `AppState` action toggles Files→Search→Files: opening Search advances the owned-field focus token; returning to Files preserves query/results. Exact modifiers leave ⌘F, ⌃⌘F, ⌥⌘F, and ⇧⌘P unclaimed; ⌘F remains separately unfinished rather than a regression. Owner smoke on the built-in keyboard produced seven consecutive Carbon callbacks with correct odd-toggle final Search state and owned `plainsong.workspaceSearch.queryField` focus; a second owner smoke confirmed normal bidirectional toggling. A final clean-build smoke under Zhuyin confirmed physical Search→Files toggling, closing the old AppKit extra-Option risk; resign logs confirmed unregistration. Temporary event diagnostics were removed. Targeted hot-key/lifecycle/focus tests, full `make lint`, `make build`, and `make test` passed. Alt: keeping any AppKit-only hook was rejected because physical events never reached it; always-global registration was rejected because the shortcut belongs only to the foreground app; `NSPrincipalClass` remains rejected because SwiftUI does not honor that route. |

| 2026-07-22 | WS3C refreshes only an actually active, root-bound workspace query | Accepted current and warm cached Markdown/MDX edits enter refresh through `scheduleDocumentTextSideEffects`, cancel only a pending exact navigation whose document identity is the edited session, then cancel/restart the existing debounced task and capture dirty overlays only after debounce; rapid edits therefore coalesce around the latest text and undo-to-saved omits the overlay, while pending navigation for another document survives. Generation advance retains the exact active query as App-owned root spelling plus physical expectation, never as the old descriptor authority. FSEvent and namespace reload supersession carry that intent across same-root generations, but only the newest successful transactional snapshot/fresh-authority install consumes it exactly once. A pending or direct newer explicit query wins; empty clear, close/switch, teardown, root mismatch, failure, cancellation, and stale work cannot replay it. Existing task-token, root/workspace/query-generation, cancellation, late-event, and selection-reset guards remain authoritative; mode, query/options, and focus receipts are untouched. Deterministic controlled-scanner App tests prove a distinct replacement snapshot plus its captured generation/fresh authority and the latest overlay edited while scan is pending, direct stale-token/context event rejection, real UI replacement/options and empty clearing, truly pending close/switch/teardown, inactive non-empty UI text, and irrelevant sessions producing no request. WS4, XCUITest, performance budgets, regex/replace, `Command-F` editor find, and overall Workspace Search completion remain open. Alt: arming refresh from query-field text, leaving edited-document navigation live, snapshotting overlays before debounce, carrying the old root authority, or starting before installation were rejected because they can apply a stale range, replay inactive/cross-root queries, or publish stale bytes/results. |

| 2026-07-23 | WS4A adds an out-of-process workspace-search acceptance gate without claiming physical input | `PlainsongUITests` is generated from `project.yml` and launches the real Debug app with a unique app-container-owned Markdown/MDX fixture instead of automating `NSOpenPanel`; termination removes only that run's fixture. XCUIApplication drives ⇧⌘F, an exact CJK query, grouped results, no-wrap keyboard selection, Return activation, results→query→editor Escape transitions, and click-then-arrow routing through stable accessibility identifiers. Activation is proven by the current filename and a Debug-only accessibility observation published only after EditorKit has installed, re-read, and accepted the exact native UTF-16 selection. App-level ASCII input without clicking proves initial query-field focus before the field is explicitly selected and replaced with the exact CJK query; native AX keyboard-focus state verifies both query-field transitions, Debug observability exposes accepted navigation without injecting App state, and subsequent key outcomes themselves prove results routing. The gate found two native-focus gaps: EditorKit's required activation focus could steal first responder while Search `FocusState` remained logically true, and first-launch focus scheduling stored its Task in SwiftUI `@State` from mount/update callbacks, producing a runtime warning and allowing the attempt to disappear across the Search-view/key-window transition. Activation now performs a tokenized next-turn results-focus handoff; non-published controller ownership replaces/cancels focus tasks without view-state mutation, a request-ID-scoped view task guarantees a post-mount attempt, and the bounded loop re-reads key-window eligibility on every iteration. Background windows cannot consume the receipt, a just-activating key window can still apply it, and newer shortcut/Escape/generation intent wins. The existing hosted keyboard smoke now waits for and asserts the post-activation handoff before its Escape/click-arrow events; existing dual-window/focus arbitration coverage exercises the lower-level key-window contract, so no duplicate suite was added. XCUITests use predicate waits, establish a real key window before the shortcut, restore the general pasteboard after exact CJK paste, and isolate each fixture. These are synthetic XCUITest events, not new physical-keyboard evidence; PR #89 remains the physical ⇧⌘F evidence. WS4 performance probes/budgets, `Command-F`, regex/replace, and overall Workspace Search completion remain open. Alt: host-path fixtures, `NSOpenPanel` automation, fixed sleeps, environment-supplied query/results, AX values derived from requested rather than accepted navigation, treating logical `FocusState` as native focus, storing task ownership in view state, relying only on an early mount callback, exiting before a pending token's key-window retry, conflating the automation runner's first paste with shortcut focus, or claiming XCUITest as physical evidence were rejected because they can leak sandbox assumptions or pass without the shipped behavior. |
| 2026-07-23 | WS4A review closes fixture leakage and focus false-greens | This entry extends the preceding WS4A evidence without closing WS4. Fixture launch is selected before ordinary `AppState` construction and uses a cleared test-only defaults suite, no-op last-opened/recent stores, transient mutation-recovery stores, and `shouldRestoreLastOpenedFile: false`; the real app factory regression proves production session and recovery metadata is never loaded, while fixture opening still uses the production workspace-open path with `rememberAsLastOpened: false`. Graceful termination clears the test suite and current fixture; the next launch clears interrupted suite state and removes only expired, real, non-symlink `ws4a-*` directories while preserving current, recent, unrelated, and symlink entries. Editor focus observation uses EditorKit's stable identity on the actual `MarkdownSTTextView`. Activation reports whether authority/fingerprint/read validation really committed: rejection synchronously reasserts results routing with no delayed task; success immediately invalidates the pre-Return results-ready claim, waits for the editor responder, then requires consecutive turns with the same intent, live sidebar, and the concrete window-local results key responder before republishing readiness. That responder owns Up/Down/Return/Escape and calls the existing reducer/authority path; logical SwiftUI focus can neither publish readiness nor steal routing later. A monotonic Debug-only activation probe lets XCUITest require this activation's restoration rather than accept a stale `results` label; a separate event-routing probe proves the back-to-back Escapes actually traversed results→query→editor before native editor AX focus is accepted. A second Escape, query-field Down, and row click each cancel the first Escape's still-running forced query-focus task. The hosted real-`NSEvent` regression covers rejected Return plus immediate arrow routing, rapid double Escape, and Down/click after the first forced query confirmation, with consecutive proof that query focus is not reclaimed. The click-then-arrow XCUITest requires the Debug-only reducer event before AX selection. Debug support remains observability-only, waits remain predicate-based, and synthetic XCUITest input is not physical-keyboard evidence. Performance probes/budgets, `Command-F`, regex/replace, WS4, and overall Workspace Search remain open. |
| 2026-07-23 | WS4A follow-up preserves key-view traversal and event-causal boundaries | The concrete results responder now implements AppKit `insertTab:` and `insertBacktab:` by selecting the next/previous key view, so Tab and Shift-Tab cannot remain trapped on the invisible router. The hosted gate sends real Tab and Shift-Tab keyDown/keyUp events through `window.sendEvent` and proves the Search field receives forward and backward traversal. Results→query Escape now records a synchronous handoff-pending intent before its asynchronous focus attempt; a second Escape received by the still-focused router in the same main-actor turn cancels that task and requests editor focus directly. The hosted regression sends both real Escape event pairs with no await between them. Debug reducer observability now carries a monotonic receipt, and XCUITest requires the exact next receipt for every no-wrap boundary event before accepting unchanged AX selection, so a dropped or late arrow cannot pass on a predicate that was already true. These remain synthetic-event acceptance results, not physical-keyboard evidence; WS4 performance gates and overall completion remain open. |
| 2026-07-24 | WS4A traversal and handoff termination are bound to the newest intent | Tab and Shift-Tab notify the sidebar before AppKit moves to the adjacent key view, synchronously canceling and invalidating every older Search-focus attempt so a later retry cannot reclaim Search. `WorkspaceSearchUIState` records the exact traversal-superseded `focusRequestID` as a shared receipt, so the delayed `.task(id:)` scheduler, a Files→Search remount, or another window sharing `AppState` cannot replay a repeated/programmatic request after controller cancellation; a later explicit request receives a newer ID and remains eligible. This notification is owned by both concrete responders: the results router handles traversal before calling `selectNextKeyView` / `selectPreviousKeyView`, while the query-field delegate handles `insertTab:` / `insertBacktab:` before returning them to AppKit. The real-`NSEvent` hosted gate uses an explicit three-node key-view loop with distinct forward/backward responders, covers immediate results-router traversal, traversal after the query field's first forced-focus confirmation, both directions immediately after repeated `focusWorkspaceSearch()`, Files→Search remount, and transfer of key-window eligibility to a second hosted window; it requires each traversal destination to remain first responder, the superseded receipt to stay unapplied, and the exact requested-attempt sequence to remain unchanged. The Debug probe also records that attempt's exact request ID, excluding an older queued attempt from satisfying the predicate. The controller deduplicates the `.onChange` and delayed `.task(id:)` paths by typed request identity, clears identity on completion, and uses a separate installation identity so an older completion cannot clear replacement work. A forced handoff clears its token-matched pending state on every terminal path, including cancellation and all 180 retries exhausting while the window is ineligible; the hosted gate predicate-waits for that exhaustion, restores eligibility, and proves the next results Escape returns to Search rather than skipping to the editor. Query-generation changes and empty-query clearing cancel only an actually pending, controller-typed `.forcedHandoff`; they cannot cancel a `.requestedFocus` retry for an unapplied ⌘⇧F receipt. Hosted coverage loses key-window eligibility, requires a Debug-only monotonic cancellation receipt within 500 ms for both production query paths, and proves neither leaves an orphan second-Escape intent. A separate shortcut race advances query generation during an ineligible requested-focus retry, restores eligibility without publishing any reschedule epoch, and proves the same exact attempt and request ID apply its receipt and focus Search. Hosted no-wrap boundaries also require exact monotonic reducer receipts before accepting unchanged selection. Debug probes remain observation-only, and WS4 performance gates plus overall completion remain open. |
| 2026-07-25 | WS4B freezes workspace-search performance budgets from production-shaped probes | `PerformanceTests/WorkspaceSearchPerformanceTests.swift` measures the real `WorkspaceSearchService` over real on-disk workspaces. The four search probes use the production `WorkspaceSearchDiskFileReader`, so candidate planning, ignore-policy probes, anchored no-follow reads, UTF-8 decoding, MarkdownCore matching, snippet construction, and stream delivery are all inside the measured region; the cancellation probe is the one deliberate exception and substitutes a controlled reader that blocks every candidate read, because deterministic cancel-to-drain measurement needs a saturated window that cannot finish on its own. Fixture creation and `WorkspaceDirectoryScanner.snapshotCapture` run before timing; each probe runs one unmeasured warm-up that is asserted with the same predicates as the measured samples, so an empty warm-up cannot make later samples cheap. Probes: a 2,000-file `.md`/`.mdx` workspace (2,893,000 bytes, 500 matching files, 1,000 matches); a file of exactly the 524,288-byte admission cap whose only match is in the final line; its 524,289-byte one-byte-over sibling, proving the cap boundary admits one and skips the other; two dense whole-word rejection shapes at the cap; and rapid cancellation of a saturated four-read window. Each dense shape also runs a non-whole-word control proving the same file really contains many literal candidates, so the rejection measurement cannot be vacuous. Every sample hard-asserts ordered results, per-file UTF-16 ranges and line numbers, exact candidate/searched/skipped/ignored/emitted accounting, exact disk read count and byte total, the exact finite event count, and the concurrent/outstanding read ceilings; buffered reads assert only their ceiling because peak buffering is completion-order dependent. Memory boundedness is asserted as structural limits (read window, finite event bound, per-file/per-query match caps, bounded snippets, admitted bytes) rather than a flaky RSS threshold. Budgets are frozen from three Release and three Debug runs recorded in `docs/perf-log.md` and are gated against Debug medians because `make test` runs Debug: 2,000-file workspace < 3,000 ms, admitted 512 KiB file < 150 ms, dense `ascii-suffix` < 200 ms, dense `unicode-periodic` < 2,500 ms, cancel-to-drain < 50 ms. Wall-clock budgets are hard locally and informational on hosted CI under R15, while deterministic counts, cancellation behavior, and resource ceilings stay hard everywhere. The `unicode-periodic` shape measured 612 ms in Release at exactly 512 KiB, which is production-shaped confirmation of the plan §2.3 admission cap rather than a regression. No production code changed. WS4's remaining regression-suite item, `Command-F`, regex/replace, UI work, and overall Workspace Search completion remain open. Alt: timing fixture creation, synthetic-reader-only measurement, an RSS assertion, a single unwarmed sample, hard CI wall-clock gates, and loosening a budget to rescue the first failing `unicode-periodic` Debug run were rejected because they would measure setup instead of search, fake the production path, add flakiness, or hide a real cost behind an invented number. |
| 2026-07-25 | `AppTests` must compile in `Release`; Debug-only App probes are guarded at the test | `xcodebuild` builds every test target even under `-only-testing`, so `AppTests` references to App symbols behind `#if DEBUG` (`AppState.openDebugWorkspaceSearchFixture`, `WorkspaceSearchKeyboardSmokeProbe`) broke the Release `PerformanceTests` command recorded in `docs/perf-log.md` with `cannot find … in scope`, forcing a `SWIFT_ACTIVE_COMPILATION_CONDITIONS='$(inherited) DEBUG'` override to measure Release budgets. `testDebugWorkspaceSearchFixtureDoesNotPersistSessionMetadata` now `XCTSkip`s in Release like the existing hosted keyboard-smoke tests, and that smoke's private probe-reading helpers — whose only callers already skipped — are compiled out behind `#if DEBUG`. Debug coverage is unchanged, no production code is touched, and Release performance evidence no longer depends on a Debug compilation condition. Alt: keeping the override was rejected because it makes the Release measurement path depend on a non-Release build setting and silently hides any future Debug-only reference; moving the Debug-only tests into their own `#if DEBUG` file (the `DebugWorkspaceSearchFixtureTests.swift` shape) was rejected here because the helpers use `private` members of the existing hosted test class. |
| 2026-07-25 | WS4B review hardening pins the resource ceilings independently and drives the global match cap for real | This entry **extends the preceding WS4B row**. The gate's resource ceilings are now literals owned by `WorkspaceSearchPerformanceTests` (`4` concurrent reads, `100` progress events, `100` reported skipped files, `500` matches per file, `10,000` matches per query, `524,288`-byte admission cap) instead of being read back from `WorkspaceSearchLimits`. Reading them back made every bound self-fulfilling: widening a production default widened the assertion with it, so a broken ceiling stayed green. `testProductionSearchLimitsStillMatchTheFrozenGateCeilings` is the single comparison point against production, so a deliberate limit change fails in exactly one obvious place. The 10,000-match per-query ceiling was previously only asserted as an inequality the 1,000-match bulk fixture could never reach; `testGlobalMatchCeilingTruncatesAndDrainsRemainingCandidates` now drives it with 24 files of 501 occurrences each, proving the ceiling is reached exactly rather than overshot, `isGloballyTruncated` is set, only the first 20 files emit results in path order, and every remaining candidate is still read and counted in the accounting-only phase. Stream-shape invariants moved into one `assertSharedStreamInvariants` helper applied to every completing run — measured sample, warm-up, and the dense-rejection literal control — because the previous documentation claimed exact event counts and read-window ceilings for "every sample" while only the bulk probe asserted them. The cancellation probe's window-saturation and post-cancel drain waits are bounded at 10 s and fail the probe on expiry, so a regression cannot stall the job to the CI timeout. PR #94 is now merged and `main` at `58740ac` is merged into this branch, so the documented Release command is reproducible at this tip with no build-setting override; three post-merge Release runs re-measured every metric with results consistent with the pre-#94 runs, converting the earlier provenance argument into measured evidence rather than leaving it as inference. (Correction: an earlier wording claimed every post-merge median fell *inside* the pre-#94 ranges. That is not literally true — the bulk-workspace medians were 697.214, 696.860 and 766.784 ms against a pre-#94 range of 680.838-713.694 ms, and the slowest run sat above it. Every metric stayed far under budget and the run-to-run spread was ordinary machine variance, which is what the evidence supports.) No production code, budget, or gate-status change. Alt: deriving ceilings from `WorkspaceSearchLimits`, leaving the global cap as an unreachable inequality, narrowing the documentation instead of adding the missing assertions, and unbounded test waits were rejected because each either cannot fail on the regression it claims to cover or converts a failure into a hang. |
| 2026-07-26 | WS4B second review pass budgets the default `.smart` case path and makes the admission-cap and progress probes falsifiable | This entry **extends the two preceding WS4B rows**. Every probe previously forced `.sensitive`, but the UI default is `.smart`, which resolves to the *insensitive* Foundation backend for any all-lowercase or CJK pattern — an unbudgeted comparison path. Three probes now cover it: an anti-vacuity gate proving the resolution behaviorally (one lowercase pattern matches three case spellings under `.smart` but one under `.sensitive`, and a CJK pattern matches both CJK occurrences), plus budgeted `.smart` runs over the 2,000-file workspace and over a 524,288-byte CJK file. `.smart` measured no slower than `.sensitive` on the bulk workspace — slightly faster in Release — so the gap was missing coverage, not a regression. The oversized admission-boundary sibling grew from `cap + 1` to 8x the cap, because at `cap + 1` a reader that wrongly read the whole file reports the same byte count as one that stops at the inclusive limit; the probe now asserts the bounded 524,289-byte read against a 4,194,304-byte file, so unbounded reads are detectable. Pinned ceilings gained the 128-ignore-file, 64 KiB-per-ignore-file, 256-UTF-16 query, and 1,024-UTF-16 snippet-context limits. Progress coalescing is compared as a whole sequence rebuilt from the documented stride rule rather than by count, monotonicity, and final value, which a stride of 1 truncated at 100 events would also satisfy. The 1,258-line test file was split into eight files under the agent.md §17.10 guidance. Re-measurement recorded a real risk rather than hiding it: on a cold first Debug run `unicode-periodic` produced a 2,709 ms sample against its 2,500 ms budget with a 2,462 ms median, so the previously documented uniform 2.4x-3.8x headroom is false cold; `docs/perf-log.md` now tabulates true per-metric headroom and flags that budget as effectively at parity. Owner decision was to fix the harness rather than the number: `WorkspaceSearchPerformanceWarmUp` is a one-per-process actor invoked from `setUp()` that runs a bounded search touching candidate planning, anchored reads, UTF-8 decoding, both case backends, composed whole-word rejection, snippet construction, and stream delivery before any probe executes. The per-probe warm-up could never absorb this because the cost is paid once per process — dyld, Foundation/ICU initialization, first task-group construction, CPU frequency ramp — and lands entirely on whichever probe XCTest runs first. On the cold first Debug run after a build, `unicode-periodic` fell from a 2462.412 ms median (worst sample 2709.261 ms, over budget) to 1149.402 ms (worst sample 1177.059 ms), and every metric's worst-case Debug headroom is now 2.2x or better with **no budget changed**. This moves the harness, not the product: process start-up is not search cost and the budgets describe steady-state search, with the explicit trade-off that the gate no longer observes process start-up regressions it only ever caught by accident. Alt: leaving the default case path unmeasured, keeping a `cap + 1` oversized sibling that cannot distinguish bounded from unbounded reads, asserting progress by shape summary only, and quietly widening `unicode-periodic` to restore the headroom claim were rejected because the first three cannot fail on the regressions they nominally cover and the fourth would be inventing a budget to rescue a run. |
| 2026-07-26 | WS4B third review pass makes read bounds, the ignore ceiling, and progress coalescing individually falsifiable | This entry **extends the preceding WS4B rows**. Three probes could not fail on the regression they nominally covered. The oversized-file probe asserted `diskReadByteCount`, which is derived from the same buffer the reader returns, so a reader that read an entire 4 MiB file and truncated to 524,289 bytes reported an identical number; it moved to asserting `WorkspaceSearchDiskFileReader.readChunk` events from the production reader, reached through `@testable import WorkspaceKit`. **Superseded by the 2026-07-27 row: chunk counts alone were still insufficient**, and the probe now asserts syscall byte counts. The 64 KiB ignore ceiling was a pinned constant with no behavior attached, so removing either the bounded read or the oversize rejection changed nothing; an oversized-`.gitignore` probe now proves the rule is discarded unparsed and the read stops at two chunks, with an under-ceiling control proving the same rule is otherwise honored so the result cannot pass by the rule never working. Progress coalescing compared a whole sequence, but every fixture had `N <= 100` or `N` divisible by 100, where `floor` and `ceil` agree and the final `N / N` event is indistinguishable from an ordinary stride hit; a 250-candidate probe separates them. The process warm-up marked itself complete before awaiting and discarded its stream, so concurrent callers could race ahead of an in-flight warm-up and a failed warm-up counted as success; callers now await a shared task, and the warm-up validates its own terminal and match counts and is not cached on failure. CJK assertions were non-discriminating because caseless text matches identically under either backend; the CJK probes now use a lowercase-Latin-suffixed pattern against an upper-case occurrence with a `.sensitive` control that must find nothing. The gate is 14 probes across 11 files, all under the §17.10 guidance, inside a 23-test `PerformanceTests` target. `docs/perf-log.md`'s WS4B section was rewritten rather than amended, so it carries one current set of numbers and reproduction commands including the separate `build-for-testing` step, instead of a chain of corrections. Alt: asserting returned byte counts, pinning the ignore ceiling without behavior, testing coalescing only at divisible candidate counts, a flag-based warm-up, and caseless CJK assertions were rejected because each passes through the exact regression it claims to cover. |
| 2026-07-27 | WS4B read bounds are proven from syscall byte counts, not chunk counts | This entry **supersedes the read-bound evidence in the preceding 2026-07-26 row**. Asserting `readChunk` indices still could not fail on the regression it existed to catch: a read loop that asked for a full 64 KiB buffer on its final call and truncated the result afterwards emits the same nine indices as a correctly bounded one, and the oversized-`.gitignore` probe's two chunks could equally have been 131,072 bytes rather than the bounded 65,537. The diagnostic hook therefore now carries the bytes **requested of** and **returned by** each `read(2)`: `WorkspaceAnchoredFileSystem.Event.readChunk` gains two associated values, `WorkspaceAnchoredFileSystem+Primitives` emits the already-computed locals, and `WorkspaceCoherentFileReader` plus `WorkspaceSearchFileReading` forward them — four production files, +12/-6, confined to an observation-only payload with no control flow touched and no change to `WorkspaceCoherentFileReader`'s published two-tuple event, so its existing test is unaffected. This is the PR's only production change and it retires the earlier "no production code changes" claim. The probes assert exact totals: 524,289 requested bytes across nine chunks with a final one-byte request for the 4 MiB sibling, and 65,537 across two for the oversized ignore file. Separately, the read-bounds, ignore, and CJK-cased control runs asserted results without terminal shape, so a run that emitted a result and then `.failed`, or never completed, would have passed; they now go through `assertSharedStreamInvariants`, except the under-ceiling ignore control whose rule removes the only candidate and which asserts terminal correctness directly. Documentation claims were narrowed to match: not every probe drives the service (the ceiling pin performs no search), not every timed probe has a warm-up (the cancellation probe repeats five independent cancellations instead), and "every completing run" is now "every completing run with at least one surviving candidate". Alt: keeping index-only assertions, leaving the new probes without terminal checks, and retaining the broader wording were rejected because each leaves a gate that cannot fail on its own subject. |
| 2026-07-27 | WS4B fifth review pass brings every CJK control under the shared invariants and repairs its evidence chronology | The two CJK-cased controls had remained outside `assertSharedStreamInvariants`, so their match-count assertions could pass even if a result was followed by `.failed` or the stream never completed; all five smart-case runs now prove their expected result count, exact progress and event shape, read-window ceilings, and completion ordering. Source budget comments were synchronized to the `a09cafb` measurement record. The preceding read-bound row was added, the earlier chunk-count row now points to its superseding syscall-byte proof, and a numerically false claim that every post-PR-#94 median fell inside the earlier ranges was corrected with the actual bulk medians. `docs/perf-log.md` and `docs/workspace-search-plan.md` were narrowed to the real probe and warm-up exceptions. This row records the fifth pass implemented by `a777dc4`; the pass had been described in commit and review history but was missing from this table. Alt: counting the match assertions as terminal evidence or keeping flattering but false historical summaries was rejected because both overstate what the artifacts prove. |
| 2026-07-27 | WS4B zero-result controls prove the candidate was searched, and the warm-up checks the whole terminal contract | This entry **extends the preceding 2026-07-27 row**. A control asserting "no results plus a clean completion" is satisfied equally well by a file the ignore policy removed or the admission cap skipped, so the CJK case-sensitive controls could not distinguish "searched and found nothing" from "never looked". `assertSearchedWithoutMatching` now requires `searchedFileCount == 1`, `ignoredFileCount == 0`, `skippedFileCount == 0`, `diskReadCount == 1`, and the exact `diskReadByteCount` for the fixture, on top of the shared stream invariants. The process warm-up retained only the most recent summary and failure, so a duplicate `.completed`, or progress arriving after a terminal, would have passed as healthy; it now requires exactly one terminal event, no failure terminal, and no events after the terminal. Evidence-attribution drift was also corrected rather than restated: the three-run Debug/Release tables belong to `a09cafb`, and the only Swift change after it adds assertions to already-collected runs outside every timed region, so later commits carry single-run Debug plus exact-tip CI as their evidence. Alt: leaving zero-result controls on stream shape alone, keeping last-value-wins terminal handling, and attributing the older measurement runs to the current tip were rejected because each reports more confidence than the artifacts support. |
| 2026-07-27 | WS4B ignore control rejoins the shared invariants; warm-up and cancellation close their last event gaps | This entry **corrects the preceding 2026-07-27 row's claim that the under-ceiling ignore control cannot use the shared invariants**. That was based on a wrong reading of the plan: `WorkspaceSearchCandidatePlan.candidateFileCount` is `items.count`, and `.ignored` entries are plan items, so an ignored-only workspace still reports `candidateFileCount == 1` and still emits its `1 / 1` progress event. The control therefore goes through `assertSharedStreamInvariants` like every other completing run, and asserting terminal shape alone had been letting a missing, wrong, or duplicated progress event through. The process warm-up treated `.validationFailure` as ignorable because it is not a terminal event, so a stream that emitted one and then completed passed every guard; it is now rejected explicitly. The cancellation probe checked only summaries, failures, terminals, and file results, while in that fixture every read blocks and no plan item finishes, so no event of any kind is legitimate — it now asserts the stream is entirely silent, which also covers progress, skipped files, and validation failures. Evidence wording was corrected again: the claim that every post-`a09cafb` Swift change runs after `collect(...)` is not exact, because the warm-up's bookkeeping is inline with stream iteration; what is true, and what the measurement attribution rests on, is that all of it sits outside every timed region. Alt: keeping the ignore control on terminal-only assertions, treating `.validationFailure` as benign, enumerating cancellation event kinds individually, and leaving the `collect(...)` wording were rejected because each either hides a real gap or overstates what the artifacts show. |
| 2026-07-27 | WS4B narrows the cancellation claim to what the consumer can observe, and stops citing a vacuous skipped-detail bound | This entry **narrows the cancellation wording used in the three preceding 2026-07-26/27 rows**, which said the probe proves no `completed` or `failed` terminal is emitted after cancellation. It does not. Cancelling the consuming Task also terminates the `AsyncStream` continuation, so a terminal the producer wrongly yielded afterwards is discarded before it can reach the collected events; `events.isEmpty` proves what the consumer observed, not what the producer attempted. Establishing the stronger claim needs a yield-observation seam or a direct pipeline test, and this gate does not have one, so the claim is now stated as: the consumer observed no events, all four blocked reads were released, and no further read started. Separately, the shared helper's `skippedFiles.count <= 100` bound is a shape check rather than evidence — no WS4B fixture produces more than one skipped file, so the bound is never approached. Cap enforcement already has a real contract test in WorkspaceKit (`WorkspaceSearchResourceContractTests.testSlowConsumerReceivesBoundedLosslessResultsDetailsProgressAndTerminal`, 600 skips against a detail limit of 7, asserting the retained prefix and `omittedSkippedFileCount`); WS4B's contribution is pinning that production's default is 100, and the documents now cite that pairing instead of implying WS4B proves enforcement. Read-bound wording is also corrected: the returned byte total is no more decisive than the chunk count, since a truncating loop reports both identically — the **requested** total is what rules it out. Alt: building a yield seam purely to keep the stronger sentence, and rebuilding a 600-skip fixture WS4B does not need, were rejected as cost without new coverage; leaving either claim as written was rejected because both overstate what the artifacts show. |
| 2026-07-27 | In-document find (⌘F) uses MarkdownCore `TextSearchEngine`, not `NSTextFinder` | Phase 3 Goal 14 freezes the find engine on pure `TextSearchEngine.matches` (identical literal semantics to ⇧⌘F workspace search: smart/sensitive/insensitive, Unicode whole-word, 256 UTF-16 pattern cap, newline-containing patterns rejected, UTF-16 `NSRange`, 1-based lines, bounded previews), a pure find-session model for ordinal/next/previous/wrap, an EditorKit controller that debounces, runs off-main, cancels, and revision-fences results, and navigation only through existing `EditorNavigationRequest` / `EditorNavigationCommand`. A later replace feature must go through the WS3B writer-activation path rather than `NSTextFinder`'s mutation path (`STTextFinderClient.replaceCharacters`). **Baseline (code-verified):** STTextView already constructs `NSTextFinder` + `STTextFinderClient` + `STTextFinderBarContainer` and forwards `performFindPanelAction` / `performTextFinderAction`; `MarkdownSTTextView` does not override them; Plainsong menus do not claim ⌘F (⌥⌘F is Format Table; Carbon ⇧⌘F matches exact `cmdKey\|shiftKey` only). Whether a reachable Edit ▸ Find / ⌘F path actually delivers into that stack is a **runtime hypothesis**, verified as a PR B precondition (launch Debug app, inspect Edit menu, physical ⌘F under ABC/Zhuyin): **(a)** reachable built-in path → replace it and disable it in the same PR that ships the Plainsong bar (PR C), never leaving `main` with ⌘F dead; **(b)** no reachable path → nothing to replace, disable is a no-op, no PR claims replacement. Spec-only gates live in `docs/editor-find-gates.md` (F0–F9; PR A); F0 is an I0-shaped blocking spike at the start of PR B; PRs B–D implement model/controller, App bar + focus arbitration, and XCUITest/perf. Alt: shipping on STTextView's built-in `NSTextFinder` bar was rejected despite the free native find-bar UI and system Find menu integration, because match semantics would diverge from workspace search, exact-range navigation would bypass the WS3A gate, and replace would escape writer activation. |

| 2026-07-27 | PR #96 owns the find navigation transport and binds pending steps to their query generation | Review of PR #96's exact tip found the stack order broken: `EditorFindController` already emitted `EditorNavigationRequest(shouldFocusEditor: false)` while that property was only added by PR #97, so #96 failed to compile on its own (`extra argument 'shouldFocusEditor' in call`) and could not merge first. The navigation **transport and effects** therefore move down into #96: `EditorNavigationRequest.shouldFocusEditor` (defaulted `true`, so existing App callers are unchanged), `EditorNavigationEffects.perform(selection:shouldFocusEditor:)`, and the `MarkdownTextViewCoordinator` apply path that restores the pre-navigation first responder when find navigation declines focus. Their EditorKit tests move with them; App's `issueEditorNavigation(shouldFocusEditor:)` stays in #97 because the default keeps #96's App target compiling untouched. Separately, `pendingStepIntent` is no longer a single unversioned enum: it carries the exact `queryGeneration` it was recorded against plus a **net signed step count**, `scheduleMatch` drops intents belonging to a superseded generation, and `applyMatchResult` consumes one only when `intent.generation == fence.queryGeneration`. Previously a ⌘G pressed while query A was computing was inherited by whatever query B the user typed next, and repeated presses during one debounce collapsed into a single step. Applying N steps uses a new pure `EditorFindSession.stepped(by:)` that reduces modulo the match count before offsetting (so `Int.min` is safe) and reproduces repeated `next()` / `previous()` exactly, including caret-anchor resolution of the first step; a counter-only generation (edit / rebind / ⌘E) still activates the current ordinal on the first press and steps only for the presses after it. Alt: keeping the controller's `shouldFocusEditor` use in #97 was rejected because the controller *is* #96's deliverable and would have to navigate with stolen focus meanwhile; preserving step intent across generations was rejected because it applies a user's step to results they never saw; an O(steps) `next()` loop was rejected in favor of the pure O(1) model operation. |

| 2026-07-27 | PR #96 restores the focus **owner**, and pending steps keep their first direction | Second review round, #96's half. **(1)** Non-focusing navigation captured `window.firstResponder` and restored that object. For a focused `NSTextField` that object is the window's **field editor**, which AppKit unmounts from the control the moment the control resigns, so restoring it aimed at a detached editor and could leave first responder on the `NSWindow` — the find field looked un-stolen but could no longer accept typing. `focusOwner(of:)` now resolves a field editor to its owning control (`isFieldEditor` + `delegate`), `restoreFocusOwner` treats "owner focused through its field editor" as already-focused, and the gate asserts the field is typable afterwards rather than merely "not the editor". The end-to-end navigation test could not force this path because STTextView does not claim first responder on every layout, so the capture/restore pair is `internal` and driven directly by `testCapturedFocusOwnerIsTheControlAndRestoreLeavesTheFindFieldTypable`, which reproduces the steal, restores, and then types into the field. **(2)** `PendingStepIntent` retains the **first press's direction** alongside the net count. A counter-only generation (edit / rebind / ⌘E) spends its first press activating the current ordinal, so a net-only model collapses next-then-previous to zero and wrongly stays on current when it must end one match back (and previous-then-next must end one forward). `steps = queryWouldNavigate ? net : net - firstDirection` expresses both. **(3)** New public `suspendNavigation()` fences navigation while **retaining** the resolved session: `cancelInFlightWork` advances the generation so a detached worker is dropped, unpublished navigation and pending steps clear, but `currentOrdinal` survives so a later ⌘G continues from where the user was. With no session yet (still debouncing) there is no ordinal to keep and fencing would strand the retained query, so that case re-runs counter-only instead. PR C consumes it for Escape / Done. Alt: restoring the raw first responder, deriving the counter-only step from the sign of the net, and re-running the query on close were rejected because each is the reported failure. |


| 2026-07-27 | In-document find PR C: App find bar, menus, ⌘E macOS convention, focus independent of ⇧⌘F | PR C on `phase3-editor-find-bar-ui` ships the App find bar (`EditorFindBar` + owned AppKit `EditorFindQueryField`), Edit-menu **Find… / Find Next / Find Previous / Use Selection for Find** (`⌘F` / `⌘G` / `⇧⌘G` / `⌘E`), and AppState lifecycle hooks (edit recompute, file-switch rebind without auto-jump, close bar on no-document / workspace close). **⌘E decision:** macOS convention — set pattern from selection only; never show/focus/close the bar; Format ▸ Inline Code keeps the menu item but drops the `⌘E` shortcut so Use Selection can claim it. Focus tokens (`EditorFindUIState.focusRequestID` / `selectAllRequestID`) are independent of `WorkspaceSearchUIState`; `⌘F` while open re-focuses + select-all and never closes. F0 physical delivery remains ordinary menu item (no Carbon). No new dependencies. **Gates F4b UI, F5 source+preview, F6 IME, F7 remain open / partial** until hosted production-path evidence (App-state unit smoke is not sufficient; earlier tip falsely closed them). F2 latency / F8 / F9 XCUITest remain PR D. Alt: showing the bar on `⌘E` was rejected as a deviation from macOS; Carbon for `⌘F` was rejected after F0 menu delivery succeeded; calling `setQuery` on file switch was rejected because it auto-jumps (F4b requires rebind-only). |
| 2026-07-27 | In-document find review: key-window focus, shared-nav cancel, lifecycle rebind, responder delivery | Review of tip `3f6b38e` blocked merge. Production path now: (1) Find focus is key-window only and consumes a request only after first-responder proof; `focusSupersededID` abandons pending Find focus on ⇧⌘F / Escape without advancing `focusRequestID`; (2) controller `pendingStepIntent` preserves ⌘G / ⇧⌘G during debounce without restarting the query; (3) query/options/⌘E/rebind/reload/rekey/close publish a newer shared-channel `.cancel` so superseded ranges cannot apply; (4) External Reload, rename/move, Save Copy, and workspace/no-document close rebind or clear Find; (5) menus deliver via `sendAction` → `STTextView` hooks (`EditorFindCommandDelivery` / `EditorFindActionHooks`). Controller test class split for SwiftLint on #96; #97 restacked and converted back to Draft. Alt: leaving menu → shared AppState only was rejected by the F0/Format responder-chain contract; consuming Find focus tokens on ⇧⌘F was rejected because F7 requires token independence. |
| 2026-07-27 | In-document find second review: shared focus receipt, chrome eligibility, and hand-off fences | Review of PR #96/#97 blocked merge on six P1s. #96's half is the preceding row. In #97: **(1)** `EditorFindUIState` gains an App-owned `focusAppliedID` beside `focusSupersededID`, arbitration moves into pure `EditorFindFocusArbitration`, and the owned query field's attempt becomes a bounded key-window retry (180 × 16 ms, live re-read every iteration) instead of one `DispatchQueue.main.async` hop against a per-coordinator receipt. `AppState` is shared across the `WindowGroup`, so the old per-coordinator receipt let a second window or a remounted bar replay a spent ⌘F token, and the single async attempt lost the first-⌘F mount race outright. **(2)** Save Copy rekeyed Find *before* the state it reads was installed: `quarantineIndeterminateSaveCopy` notified before `indeterminateSessionWriteContexts` and `session.reset(newURL)`, and `completeDetachedSaveCopy` before `adoptAnchoredFileBinding`, so `sessionStateURL` still returned the old URL and no second rebind ever corrected it. Both notifications move after their transaction completes. **(3)** `commitWorkspaceSearchActivation` skips `setCurrentDocument` when the result is already the current document, so nothing fenced find there; a find generation started earlier landed afterwards, drew a higher ID from the shared navigation generation, and dragged the selection back to the find hit. `activateWorkspaceSearchResult` now calls `notifyEditorFindWorkspaceSearchWillNavigate(to:)` before `issueEditorNavigation`, which re-runs the retained query as a counter-only `.patternOnly` generation anchored at the activated range — the counter stays accurate, no navigation is emitted, and the first later ⌘G continues from the search result. Escape / Done uses the same fence, so a query still inside the debounce window cannot move the selection after the bar is gone. **(4)** Find-bar chrome no longer no-ops under Full Keyboard Access: the bar's own Next / Previous / Return call `stepEditorFindFromBarControl`, which deliberately skips the responder guard (that guard exists for *menu* commands), and `EditorFindResponderSupport` additionally accepts a first responder inside the find bar's own subtree — located by walking up from the responder to the nearest ancestor containing the owned query `NSTextField` and rejecting it if that ancestor also contains the editor. SwiftUI accessibility identifiers are deliberately not used for this, because they do not materialize without an assistive client (WS3C PR C precedent). **(5)** ⌘E no longer trusts a *published* find navigation: publishing is not applying, so the selection cache is fed by `EditorSelectionProbe.keyWindowAppliedEditorSelection()`, which reads the editor view regardless of first responder. Coverage is production-path, not hook-level: `EditorFindLifecycleCombinationTests` drives `refreshWorkspaceAfterFileSystemChange`, `keepMineForExternallyChangedFile`, `renameWorkspaceItem`, and `saveDetachedCurrentDocument` (including an injected `committedButIndeterminate` quarantine), and each new fix was verified to fail when its production change is reverted. `AppState+EditorFind.swift` split into `AppState+EditorFindLifecycle.swift` and `EditorFindResponderSupport.swift` for §17.10. **No gate box was checked**: F4b UI, F5 source+preview, F6, and F7 still need hosted first-responder / UI-visibility evidence, and F2 latency, F8, F9 remain PR D. Alt: keeping a per-coordinator focus receipt, rekeying Find mid-transaction, fencing find only on document switch, gating the bar's own buttons on the menu responder check, and caching a published range as the selection were rejected because each is exactly the reported failure. |

| 2026-07-27 | PR #97 second review: SwiftUI-reported chrome focus, no Escape key equivalent, shared select-all, selection provenance | **(1)** The AppKit ancestor walk added in the first review round could not fire in production: macOS SwiftUI flattens the find bar's toggles/buttons and the editor into one hosting view, so there is no find-bar-specific ancestor and no per-control `NSView` to identify — the walk's test built a hierarchy production never produces. It is removed. Focus on those controls is now reported by SwiftUI's own `FocusState` as `EditorFindChromeFocus`, trusted only while `keyWindowHostsFindBar()` confirms the key window is the one showing the bar (the owned query `NSTextField` is a real view with a stable identifier, so that much *is* answerable in AppKit). This is observation only — query-field focus is still driven through the owned control, per the WS3C precedent against `FocusState`-driven focus. Residual, accepted: if SwiftUI failed to clear its own focus when focus left the bar, a find command could fire while focus sits elsewhere in that same window; that is bounded to one window and one document and is strictly less harmful than the commands and the bar's own buttons going dead under Full Keyboard Access. **(2)** Done drops `.keyboardShortcut(.cancelAction)`. A key equivalent is resolved before the field editor sees the event, so Escape bypassed the query field's marked-text reservation and closed the bar mid-IME composition instead of cancelling the composition. Escape now closes only through the query field's `doCommandBy`, which is also what `NSTextFinder`'s own bar does. **(3)** Escape / Done calls the new `suspendNavigation()` instead of re-running the query counter-only. Re-running re-resolved the ordinal from the caret anchor, so a user sitting on hit 2 of 3 got hit 1 back on the next ⌘G instead of hit 3. **(4)** `selectAllAppliedID` joins `focusAppliedID` as an App-owned receipt; the coordinator-local one started at zero in every new window, so a second window replayed a spent select-all on its first binding update and the next keystroke replaced the whole query. **(5)** `EditorSelectionProbe` returns `EditorAppliedSelection` carrying the installed document identity, the App-owned source revision, and installed state. A native range is an offset into whatever source the editor currently holds, so during a document switch or a same-URL Reload stamping it with App's current identity let ⌘E index new content with an old range; the pure `EditorFindAppliedSelectionPolicy` now requires identity, revision, installed state, and bounds to agree. Test-shape corrections, all three found by CI or by reverting the fix under test: the find-bar chrome eligibility test was **order-dependent** — a sibling test built a window carrying the owned query-field identifier and left it key, so `keyWindowHostsFindBar()` returned true for every later test in the process; that window is now ordered out in a `defer` (with `isReleasedWhenClosed = false`, because a programmatic `NSWindow` defaults to release-on-close and the first teardown attempt crashed the test host mid-suite), and the gate itself gained an AppState override so both directions are driven explicitly instead of read from ambient `NSApp` state. `EditorFindUITests`' marked-text gate stopped scraping `EditorFindQueryField.swift` for substrings — that proved nothing about behaviour and deadlocked the sandboxed test host in `open()` once the file's cached sandbox grant went stale — and now drives the real `control(_:textView:doCommandBy:)` callback with live marked text. The EditorKit windowed fixture now stamps the editor accessibility identifier `MarkdownTextView.makeNSView` sets in production (the probes locate the editor by it), and the probes gained window-scoped internal seams because a test runner does not report a programmatic window as `NSApp.keyWindow` — the first attempt at these tests skipped rather than ran. No gate box changed. Alt: keeping the ancestor walk, conditioning the Escape key equivalent on a published composing flag (a SwiftUI re-render race), re-running the query on close, a coordinator-local select-all receipt, and consuming a bare native range were rejected because each is the reported failure or an unprovable variant of it. |

| 2026-07-28 | PR #97 third review: window-scoped chrome focus, Escape from anywhere in find context, revision-scoped selection cache | **(1)** `chromeFocus` was one App-global value while every `WindowGroup` window's `EditorFindBar` reports its own SwiftUI `FocusState`, so focus stranded in a background window made ⌘G / ⌘E eligible in whichever window was key. Reports are now `EditorFindChromeFocusReport(windowNumber:focus:)`, tagged with the `NSWindow` learned from the owned query field, and eligibility requires the report's window to *be* the key window. The prior test seam stubbed the answer outright; it is replaced by a `keyWindowNumberOverride` that stubs only which window is key, so the production comparison still runs and a cross-window regression is observable — including the case where nothing changes but which window is key. `keyWindowHostsFindBar()` is gone: the window match subsumes it. **(2)** Removing the Done button's `.cancelAction` fixed the IME bypass but left the query field's delegate as the *only* Escape handler, so Escape did nothing from the editor or from Aa / whole-word / Previous / Next / Done — breaking the product contract that Escape is a close path. Two routes restore it without reintroducing a key equivalent: `MarkdownSTTextView.cancelOperation` calls a new `EditorFindActionHooks.cancelFind` **after** ruling out marked text and an open completion list, and falls through to `super` when the hook declines so STTextView's own Escape behaviour is preserved; the bar uses SwiftUI `.onExitCommand`, re-checking live query-field composition at dispatch because a field editor that declines `cancelOperation:` lets the event bubble past it. **(3)** `EditorFindCachedSelection` carries `sourceRevision`. The live probe already validated identity + revision + installed state, but every failure fell back to an identity-only cache, so a same-URL Reload let ⌘E read an old range out of new text — the regression reproduces literally, picking up `"zzzzz"` from replaced content. All five new tests were verified to fail with their production change reverted. Splitting `AppState+EditorFindSelection.swift` out keeps §17.10 file lengths at the prior lint baseline. No gate box changed; F6's Escape-during-composition path and Full Keyboard Access eligibility both still need owner/hosted evidence. Alt: an App-global focus value, a stubbed eligibility answer, re-adding a `.cancelAction` key equivalent, and an identity-only selection cache were rejected because each is the reported failure. |

| 2026-07-28 | PR #97 fourth review: window-scoped *clearing*, deferred window bridge, restack onto merged main | **(1)** The previous round tagged chrome-focus reports with their window but left clearing unconditional, on the explicit reasoning that clearing "only ever removes eligibility" and is therefore safe. That reasoning was wrong: the report store is App-global, so a background window losing focus or unmounting wiped the **key** window's live report and took ⌘G / ⌘E down with it — the same cross-window bug from the other direction. Every mutation is now scoped: a window may overwrite or clear only a report it owns, and an untagged call is a no-op rather than a clear. `clearEditorFindChromeFocus()` remains unconditional but is now explicitly an **App-scoped** intent (bar closed / document switched / workspace closed), where no window's report can still describe focus inside the bar. `EditorFindBar.onDisappear` passes its own window so one bar unmounting cannot wipe another's. **(2)** The window number was read in `EditorFindQueryField.updateNSView` and written straight into the bar's `@State`, which is a "Modifying state during view update" hazard with undefined publication — and no test exercised that path, because the tests wrote `AppState` directly. It is replaced by `EditorFindBarWindowBridge`, an `ObservableObject` following the existing `WindowKeyStateTracker` shape: the window comes from an AppKit probe's attachment callbacks and is published on the **next** main-actor turn. Because publication is a turn late, `.onChange(of:windowNumber)` re-homes a report that was made before the window was known. **(3)** #96 merged as `8cab153`, so this branch was semantically restacked with `git rebase --onto origin/main 5b77690`; the previous tip was `CONFLICTING` and had no current-tip `build-and-test`. All three new tests were verified to fail with their production change reverted, including the synchronous-publish variant. `AppState+EditorFindChromeFocus.swift` splits out to hold the §17.10 lint baseline. No gate box changed. Alt: unconditional clearing, synchronous `@State` publication from `updateNSView`, and a plain merge of `main` instead of a semantic restack were rejected because each is the reported failure. |

| 2026-07-28 | PR #97 fifth review: chrome focus becomes one report **per window**; the window bridge gets generation fencing | **(1)** Tagging a single shared report with its window fixed unauthorized *grants* and, after the fourth round, unauthorized *clears* — but a non-`nil` report from any window still replaced the slot outright, because there was only one. The failure the tag cannot reach: windows A and B each retain their own SwiftUI `FocusState`; B writes last; returning to A republishes nothing (neither A's focus nor A's window number changed, so no `onChange` fires) and A's ⌘G / ⌘E stay dead. Storage is now `chromeFocusByWindow: [Int: EditorFindChromeFocus]` — one entry per window, written only for the reporting window in both directions, read by key-window lookup, with `clearEditorFindChromeFocus()` remaining an App-scoped clear-all because `isBarVisible` is shared. The old `EditorFindChromeFocusReport` is gone. **The previous round's test was actively misleading**: `testBackgroundWindowCannotOverwrite…` asserted that background 101 *did* overwrite key 202 and that the context went inactive — the assertions were written to match the implementation instead of the contract the name states. It now asserts what its name says, and a new test drives the switch-back scenario directly. **(2)** `EditorFindBarWindowBridge.attach` compared against the **published** value, which is a turn stale: `attach(A)` then `attach(nil)` saw `windowNumber == nil`, returned early, and let A's in-flight task publish a window that was no longer attached; A→B→nil could likewise settle on a stale window. It now compares against a retained `desiredWindowNumber`, bumps a generation, cancels the prior task, and re-checks the generation after the hop. `lastAttachedWindowNumber` survives detach so `onDisappear` can still address the report it made once the probe has already published `nil`, and `dismantleNSView` detaches explicitly. Tests cover attach→detach with no intermediate publication, A→B→nil, and dismantle. All new tests were verified to fail with their production change reverted. No gate box changed. Alt: keeping a single scalar report with owner-checked clears only, and comparing the bridge's desired window against its published value, were rejected because each is the reported failure. |

| 2026-07-28 | PR #97 sixth review: find command selectors move behind EditorKit; stale comments and PR topology corrected | **(1)** `EditorFindCommandDelivery` was the only App file importing STTextView, naming `#selector(STTextView.plainsong…)` directly — a violation of the §6.1 locked abstraction that App/ and MarkdownCore/ never import STTextView types. A new public `EditorFindCommandDispatcher` in EditorKit owns the `EditorFindCommand` → selector mapping, mirroring `EditorCommandDispatcher` for Format; App sends a command and consumes only the delivered/not-delivered `Bool`, which is what its fallback needs (the Format dispatcher returns nothing because it has no fallback). That fallback is not find-field-only: it serves every focusable find surface the responder chain cannot reach an editor through — the owned query field and its field editor, and the bar's SwiftUI chrome (Aa, whole-word, Previous, Next, Done). App now imports STTextView nowhere. The dispatcher uses `NSApplication.shared` rather than `NSApp`, because `NSApp` is nil until something instantiates the application and made delivery depend on unit-test ordering — the first version of its test crashed on exactly that. **(2)** Comment/doc accuracy, all wrong in ways that would mislead the next reader: `clearEditorFindChromeFocus` and `chromeFocusByWindow` claimed a document switch clears focus and closes the bar, when F4b keeps the bar open across a file switch and the clear-all runs only for Escape / Done, the last document closing, and workspace close; `EditorFindResponderSupport` described only the "no entry for the key window" fallback when the implementation also falls back with the bar hidden, and still used the removed singular `chromeFocus` name (also fixed in `isEditorFindCommandContextActive`); `docs/editor-find-gates.md` still said "stacked on PR B", still called #96 "this PR", and its F5 heading still read as though source+preview belonged to PR C as settled work when that box is unchecked. Alt: leaving the selectors in App with a comment acknowledging the exception was rejected because §6.1 is a locked decision with an existing dispatcher precedent, and paraphrasing the lifecycle comments rather than correcting them was rejected because they asserted behaviour the code does not have. |

| 2026-07-28 | PR #97 seventh review: editor-focus probing becomes window-scoped; test windows are torn down | **(1)** `EditorFindResponderSupport.windowHasEditorOrFindChrome(_:)` documented itself as answering about the window it is given, but consulted `EditorSelectionProbe.keyWindowHasEditorFocus()` first — a **global** read — so asking about window B returned `true` whenever window A held editor focus. `EditorSelectionProbe` now exposes window-scoped `hasEditorFocus(in:)` and `editorTextView(focusedIn:)`; the key-window entry points delegate to them, and the eligibility check uses the scoped form. **Where the evidence lives matters here.** The App-level A/B two-window test that the review asked for cannot reproduce the failure: `NSApp.keyWindow` is nil in the App test host (diagnosed directly — the host app is not active), so a keyWindow-based implementation falls through and both windows answer correctly anyway; reverting the fix left that test passing. The deterministic guard is therefore `EditorSelectionProbeTests.testEditorFocusIsScopedToTheWindowAsked` in EditorKit, which does fail when the probe is routed back through the key window. The App test is retained with a comment stating what it does and does not prove, rather than presented as coverage it is not. **(2)** The positive find spike test made an *editor* first responder and key without ordering the window out, so AppKit could keep it reachable and pollute later tests' responder chains — the same class of leak CI caught two rounds earlier. It now sets `isReleasedWhenClosed = false` and orders out in a `defer`. **(3)** The PR body claimed both "full `make test` green" and "local `make test` not green"; the green full-suite result is exact-tip CI, and the local run is now listed separately with what actually fails on it. Alt: leaving the global read with a narrowed doc comment was rejected because the function's callers pass a specific window; and presenting the App A/B test as the regression guard was rejected once it was shown to pass with the fix reverted. |

| 2026-07-29 | PR #97 eighth review: fixture windows are torn down, and the window-scoping guard reaches the production call site | **(1)** `EditorFindControllerTestSupport.makeWindowedFixture` created and *showed* a key window whose first responder is an editor, and never ordered it out. The window outlives the local variable in `NSApplication.shared.windows`, where it can still answer responder-chain questions for later tests — the same leak class CI caught two rounds earlier, in the shared helper rather than one test. Fixture windows are now registered with `isReleasedWhenClosed = false`, an earlier one is ordered out whenever a new fixture is built, and all six fixture-using classes call `tearDownWindows()` from `tearDown()`. **(2)** The previous round shipped an App-level A/B test that could not distinguish the window-scoped probe from the global one, because `NSApp.keyWindow` is nil in the App test host — and documented that limitation in a comment instead of closing it. Documenting a gap is not a substitute for a guard when a seam is available: `EditorSelectionProbe` now exposes `keyWindowOverrideForTesting`, standing in for `NSApp.keyWindow` across every `keyWindow…` entry point (the same shape as `AppState.workspaceSearchFocusKeyWindowCheck`). The test makes window A editor-focused **and** key, asserts that precondition through `keyWindowHasEditorFocus()`, then requires window B to remain ineligible; reverting `windowHasEditorOrFindChrome` to the global probe now fails it. The EditorKit `hasEditorFocus(in:)` scoping test is retained as the lower-level guard. Alt: leaving the fixture leak to per-test `defer` was rejected because 27 call sites across six classes each have to remember it, and leaving the App test as documentation-only was rejected because the production call site would have had no regression guard at all. |

| 2026-07-29 | PR #97 ninth review: the fixture teardown claim was false; every test window now registers, and the key-window seam is internal | **This entry corrects the preceding row.** That row said all six fixture-using classes call `tearDownWindows()` and are therefore cleaned up. Two of them — `EditorNavigationIntegrationTests` and `EditorInstalledDocumentTransitionTests` — build their windows in their **own** factories, which never registered with the shared registry, so the `tearDown()` added for them was a no-op that read like cleanup: eight navigation fixtures plus a window built inline for the attach test could still be left ordered in with an editor first responder. Registration is now a named `registerWindowForTeardown(_:)` and every such site routes through it. `EditorSelectionProbeTests.testTearDownReleasesRegisteredFixtureWindows` asserts the window is actually gone afterwards through a weak reference — measured first, because `orderOut` hides a window and drops key status but leaves it in `NSApplication.shared.windows`, and `close()` does not remove it either; only releasing the last reference does, so the registry releases as well as hides. Scope boundary stated rather than implied: other EditorKit suites (WYSIWYG gates, document-binding lifecycle, programmatic writer activation) also build windows directly and are deliberately untouched by this find-bar PR. Separately, `keyWindowOverrideForTesting` is **internal**, not public: AppTests reaches it through `@testable import EditorKit` (verified building and running), so a test-only seam does not become mutable global state on a public production API. Alt: leaving the two local factories to per-test `defer` was rejected because a shared `tearDown()` that silently covers nothing is worse than no teardown; expanding into the other window-building suites was rejected as unrelated scope for this PR rather than because those windows are safe. |
| 2026-07-29 | Phase 3 export is gated on offscreen preview rendering and self-contained derived artifacts | Five spec-only choices: **(1)** a dedicated offscreen `PreviewController`, remote fetch disabled, matching `renderComplete`, then one shared successful export-ready barrier; **(2)** typed protocol-v6 `exportHTML` / `exportHTMLResult` with mirrored Swift/TypeScript + bundle rebuild; **(3)** self-contained static HTML with bounded contained-raster data URIs, deterministic omission, bundled styles/fonts, and generated-only SVG; **(4)** full-content `WKWebView.createPDF` plus separate panel-paginated `printOperation(with:)`; **(5)** one-shot `NSSavePanel` authority with exclusive-new/swap-overwrite staging and identity postflight, never session/workspace/recovery authority. PDF's default user space caps one page at 14,400 units (200 inches) per side, which an ordinary long-form post exceeds before `Fixtures/perf-100kb.md` does, so the continuous full-document page is a target rather than an assumption: E0 must measure a fixture past that bound, and a fixed-height pagination fallback that duplicates and drops nothing is pre-fixed so an E0 NO-GO cannot block PR G or silently redirect silent export to `printOperation`. D5's publication primitives are hosted by a one-shot non-retained WorkspaceKit writer that App calls directly, reusing the existing anchored no-follow and `renameatx_np` implementations rather than adding a second copy in App or PreviewKit, and adding no PreviewKit → WorkspaceKit edge. E0 blocks implementation; D3–D5 need explicit owner sign-off. Alt: visible preview capture, raw `outerHTML`, multi-file/relative/remote assets, one PDF/Print path, asserting a continuous page without measuring the platform bound, duplicating filesystem primitives outside WorkspaceKit, destructive writes, or retained export authority were rejected. Spec only: no behavior, dependency, or gate-status change. |

| 2026-07-29 | Export E0 is GO with fixed-height pagination, not a continuous page | The app-hosted E0 spike measured a generated **28,816 pt** fixture whose 420 rendered blocks each carry one unique sentinel. A full-content `WKPDFConfiguration.rect` did not yield one continuous page: WebKit clipped the claimed height into three valid default-user-space page boxes of **14,400 + 14,400 + 16 pt**. First and last sentinels were present in order, so this was neither scaling nor silent truncation, but the continuous D4 model is unreachable. Accepting unplanned cuts was rejected after both WebKit's automatic cut and a naive 7,200 pt slice duplicated a boundary sentinel on the earlier diagnostic fixture; CSS `break-inside` did not change `createPDF` slicing. The accepted fallback measures sentinel-bearing block bounds, selects one operation-fixed height at or below 14,400 pt whose boundaries do not cross a block, and captures adjacent rects. On the final all-block-sentinel fixture it selected **14,400 pt**, produced three pages, and extracted every sentinel exactly once in order: **GO (paginated)**, so v1 Export as PDF is paginated. The separate 11,533 pt multi-viewport fixture captured both sentinels with its full rect, while the same settled render's 600 pt negative-control rect omitted the last sentinel. Exact-ID evidence required one minimal PreviewKit seam, internal `renderForTesting(_:)`, which returns the actual submitted render ID while the public render API remains unchanged. `renderComplete` alone was insufficient: malformed MDX posted it for the exact request while retaining the stale last-good DOM, and the spike rejected capture. E0 used direct JavaScript for full-content and block-bound measurements because protocol v5 exposes neither; D1's diagnostic-PDF exception does not currently name that JS measurement, so a follow-up docs change is required before any production path adopts direct JS measurement (protocol v6 may instead carry typed geometry). The protocol remains v5 with eight messages; no bridge/bundle, product entry point, destination write, dependency, or visible-preview fallback was added. |

| 2026-07-29 | In-document Replace is gated on existing literal Find semantics and WS3B writer authority | `docs/editor-replace-gates.md` fixes current-document v1 on `TextSearchEngine` + `EditorFindSession`; every character mutation must pass App-owned commit authorization and EditorKit's WS3B writer activation, never `STTextFinderClient.replaceCharacters` or direct `textStorage` writes. Regex/capture templates and workspace-wide replace stay separately deferred, and App/MarkdownCore remain free of STTextView types. Replace All requires R0 to prove one writer-authorized batch is one native undo step before implementation proceeds; a per-match undo fallback is not acceptable. This is spec only: no behavior, dependency, code, test, or gate-status change. Alt: a second matcher, NSTextFinder mutation, App-layer source assignment, partial first-10,000 replacement, or shipping Replace All before the undo mechanism is proven were rejected because they diverge from Find, bypass retained authority, or expose partial destructive state. |
| 2026-07-29 | Whole-word boundary detection stops allocating per character; the composed-range cache becomes a ring | `TextSearchResourceBoundTests.testOneMegabyteContinuousUnicodeWordSkipsRejectedCandidatesLinearly` failed intermittently on `main` itself, which under risk R15 (wall-clock budgets hard locally) broke `make test` for every contributor and let PR #98 report a misleading "pre-existing failure". Diagnosis, not budget: the 3.0 s budget had about 1.2x headroom on a quiet machine, and identical `dbc341c` source measured 2.25 s and 8.41 s in the same scenario hours apart. A one-per-process warm-up in the `WorkspaceSearchPerformanceWarmUp` shape (the 2026-07-26 WS4B precedent) was considered and **rejected on measurement**: the test runs eighth of nine in its class and the class scenario was reliably *slower* than running it alone, which is the opposite of a cold-start signature. The avoidable work was real and in production code. Three semantics-preserving changes in `TextSearchComposedSequences.swift`: (1) `isWordCharacter(in:storage:)` decodes UTF-16 straight out of the storage, hand-decoding surrogate pairs, instead of materializing a `String` per composed character — a range splitting a pair yields U+FFFD from `substring(with:)`, which is not a word scalar, so skipping the unpaired unit reaches the same answer; (2) `isWordScalar` resolves ASCII without an ICU general-category lookup, which does not help non-ASCII text but helps ordinary Markdown; (3) the composed-range LRU `Array` (`append` + `removeFirst` on **every** miss, and a marching scan misses on every character) becomes a fixed-capacity ring that overwrites its oldest slot, plus a retained most-recent range serving marching scans in one bounds check — composed sequences partition the storage, so at most one retained range can contain a location and retention order affects hit rate only, never the answer. Debug worst-case headroom goes from under 1.0x to 3.2x with the **budget unchanged at 3.0 s**, the 0.903-0.932 s band is now scenario-independent, and the MarkdownCore package suite drops 12.6 s → 5.3 s. `TextSearchWordBoundaryScalarTests` is mutation-tested: a wrong surrogate shift, either ASCII range off by one, a ring that never advances its eviction slot, and an unguarded most-recent fast path all fail (the last non-terminates). That battery found and closed a real gap — the first ASCII test compared the new decoding against the substring overload, but both call the same `isWordScalar`, making its ASCII shortcut invisible, so `testEveryASCIIScalarAgreesWithTheGeneralCategoryRule` restates the general-category rule as an independent oracle over all 128 ASCII scalars. Because this is the whole-word path the frozen WS4B probes exercise, those were measured head to head rather than assumed unaffected: all 14 pass in both configurations, `unicode-periodic` improves 42% (1154.951 → 669.050 ms), and everything else is within noise. **No WS4B budget is re-frozen against those faster medians** — that would be a separate decision with its own evidence. Measurements are in `docs/perf-log.md`. Existing instrumentation bounds (`literalCandidatesExamined`, `uncachedComposedUTF16UnitsVisited`) and every §12 budget are unchanged. `make test` still fails at this tip on `PlainsongUITests.WorkspaceSearchAcceptanceTests` `testClickThenArrowKeysUseSearchSelection` and `testShortcutKeyboardActivationAndEscapeTransitions` (`Failed to activate application … Running Background`); that was **verified** pre-existing by rerunning the same stage with only this file reverted to `dbc341c`, not inferred. Alt: widening the budget was rejected by the 2026-07-26 WS4B precedent and never reached; changing the 1 MiB fixture shape was rejected because its continuous-word text is what makes the linear-skip assertion meaningful; a process warm-up was rejected on the measured evidence above; keeping true LRU move-to-front was rejected because the retained most-recent range already serves the hot case and ordering cannot affect correctness. |


| 2026-07-29 | Preview Mermaid memo is exact, ordinal-scoped, theme-invalidated, success-only, bounded, and real-DOM tested | The documented memo did not exist: the preview only wrote a 32-bit `data-mermaid-hash`, every fresh tree still held `pre/code`, and `morphdom` replaced each live `div.mermaid-rendered`, so every debounce called `mermaid.render`. Fresh trees now carry same-tag pending wrappers and the render path calls one exported prepare → morphdom/collect → resolve seam. A successful live wrapper is authoritative only when its exact JavaScript source string, absolute Mermaid-fence ordinal, and Mermaid-theme generation all match; remaining wrappers use a 64-entry success-only LRU whose JSON tuple key and stored fields compare the exact source + ordinal, so djb2 collisions cannot authorize reuse. An installed Mermaid 11.15 probe produced 18 IDs, including the renderer ID on the root SVG and in a `url(#…)` reference; inserting that SVG string twice duplicated all 18 IDs. The ordinal therefore isolates equal-source instances, with the deliberate consequence that inserting or removing a fence re-renders every shifted following diagram. Changed wrappers receive the new descriptor and `.mermaid-pending` state but keep the exact prior SVG children as stale visual content until success or error replaces them; pending content is never accepted as authoritative. Concurrent identical misses coalesce in a separately bounded 64-entry in-flight table. A theme change reinitializes Mermaid, advances the generation, clears both tables, and re-renders every visible diagram because theme styling is baked into SVG; failures remain `.mermaid-error`, are not cached, and retry. Both bounds prevent growth across document switches. Vitest now uses the `jsdom` dev dependency to exercise actual HTML/SVG nodes and morphdom callback order; fake node doubles were rejected because they let the broken production wiring pass, while a hosted WKWebView/performance harness is deferred to the measurement follow-up. No measured performance improvement is claimed. Alt: hash-only or source-only reuse, caching failures, an unbounded cache, blanking changed wrappers, retaining SVGs across themes, or continuing fake-DOM-only tests was rejected for collision safety, SVG ID uniqueness, recovery, memory, visual stability, theme fidelity, and falsifiability. |
| 2026-08-08 | PR #108 hosted Editor Find gates: forced preview navigation, main-actor lifecycle, and conservative closure | The original #108 head passed locally but exact-tip CI exposed `EditorFindQueryField.Coordinator` performing AppKit work off-main; Main Thread Checker aborted the test host with signal 6. The coordinator is now `@MainActor`; the hosted gate owns that hunk, and any later F9 integration must drop its duplicate. F5's source+preview path now uses typed scroll intents: ordinary viewport observation remains preference/owner gated, while explicit navigation reaches the mounted preview even with typewriter sync disabled or a preview-owned token active. Navigation received under preview ownership re-arms its 100 ms decay token, and the completion receipt records dispatch-time ownership, so the nearly-expired-token race is deterministic rather than inferred from an immediate assertion; the renewed owner still suppresses the queued editor echo. A JavaScript-evaluation completion receipt plus a separate DOM-visibility assertion prevents a false green, and a missing controller produces an immediate failed receipt rather than a timeout. The hosted suite also revealed SwiftUI selection publication during `NSViewRepresentable` updates; applied/observed selection now defers one main turn only inside that update, fenced by publication generation, document identity, and installed candidate generation. No visible-range lifecycle was deferred. Hosted teardown cancels AppState task inventories, observes root disappearance without calling it graph dismantling, performs test-owned WebKit shutdown, and waits on host-scoped weak `PreviewController` + `WKWebView` deinit proof; production retains the controller across preview hiding. Final local evidence: coordinator unit gates 3/3; PreviewKit delivery/deinit 1/1; exact hosted gates 5/5, with all five host-scoped deinit predicates passing and zero Main Thread Checker hits, SwiftUI state-during-update warnings, exit-barrier timeouts, or `activeSendTransactions` hits. Gate status stays conservative: F5 source+preview closes from two hosted cases with exact source bytes; F4b gains only Reload + missing-file close hosted visibility; F6 gains programmatic live AppKit marked text but still needs real IME/physical input; F7 gains ineligible-host supersession + real Search first responder but still needs eligible dual-window, Full Keyboard Access, and repeated-⌘F evidence. Alt: treating explicit navigation as typewriter sync, leaving the preview token near expiry, accepting command emission or a pre-dispatch owner assertion as preview evidence, synchronously mutating SwiftUI selection during representable update, treating `onDisappear` as deinit proof, fixed-delay teardown, or promoting partial F4b/F6/F7 evidence were rejected because each recreates the observed failure or overstates the gate. |

| 2026-07-29 | Phase 3 F2 freezes production-shaped in-document find performance gates | The committed 1 MiB fixture is driven through App query handling, the production 150 ms debounce, detached matching, fenced application, and App presentation for deterministic zero, sparse, and 10,000-retained truncated cases. Three authoritative Debug and three Release runs at source commit `e7be9b0` are retained with raw samples in `docs/perf-log.md`; the final `<400 / <400 / <1,100 ms` query-completion ceilings leave 1.65x / 1.58x / 1.45x headroom over the slowest retained Debug run medians, and no provisional ceiling was widened after a run. A mounted public editor also drives native insertion through the production writer/App binding while dense find is live: the old session invalidates synchronously without matching, the scheduled representable update stays below the existing `<16 ms` product gate, and the eventual recompute must finish off-main. Wall clocks are hard locally and informational on CI under R15; fixture shape, counts, truncation, invalidation, and off-main assertions remain hard everywhere. F8 stays explicitly open because measured `main` has no production highlight-all apply/clear surface; the harness is reusable later but records no manufactured preservation/apply/clear evidence. Direct-engine microbenchmarks, removing the production debounce, and widening after a slow run were rejected because none would represent the product path or an honest initial gate. |

| 2026-07-29 | F2 review correction narrows the measured contract and preserves real cold-path evidence | **This entry supersedes the F2 closure and standalone-editor claims in the preceding F2 row.** The final measured source is `9de4157f71a1e9fb12cdde696d7e3214b8feff4f`. The hosted probe now mounts the shipped `WorkspaceWindow` with the real observed `AppState`, public editor, and shipped `EditorFindBar`; it measures five-edit medians for synchronous native-input admission (`<5 ms`) and entry of the invalidated AppState snapshot into the root SwiftUI/AppKit update transaction (`<15 ms`). That receipt does not require or prove child-layout completion and is not compositor presentation, physical input, or keystroke-to-screen evidence, so the full §12 `<16 ms` product criterion remains open. The exact 1,048,962-byte fixture is frozen by SHA-256 `d174f48ea6175db568abe44e5b71e82ee92f1cf9c0ed081d8f8308cc1961d247` plus sparse/dense endpoint and line pins. Three Debug and three Release runs retain every raw sample; no budget was widened. A repeatable one-off admission spike is real production work: TextKit selection repair reaches `EditorScrollProxy` and synchronously rebuilds the 1 MiB UTF-16 `EditorScrollLineIndex`; it is recorded rather than prewarmed or hidden and is separate from find matching. Every hosted run's retained console log also contains exactly two pre-measure mount warnings and one pre-measure dense-prime warning from pre-F2 selection-binding paths; each `.xcresult` aggregates them into one runtime-warning issue. The gate accepts only that narrow pre-measure signature/count and does not call the baseline warning-free. Any added/during-measure warning or relevant editor/F8/toolchain/OS change invalidates it. F8 apply/clear remains unmeasured and deferred. Alt: retaining the old standalone-host/max-16 claim, treating root receipt as screen presentation, byte-count-only fixture identity, hiding the line-index spike, or calling a warning-bearing run clean were rejected because each overstates or weakens the evidence. |

| 2026-07-30 | Preview npm bump supersedes Dependabot #102; dompurify's new SVG allowlist entries do not reach Mermaid output | The preview ships as a committed dist bundle (§7.4), so a lockfile-only Dependabot PR leaves `App/Resources/preview/bundle.js` disagreeing with `preview-src/package-lock.json`; rebuilding #102's lockfile reproduces a 318-line bundle diff. Superseding rather than merging is the PR #63 pattern recorded in `docs/codex-handoff.md`. Bumps, matching #102 byte-for-byte: **dompurify 3.4.11 → 3.4.12** (runtime, via `mermaid`; closes open low-severity GHSA-c2j3-45gr-mqc4, where `CUSTOM_ELEMENT_HANDLING` let an allowed custom element skip the `afterSanitizeElements` hook); **postcss 8.5.15 → 8.5.25** (dev-only, via `vitest` → `vite`; closes open **high**-severity GHSA-r28c-9q8g-f849 source-map path traversal); **nanoid 3.3.12 → 3.3.16** (dev-only, pulled in by postcss's `^3.3.12` → `^3.3.16` range bump, not independently security-driven). `package.json` is unchanged — all three are transitive. This entry is recorded despite §17.7 not applying (no dependency added or removed) to preserve one negative finding, so a later reader does not re-derive the wrong conclusion from dompurify's changelog. 3.4.12 does add `dominant-baseline` and `text-orientation` to DOMPurify's SVG attribute allowlist, and the committed bundle shows one additional occurrence of each token. That allowlist change **does not reach Plainsong's rendered output**: real `mermaid.render()` at `securityLevel: "strict"` with `theme: "default"`, run under both lockfiles with the same `mermaid` version, produces identical `dominant-baseline` counts for every diagram type tried (sequence 6, xychart 15, journey 2, and flowchart/pie/state/class/gantt 0), zero `text-orientation` in both, and byte-identical flowchart output. Wherever Mermaid emits the attribute, 3.4.11 already preserved it, because Mermaid's own sanitize configuration already permitted it. `classDiagram` was the only type whose bytes differed, and it differs run-to-run within a single version because rough.js randomizes its hand-drawn path coordinates. Measurement caveat: rendering used jsdom with stubbed `getBBox`/`getComputedTextLength`, which exercises Mermaid's real sanitize path but is not WebKit, so this is evidence about attribute survival rather than a pixel-level guarantee. **No test covers Mermaid output either way**: `test/mermaid-dom.test.ts` stubs the render function, so the suite exercises DOM patching and memoization, not real Mermaid output through DOMPurify — the green suite is not evidence that Mermaid rendering is unchanged, and the finding above is what supports that claim. No pipeline snapshot changed. The bundle diff is dominated by esbuild identifier reallocation (`hVe`→`fVe`→`dVe`…) rather than semantic change, and a rebuild from `main`'s lockfile reproduces `main`'s committed bundle byte-for-byte, so the entire diff is attributable to this bump. Bridge protocol stays v5. Alt: merging #102 as-is was rejected because it lands a lockfile disagreeing with the shipped bundle; `npm audit fix`-style broader updates were rejected to keep the change byte-identical to #102; claiming a latent vertical-text-alignment fix from the allowlist diff alone was rejected because an end-to-end render across eight diagram types does not reproduce it; re-recording snapshots was unnecessary because none moved. |

| 2026-07-30 | F2 final evidence is exact-source, package-sealed, and warning-phase verified | **This entry supersedes only the evidence source, raw numbers, and warning-enforcement details in the two preceding F2 rows; their historical decisions remain recorded.** Six replacement local-hard runs all measure clean commit `c871ddf5c66c17f03fd9456b53f79411f9b2e979`, which contains current `origin/main` at `250e91e`. The wrappers build immutable commit archives, hash the generated source input and the exact resolved checkout/artifact/workspace-state bytes consumed by Xcode while excluding mutable bare Git administration, freeze the host and `.xctestrun`, refuse reused outputs, stream and seal raw logs/results, and recheck all hashes after each run. Final Debug medians of run medians are 244.517 / 251.572 / 654.409 ms for zero/sparse/dense query completion, 1.664 ms for native edit admission, and 6.272 ms for root state-update receipt; the unchanged 400 / 400 / 1,100 / 5 / 15 ms ceilings retain 1.35x / 1.54x / 1.64x / 2.80x / 1.74x headroom over the slowest Debug run medians. The exact fixture SHA and endpoint pins remain unchanged. Each five-edit interval is now bounded by same-UUID `BEGIN`/`END` console markers. The authoritative raw-log checker requires exactly three known warnings before `BEGIN`, zero during, zero after, no unknown SwiftUI diagnostics, two local-hard markers, and one matching coalesced `.xcresult` issue; a negative control moving one warning inside the interval exits 1 despite the coalesced issue. Therefore the current exception is machine-verifiable but still not warning-free or final keystroke-to-screen evidence. The repeated cold-path sample reaches 25.264 ms in Debug, the root receipt excludes physical delivery, child-layout completion, and compositor presentation, and the full `<16 ms` criterion remains open. F8/F9 and highlight apply/clear remain unmeasured; no combined-tip evidence is claimed. Alt: trusting `.xcresult` warning count alone, accepting an unphased warning, sealing mutable Git caches as package provenance, mixing old/new runs, or widening a budget were rejected because they would make the retained evidence non-falsifiable or non-reproducible. |

| 2026-08-08 | F2 retained evidence is versioned, independently auditable, and scoped to proxy gates | **This entry supersedes the retained-artifact availability and raw numbers in the 2026-07-30 F2 row; its source-integrity and warning-policy decisions remain historical context.** Six fresh local-hard runs measure the same clean product source `c871ddf5c66c17f03fd9456b53f79411f9b2e979`, while later tooling commit `03ffd7024ac248977a802bb46b7f0413293979cd` owns the fail-closed capture, process monitor, pack builder, and auditor; product source and evidence tooling are never conflated. The 145-file/916 KiB compact pack is versioned at `docs/evidence/editor-find-f2-c871ddf-retained-pack/` with all six raw logs, digests, monitor boundaries, warning checks, normalized xcresult summaries, manifests, and exact retained tooling. Its manifest SHA-256 is `c7d1a3c68285aa0aac35914fe7d4d60c1bfbc8401b0055e689365b1bbe9989c5`; its `SHA256SUMS` file SHA-256 is `23d3ec514e1a99f65093dede22af190dece22a936d56e999c2bf0740b5bb50bd`. The 1.0 GiB source/package/product/xcresult set remains owner-local and non-portable; supplying it makes the auditor rehash every full artifact and pass six runs, while the versioned pack alone deliberately reports partial rather than pretending Git contains those bytes. Debug median-of-run-medians are 238.206 / 247.120 / 600.940 ms for zero/sparse/dense, 1.109 ms admission, and 4.035 ms root receipt; Release values are 175.865 / 187.466 / 231.986 / 0.944 / 3.465 ms. The unchanged 400 / 400 / 1,100 / 5 / 15 ms budgets were not widened. All six logs retain exactly three known pre-measure warnings and zero during/after the five-edit interval; the auditor replays the warning-placement negative control. The manifest and both audit modes explicitly keep full keystroke-to-screen, F8 apply/clear, F9, and combined-tip open. Alt: keeping docs-only `/private/tmp` prefixes after their raw logs disappeared, calling a compact pack a full-artifact audit, measuring the tooling commit instead of the product commit, or using later F8/F9 work to retroactively close this baseline were rejected because each makes the evidence unavailable, non-falsifiable, or broader than the bytes measured. |
| 2026-08-08 | F2 integration keeps the PR-tip baseline and authenticates newer maintained tooling separately | A normal merge descendant preserves both branch histories without force-push or discarding either implementation, but the active tree selects the PR-tip `docs/evidence/editor-find-f2-c871ddf-retained-pack/` as the sole authoritative six-run family. Its immutable manifest/SHA256SUMS remain `c7d1a3c6…` / `23d3ec51…`; Debug median-of-run-medians remain 238.206 / 247.120 / 600.940 / 1.109 / 4.035 ms and Release remain 175.865 / 187.466 / 231.986 / 0.944 / 3.465 ms. The parallel local pack, assembler, and alternate auditor family remain recoverable only from merge ancestry and are not presented or documented as a second current baseline. Historical `03ffd702…` capture/auditor references and measured-source `c871ddf` build/runner bytes remain unchanged and are explicitly the bytes that produced the retained runs; the current refactor did not. Maintained tooling is split by responsibility (largest source 400 lines), uses a 34-file external operator inventory `3aaebac6…`, isolated/hash-pinned Python bootstrap, `bash -p` hash-pinned shell loaders, canonical current-UID/no-group-or-world-write/no-ACL checks, no-follow identity-checked file hashing, an externally pinned exact pack inventory, and a full audit that cross-binds all nine artifact hashes to the retained evidence/build manifests. Historical format-2 reference bytes stay unchanged; future format-3 packs also retain the bootstrap required by their current thin wrappers. The maintained auditor authenticates the retained historical reference family independently instead of requiring byte equality with newer executing code. The six-run pack alone deliberately exits 3 unless `--allow-partial`; the current owner-local `/private/tmp` full root rehashes successfully but is purgeable and has no independently retained replica, so independent durability remains open. The retained monitor recorded zero matches under its historical classifier, but that classifier exempted every same-path frozen host; the 2026-08-09 correction below therefore keeps uncorrelated-target-process isolation open. The configured 200 ms inter-scan sleep is neither a 200 ms cadence nor continuous-absence proof. Same-current-UID concurrent pathname mutation remains an explicit trust boundary. Full keystroke-to-screen, F8, F9, combined-tip, and independent durable retention remain open. Alt: mixing two baselines, rewriting historical bytes, claiming new tools produced old runs, retaining two active schemas, force-pushing one history away, treating `/private/tmp` as durable, or describing sampled monitoring as continuous were rejected because each breaks provenance, delivery safety, or evidence scope. |
| 2026-08-09 | F2 maintained capture correlates one host to the runner launch; historical process isolation remains open | Review reproduced two trust and ownership flaws in the maintained shell tooling: `/usr/bin/umask 077` ran a child script instead of changing the caller and consulted inherited `PATH`, while the capture module resolved `dirname` through inherited `PATH`; all three shell entry points now use the Bash `umask` builtin and only builtins or fixed absolute system paths before owner/hash verification. The maintained monitor now samples PID, PPID, process group, and executable; the runner starts a dedicated session/process group, exactly one frozen host may remain correlated through that group after reparenting, and a duplicate or unrelated same-path host is a competitor. Cleanup signals only that process group and never enumerates every matching pathname. Deterministic hostile-`PATH`, permissive inherited-umask, duplicate-host, two-correlated-host, escaped-group, and live cleanup-safety negatives enforce those contracts. The immutable format-2 pack and its reference bytes are not rewritten: the current auditor loads that retained schema to authenticate the old artifacts, then reports `historical-uncorrelated-target-process` as OPEN because the old classifier exempted every exact host path. No fresh six-run evidence was captured, so the historical timing samples remain proxy data within their budgets but do not prove process isolation; closing that boundary requires a new exact-source pack from corrected capture bytes. Alt: killing or exempting all same-path PIDs, retrofitting old artifacts, or claiming the refactor produced new evidence was rejected because each breaks ownership safety or provenance. |
| 2026-08-09 | F2 review hardens lifecycle ownership and externally anchors retained source semantics | The maintained capture now keeps a dedicated session leader alive until every other launch-group member is drained, then reaps it and permanently disables that PID's signal path; timeout cleanup signals only while that identity is live. Monitor shutdown is bounded TERM → wait → KILL → wait/reap, and the control directory remains owned until reaping completes. The maintained schema externally pins the retained Git archive SHA-256 `f0b84f1b…` and its reconstructed tree SHA-256 `51b3c530…`; compact audit checks the archive claim against that trust root, while full audit parses the uncompressed tar without extraction, rejects unsafe/non-tree members, reconstructs the logical tree hash, and binds both values to the retained build manifest. Retained `.xctestrun` inputs must match `Build/Products/*.xctestrun`, and retained artifact paths cannot overlap. `make test` now runs the full maintained Python suite, the operator inventory, and the exact-inventory compact audit before product tests, so required `build-and-test` validates this tooling on every tip. The immutable format-2 pack remains unchanged, and historical process isolation, independent durable retention, physical/full keystroke-to-screen latency, F8, F9, and combined-tip remain open. Alt: post-reap group signaling, unbounded monitor waits, pack-rewritable source hashes, arbitrary regular files as `.xctestrun`, or optional-only tooling checks were rejected because they permit PID reuse hazards, cleanup hangs, false source provenance, semantic substitution, or green CI without exercising the PR's implementation. |
| 2026-08-09 | F2 lifecycle handoffs and retained-source binding fail closed across PID and filesystem alias races | Runner and monitor ownership now has explicit `signalable` → `reap-only` → `cleared` states. Each cooperative child stays alive until the parent closes its signal path; final KILL transitions before signaling, Bash's job table prevents a reused PID from regaining ownership, and failed `ps`/state inspection is an error rather than a drained result. Session exit status is fsynced to a private file and atomically renamed, so the parent cannot consume an empty publication. The source-tree hash uses a synthetic 0755 extraction root, making a legitimate private 0700 extraction agree with the externally anchored archive tree; builder and full audit also compare every archive member to `sourceSnapshot`, permitting only the documented generated `Plainsong.xcodeproj` tree, `App/Info.plist`, `App/Plainsong.entitlements`, and Xcode's empty `Packages/*/.swiftpm/xcode` chain for archived local packages. Retained artifact paths reject case-folded ancestry aliases as well as resolved inode/ancestry overlap, matching default case-insensitive APFS. Deterministic trap-window, denied-inspection, atomic-publication, snapshot-reseal, APFS-alias/hard-link, and capture-to-audit hash-contract regressions enforce these rules. Historical format-2 bytes and all six open boundaries remain unchanged. Alt: trusting PID text after cooperative exit, treating inspection failure as absence, direct status creation, mutable snapshot hashes, case-sensitive lexical separation, or hashing the extraction container's ambient mode were rejected because each permits reused-process signaling, partial status reads, source substitution, retained-artifact aliasing, or legitimate capture/audit disagreement. |
| 2026-08-10 | F2 session readiness is published only after the supervisor becomes cancellation-safe | The maintained `session_status.py` installs an absorbing session-leader HUP/INT/TERM handler before creating `session-ready`, treats a pre-go cancellation/`session-drain` as a reap-only release without forking, restores `SIG_DFL` in the child before executing the real runner, and forwards any cancellation caught across the fork boundary. Therefore cleanup immediately after readiness can transition `signalable` → `reap-only` → `cleared`, reap the owned supervisor, and release the control directory instead of losing the supervisor to TERM, racing a post-cancel fork, or hanging while it waits for a `go` that will never arrive. Real-process pre-go and post-go regressions exercise the maintained Python supervisor through the shell ownership lifecycle; the post-go case also proves TERM reaches the runner and is published as status 143. Historical format-2 bytes and all six open boundaries remain unchanged. Alt: publishing readiness under the inherited default handlers, merely moving `SIG_IGN` earlier without recognizing drain, or allowing the child to exec with an inherited non-default handler were rejected because they strand ownership during cancellation or make the runner TERM-resistant. |
| 2026-08-11 | PR #108 forced preview navigation is render- and document-provenanced | Review found that JavaScript delivery alone could succeed before the requested block existed: `scrollToLine` ran immediately while the bridge or latest render was still pending, and the JavaScript side intentionally no-opped when no matching `data-line` existed. Explicit editor navigation now carries its `EditorDocumentIdentity` through the typed scroll intent and App receipt. `PreviewController` binds the latest request to the matching App-supplied document identifier, waits for bridge readiness plus that document's latest `renderComplete`, advances the barrier across newer same-document renders, coalesces to the newest navigation, and fails a pending request when another document supersedes it, even before the first matching render binds the barrier. This prevents both the open-file/immediate-find loss and stale cross-document replay, including detached documents whose render has no file URL. Real-WebView regressions issue navigation before readiness/render completion and require the target to become visible, then prove a pending request fails and leaves the replacement document at the top; a pre-render negative covers the unbound document-switch window. The existing hosted production path remains responsible for editor selection, App routing, and visible source+preview movement; the new controller negatives close the timing/provenance hole in that evidence. Alt: retrying blindly in JavaScript, keying only by per-document version, treating evaluation completion as DOM success, or replaying an unproven request after document switch were rejected because they permit false-green delivery or cross-document movement. |
| 2026-08-13 | Replace All R0 is GO on one enclosing-range native edit | Hosted writer-activation spike compared the three allowed batch mechanisms. Candidate A (`breakUndoCoalescing` + outer undo group + reverse `insertText`) is one Undo/Redo but publishes once per match, so it is NO-GO for Replace All. Candidate B1 builds the exact enclosing-range replacement off-side, then performs one authorized native edit inside one `performPreflightedTextMutation`; it is one writer activation, one publication, one undo group, restores exact pre-batch selection via a first-registered undo action, does not merge with prior typing, and survives `Fixtures/large-1mb.md` `an` × 8,921 with a 256-unit replacement (construction ≈ 4.5 ms, commit ≈ 42 ms, post-batch insert ≈ 0.5 ms). Candidate B2 (full-document replace) also one-publishes and is kept only as fallback. App authorization refusal and stale-writer preflight open no replacement undo. No user-facing Replace shipped. Alt: shipping A because undo grouped, or choosing B2 first, were rejected because N intermediate publications and a full-document native edit are the failures R0 exists to catch. |

---

## 19. Reference Links

- STTextView: https://github.com/krzyzanowskim/STTextView
- Neon: https://github.com/ChimeHQ/Neon
- SwiftTreeSitter: https://github.com/tree-sitter/swift-tree-sitter
- tree-sitter-markdown: https://github.com/tree-sitter-grammars/tree-sitter-markdown
- swift-markdown (export use): https://github.com/swiftlang/swift-markdown
- unified/remark: https://unifiedjs.com · remark-mdx: https://mdxjs.com/packages/remark-mdx/
- Yams (YAML): https://github.com/jpsim/Yams
- XcodeGen: https://github.com/yonaskolb/XcodeGen
- Typora (behavior reference): https://typora.io
