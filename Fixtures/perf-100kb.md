---
title: "Performance 100KB Fixture"
description: "Deterministic 100 KB markdown fixture for Plainsong performance checks."
date: 2026-06-16
draft: false
tags:
  - performance
  - fixture
  - plainsong
---

# Performance 100KB Fixture

This file is generated deterministically for editor highlighting, preview rendering, open-file, and memory smoke checks. Keep the repeated structure stable so regressions are comparable across runs.

## Section 00001

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00001.md), and an image reference ![sample](./assets/image-00001.png). The prose also carries inline math like $score_1 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00001
- [x] Reviewed checkpoint for section 00001
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00001 | repeated table row |
| words | 64 | stable measurement text |

$$
render_1 = parse_1 + highlight_1 + preview_1
$$

```swift
struct PerfSection00001 {
    let title = "Section 00001"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00001","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00001 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00002

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00002.md), and an image reference ![sample](./assets/image-00002.png). The prose also carries inline math like $score_2 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00002
- [x] Reviewed checkpoint for section 00002
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00002 | repeated table row |
| words | 64 | stable measurement text |

$$
render_2 = parse_2 + highlight_2 + preview_2
$$

```swift
struct PerfSection00002 {
    let title = "Section 00002"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00002","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00002 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00003

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00003.md), and an image reference ![sample](./assets/image-00003.png). The prose also carries inline math like $score_3 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00003
- [x] Reviewed checkpoint for section 00003
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00003 | repeated table row |
| words | 64 | stable measurement text |

$$
render_3 = parse_3 + highlight_3 + preview_3
$$

```swift
struct PerfSection00003 {
    let title = "Section 00003"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00003","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00003 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00004

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00004.md), and an image reference ![sample](./assets/image-00004.png). The prose also carries inline math like $score_4 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00004
- [x] Reviewed checkpoint for section 00004
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00004 | repeated table row |
| words | 64 | stable measurement text |

$$
render_4 = parse_4 + highlight_4 + preview_4
$$

```swift
struct PerfSection00004 {
    let title = "Section 00004"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00004","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00004 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00005

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00005.md), and an image reference ![sample](./assets/image-00005.png). The prose also carries inline math like $score_5 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00005
- [x] Reviewed checkpoint for section 00005
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00005 | repeated table row |
| words | 64 | stable measurement text |

$$
render_5 = parse_5 + highlight_5 + preview_5
$$

```swift
struct PerfSection00005 {
    let title = "Section 00005"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00005","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00005 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00006

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00006.md), and an image reference ![sample](./assets/image-00006.png). The prose also carries inline math like $score_6 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00006
- [x] Reviewed checkpoint for section 00006
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00006 | repeated table row |
| words | 64 | stable measurement text |

$$
render_6 = parse_6 + highlight_6 + preview_6
$$

```swift
struct PerfSection00006 {
    let title = "Section 00006"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00006","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00006 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00007

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00007.md), and an image reference ![sample](./assets/image-00007.png). The prose also carries inline math like $score_7 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00007
- [x] Reviewed checkpoint for section 00007
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00007 | repeated table row |
| words | 64 | stable measurement text |

$$
render_7 = parse_7 + highlight_7 + preview_7
$$

```swift
struct PerfSection00007 {
    let title = "Section 00007"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00007","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00007 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00008

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00008.md), and an image reference ![sample](./assets/image-00008.png). The prose also carries inline math like $score_8 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00008
- [x] Reviewed checkpoint for section 00008
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00008 | repeated table row |
| words | 64 | stable measurement text |

$$
render_8 = parse_8 + highlight_8 + preview_8
$$

```swift
struct PerfSection00008 {
    let title = "Section 00008"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00008","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00008 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00009

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00009.md), and an image reference ![sample](./assets/image-00009.png). The prose also carries inline math like $score_9 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00009
- [x] Reviewed checkpoint for section 00009
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00009 | repeated table row |
| words | 64 | stable measurement text |

$$
render_9 = parse_9 + highlight_9 + preview_9
$$

```swift
struct PerfSection00009 {
    let title = "Section 00009"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00009","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00009 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00010

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00010.md), and an image reference ![sample](./assets/image-00010.png). The prose also carries inline math like $score_10 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00010
- [x] Reviewed checkpoint for section 00010
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00010 | repeated table row |
| words | 64 | stable measurement text |

$$
render_10 = parse_10 + highlight_10 + preview_10
$$

```swift
struct PerfSection00010 {
    let title = "Section 00010"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00010","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00010 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00011

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00011.md), and an image reference ![sample](./assets/image-00011.png). The prose also carries inline math like $score_11 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00011
- [x] Reviewed checkpoint for section 00011
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00011 | repeated table row |
| words | 64 | stable measurement text |

$$
render_11 = parse_11 + highlight_11 + preview_11
$$

```swift
struct PerfSection00011 {
    let title = "Section 00011"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00011","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00011 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00012

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00012.md), and an image reference ![sample](./assets/image-00012.png). The prose also carries inline math like $score_12 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00012
- [x] Reviewed checkpoint for section 00012
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00012 | repeated table row |
| words | 64 | stable measurement text |

$$
render_12 = parse_12 + highlight_12 + preview_12
$$

```swift
struct PerfSection00012 {
    let title = "Section 00012"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00012","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00012 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00013

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00013.md), and an image reference ![sample](./assets/image-00013.png). The prose also carries inline math like $score_13 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00013
- [x] Reviewed checkpoint for section 00013
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00013 | repeated table row |
| words | 64 | stable measurement text |

$$
render_13 = parse_13 + highlight_13 + preview_13
$$

```swift
struct PerfSection00013 {
    let title = "Section 00013"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00013","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00013 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00014

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00014.md), and an image reference ![sample](./assets/image-00014.png). The prose also carries inline math like $score_14 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00014
- [x] Reviewed checkpoint for section 00014
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00014 | repeated table row |
| words | 64 | stable measurement text |

$$
render_14 = parse_14 + highlight_14 + preview_14
$$

```swift
struct PerfSection00014 {
    let title = "Section 00014"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00014","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00014 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00015

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00015.md), and an image reference ![sample](./assets/image-00015.png). The prose also carries inline math like $score_15 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00015
- [x] Reviewed checkpoint for section 00015
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00015 | repeated table row |
| words | 64 | stable measurement text |

$$
render_15 = parse_15 + highlight_15 + preview_15
$$

```swift
struct PerfSection00015 {
    let title = "Section 00015"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00015","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00015 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00016

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00016.md), and an image reference ![sample](./assets/image-00016.png). The prose also carries inline math like $score_16 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00016
- [x] Reviewed checkpoint for section 00016
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00016 | repeated table row |
| words | 64 | stable measurement text |

$$
render_16 = parse_16 + highlight_16 + preview_16
$$

```swift
struct PerfSection00016 {
    let title = "Section 00016"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00016","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00016 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00017

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00017.md), and an image reference ![sample](./assets/image-00017.png). The prose also carries inline math like $score_17 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00017
- [x] Reviewed checkpoint for section 00017
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00017 | repeated table row |
| words | 64 | stable measurement text |

$$
render_17 = parse_17 + highlight_17 + preview_17
$$

```swift
struct PerfSection00017 {
    let title = "Section 00017"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00017","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00017 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00018

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00018.md), and an image reference ![sample](./assets/image-00018.png). The prose also carries inline math like $score_18 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00018
- [x] Reviewed checkpoint for section 00018
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00018 | repeated table row |
| words | 64 | stable measurement text |

$$
render_18 = parse_18 + highlight_18 + preview_18
$$

```swift
struct PerfSection00018 {
    let title = "Section 00018"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00018","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00018 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00019

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00019.md), and an image reference ![sample](./assets/image-00019.png). The prose also carries inline math like $score_19 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00019
- [x] Reviewed checkpoint for section 00019
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00019 | repeated table row |
| words | 64 | stable measurement text |

$$
render_19 = parse_19 + highlight_19 + preview_19
$$

```swift
struct PerfSection00019 {
    let title = "Section 00019"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00019","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00019 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00020

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00020.md), and an image reference ![sample](./assets/image-00020.png). The prose also carries inline math like $score_20 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00020
- [x] Reviewed checkpoint for section 00020
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00020 | repeated table row |
| words | 64 | stable measurement text |

$$
render_20 = parse_20 + highlight_20 + preview_20
$$

```swift
struct PerfSection00020 {
    let title = "Section 00020"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00020","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00020 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00021

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00021.md), and an image reference ![sample](./assets/image-00021.png). The prose also carries inline math like $score_21 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00021
- [x] Reviewed checkpoint for section 00021
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00021 | repeated table row |
| words | 64 | stable measurement text |

$$
render_21 = parse_21 + highlight_21 + preview_21
$$

```swift
struct PerfSection00021 {
    let title = "Section 00021"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00021","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00021 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00022

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00022.md), and an image reference ![sample](./assets/image-00022.png). The prose also carries inline math like $score_22 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00022
- [x] Reviewed checkpoint for section 00022
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00022 | repeated table row |
| words | 64 | stable measurement text |

$$
render_22 = parse_22 + highlight_22 + preview_22
$$

```swift
struct PerfSection00022 {
    let title = "Section 00022"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00022","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00022 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00023

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00023.md), and an image reference ![sample](./assets/image-00023.png). The prose also carries inline math like $score_23 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00023
- [x] Reviewed checkpoint for section 00023
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00023 | repeated table row |
| words | 64 | stable measurement text |

$$
render_23 = parse_23 + highlight_23 + preview_23
$$

```swift
struct PerfSection00023 {
    let title = "Section 00023"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00023","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00023 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00024

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00024.md), and an image reference ![sample](./assets/image-00024.png). The prose also carries inline math like $score_24 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00024
- [x] Reviewed checkpoint for section 00024
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00024 | repeated table row |
| words | 64 | stable measurement text |

$$
render_24 = parse_24 + highlight_24 + preview_24
$$

```swift
struct PerfSection00024 {
    let title = "Section 00024"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00024","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00024 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00025

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00025.md), and an image reference ![sample](./assets/image-00025.png). The prose also carries inline math like $score_25 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00025
- [x] Reviewed checkpoint for section 00025
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00025 | repeated table row |
| words | 64 | stable measurement text |

$$
render_25 = parse_25 + highlight_25 + preview_25
$$

```swift
struct PerfSection00025 {
    let title = "Section 00025"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00025","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00025 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00026

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00026.md), and an image reference ![sample](./assets/image-00026.png). The prose also carries inline math like $score_26 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00026
- [x] Reviewed checkpoint for section 00026
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00026 | repeated table row |
| words | 64 | stable measurement text |

$$
render_26 = parse_26 + highlight_26 + preview_26
$$

```swift
struct PerfSection00026 {
    let title = "Section 00026"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00026","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00026 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00027

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00027.md), and an image reference ![sample](./assets/image-00027.png). The prose also carries inline math like $score_27 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00027
- [x] Reviewed checkpoint for section 00027
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00027 | repeated table row |
| words | 64 | stable measurement text |

$$
render_27 = parse_27 + highlight_27 + preview_27
$$

```swift
struct PerfSection00027 {
    let title = "Section 00027"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00027","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00027 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00028

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00028.md), and an image reference ![sample](./assets/image-00028.png). The prose also carries inline math like $score_28 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00028
- [x] Reviewed checkpoint for section 00028
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00028 | repeated table row |
| words | 64 | stable measurement text |

$$
render_28 = parse_28 + highlight_28 + preview_28
$$

```swift
struct PerfSection00028 {
    let title = "Section 00028"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00028","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00028 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00029

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00029.md), and an image reference ![sample](./assets/image-00029.png). The prose also carries inline math like $score_29 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00029
- [x] Reviewed checkpoint for section 00029
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00029 | repeated table row |
| words | 64 | stable measurement text |

$$
render_29 = parse_29 + highlight_29 + preview_29
$$

```swift
struct PerfSection00029 {
    let title = "Section 00029"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00029","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00029 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00030

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00030.md), and an image reference ![sample](./assets/image-00030.png). The prose also carries inline math like $score_30 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00030
- [x] Reviewed checkpoint for section 00030
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00030 | repeated table row |
| words | 64 | stable measurement text |

$$
render_30 = parse_30 + highlight_30 + preview_30
$$

```swift
struct PerfSection00030 {
    let title = "Section 00030"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00030","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00030 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00031

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00031.md), and an image reference ![sample](./assets/image-00031.png). The prose also carries inline math like $score_31 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00031
- [x] Reviewed checkpoint for section 00031
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00031 | repeated table row |
| words | 64 | stable measurement text |

$$
render_31 = parse_31 + highlight_31 + preview_31
$$

```swift
struct PerfSection00031 {
    let title = "Section 00031"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00031","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00031 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00032

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00032.md), and an image reference ![sample](./assets/image-00032.png). The prose also carries inline math like $score_32 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00032
- [x] Reviewed checkpoint for section 00032
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00032 | repeated table row |
| words | 64 | stable measurement text |

$$
render_32 = parse_32 + highlight_32 + preview_32
$$

```swift
struct PerfSection00032 {
    let title = "Section 00032"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00032","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00032 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00033

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00033.md), and an image reference ![sample](./assets/image-00033.png). The prose also carries inline math like $score_33 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00033
- [x] Reviewed checkpoint for section 00033
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00033 | repeated table row |
| words | 64 | stable measurement text |

$$
render_33 = parse_33 + highlight_33 + preview_33
$$

```swift
struct PerfSection00033 {
    let title = "Section 00033"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00033","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00033 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00034

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00034.md), and an image reference ![sample](./assets/image-00034.png). The prose also carries inline math like $score_34 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00034
- [x] Reviewed checkpoint for section 00034
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00034 | repeated table row |
| words | 64 | stable measurement text |

$$
render_34 = parse_34 + highlight_34 + preview_34
$$

```swift
struct PerfSection00034 {
    let title = "Section 00034"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00034","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00034 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00035

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00035.md), and an image reference ![sample](./assets/image-00035.png). The prose also carries inline math like $score_35 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00035
- [x] Reviewed checkpoint for section 00035
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00035 | repeated table row |
| words | 64 | stable measurement text |

$$
render_35 = parse_35 + highlight_35 + preview_35
$$

```swift
struct PerfSection00035 {
    let title = "Section 00035"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00035","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00035 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00036

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00036.md), and an image reference ![sample](./assets/image-00036.png). The prose also carries inline math like $score_36 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00036
- [x] Reviewed checkpoint for section 00036
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00036 | repeated table row |
| words | 64 | stable measurement text |

$$
render_36 = parse_36 + highlight_36 + preview_36
$$

```swift
struct PerfSection00036 {
    let title = "Section 00036"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00036","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00036 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00037

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00037.md), and an image reference ![sample](./assets/image-00037.png). The prose also carries inline math like $score_37 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00037
- [x] Reviewed checkpoint for section 00037
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00037 | repeated table row |
| words | 64 | stable measurement text |

$$
render_37 = parse_37 + highlight_37 + preview_37
$$

```swift
struct PerfSection00037 {
    let title = "Section 00037"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00037","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00037 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00038

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00038.md), and an image reference ![sample](./assets/image-00038.png). The prose also carries inline math like $score_38 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00038
- [x] Reviewed checkpoint for section 00038
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00038 | repeated table row |
| words | 64 | stable measurement text |

$$
render_38 = parse_38 + highlight_38 + preview_38
$$

```swift
struct PerfSection00038 {
    let title = "Section 00038"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00038","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00038 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00039

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00039.md), and an image reference ![sample](./assets/image-00039.png). The prose also carries inline math like $score_39 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00039
- [x] Reviewed checkpoint for section 00039
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00039 | repeated table row |
| words | 64 | stable measurement text |

$$
render_39 = parse_39 + highlight_39 + preview_39
$$

```swift
struct PerfSection00039 {
    let title = "Section 00039"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00039","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00039 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00040

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00040.md), and an image reference ![sample](./assets/image-00040.png). The prose also carries inline math like $score_40 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00040
- [x] Reviewed checkpoint for section 00040
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00040 | repeated table row |
| words | 64 | stable measurement text |

$$
render_40 = parse_40 + highlight_40 + preview_40
$$

```swift
struct PerfSection00040 {
    let title = "Section 00040"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00040","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00040 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00041

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00041.md), and an image reference ![sample](./assets/image-00041.png). The prose also carries inline math like $score_41 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00041
- [x] Reviewed checkpoint for section 00041
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00041 | repeated table row |
| words | 64 | stable measurement text |

$$
render_41 = parse_41 + highlight_41 + preview_41
$$

```swift
struct PerfSection00041 {
    let title = "Section 00041"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00041","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00041 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00042

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00042.md), and an image reference ![sample](./assets/image-00042.png). The prose also carries inline math like $score_42 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00042
- [x] Reviewed checkpoint for section 00042
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00042 | repeated table row |
| words | 64 | stable measurement text |

$$
render_42 = parse_42 + highlight_42 + preview_42
$$

```swift
struct PerfSection00042 {
    let title = "Section 00042"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00042","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00042 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00043

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00043.md), and an image reference ![sample](./assets/image-00043.png). The prose also carries inline math like $score_43 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00043
- [x] Reviewed checkpoint for section 00043
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00043 | repeated table row |
| words | 64 | stable measurement text |

$$
render_43 = parse_43 + highlight_43 + preview_43
$$

```swift
struct PerfSection00043 {
    let title = "Section 00043"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00043","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00043 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00044

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00044.md), and an image reference ![sample](./assets/image-00044.png). The prose also carries inline math like $score_44 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00044
- [x] Reviewed checkpoint for section 00044
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00044 | repeated table row |
| words | 64 | stable measurement text |

$$
render_44 = parse_44 + highlight_44 + preview_44
$$

```swift
struct PerfSection00044 {
    let title = "Section 00044"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00044","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00044 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00045

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00045.md), and an image reference ![sample](./assets/image-00045.png). The prose also carries inline math like $score_45 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00045
- [x] Reviewed checkpoint for section 00045
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00045 | repeated table row |
| words | 64 | stable measurement text |

$$
render_45 = parse_45 + highlight_45 + preview_45
$$

```swift
struct PerfSection00045 {
    let title = "Section 00045"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00045","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00045 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00046

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00046.md), and an image reference ![sample](./assets/image-00046.png). The prose also carries inline math like $score_46 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00046
- [x] Reviewed checkpoint for section 00046
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00046 | repeated table row |
| words | 64 | stable measurement text |

$$
render_46 = parse_46 + highlight_46 + preview_46
$$

```swift
struct PerfSection00046 {
    let title = "Section 00046"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00046","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00046 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00047

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00047.md), and an image reference ![sample](./assets/image-00047.png). The prose also carries inline math like $score_47 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00047
- [x] Reviewed checkpoint for section 00047
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00047 | repeated table row |
| words | 64 | stable measurement text |

$$
render_47 = parse_47 + highlight_47 + preview_47
$$

```swift
struct PerfSection00047 {
    let title = "Section 00047"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00047","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00047 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00048

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00048.md), and an image reference ![sample](./assets/image-00048.png). The prose also carries inline math like $score_48 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00048
- [x] Reviewed checkpoint for section 00048
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00048 | repeated table row |
| words | 64 | stable measurement text |

$$
render_48 = parse_48 + highlight_48 + preview_48
$$

```swift
struct PerfSection00048 {
    let title = "Section 00048"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00048","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00048 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00049

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00049.md), and an image reference ![sample](./assets/image-00049.png). The prose also carries inline math like $score_49 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00049
- [x] Reviewed checkpoint for section 00049
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00049 | repeated table row |
| words | 64 | stable measurement text |

$$
render_49 = parse_49 + highlight_49 + preview_49
$$

```swift
struct PerfSection00049 {
    let title = "Section 00049"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00049","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00049 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00050

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00050.md), and an image reference ![sample](./assets/image-00050.png). The prose also carries inline math like $score_50 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00050
- [x] Reviewed checkpoint for section 00050
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00050 | repeated table row |
| words | 64 | stable measurement text |

$$
render_50 = parse_50 + highlight_50 + preview_50
$$

```swift
struct PerfSection00050 {
    let title = "Section 00050"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00050","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00050 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00051

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00051.md), and an image reference ![sample](./assets/image-00051.png). The prose also carries inline math like $score_51 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00051
- [x] Reviewed checkpoint for section 00051
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00051 | repeated table row |
| words | 64 | stable measurement text |

$$
render_51 = parse_51 + highlight_51 + preview_51
$$

```swift
struct PerfSection00051 {
    let title = "Section 00051"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00051","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00051 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00052

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00052.md), and an image reference ![sample](./assets/image-00052.png). The prose also carries inline math like $score_52 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00052
- [x] Reviewed checkpoint for section 00052
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00052 | repeated table row |
| words | 64 | stable measurement text |

$$
render_52 = parse_52 + highlight_52 + preview_52
$$

```swift
struct PerfSection00052 {
    let title = "Section 00052"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00052","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00052 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00053

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00053.md), and an image reference ![sample](./assets/image-00053.png). The prose also carries inline math like $score_53 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00053
- [x] Reviewed checkpoint for section 00053
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00053 | repeated table row |
| words | 64 | stable measurement text |

$$
render_53 = parse_53 + highlight_53 + preview_53
$$

```swift
struct PerfSection00053 {
    let title = "Section 00053"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00053","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00053 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00054

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00054.md), and an image reference ![sample](./assets/image-00054.png). The prose also carries inline math like $score_54 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00054
- [x] Reviewed checkpoint for section 00054
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00054 | repeated table row |
| words | 64 | stable measurement text |

$$
render_54 = parse_54 + highlight_54 + preview_54
$$

```swift
struct PerfSection00054 {
    let title = "Section 00054"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00054","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00054 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00055

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00055.md), and an image reference ![sample](./assets/image-00055.png). The prose also carries inline math like $score_55 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00055
- [x] Reviewed checkpoint for section 00055
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00055 | repeated table row |
| words | 64 | stable measurement text |

$$
render_55 = parse_55 + highlight_55 + preview_55
$$

```swift
struct PerfSection00055 {
    let title = "Section 00055"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00055","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00055 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00056

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00056.md), and an image reference ![sample](./assets/image-00056.png). The prose also carries inline math like $score_56 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00056
- [x] Reviewed checkpoint for section 00056
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00056 | repeated table row |
| words | 64 | stable measurement text |

$$
render_56 = parse_56 + highlight_56 + preview_56
$$

```swift
struct PerfSection00056 {
    let title = "Section 00056"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00056","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00056 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00057

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00057.md), and an image reference ![sample](./assets/image-00057.png). The prose also carries inline math like $score_57 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00057
- [x] Reviewed checkpoint for section 00057
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00057 | repeated table row |
| words | 64 | stable measurement text |

$$
render_57 = parse_57 + highlight_57 + preview_57
$$

```swift
struct PerfSection00057 {
    let title = "Section 00057"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00057","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00057 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00058

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00058.md), and an image reference ![sample](./assets/image-00058.png). The prose also carries inline math like $score_58 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00058
- [x] Reviewed checkpoint for section 00058
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00058 | repeated table row |
| words | 64 | stable measurement text |

$$
render_58 = parse_58 + highlight_58 + preview_58
$$

```swift
struct PerfSection00058 {
    let title = "Section 00058"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00058","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00058 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00059

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00059.md), and an image reference ![sample](./assets/image-00059.png). The prose also carries inline math like $score_59 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00059
- [x] Reviewed checkpoint for section 00059
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00059 | repeated table row |
| words | 64 | stable measurement text |

$$
render_59 = parse_59 + highlight_59 + preview_59
$$

```swift
struct PerfSection00059 {
    let title = "Section 00059"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00059","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00059 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00060

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00060.md), and an image reference ![sample](./assets/image-00060.png). The prose also carries inline math like $score_60 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00060
- [x] Reviewed checkpoint for section 00060
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00060 | repeated table row |
| words | 64 | stable measurement text |

$$
render_60 = parse_60 + highlight_60 + preview_60
$$

```swift
struct PerfSection00060 {
    let title = "Section 00060"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00060","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00060 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00061

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00061.md), and an image reference ![sample](./assets/image-00061.png). The prose also carries inline math like $score_61 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00061
- [x] Reviewed checkpoint for section 00061
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00061 | repeated table row |
| words | 64 | stable measurement text |

$$
render_61 = parse_61 + highlight_61 + preview_61
$$

```swift
struct PerfSection00061 {
    let title = "Section 00061"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00061","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00061 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00062

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00062.md), and an image reference ![sample](./assets/image-00062.png). The prose also carries inline math like $score_62 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00062
- [x] Reviewed checkpoint for section 00062
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00062 | repeated table row |
| words | 64 | stable measurement text |

$$
render_62 = parse_62 + highlight_62 + preview_62
$$

```swift
struct PerfSection00062 {
    let title = "Section 00062"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00062","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00062 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00063

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00063.md), and an image reference ![sample](./assets/image-00063.png). The prose also carries inline math like $score_63 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00063
- [x] Reviewed checkpoint for section 00063
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00063 | repeated table row |
| words | 64 | stable measurement text |

$$
render_63 = parse_63 + highlight_63 + preview_63
$$

```swift
struct PerfSection00063 {
    let title = "Section 00063"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00063","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00063 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00064

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00064.md), and an image reference ![sample](./assets/image-00064.png). The prose also carries inline math like $score_64 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00064
- [x] Reviewed checkpoint for section 00064
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00064 | repeated table row |
| words | 64 | stable measurement text |

$$
render_64 = parse_64 + highlight_64 + preview_64
$$

```swift
struct PerfSection00064 {
    let title = "Section 00064"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00064","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00064 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00065

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00065.md), and an image reference ![sample](./assets/image-00065.png). The prose also carries inline math like $score_65 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00065
- [x] Reviewed checkpoint for section 00065
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00065 | repeated table row |
| words | 64 | stable measurement text |

$$
render_65 = parse_65 + highlight_65 + preview_65
$$

```swift
struct PerfSection00065 {
    let title = "Section 00065"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00065","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00065 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00066

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00066.md), and an image reference ![sample](./assets/image-00066.png). The prose also carries inline math like $score_66 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00066
- [x] Reviewed checkpoint for section 00066
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00066 | repeated table row |
| words | 64 | stable measurement text |

$$
render_66 = parse_66 + highlight_66 + preview_66
$$

```swift
struct PerfSection00066 {
    let title = "Section 00066"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00066","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00066 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00067

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00067.md), and an image reference ![sample](./assets/image-00067.png). The prose also carries inline math like $score_67 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00067
- [x] Reviewed checkpoint for section 00067
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00067 | repeated table row |
| words | 64 | stable measurement text |

$$
render_67 = parse_67 + highlight_67 + preview_67
$$

```swift
struct PerfSection00067 {
    let title = "Section 00067"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00067","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00067 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00068

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00068.md), and an image reference ![sample](./assets/image-00068.png). The prose also carries inline math like $score_68 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00068
- [x] Reviewed checkpoint for section 00068
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00068 | repeated table row |
| words | 64 | stable measurement text |

$$
render_68 = parse_68 + highlight_68 + preview_68
$$

```swift
struct PerfSection00068 {
    let title = "Section 00068"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00068","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00068 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00069

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00069.md), and an image reference ![sample](./assets/image-00069.png). The prose also carries inline math like $score_69 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00069
- [x] Reviewed checkpoint for section 00069
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00069 | repeated table row |
| words | 64 | stable measurement text |

$$
render_69 = parse_69 + highlight_69 + preview_69
$$

```swift
struct PerfSection00069 {
    let title = "Section 00069"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00069","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00069 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00070

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00070.md), and an image reference ![sample](./assets/image-00070.png). The prose also carries inline math like $score_70 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00070
- [x] Reviewed checkpoint for section 00070
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00070 | repeated table row |
| words | 64 | stable measurement text |

$$
render_70 = parse_70 + highlight_70 + preview_70
$$

```swift
struct PerfSection00070 {
    let title = "Section 00070"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00070","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00070 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00071

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00071.md), and an image reference ![sample](./assets/image-00071.png). The prose also carries inline math like $score_71 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00071
- [x] Reviewed checkpoint for section 00071
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00071 | repeated table row |
| words | 64 | stable measurement text |

$$
render_71 = parse_71 + highlight_71 + preview_71
$$

```swift
struct PerfSection00071 {
    let title = "Section 00071"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00071","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00071 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00072

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00072.md), and an image reference ![sample](./assets/image-00072.png). The prose also carries inline math like $score_72 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00072
- [x] Reviewed checkpoint for section 00072
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00072 | repeated table row |
| words | 64 | stable measurement text |

$$
render_72 = parse_72 + highlight_72 + preview_72
$$

```swift
struct PerfSection00072 {
    let title = "Section 00072"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00072","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00072 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00073

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00073.md), and an image reference ![sample](./assets/image-00073.png). The prose also carries inline math like $score_73 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00073
- [x] Reviewed checkpoint for section 00073
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00073 | repeated table row |
| words | 64 | stable measurement text |

$$
render_73 = parse_73 + highlight_73 + preview_73
$$

```swift
struct PerfSection00073 {
    let title = "Section 00073"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00073","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00073 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00074

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00074.md), and an image reference ![sample](./assets/image-00074.png). The prose also carries inline math like $score_74 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00074
- [x] Reviewed checkpoint for section 00074
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00074 | repeated table row |
| words | 64 | stable measurement text |

$$
render_74 = parse_74 + highlight_74 + preview_74
$$

```swift
struct PerfSection00074 {
    let title = "Section 00074"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00074","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00074 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00075

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00075.md), and an image reference ![sample](./assets/image-00075.png). The prose also carries inline math like $score_75 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00075
- [x] Reviewed checkpoint for section 00075
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00075 | repeated table row |
| words | 64 | stable measurement text |

$$
render_75 = parse_75 + highlight_75 + preview_75
$$

```swift
struct PerfSection00075 {
    let title = "Section 00075"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00075","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00075 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00076

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00076.md), and an image reference ![sample](./assets/image-00076.png). The prose also carries inline math like $score_76 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00076
- [x] Reviewed checkpoint for section 00076
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00076 | repeated table row |
| words | 64 | stable measurement text |

$$
render_76 = parse_76 + highlight_76 + preview_76
$$

```swift
struct PerfSection00076 {
    let title = "Section 00076"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00076","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00076 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00077

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00077.md), and an image reference ![sample](./assets/image-00077.png). The prose also carries inline math like $score_77 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00077
- [x] Reviewed checkpoint for section 00077
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00077 | repeated table row |
| words | 64 | stable measurement text |

$$
render_77 = parse_77 + highlight_77 + preview_77
$$

```swift
struct PerfSection00077 {
    let title = "Section 00077"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00077","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00077 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00078

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00078.md), and an image reference ![sample](./assets/image-00078.png). The prose also carries inline math like $score_78 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00078
- [x] Reviewed checkpoint for section 00078
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00078 | repeated table row |
| words | 64 | stable measurement text |

$$
render_78 = parse_78 + highlight_78 + preview_78
$$

```swift
struct PerfSection00078 {
    let title = "Section 00078"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00078","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00078 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00079

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00079.md), and an image reference ![sample](./assets/image-00079.png). The prose also carries inline math like $score_79 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00079
- [x] Reviewed checkpoint for section 00079
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00079 | repeated table row |
| words | 64 | stable measurement text |

$$
render_79 = parse_79 + highlight_79 + preview_79
$$

```swift
struct PerfSection00079 {
    let title = "Section 00079"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00079","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00079 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00080

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00080.md), and an image reference ![sample](./assets/image-00080.png). The prose also carries inline math like $score_80 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00080
- [x] Reviewed checkpoint for section 00080
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00080 | repeated table row |
| words | 64 | stable measurement text |

$$
render_80 = parse_80 + highlight_80 + preview_80
$$

```swift
struct PerfSection00080 {
    let title = "Section 00080"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00080","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00080 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00081

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00081.md), and an image reference ![sample](./assets/image-00081.png). The prose also carries inline math like $score_81 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00081
- [x] Reviewed checkpoint for section 00081
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00081 | repeated table row |
| words | 64 | stable measurement text |

$$
render_81 = parse_81 + highlight_81 + preview_81
$$

```swift
struct PerfSection00081 {
    let title = "Section 00081"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00081","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00081 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00082

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00082.md), and an image reference ![sample](./assets/image-00082.png). The prose also carries inline math like $score_82 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00082
- [x] Reviewed checkpoint for section 00082
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00082 | repeated table row |
| words | 64 | stable measurement text |

$$
render_82 = parse_82 + highlight_82 + preview_82
$$

```swift
struct PerfSection00082 {
    let title = "Section 00082"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00082","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00082 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00083

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00083.md), and an image reference ![sample](./assets/image-00083.png). The prose also carries inline math like $score_83 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00083
- [x] Reviewed checkpoint for section 00083
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00083 | repeated table row |
| words | 64 | stable measurement text |

$$
render_83 = parse_83 + highlight_83 + preview_83
$$

```swift
struct PerfSection00083 {
    let title = "Section 00083"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00083","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00083 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00084

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00084.md), and an image reference ![sample](./assets/image-00084.png). The prose also carries inline math like $score_84 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00084
- [x] Reviewed checkpoint for section 00084
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00084 | repeated table row |
| words | 64 | stable measurement text |

$$
render_84 = parse_84 + highlight_84 + preview_84
$$

```swift
struct PerfSection00084 {
    let title = "Section 00084"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00084","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00084 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00085

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00085.md), and an image reference ![sample](./assets/image-00085.png). The prose also carries inline math like $score_85 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00085
- [x] Reviewed checkpoint for section 00085
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00085 | repeated table row |
| words | 64 | stable measurement text |

$$
render_85 = parse_85 + highlight_85 + preview_85
$$

```swift
struct PerfSection00085 {
    let title = "Section 00085"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00085","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00085 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00086

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00086.md), and an image reference ![sample](./assets/image-00086.png). The prose also carries inline math like $score_86 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00086
- [x] Reviewed checkpoint for section 00086
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00086 | repeated table row |
| words | 64 | stable measurement text |

$$
render_86 = parse_86 + highlight_86 + preview_86
$$

```swift
struct PerfSection00086 {
    let title = "Section 00086"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00086","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00086 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00087

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00087.md), and an image reference ![sample](./assets/image-00087.png). The prose also carries inline math like $score_87 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00087
- [x] Reviewed checkpoint for section 00087
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00087 | repeated table row |
| words | 64 | stable measurement text |

$$
render_87 = parse_87 + highlight_87 + preview_87
$$

```swift
struct PerfSection00087 {
    let title = "Section 00087"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00087","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00087 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00088

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00088.md), and an image reference ![sample](./assets/image-00088.png). The prose also carries inline math like $score_88 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00088
- [x] Reviewed checkpoint for section 00088
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00088 | repeated table row |
| words | 64 | stable measurement text |

$$
render_88 = parse_88 + highlight_88 + preview_88
$$

```swift
struct PerfSection00088 {
    let title = "Section 00088"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00088","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00088 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00089

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00089.md), and an image reference ![sample](./assets/image-00089.png). The prose also carries inline math like $score_89 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00089
- [x] Reviewed checkpoint for section 00089
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00089 | repeated table row |
| words | 64 | stable measurement text |

$$
render_89 = parse_89 + highlight_89 + preview_89
$$

```swift
struct PerfSection00089 {
    let title = "Section 00089"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00089","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00089 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00090

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00090.md), and an image reference ![sample](./assets/image-00090.png). The prose also carries inline math like $score_90 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00090
- [x] Reviewed checkpoint for section 00090
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00090 | repeated table row |
| words | 64 | stable measurement text |

$$
render_90 = parse_90 + highlight_90 + preview_90
$$

```swift
struct PerfSection00090 {
    let title = "Section 00090"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00090","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00090 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00091

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00091.md), and an image reference ![sample](./assets/image-00091.png). The prose also carries inline math like $score_91 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00091
- [x] Reviewed checkpoint for section 00091
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00091 | repeated table row |
| words | 64 | stable measurement text |

$$
render_91 = parse_91 + highlight_91 + preview_91
$$

```swift
struct PerfSection00091 {
    let title = "Section 00091"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00091","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00091 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00092

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00092.md), and an image reference ![sample](./assets/image-00092.png). The prose also carries inline math like $score_92 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00092
- [x] Reviewed checkpoint for section 00092
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00092 | repeated table row |
| words | 64 | stable measurement text |

$$
render_92 = parse_92 + highlight_92 + preview_92
$$

```swift
struct PerfSection00092 {
    let title = "Section 00092"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00092","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00092 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00093

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00093.md), and an image reference ![sample](./assets/image-00093.png). The prose also carries inline math like $score_93 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00093
- [x] Reviewed checkpoint for section 00093
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00093 | repeated table row |
| words | 64 | stable measurement text |

$$
render_93 = parse_93 + highlight_93 + preview_93
$$

```swift
struct PerfSection00093 {
    let title = "Section 00093"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00093","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00093 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00094

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00094.md), and an image reference ![sample](./assets/image-00094.png). The prose also carries inline math like $score_94 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00094
- [x] Reviewed checkpoint for section 00094
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00094 | repeated table row |
| words | 64 | stable measurement text |

$$
render_94 = parse_94 + highlight_94 + preview_94
$$

```swift
struct PerfSection00094 {
    let title = "Section 00094"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00094","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00094 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00095

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00095.md), and an image reference ![sample](./assets/image-00095.png). The prose also carries inline math like $score_95 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00095
- [x] Reviewed checkpoint for section 00095
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00095 | repeated table row |
| words | 64 | stable measurement text |

$$
render_95 = parse_95 + highlight_95 + preview_95
$$

```swift
struct PerfSection00095 {
    let title = "Section 00095"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00095","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00095 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00096

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00096.md), and an image reference ![sample](./assets/image-00096.png). The prose also carries inline math like $score_96 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00096
- [x] Reviewed checkpoint for section 00096
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00096 | repeated table row |
| words | 64 | stable measurement text |

$$
render_96 = parse_96 + highlight_96 + preview_96
$$

```swift
struct PerfSection00096 {
    let title = "Section 00096"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00096","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00096 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00097

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00097.md), and an image reference ![sample](./assets/image-00097.png). The prose also carries inline math like $score_97 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00097
- [x] Reviewed checkpoint for section 00097
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00097 | repeated table row |
| words | 64 | stable measurement text |

$$
render_97 = parse_97 + highlight_97 + preview_97
$$

```swift
struct PerfSection00097 {
    let title = "Section 00097"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00097","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00097 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00098

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00098.md), and an image reference ![sample](./assets/image-00098.png). The prose also carries inline math like $score_98 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00098
- [x] Reviewed checkpoint for section 00098
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00098 | repeated table row |
| words | 64 | stable measurement text |

$$
render_98 = parse_98 + highlight_98 + preview_98
$$

```swift
struct PerfSection00098 {
    let title = "Section 00098"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00098","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00098 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00099

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00099.md), and an image reference ![sample](./assets/image-00099.png). The prose also carries inline math like $score_99 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00099
- [x] Reviewed checkpoint for section 00099
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00099 | repeated table row |
| words | 64 | stable measurement text |

$$
render_99 = parse_99 + highlight_99 + preview_99
$$

```swift
struct PerfSection00099 {
    let title = "Section 00099"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00099","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00099 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00100

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00100.md), and an image reference ![sample](./assets/image-00100.png). The prose also carries inline math like $score_100 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00100
- [x] Reviewed checkpoint for section 00100
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00100 | repeated table row |
| words | 64 | stable measurement text |

$$
render_100 = parse_100 + highlight_100 + preview_100
$$

```swift
struct PerfSection00100 {
    let title = "Section 00100"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00100","fixture":"perf-100kb","stable":true}
```

> Quoted line for section 00100 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

