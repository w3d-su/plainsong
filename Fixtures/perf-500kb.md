---
title: "Performance 500KB Fixture"
description: "Deterministic markdown fixture for Plainsong performance checks."
date: 2026-06-17
draft: false
tags:
  - performance
  - fixture
  - plainsong
---

# Performance 500KB Fixture

This file is generated by `Scripts/generate-perf-fixtures.swift` for repeatable editor,
preview, file-open, and memory measurements. It keeps Markdown structure stable while
spreading heavier code and math blocks through the document instead of repeating them
in every section.

## Section 00001

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00001.md),
and an image reference ![sample](./assets/image-00001.png).

Paragraph 1 in section 00001 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00001 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00001 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00001 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00001 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00001 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00001 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00001 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00001
- [x] Reviewed checkpoint for section 00001
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00001 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00002

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00002.md),
and an image reference ![sample](./assets/image-00002.png).

Paragraph 1 in section 00002 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00002 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00002 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00002 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00002 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00002 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00002 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00002 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00002
- [x] Reviewed checkpoint for section 00002
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00002 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00003

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00003.md),
and an image reference ![sample](./assets/image-00003.png).

Paragraph 1 in section 00003 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00003 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00003 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00003 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00003 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00003 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00003 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00003 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00003
- [x] Reviewed checkpoint for section 00003
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00003 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00004

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00004.md),
and an image reference ![sample](./assets/image-00004.png).

Paragraph 1 in section 00004 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00004 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00004 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00004 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00004 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00004 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00004 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00004 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00004
- [x] Reviewed checkpoint for section 00004
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00004 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00005

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00005.md),
and an image reference ![sample](./assets/image-00005.png).

Paragraph 1 in section 00005 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00005 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00005 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00005 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00005 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00005 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00005 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00005 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00005
- [x] Reviewed checkpoint for section 00005
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00005 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{5} = parse_{5} + highlight_{5} + preview_{5}
$$

## Section 00006

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00006.md),
and an image reference ![sample](./assets/image-00006.png).

Paragraph 1 in section 00006 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00006 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00006 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00006 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00006 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00006 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00006 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00006 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00006
- [x] Reviewed checkpoint for section 00006
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00006 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00006 | repeated table row |
| words | 72 | stable measurement text |

## Section 00007

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00007.md),
and an image reference ![sample](./assets/image-00007.png).

Paragraph 1 in section 00007 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00007 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00007 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00007 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00007 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00007 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00007 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00007 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00007
- [x] Reviewed checkpoint for section 00007
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00007 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```swift
struct PerfSection00007 {
    let title = "Section 00007"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00008

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00008.md),
and an image reference ![sample](./assets/image-00008.png).

Paragraph 1 in section 00008 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00008 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00008 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00008 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00008 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00008 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00008 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00008 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00008
- [x] Reviewed checkpoint for section 00008
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00008 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00009

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00009.md),
and an image reference ![sample](./assets/image-00009.png).

Paragraph 1 in section 00009 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00009 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00009 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00009 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00009 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00009 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00009 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00009 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00009
- [x] Reviewed checkpoint for section 00009
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00009 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00010

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00010.md),
and an image reference ![sample](./assets/image-00010.png).

Paragraph 1 in section 00010 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00010 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00010 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00010 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00010 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00010 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00010 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00010 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00010
- [x] Reviewed checkpoint for section 00010
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00010 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{10} = parse_{10} + highlight_{10} + preview_{10}
$$

## Section 00011

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00011.md),
and an image reference ![sample](./assets/image-00011.png).

Paragraph 1 in section 00011 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00011 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00011 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00011 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00011 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00011 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00011 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00011 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00011
- [x] Reviewed checkpoint for section 00011
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00011 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```json
{"section":"00011","fixture":"perf-500kb","stable":true}
```

## Section 00012

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00012.md),
and an image reference ![sample](./assets/image-00012.png).

Paragraph 1 in section 00012 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00012 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00012 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00012 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00012 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00012 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00012 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00012 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00012
- [x] Reviewed checkpoint for section 00012
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00012 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00012 | repeated table row |
| words | 72 | stable measurement text |

## Section 00013

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00013.md),
and an image reference ![sample](./assets/image-00013.png).

Paragraph 1 in section 00013 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00013 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00013 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00013 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00013 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00013 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00013 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00013 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00013
- [x] Reviewed checkpoint for section 00013
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00013 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00014

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00014.md),
and an image reference ![sample](./assets/image-00014.png).

Paragraph 1 in section 00014 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00014 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00014 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00014 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00014 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00014 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00014 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00014 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00014
- [x] Reviewed checkpoint for section 00014
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00014 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```swift
struct PerfSection00014 {
    let title = "Section 00014"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00015

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00015.md),
and an image reference ![sample](./assets/image-00015.png).

Paragraph 1 in section 00015 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00015 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00015 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00015 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00015 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00015 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00015 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00015 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00015
- [x] Reviewed checkpoint for section 00015
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00015 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{15} = parse_{15} + highlight_{15} + preview_{15}
$$

## Section 00016

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00016.md),
and an image reference ![sample](./assets/image-00016.png).

Paragraph 1 in section 00016 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00016 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00016 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00016 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00016 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00016 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00016 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00016 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00016
- [x] Reviewed checkpoint for section 00016
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00016 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00017

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00017.md),
and an image reference ![sample](./assets/image-00017.png).

Paragraph 1 in section 00017 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00017 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00017 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00017 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00017 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00017 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00017 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00017 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00017
- [x] Reviewed checkpoint for section 00017
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00017 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00018

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00018.md),
and an image reference ![sample](./assets/image-00018.png).

Paragraph 1 in section 00018 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00018 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00018 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00018 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00018 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00018 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00018 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00018 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00018
- [x] Reviewed checkpoint for section 00018
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00018 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00018 | repeated table row |
| words | 72 | stable measurement text |

## Section 00019

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00019.md),
and an image reference ![sample](./assets/image-00019.png).

Paragraph 1 in section 00019 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00019 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00019 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00019 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00019 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00019 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00019 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00019 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00019
- [x] Reviewed checkpoint for section 00019
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00019 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00020

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00020.md),
and an image reference ![sample](./assets/image-00020.png).

Paragraph 1 in section 00020 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00020 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00020 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00020 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00020 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00020 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00020 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00020 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00020
- [x] Reviewed checkpoint for section 00020
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00020 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{20} = parse_{20} + highlight_{20} + preview_{20}
$$

## Section 00021

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00021.md),
and an image reference ![sample](./assets/image-00021.png).

Paragraph 1 in section 00021 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00021 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00021 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00021 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00021 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00021 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00021 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00021 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00021
- [x] Reviewed checkpoint for section 00021
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00021 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```swift
struct PerfSection00021 {
    let title = "Section 00021"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00022

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00022.md),
and an image reference ![sample](./assets/image-00022.png).

Paragraph 1 in section 00022 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00022 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00022 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00022 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00022 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00022 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00022 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00022 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00022
- [x] Reviewed checkpoint for section 00022
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00022 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```json
{"section":"00022","fixture":"perf-500kb","stable":true}
```

## Section 00023

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00023.md),
and an image reference ![sample](./assets/image-00023.png).

Paragraph 1 in section 00023 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00023 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00023 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00023 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00023 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00023 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00023 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00023 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00023
- [x] Reviewed checkpoint for section 00023
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00023 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00024

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00024.md),
and an image reference ![sample](./assets/image-00024.png).

Paragraph 1 in section 00024 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00024 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00024 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00024 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00024 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00024 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00024 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00024 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00024
- [x] Reviewed checkpoint for section 00024
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00024 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00024 | repeated table row |
| words | 72 | stable measurement text |

## Section 00025

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00025.md),
and an image reference ![sample](./assets/image-00025.png).

Paragraph 1 in section 00025 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00025 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00025 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00025 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00025 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00025 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00025 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00025 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00025
- [x] Reviewed checkpoint for section 00025
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00025 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{25} = parse_{25} + highlight_{25} + preview_{25}
$$

## Section 00026

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00026.md),
and an image reference ![sample](./assets/image-00026.png).

Paragraph 1 in section 00026 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00026 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00026 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00026 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00026 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00026 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00026 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00026 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00026
- [x] Reviewed checkpoint for section 00026
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00026 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00027

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00027.md),
and an image reference ![sample](./assets/image-00027.png).

Paragraph 1 in section 00027 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00027 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00027 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00027 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00027 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00027 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00027 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00027 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00027
- [x] Reviewed checkpoint for section 00027
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00027 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00028

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00028.md),
and an image reference ![sample](./assets/image-00028.png).

Paragraph 1 in section 00028 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00028 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00028 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00028 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00028 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00028 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00028 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00028 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00028
- [x] Reviewed checkpoint for section 00028
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00028 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```swift
struct PerfSection00028 {
    let title = "Section 00028"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00029

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00029.md),
and an image reference ![sample](./assets/image-00029.png).

Paragraph 1 in section 00029 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00029 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00029 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00029 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00029 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00029 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00029 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00029 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00029
- [x] Reviewed checkpoint for section 00029
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00029 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00030

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00030.md),
and an image reference ![sample](./assets/image-00030.png).

Paragraph 1 in section 00030 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00030 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00030 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00030 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00030 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00030 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00030 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00030 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00030
- [x] Reviewed checkpoint for section 00030
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00030 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00030 | repeated table row |
| words | 72 | stable measurement text |

$$
render_{30} = parse_{30} + highlight_{30} + preview_{30}
$$

## Section 00031

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00031.md),
and an image reference ![sample](./assets/image-00031.png).

Paragraph 1 in section 00031 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00031 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00031 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00031 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00031 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00031 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00031 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00031 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00031
- [x] Reviewed checkpoint for section 00031
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00031 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00032

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00032.md),
and an image reference ![sample](./assets/image-00032.png).

Paragraph 1 in section 00032 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00032 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00032 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00032 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00032 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00032 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00032 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00032 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00032
- [x] Reviewed checkpoint for section 00032
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00032 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00033

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00033.md),
and an image reference ![sample](./assets/image-00033.png).

Paragraph 1 in section 00033 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00033 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00033 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00033 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00033 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00033 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00033 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00033 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00033
- [x] Reviewed checkpoint for section 00033
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00033 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```json
{"section":"00033","fixture":"perf-500kb","stable":true}
```

## Section 00034

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00034.md),
and an image reference ![sample](./assets/image-00034.png).

Paragraph 1 in section 00034 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00034 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00034 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00034 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00034 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00034 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00034 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00034 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00034
- [x] Reviewed checkpoint for section 00034
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00034 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00035

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00035.md),
and an image reference ![sample](./assets/image-00035.png).

Paragraph 1 in section 00035 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00035 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00035 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00035 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00035 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00035 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00035 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00035 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00035
- [x] Reviewed checkpoint for section 00035
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00035 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{35} = parse_{35} + highlight_{35} + preview_{35}
$$

```swift
struct PerfSection00035 {
    let title = "Section 00035"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00036

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00036.md),
and an image reference ![sample](./assets/image-00036.png).

Paragraph 1 in section 00036 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00036 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00036 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00036 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00036 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00036 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00036 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00036 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00036
- [x] Reviewed checkpoint for section 00036
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00036 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00036 | repeated table row |
| words | 72 | stable measurement text |

## Section 00037

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00037.md),
and an image reference ![sample](./assets/image-00037.png).

Paragraph 1 in section 00037 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00037 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00037 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00037 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00037 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00037 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00037 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00037 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00037
- [x] Reviewed checkpoint for section 00037
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00037 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00038

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00038.md),
and an image reference ![sample](./assets/image-00038.png).

Paragraph 1 in section 00038 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00038 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00038 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00038 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00038 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00038 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00038 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00038 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00038
- [x] Reviewed checkpoint for section 00038
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00038 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00039

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00039.md),
and an image reference ![sample](./assets/image-00039.png).

Paragraph 1 in section 00039 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00039 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00039 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00039 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00039 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00039 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00039 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00039 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00039
- [x] Reviewed checkpoint for section 00039
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00039 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00040

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00040.md),
and an image reference ![sample](./assets/image-00040.png).

Paragraph 1 in section 00040 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00040 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00040 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00040 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00040 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00040 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00040 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00040 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00040
- [x] Reviewed checkpoint for section 00040
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00040 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{40} = parse_{40} + highlight_{40} + preview_{40}
$$

## Section 00041

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00041.md),
and an image reference ![sample](./assets/image-00041.png).

Paragraph 1 in section 00041 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00041 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00041 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00041 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00041 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00041 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00041 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00041 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00041
- [x] Reviewed checkpoint for section 00041
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00041 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00042

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00042.md),
and an image reference ![sample](./assets/image-00042.png).

Paragraph 1 in section 00042 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00042 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00042 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00042 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00042 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00042 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00042 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00042 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00042
- [x] Reviewed checkpoint for section 00042
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00042 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00042 | repeated table row |
| words | 72 | stable measurement text |

```swift
struct PerfSection00042 {
    let title = "Section 00042"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00043

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00043.md),
and an image reference ![sample](./assets/image-00043.png).

Paragraph 1 in section 00043 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00043 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00043 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00043 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00043 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00043 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00043 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00043 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00043
- [x] Reviewed checkpoint for section 00043
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00043 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00044

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00044.md),
and an image reference ![sample](./assets/image-00044.png).

Paragraph 1 in section 00044 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00044 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00044 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00044 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00044 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00044 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00044 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00044 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00044
- [x] Reviewed checkpoint for section 00044
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00044 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```json
{"section":"00044","fixture":"perf-500kb","stable":true}
```

## Section 00045

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00045.md),
and an image reference ![sample](./assets/image-00045.png).

Paragraph 1 in section 00045 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00045 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00045 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00045 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00045 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00045 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00045 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00045 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00045
- [x] Reviewed checkpoint for section 00045
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00045 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{45} = parse_{45} + highlight_{45} + preview_{45}
$$

## Section 00046

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00046.md),
and an image reference ![sample](./assets/image-00046.png).

Paragraph 1 in section 00046 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00046 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00046 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00046 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00046 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00046 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00046 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00046 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00046
- [x] Reviewed checkpoint for section 00046
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00046 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00047

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00047.md),
and an image reference ![sample](./assets/image-00047.png).

Paragraph 1 in section 00047 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00047 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00047 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00047 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00047 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00047 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00047 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00047 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00047
- [x] Reviewed checkpoint for section 00047
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00047 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00048

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00048.md),
and an image reference ![sample](./assets/image-00048.png).

Paragraph 1 in section 00048 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00048 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00048 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00048 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00048 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00048 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00048 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00048 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00048
- [x] Reviewed checkpoint for section 00048
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00048 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00048 | repeated table row |
| words | 72 | stable measurement text |

## Section 00049

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00049.md),
and an image reference ![sample](./assets/image-00049.png).

Paragraph 1 in section 00049 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00049 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00049 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00049 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00049 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00049 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00049 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00049 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00049
- [x] Reviewed checkpoint for section 00049
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00049 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```swift
struct PerfSection00049 {
    let title = "Section 00049"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00050

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00050.md),
and an image reference ![sample](./assets/image-00050.png).

Paragraph 1 in section 00050 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00050 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00050 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00050 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00050 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00050 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00050 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00050 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00050
- [x] Reviewed checkpoint for section 00050
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00050 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{50} = parse_{50} + highlight_{50} + preview_{50}
$$

## Section 00051

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00051.md),
and an image reference ![sample](./assets/image-00051.png).

Paragraph 1 in section 00051 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00051 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00051 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00051 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00051 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00051 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00051 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00051 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00051
- [x] Reviewed checkpoint for section 00051
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00051 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00052

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00052.md),
and an image reference ![sample](./assets/image-00052.png).

Paragraph 1 in section 00052 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00052 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00052 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00052 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00052 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00052 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00052 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00052 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00052
- [x] Reviewed checkpoint for section 00052
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00052 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00053

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00053.md),
and an image reference ![sample](./assets/image-00053.png).

Paragraph 1 in section 00053 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00053 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00053 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00053 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00053 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00053 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00053 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00053 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00053
- [x] Reviewed checkpoint for section 00053
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00053 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00054

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00054.md),
and an image reference ![sample](./assets/image-00054.png).

Paragraph 1 in section 00054 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00054 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00054 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00054 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00054 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00054 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00054 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00054 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00054
- [x] Reviewed checkpoint for section 00054
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00054 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00054 | repeated table row |
| words | 72 | stable measurement text |

## Section 00055

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00055.md),
and an image reference ![sample](./assets/image-00055.png).

Paragraph 1 in section 00055 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00055 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00055 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00055 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00055 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00055 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00055 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00055 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00055
- [x] Reviewed checkpoint for section 00055
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00055 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{55} = parse_{55} + highlight_{55} + preview_{55}
$$

```json
{"section":"00055","fixture":"perf-500kb","stable":true}
```

## Section 00056

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00056.md),
and an image reference ![sample](./assets/image-00056.png).

Paragraph 1 in section 00056 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00056 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00056 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00056 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00056 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00056 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00056 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00056 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00056
- [x] Reviewed checkpoint for section 00056
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00056 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```swift
struct PerfSection00056 {
    let title = "Section 00056"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00057

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00057.md),
and an image reference ![sample](./assets/image-00057.png).

Paragraph 1 in section 00057 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00057 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00057 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00057 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00057 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00057 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00057 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00057 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00057
- [x] Reviewed checkpoint for section 00057
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00057 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00058

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00058.md),
and an image reference ![sample](./assets/image-00058.png).

Paragraph 1 in section 00058 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00058 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00058 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00058 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00058 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00058 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00058 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00058 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00058
- [x] Reviewed checkpoint for section 00058
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00058 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00059

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00059.md),
and an image reference ![sample](./assets/image-00059.png).

Paragraph 1 in section 00059 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00059 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00059 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00059 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00059 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00059 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00059 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00059 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00059
- [x] Reviewed checkpoint for section 00059
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00059 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00060

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00060.md),
and an image reference ![sample](./assets/image-00060.png).

Paragraph 1 in section 00060 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00060 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00060 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00060 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00060 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00060 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00060 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00060 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00060
- [x] Reviewed checkpoint for section 00060
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00060 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00060 | repeated table row |
| words | 72 | stable measurement text |

$$
render_{60} = parse_{60} + highlight_{60} + preview_{60}
$$

## Section 00061

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00061.md),
and an image reference ![sample](./assets/image-00061.png).

Paragraph 1 in section 00061 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00061 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00061 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00061 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00061 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00061 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00061 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00061 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00061
- [x] Reviewed checkpoint for section 00061
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00061 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00062

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00062.md),
and an image reference ![sample](./assets/image-00062.png).

Paragraph 1 in section 00062 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00062 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00062 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00062 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00062 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00062 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00062 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00062 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00062
- [x] Reviewed checkpoint for section 00062
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00062 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00063

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00063.md),
and an image reference ![sample](./assets/image-00063.png).

Paragraph 1 in section 00063 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00063 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00063 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00063 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00063 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00063 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00063 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00063 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00063
- [x] Reviewed checkpoint for section 00063
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00063 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```swift
struct PerfSection00063 {
    let title = "Section 00063"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00064

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00064.md),
and an image reference ![sample](./assets/image-00064.png).

Paragraph 1 in section 00064 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00064 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00064 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00064 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00064 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00064 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00064 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00064 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00064
- [x] Reviewed checkpoint for section 00064
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00064 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00065

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00065.md),
and an image reference ![sample](./assets/image-00065.png).

Paragraph 1 in section 00065 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00065 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00065 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00065 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00065 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00065 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00065 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00065 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00065
- [x] Reviewed checkpoint for section 00065
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00065 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{65} = parse_{65} + highlight_{65} + preview_{65}
$$

## Section 00066

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00066.md),
and an image reference ![sample](./assets/image-00066.png).

Paragraph 1 in section 00066 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00066 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00066 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00066 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00066 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00066 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00066 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00066 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00066
- [x] Reviewed checkpoint for section 00066
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00066 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00066 | repeated table row |
| words | 72 | stable measurement text |

```json
{"section":"00066","fixture":"perf-500kb","stable":true}
```

## Section 00067

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00067.md),
and an image reference ![sample](./assets/image-00067.png).

Paragraph 1 in section 00067 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00067 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00067 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00067 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00067 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00067 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00067 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00067 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00067
- [x] Reviewed checkpoint for section 00067
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00067 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00068

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00068.md),
and an image reference ![sample](./assets/image-00068.png).

Paragraph 1 in section 00068 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00068 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00068 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00068 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00068 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00068 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00068 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00068 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00068
- [x] Reviewed checkpoint for section 00068
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00068 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00069

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00069.md),
and an image reference ![sample](./assets/image-00069.png).

Paragraph 1 in section 00069 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00069 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00069 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00069 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00069 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00069 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00069 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00069 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00069
- [x] Reviewed checkpoint for section 00069
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00069 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00070

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00070.md),
and an image reference ![sample](./assets/image-00070.png).

Paragraph 1 in section 00070 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00070 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00070 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00070 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00070 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00070 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00070 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00070 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00070
- [x] Reviewed checkpoint for section 00070
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00070 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{70} = parse_{70} + highlight_{70} + preview_{70}
$$

```swift
struct PerfSection00070 {
    let title = "Section 00070"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00071

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00071.md),
and an image reference ![sample](./assets/image-00071.png).

Paragraph 1 in section 00071 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00071 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00071 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00071 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00071 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00071 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00071 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00071 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00071
- [x] Reviewed checkpoint for section 00071
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00071 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00072

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00072.md),
and an image reference ![sample](./assets/image-00072.png).

Paragraph 1 in section 00072 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00072 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00072 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00072 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00072 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00072 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00072 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00072 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00072
- [x] Reviewed checkpoint for section 00072
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00072 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00072 | repeated table row |
| words | 72 | stable measurement text |

## Section 00073

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00073.md),
and an image reference ![sample](./assets/image-00073.png).

Paragraph 1 in section 00073 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00073 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00073 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00073 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00073 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00073 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00073 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00073 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00073
- [x] Reviewed checkpoint for section 00073
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00073 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00074

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00074.md),
and an image reference ![sample](./assets/image-00074.png).

Paragraph 1 in section 00074 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00074 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00074 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00074 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00074 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00074 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00074 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00074 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00074
- [x] Reviewed checkpoint for section 00074
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00074 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00075

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00075.md),
and an image reference ![sample](./assets/image-00075.png).

Paragraph 1 in section 00075 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00075 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00075 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00075 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00075 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00075 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00075 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00075 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00075
- [x] Reviewed checkpoint for section 00075
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00075 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{75} = parse_{75} + highlight_{75} + preview_{75}
$$

## Section 00076

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00076.md),
and an image reference ![sample](./assets/image-00076.png).

Paragraph 1 in section 00076 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00076 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00076 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00076 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00076 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00076 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00076 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00076 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00076
- [x] Reviewed checkpoint for section 00076
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00076 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00077

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00077.md),
and an image reference ![sample](./assets/image-00077.png).

Paragraph 1 in section 00077 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00077 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00077 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00077 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00077 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00077 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00077 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00077 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00077
- [x] Reviewed checkpoint for section 00077
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00077 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```swift
struct PerfSection00077 {
    let title = "Section 00077"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00077","fixture":"perf-500kb","stable":true}
```

## Section 00078

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00078.md),
and an image reference ![sample](./assets/image-00078.png).

Paragraph 1 in section 00078 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00078 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00078 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00078 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00078 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00078 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00078 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00078 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00078
- [x] Reviewed checkpoint for section 00078
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00078 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00078 | repeated table row |
| words | 72 | stable measurement text |

## Section 00079

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00079.md),
and an image reference ![sample](./assets/image-00079.png).

Paragraph 1 in section 00079 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00079 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00079 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00079 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00079 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00079 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00079 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00079 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00079
- [x] Reviewed checkpoint for section 00079
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00079 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00080

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00080.md),
and an image reference ![sample](./assets/image-00080.png).

Paragraph 1 in section 00080 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00080 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00080 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00080 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00080 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00080 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00080 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00080 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00080
- [x] Reviewed checkpoint for section 00080
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00080 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{80} = parse_{80} + highlight_{80} + preview_{80}
$$

## Section 00081

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00081.md),
and an image reference ![sample](./assets/image-00081.png).

Paragraph 1 in section 00081 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00081 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00081 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00081 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00081 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00081 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00081 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00081 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00081
- [x] Reviewed checkpoint for section 00081
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00081 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00082

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00082.md),
and an image reference ![sample](./assets/image-00082.png).

Paragraph 1 in section 00082 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00082 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00082 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00082 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00082 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00082 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00082 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00082 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00082
- [x] Reviewed checkpoint for section 00082
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00082 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00083

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00083.md),
and an image reference ![sample](./assets/image-00083.png).

Paragraph 1 in section 00083 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00083 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00083 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00083 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00083 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00083 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00083 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00083 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00083
- [x] Reviewed checkpoint for section 00083
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00083 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00084

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00084.md),
and an image reference ![sample](./assets/image-00084.png).

Paragraph 1 in section 00084 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00084 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00084 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00084 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00084 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00084 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00084 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00084 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00084
- [x] Reviewed checkpoint for section 00084
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00084 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00084 | repeated table row |
| words | 72 | stable measurement text |

```swift
struct PerfSection00084 {
    let title = "Section 00084"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00085

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00085.md),
and an image reference ![sample](./assets/image-00085.png).

Paragraph 1 in section 00085 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00085 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00085 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00085 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00085 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00085 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00085 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00085 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00085
- [x] Reviewed checkpoint for section 00085
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00085 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{85} = parse_{85} + highlight_{85} + preview_{85}
$$

## Section 00086

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00086.md),
and an image reference ![sample](./assets/image-00086.png).

Paragraph 1 in section 00086 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00086 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00086 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00086 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00086 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00086 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00086 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00086 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00086
- [x] Reviewed checkpoint for section 00086
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00086 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00087

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00087.md),
and an image reference ![sample](./assets/image-00087.png).

Paragraph 1 in section 00087 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00087 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00087 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00087 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00087 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00087 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00087 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00087 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00087
- [x] Reviewed checkpoint for section 00087
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00087 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00088

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00088.md),
and an image reference ![sample](./assets/image-00088.png).

Paragraph 1 in section 00088 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00088 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00088 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00088 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00088 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00088 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00088 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00088 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00088
- [x] Reviewed checkpoint for section 00088
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00088 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```json
{"section":"00088","fixture":"perf-500kb","stable":true}
```

## Section 00089

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00089.md),
and an image reference ![sample](./assets/image-00089.png).

Paragraph 1 in section 00089 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00089 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00089 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00089 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00089 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00089 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00089 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00089 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00089
- [x] Reviewed checkpoint for section 00089
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00089 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00090

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00090.md),
and an image reference ![sample](./assets/image-00090.png).

Paragraph 1 in section 00090 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00090 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00090 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00090 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00090 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00090 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00090 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00090 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00090
- [x] Reviewed checkpoint for section 00090
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00090 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00090 | repeated table row |
| words | 72 | stable measurement text |

$$
render_{90} = parse_{90} + highlight_{90} + preview_{90}
$$

## Section 00091

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00091.md),
and an image reference ![sample](./assets/image-00091.png).

Paragraph 1 in section 00091 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00091 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00091 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00091 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00091 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00091 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00091 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00091 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00091
- [x] Reviewed checkpoint for section 00091
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00091 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```swift
struct PerfSection00091 {
    let title = "Section 00091"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00092

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00092.md),
and an image reference ![sample](./assets/image-00092.png).

Paragraph 1 in section 00092 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00092 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00092 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00092 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00092 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00092 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00092 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00092 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00092
- [x] Reviewed checkpoint for section 00092
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00092 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00093

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00093.md),
and an image reference ![sample](./assets/image-00093.png).

Paragraph 1 in section 00093 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00093 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00093 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00093 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00093 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00093 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00093 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00093 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00093
- [x] Reviewed checkpoint for section 00093
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00093 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00094

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00094.md),
and an image reference ![sample](./assets/image-00094.png).

Paragraph 1 in section 00094 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00094 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00094 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00094 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00094 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00094 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00094 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00094 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00094
- [x] Reviewed checkpoint for section 00094
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00094 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00095

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00095.md),
and an image reference ![sample](./assets/image-00095.png).

Paragraph 1 in section 00095 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00095 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00095 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00095 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00095 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00095 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00095 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00095 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00095
- [x] Reviewed checkpoint for section 00095
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00095 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{95} = parse_{95} + highlight_{95} + preview_{95}
$$

## Section 00096

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00096.md),
and an image reference ![sample](./assets/image-00096.png).

Paragraph 1 in section 00096 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00096 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00096 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00096 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00096 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00096 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00096 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00096 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00096
- [x] Reviewed checkpoint for section 00096
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00096 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00096 | repeated table row |
| words | 72 | stable measurement text |

## Section 00097

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00097.md),
and an image reference ![sample](./assets/image-00097.png).

Paragraph 1 in section 00097 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00097 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00097 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00097 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00097 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00097 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00097 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00097 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00097
- [x] Reviewed checkpoint for section 00097
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00097 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00098

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00098.md),
and an image reference ![sample](./assets/image-00098.png).

Paragraph 1 in section 00098 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00098 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00098 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00098 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00098 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00098 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00098 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00098 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00098
- [x] Reviewed checkpoint for section 00098
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00098 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```swift
struct PerfSection00098 {
    let title = "Section 00098"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00099

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00099.md),
and an image reference ![sample](./assets/image-00099.png).

Paragraph 1 in section 00099 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00099 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00099 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00099 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00099 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00099 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00099 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00099 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00099
- [x] Reviewed checkpoint for section 00099
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00099 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

```json
{"section":"00099","fixture":"perf-500kb","stable":true}
```

## Section 00100

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00100.md),
and an image reference ![sample](./assets/image-00100.png).

Paragraph 1 in section 00100 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00100 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00100 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00100 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00100 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00100 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00100 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00100 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00100
- [x] Reviewed checkpoint for section 00100
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00100 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{100} = parse_{100} + highlight_{100} + preview_{100}
$$

## Section 00101

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00101.md),
and an image reference ![sample](./assets/image-00101.png).

Paragraph 1 in section 00101 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00101 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00101 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00101 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00101 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00101 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00101 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00101 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00101
- [x] Reviewed checkpoint for section 00101
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00101 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00102

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00102.md),
and an image reference ![sample](./assets/image-00102.png).

Paragraph 1 in section 00102 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00102 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00102 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00102 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00102 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00102 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00102 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00102 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00102
- [x] Reviewed checkpoint for section 00102
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00102 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00102 | repeated table row |
| words | 72 | stable measurement text |

## Section 00103

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00103.md),
and an image reference ![sample](./assets/image-00103.png).

Paragraph 1 in section 00103 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00103 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00103 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00103 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00103 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00103 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00103 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00103 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00103
- [x] Reviewed checkpoint for section 00103
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00103 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00104

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00104.md),
and an image reference ![sample](./assets/image-00104.png).

Paragraph 1 in section 00104 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00104 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00104 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00104 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00104 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00104 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00104 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00104 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00104
- [x] Reviewed checkpoint for section 00104
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00104 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00105

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00105.md),
and an image reference ![sample](./assets/image-00105.png).

Paragraph 1 in section 00105 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00105 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00105 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00105 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00105 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00105 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00105 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00105 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00105
- [x] Reviewed checkpoint for section 00105
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00105 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{105} = parse_{105} + highlight_{105} + preview_{105}
$$

```swift
struct PerfSection00105 {
    let title = "Section 00105"
    let tags = ["performance", "markdown", "plainsong"]
}
```

## Section 00106

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00106.md),
and an image reference ![sample](./assets/image-00106.png).

Paragraph 1 in section 00106 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00106 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00106 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00106 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00106 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00106 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00106 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00106 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00106
- [x] Reviewed checkpoint for section 00106
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00106 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00107

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00107.md),
and an image reference ![sample](./assets/image-00107.png).

Paragraph 1 in section 00107 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00107 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00107 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00107 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00107 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00107 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00107 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00107 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00107
- [x] Reviewed checkpoint for section 00107
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00107 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00108

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00108.md),
and an image reference ![sample](./assets/image-00108.png).

Paragraph 1 in section 00108 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00108 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00108 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00108 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00108 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00108 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00108 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00108 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00108
- [x] Reviewed checkpoint for section 00108
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00108 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

| Column | Value | Notes |
|---|---:|---|
| section | 00108 | repeated table row |
| words | 72 | stable measurement text |

## Section 00109

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00109.md),
and an image reference ![sample](./assets/image-00109.png).

Paragraph 1 in section 00109 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00109 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00109 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00109 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00109 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00109 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00109 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00109 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00109
- [x] Reviewed checkpoint for section 00109
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00109 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

## Section 00110

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight.
It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00110.md),
and an image reference ![sample](./assets/image-00110.png).

Paragraph 1 in section 00110 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 2 in section 00110 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 3 in section 00110 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 4 in section 00110 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 5 in section 00110 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 6 in section 00110 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 7 in section 00110 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

Paragraph 8 in section 00110 keeps the fixture closer to a real long-form
article: mostly prose with stable wording, enough punctuation for wrapping behavior, and
predictable sentence length for repeatable preview parsing. Plainsong should handle this
kind of content smoothly while preserving headings, lists, code samples, math notes, and
frontmatter around the document. The repeated prose is intentional so performance deltas
point at renderer behavior instead of changing fixture entropy.

- [ ] Draft checkpoint for section 00110
- [x] Reviewed checkpoint for section 00110
- Nested values: alpha, beta, gamma, delta, epsilon

> Quoted line for section 00110 with enough text to exercise source styling, preview anchors,
> and paragraph wrapping in a stable way.

$$
render_{110} = parse_{110} + highlight_{110} + preview_{110}
$$

```json
{"section":"00110","fixture":"perf-500kb","stable":true}
```
