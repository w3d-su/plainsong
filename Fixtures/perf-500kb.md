---
title: "Performance 500KB Fixture"
description: "Deterministic 500 KB markdown fixture for Plainsong performance checks."
date: 2026-06-16
draft: false
tags:
  - performance
  - fixture
  - plainsong
---

# Performance 500KB Fixture

This file is generated deterministically for editor highlighting, preview rendering, open-file, and memory smoke checks. Keep the repeated structure stable so regressions are comparable across runs.

## Section 00001

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00001.md), and an image reference ![sample](./assets/image-00001.png). The prose also carries inline math like $score_1 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00001
- [x] Reviewed checkpoint for section 00001
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00001 | repeated table row |
| words | 64 | stable measurement text |

$$
render_1 = parse_1 + highlight_1 + preview_1
$$

```swift
struct PerfSection00001 {
    let title = "Section 00001"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00001","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00001 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00002

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00002.md), and an image reference ![sample](./assets/image-00002.png). The prose also carries inline math like $score_2 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00002
- [x] Reviewed checkpoint for section 00002
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00002 | repeated table row |
| words | 64 | stable measurement text |

$$
render_2 = parse_2 + highlight_2 + preview_2
$$

```swift
struct PerfSection00002 {
    let title = "Section 00002"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00002","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00002 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00003

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00003.md), and an image reference ![sample](./assets/image-00003.png). The prose also carries inline math like $score_3 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00003
- [x] Reviewed checkpoint for section 00003
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00003 | repeated table row |
| words | 64 | stable measurement text |

$$
render_3 = parse_3 + highlight_3 + preview_3
$$

```swift
struct PerfSection00003 {
    let title = "Section 00003"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00003","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00003 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00004

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00004.md), and an image reference ![sample](./assets/image-00004.png). The prose also carries inline math like $score_4 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00004
- [x] Reviewed checkpoint for section 00004
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00004 | repeated table row |
| words | 64 | stable measurement text |

$$
render_4 = parse_4 + highlight_4 + preview_4
$$

```swift
struct PerfSection00004 {
    let title = "Section 00004"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00004","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00004 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00005

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00005.md), and an image reference ![sample](./assets/image-00005.png). The prose also carries inline math like $score_5 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00005
- [x] Reviewed checkpoint for section 00005
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00005 | repeated table row |
| words | 64 | stable measurement text |

$$
render_5 = parse_5 + highlight_5 + preview_5
$$

```swift
struct PerfSection00005 {
    let title = "Section 00005"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00005","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00005 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00006

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00006.md), and an image reference ![sample](./assets/image-00006.png). The prose also carries inline math like $score_6 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00006
- [x] Reviewed checkpoint for section 00006
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00006 | repeated table row |
| words | 64 | stable measurement text |

$$
render_6 = parse_6 + highlight_6 + preview_6
$$

```swift
struct PerfSection00006 {
    let title = "Section 00006"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00006","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00006 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00007

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00007.md), and an image reference ![sample](./assets/image-00007.png). The prose also carries inline math like $score_7 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00007
- [x] Reviewed checkpoint for section 00007
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00007 | repeated table row |
| words | 64 | stable measurement text |

$$
render_7 = parse_7 + highlight_7 + preview_7
$$

```swift
struct PerfSection00007 {
    let title = "Section 00007"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00007","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00007 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00008

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00008.md), and an image reference ![sample](./assets/image-00008.png). The prose also carries inline math like $score_8 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00008
- [x] Reviewed checkpoint for section 00008
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00008 | repeated table row |
| words | 64 | stable measurement text |

$$
render_8 = parse_8 + highlight_8 + preview_8
$$

```swift
struct PerfSection00008 {
    let title = "Section 00008"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00008","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00008 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00009

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00009.md), and an image reference ![sample](./assets/image-00009.png). The prose also carries inline math like $score_9 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00009
- [x] Reviewed checkpoint for section 00009
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00009 | repeated table row |
| words | 64 | stable measurement text |

$$
render_9 = parse_9 + highlight_9 + preview_9
$$

```swift
struct PerfSection00009 {
    let title = "Section 00009"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00009","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00009 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00010

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00010.md), and an image reference ![sample](./assets/image-00010.png). The prose also carries inline math like $score_10 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00010
- [x] Reviewed checkpoint for section 00010
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00010 | repeated table row |
| words | 64 | stable measurement text |

$$
render_10 = parse_10 + highlight_10 + preview_10
$$

```swift
struct PerfSection00010 {
    let title = "Section 00010"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00010","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00010 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00011

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00011.md), and an image reference ![sample](./assets/image-00011.png). The prose also carries inline math like $score_11 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00011
- [x] Reviewed checkpoint for section 00011
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00011 | repeated table row |
| words | 64 | stable measurement text |

$$
render_11 = parse_11 + highlight_11 + preview_11
$$

```swift
struct PerfSection00011 {
    let title = "Section 00011"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00011","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00011 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00012

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00012.md), and an image reference ![sample](./assets/image-00012.png). The prose also carries inline math like $score_12 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00012
- [x] Reviewed checkpoint for section 00012
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00012 | repeated table row |
| words | 64 | stable measurement text |

$$
render_12 = parse_12 + highlight_12 + preview_12
$$

```swift
struct PerfSection00012 {
    let title = "Section 00012"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00012","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00012 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00013

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00013.md), and an image reference ![sample](./assets/image-00013.png). The prose also carries inline math like $score_13 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00013
- [x] Reviewed checkpoint for section 00013
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00013 | repeated table row |
| words | 64 | stable measurement text |

$$
render_13 = parse_13 + highlight_13 + preview_13
$$

```swift
struct PerfSection00013 {
    let title = "Section 00013"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00013","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00013 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00014

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00014.md), and an image reference ![sample](./assets/image-00014.png). The prose also carries inline math like $score_14 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00014
- [x] Reviewed checkpoint for section 00014
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00014 | repeated table row |
| words | 64 | stable measurement text |

$$
render_14 = parse_14 + highlight_14 + preview_14
$$

```swift
struct PerfSection00014 {
    let title = "Section 00014"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00014","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00014 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00015

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00015.md), and an image reference ![sample](./assets/image-00015.png). The prose also carries inline math like $score_15 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00015
- [x] Reviewed checkpoint for section 00015
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00015 | repeated table row |
| words | 64 | stable measurement text |

$$
render_15 = parse_15 + highlight_15 + preview_15
$$

```swift
struct PerfSection00015 {
    let title = "Section 00015"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00015","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00015 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00016

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00016.md), and an image reference ![sample](./assets/image-00016.png). The prose also carries inline math like $score_16 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00016
- [x] Reviewed checkpoint for section 00016
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00016 | repeated table row |
| words | 64 | stable measurement text |

$$
render_16 = parse_16 + highlight_16 + preview_16
$$

```swift
struct PerfSection00016 {
    let title = "Section 00016"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00016","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00016 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00017

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00017.md), and an image reference ![sample](./assets/image-00017.png). The prose also carries inline math like $score_17 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00017
- [x] Reviewed checkpoint for section 00017
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00017 | repeated table row |
| words | 64 | stable measurement text |

$$
render_17 = parse_17 + highlight_17 + preview_17
$$

```swift
struct PerfSection00017 {
    let title = "Section 00017"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00017","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00017 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00018

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00018.md), and an image reference ![sample](./assets/image-00018.png). The prose also carries inline math like $score_18 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00018
- [x] Reviewed checkpoint for section 00018
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00018 | repeated table row |
| words | 64 | stable measurement text |

$$
render_18 = parse_18 + highlight_18 + preview_18
$$

```swift
struct PerfSection00018 {
    let title = "Section 00018"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00018","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00018 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00019

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00019.md), and an image reference ![sample](./assets/image-00019.png). The prose also carries inline math like $score_19 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00019
- [x] Reviewed checkpoint for section 00019
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00019 | repeated table row |
| words | 64 | stable measurement text |

$$
render_19 = parse_19 + highlight_19 + preview_19
$$

```swift
struct PerfSection00019 {
    let title = "Section 00019"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00019","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00019 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00020

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00020.md), and an image reference ![sample](./assets/image-00020.png). The prose also carries inline math like $score_20 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00020
- [x] Reviewed checkpoint for section 00020
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00020 | repeated table row |
| words | 64 | stable measurement text |

$$
render_20 = parse_20 + highlight_20 + preview_20
$$

```swift
struct PerfSection00020 {
    let title = "Section 00020"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00020","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00020 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00021

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00021.md), and an image reference ![sample](./assets/image-00021.png). The prose also carries inline math like $score_21 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00021
- [x] Reviewed checkpoint for section 00021
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00021 | repeated table row |
| words | 64 | stable measurement text |

$$
render_21 = parse_21 + highlight_21 + preview_21
$$

```swift
struct PerfSection00021 {
    let title = "Section 00021"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00021","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00021 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00022

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00022.md), and an image reference ![sample](./assets/image-00022.png). The prose also carries inline math like $score_22 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00022
- [x] Reviewed checkpoint for section 00022
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00022 | repeated table row |
| words | 64 | stable measurement text |

$$
render_22 = parse_22 + highlight_22 + preview_22
$$

```swift
struct PerfSection00022 {
    let title = "Section 00022"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00022","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00022 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00023

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00023.md), and an image reference ![sample](./assets/image-00023.png). The prose also carries inline math like $score_23 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00023
- [x] Reviewed checkpoint for section 00023
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00023 | repeated table row |
| words | 64 | stable measurement text |

$$
render_23 = parse_23 + highlight_23 + preview_23
$$

```swift
struct PerfSection00023 {
    let title = "Section 00023"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00023","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00023 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00024

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00024.md), and an image reference ![sample](./assets/image-00024.png). The prose also carries inline math like $score_24 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00024
- [x] Reviewed checkpoint for section 00024
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00024 | repeated table row |
| words | 64 | stable measurement text |

$$
render_24 = parse_24 + highlight_24 + preview_24
$$

```swift
struct PerfSection00024 {
    let title = "Section 00024"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00024","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00024 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00025

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00025.md), and an image reference ![sample](./assets/image-00025.png). The prose also carries inline math like $score_25 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00025
- [x] Reviewed checkpoint for section 00025
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00025 | repeated table row |
| words | 64 | stable measurement text |

$$
render_25 = parse_25 + highlight_25 + preview_25
$$

```swift
struct PerfSection00025 {
    let title = "Section 00025"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00025","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00025 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00026

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00026.md), and an image reference ![sample](./assets/image-00026.png). The prose also carries inline math like $score_26 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00026
- [x] Reviewed checkpoint for section 00026
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00026 | repeated table row |
| words | 64 | stable measurement text |

$$
render_26 = parse_26 + highlight_26 + preview_26
$$

```swift
struct PerfSection00026 {
    let title = "Section 00026"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00026","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00026 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00027

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00027.md), and an image reference ![sample](./assets/image-00027.png). The prose also carries inline math like $score_27 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00027
- [x] Reviewed checkpoint for section 00027
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00027 | repeated table row |
| words | 64 | stable measurement text |

$$
render_27 = parse_27 + highlight_27 + preview_27
$$

```swift
struct PerfSection00027 {
    let title = "Section 00027"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00027","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00027 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00028

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00028.md), and an image reference ![sample](./assets/image-00028.png). The prose also carries inline math like $score_28 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00028
- [x] Reviewed checkpoint for section 00028
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00028 | repeated table row |
| words | 64 | stable measurement text |

$$
render_28 = parse_28 + highlight_28 + preview_28
$$

```swift
struct PerfSection00028 {
    let title = "Section 00028"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00028","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00028 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00029

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00029.md), and an image reference ![sample](./assets/image-00029.png). The prose also carries inline math like $score_29 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00029
- [x] Reviewed checkpoint for section 00029
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00029 | repeated table row |
| words | 64 | stable measurement text |

$$
render_29 = parse_29 + highlight_29 + preview_29
$$

```swift
struct PerfSection00029 {
    let title = "Section 00029"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00029","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00029 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00030

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00030.md), and an image reference ![sample](./assets/image-00030.png). The prose also carries inline math like $score_30 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00030
- [x] Reviewed checkpoint for section 00030
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00030 | repeated table row |
| words | 64 | stable measurement text |

$$
render_30 = parse_30 + highlight_30 + preview_30
$$

```swift
struct PerfSection00030 {
    let title = "Section 00030"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00030","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00030 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00031

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00031.md), and an image reference ![sample](./assets/image-00031.png). The prose also carries inline math like $score_31 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00031
- [x] Reviewed checkpoint for section 00031
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00031 | repeated table row |
| words | 64 | stable measurement text |

$$
render_31 = parse_31 + highlight_31 + preview_31
$$

```swift
struct PerfSection00031 {
    let title = "Section 00031"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00031","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00031 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00032

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00032.md), and an image reference ![sample](./assets/image-00032.png). The prose also carries inline math like $score_32 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00032
- [x] Reviewed checkpoint for section 00032
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00032 | repeated table row |
| words | 64 | stable measurement text |

$$
render_32 = parse_32 + highlight_32 + preview_32
$$

```swift
struct PerfSection00032 {
    let title = "Section 00032"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00032","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00032 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00033

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00033.md), and an image reference ![sample](./assets/image-00033.png). The prose also carries inline math like $score_33 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00033
- [x] Reviewed checkpoint for section 00033
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00033 | repeated table row |
| words | 64 | stable measurement text |

$$
render_33 = parse_33 + highlight_33 + preview_33
$$

```swift
struct PerfSection00033 {
    let title = "Section 00033"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00033","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00033 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00034

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00034.md), and an image reference ![sample](./assets/image-00034.png). The prose also carries inline math like $score_34 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00034
- [x] Reviewed checkpoint for section 00034
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00034 | repeated table row |
| words | 64 | stable measurement text |

$$
render_34 = parse_34 + highlight_34 + preview_34
$$

```swift
struct PerfSection00034 {
    let title = "Section 00034"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00034","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00034 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00035

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00035.md), and an image reference ![sample](./assets/image-00035.png). The prose also carries inline math like $score_35 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00035
- [x] Reviewed checkpoint for section 00035
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00035 | repeated table row |
| words | 64 | stable measurement text |

$$
render_35 = parse_35 + highlight_35 + preview_35
$$

```swift
struct PerfSection00035 {
    let title = "Section 00035"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00035","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00035 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00036

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00036.md), and an image reference ![sample](./assets/image-00036.png). The prose also carries inline math like $score_36 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00036
- [x] Reviewed checkpoint for section 00036
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00036 | repeated table row |
| words | 64 | stable measurement text |

$$
render_36 = parse_36 + highlight_36 + preview_36
$$

```swift
struct PerfSection00036 {
    let title = "Section 00036"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00036","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00036 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00037

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00037.md), and an image reference ![sample](./assets/image-00037.png). The prose also carries inline math like $score_37 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00037
- [x] Reviewed checkpoint for section 00037
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00037 | repeated table row |
| words | 64 | stable measurement text |

$$
render_37 = parse_37 + highlight_37 + preview_37
$$

```swift
struct PerfSection00037 {
    let title = "Section 00037"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00037","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00037 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00038

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00038.md), and an image reference ![sample](./assets/image-00038.png). The prose also carries inline math like $score_38 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00038
- [x] Reviewed checkpoint for section 00038
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00038 | repeated table row |
| words | 64 | stable measurement text |

$$
render_38 = parse_38 + highlight_38 + preview_38
$$

```swift
struct PerfSection00038 {
    let title = "Section 00038"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00038","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00038 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00039

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00039.md), and an image reference ![sample](./assets/image-00039.png). The prose also carries inline math like $score_39 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00039
- [x] Reviewed checkpoint for section 00039
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00039 | repeated table row |
| words | 64 | stable measurement text |

$$
render_39 = parse_39 + highlight_39 + preview_39
$$

```swift
struct PerfSection00039 {
    let title = "Section 00039"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00039","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00039 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00040

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00040.md), and an image reference ![sample](./assets/image-00040.png). The prose also carries inline math like $score_40 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00040
- [x] Reviewed checkpoint for section 00040
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00040 | repeated table row |
| words | 64 | stable measurement text |

$$
render_40 = parse_40 + highlight_40 + preview_40
$$

```swift
struct PerfSection00040 {
    let title = "Section 00040"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00040","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00040 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00041

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00041.md), and an image reference ![sample](./assets/image-00041.png). The prose also carries inline math like $score_41 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00041
- [x] Reviewed checkpoint for section 00041
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00041 | repeated table row |
| words | 64 | stable measurement text |

$$
render_41 = parse_41 + highlight_41 + preview_41
$$

```swift
struct PerfSection00041 {
    let title = "Section 00041"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00041","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00041 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00042

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00042.md), and an image reference ![sample](./assets/image-00042.png). The prose also carries inline math like $score_42 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00042
- [x] Reviewed checkpoint for section 00042
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00042 | repeated table row |
| words | 64 | stable measurement text |

$$
render_42 = parse_42 + highlight_42 + preview_42
$$

```swift
struct PerfSection00042 {
    let title = "Section 00042"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00042","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00042 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00043

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00043.md), and an image reference ![sample](./assets/image-00043.png). The prose also carries inline math like $score_43 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00043
- [x] Reviewed checkpoint for section 00043
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00043 | repeated table row |
| words | 64 | stable measurement text |

$$
render_43 = parse_43 + highlight_43 + preview_43
$$

```swift
struct PerfSection00043 {
    let title = "Section 00043"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00043","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00043 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00044

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00044.md), and an image reference ![sample](./assets/image-00044.png). The prose also carries inline math like $score_44 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00044
- [x] Reviewed checkpoint for section 00044
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00044 | repeated table row |
| words | 64 | stable measurement text |

$$
render_44 = parse_44 + highlight_44 + preview_44
$$

```swift
struct PerfSection00044 {
    let title = "Section 00044"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00044","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00044 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00045

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00045.md), and an image reference ![sample](./assets/image-00045.png). The prose also carries inline math like $score_45 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00045
- [x] Reviewed checkpoint for section 00045
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00045 | repeated table row |
| words | 64 | stable measurement text |

$$
render_45 = parse_45 + highlight_45 + preview_45
$$

```swift
struct PerfSection00045 {
    let title = "Section 00045"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00045","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00045 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00046

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00046.md), and an image reference ![sample](./assets/image-00046.png). The prose also carries inline math like $score_46 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00046
- [x] Reviewed checkpoint for section 00046
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00046 | repeated table row |
| words | 64 | stable measurement text |

$$
render_46 = parse_46 + highlight_46 + preview_46
$$

```swift
struct PerfSection00046 {
    let title = "Section 00046"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00046","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00046 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00047

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00047.md), and an image reference ![sample](./assets/image-00047.png). The prose also carries inline math like $score_47 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00047
- [x] Reviewed checkpoint for section 00047
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00047 | repeated table row |
| words | 64 | stable measurement text |

$$
render_47 = parse_47 + highlight_47 + preview_47
$$

```swift
struct PerfSection00047 {
    let title = "Section 00047"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00047","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00047 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00048

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00048.md), and an image reference ![sample](./assets/image-00048.png). The prose also carries inline math like $score_48 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00048
- [x] Reviewed checkpoint for section 00048
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00048 | repeated table row |
| words | 64 | stable measurement text |

$$
render_48 = parse_48 + highlight_48 + preview_48
$$

```swift
struct PerfSection00048 {
    let title = "Section 00048"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00048","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00048 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00049

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00049.md), and an image reference ![sample](./assets/image-00049.png). The prose also carries inline math like $score_49 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00049
- [x] Reviewed checkpoint for section 00049
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00049 | repeated table row |
| words | 64 | stable measurement text |

$$
render_49 = parse_49 + highlight_49 + preview_49
$$

```swift
struct PerfSection00049 {
    let title = "Section 00049"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00049","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00049 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00050

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00050.md), and an image reference ![sample](./assets/image-00050.png). The prose also carries inline math like $score_50 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00050
- [x] Reviewed checkpoint for section 00050
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00050 | repeated table row |
| words | 64 | stable measurement text |

$$
render_50 = parse_50 + highlight_50 + preview_50
$$

```swift
struct PerfSection00050 {
    let title = "Section 00050"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00050","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00050 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00051

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00051.md), and an image reference ![sample](./assets/image-00051.png). The prose also carries inline math like $score_51 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00051
- [x] Reviewed checkpoint for section 00051
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00051 | repeated table row |
| words | 64 | stable measurement text |

$$
render_51 = parse_51 + highlight_51 + preview_51
$$

```swift
struct PerfSection00051 {
    let title = "Section 00051"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00051","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00051 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00052

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00052.md), and an image reference ![sample](./assets/image-00052.png). The prose also carries inline math like $score_52 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00052
- [x] Reviewed checkpoint for section 00052
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00052 | repeated table row |
| words | 64 | stable measurement text |

$$
render_52 = parse_52 + highlight_52 + preview_52
$$

```swift
struct PerfSection00052 {
    let title = "Section 00052"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00052","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00052 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00053

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00053.md), and an image reference ![sample](./assets/image-00053.png). The prose also carries inline math like $score_53 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00053
- [x] Reviewed checkpoint for section 00053
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00053 | repeated table row |
| words | 64 | stable measurement text |

$$
render_53 = parse_53 + highlight_53 + preview_53
$$

```swift
struct PerfSection00053 {
    let title = "Section 00053"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00053","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00053 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00054

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00054.md), and an image reference ![sample](./assets/image-00054.png). The prose also carries inline math like $score_54 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00054
- [x] Reviewed checkpoint for section 00054
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00054 | repeated table row |
| words | 64 | stable measurement text |

$$
render_54 = parse_54 + highlight_54 + preview_54
$$

```swift
struct PerfSection00054 {
    let title = "Section 00054"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00054","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00054 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00055

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00055.md), and an image reference ![sample](./assets/image-00055.png). The prose also carries inline math like $score_55 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00055
- [x] Reviewed checkpoint for section 00055
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00055 | repeated table row |
| words | 64 | stable measurement text |

$$
render_55 = parse_55 + highlight_55 + preview_55
$$

```swift
struct PerfSection00055 {
    let title = "Section 00055"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00055","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00055 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00056

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00056.md), and an image reference ![sample](./assets/image-00056.png). The prose also carries inline math like $score_56 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00056
- [x] Reviewed checkpoint for section 00056
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00056 | repeated table row |
| words | 64 | stable measurement text |

$$
render_56 = parse_56 + highlight_56 + preview_56
$$

```swift
struct PerfSection00056 {
    let title = "Section 00056"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00056","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00056 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00057

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00057.md), and an image reference ![sample](./assets/image-00057.png). The prose also carries inline math like $score_57 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00057
- [x] Reviewed checkpoint for section 00057
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00057 | repeated table row |
| words | 64 | stable measurement text |

$$
render_57 = parse_57 + highlight_57 + preview_57
$$

```swift
struct PerfSection00057 {
    let title = "Section 00057"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00057","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00057 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00058

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00058.md), and an image reference ![sample](./assets/image-00058.png). The prose also carries inline math like $score_58 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00058
- [x] Reviewed checkpoint for section 00058
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00058 | repeated table row |
| words | 64 | stable measurement text |

$$
render_58 = parse_58 + highlight_58 + preview_58
$$

```swift
struct PerfSection00058 {
    let title = "Section 00058"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00058","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00058 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00059

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00059.md), and an image reference ![sample](./assets/image-00059.png). The prose also carries inline math like $score_59 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00059
- [x] Reviewed checkpoint for section 00059
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00059 | repeated table row |
| words | 64 | stable measurement text |

$$
render_59 = parse_59 + highlight_59 + preview_59
$$

```swift
struct PerfSection00059 {
    let title = "Section 00059"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00059","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00059 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00060

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00060.md), and an image reference ![sample](./assets/image-00060.png). The prose also carries inline math like $score_60 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00060
- [x] Reviewed checkpoint for section 00060
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00060 | repeated table row |
| words | 64 | stable measurement text |

$$
render_60 = parse_60 + highlight_60 + preview_60
$$

```swift
struct PerfSection00060 {
    let title = "Section 00060"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00060","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00060 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00061

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00061.md), and an image reference ![sample](./assets/image-00061.png). The prose also carries inline math like $score_61 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00061
- [x] Reviewed checkpoint for section 00061
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00061 | repeated table row |
| words | 64 | stable measurement text |

$$
render_61 = parse_61 + highlight_61 + preview_61
$$

```swift
struct PerfSection00061 {
    let title = "Section 00061"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00061","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00061 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00062

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00062.md), and an image reference ![sample](./assets/image-00062.png). The prose also carries inline math like $score_62 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00062
- [x] Reviewed checkpoint for section 00062
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00062 | repeated table row |
| words | 64 | stable measurement text |

$$
render_62 = parse_62 + highlight_62 + preview_62
$$

```swift
struct PerfSection00062 {
    let title = "Section 00062"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00062","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00062 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00063

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00063.md), and an image reference ![sample](./assets/image-00063.png). The prose also carries inline math like $score_63 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00063
- [x] Reviewed checkpoint for section 00063
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00063 | repeated table row |
| words | 64 | stable measurement text |

$$
render_63 = parse_63 + highlight_63 + preview_63
$$

```swift
struct PerfSection00063 {
    let title = "Section 00063"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00063","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00063 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00064

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00064.md), and an image reference ![sample](./assets/image-00064.png). The prose also carries inline math like $score_64 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00064
- [x] Reviewed checkpoint for section 00064
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00064 | repeated table row |
| words | 64 | stable measurement text |

$$
render_64 = parse_64 + highlight_64 + preview_64
$$

```swift
struct PerfSection00064 {
    let title = "Section 00064"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00064","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00064 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00065

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00065.md), and an image reference ![sample](./assets/image-00065.png). The prose also carries inline math like $score_65 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00065
- [x] Reviewed checkpoint for section 00065
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00065 | repeated table row |
| words | 64 | stable measurement text |

$$
render_65 = parse_65 + highlight_65 + preview_65
$$

```swift
struct PerfSection00065 {
    let title = "Section 00065"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00065","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00065 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00066

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00066.md), and an image reference ![sample](./assets/image-00066.png). The prose also carries inline math like $score_66 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00066
- [x] Reviewed checkpoint for section 00066
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00066 | repeated table row |
| words | 64 | stable measurement text |

$$
render_66 = parse_66 + highlight_66 + preview_66
$$

```swift
struct PerfSection00066 {
    let title = "Section 00066"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00066","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00066 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00067

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00067.md), and an image reference ![sample](./assets/image-00067.png). The prose also carries inline math like $score_67 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00067
- [x] Reviewed checkpoint for section 00067
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00067 | repeated table row |
| words | 64 | stable measurement text |

$$
render_67 = parse_67 + highlight_67 + preview_67
$$

```swift
struct PerfSection00067 {
    let title = "Section 00067"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00067","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00067 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00068

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00068.md), and an image reference ![sample](./assets/image-00068.png). The prose also carries inline math like $score_68 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00068
- [x] Reviewed checkpoint for section 00068
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00068 | repeated table row |
| words | 64 | stable measurement text |

$$
render_68 = parse_68 + highlight_68 + preview_68
$$

```swift
struct PerfSection00068 {
    let title = "Section 00068"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00068","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00068 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00069

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00069.md), and an image reference ![sample](./assets/image-00069.png). The prose also carries inline math like $score_69 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00069
- [x] Reviewed checkpoint for section 00069
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00069 | repeated table row |
| words | 64 | stable measurement text |

$$
render_69 = parse_69 + highlight_69 + preview_69
$$

```swift
struct PerfSection00069 {
    let title = "Section 00069"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00069","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00069 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00070

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00070.md), and an image reference ![sample](./assets/image-00070.png). The prose also carries inline math like $score_70 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00070
- [x] Reviewed checkpoint for section 00070
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00070 | repeated table row |
| words | 64 | stable measurement text |

$$
render_70 = parse_70 + highlight_70 + preview_70
$$

```swift
struct PerfSection00070 {
    let title = "Section 00070"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00070","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00070 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00071

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00071.md), and an image reference ![sample](./assets/image-00071.png). The prose also carries inline math like $score_71 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00071
- [x] Reviewed checkpoint for section 00071
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00071 | repeated table row |
| words | 64 | stable measurement text |

$$
render_71 = parse_71 + highlight_71 + preview_71
$$

```swift
struct PerfSection00071 {
    let title = "Section 00071"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00071","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00071 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00072

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00072.md), and an image reference ![sample](./assets/image-00072.png). The prose also carries inline math like $score_72 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00072
- [x] Reviewed checkpoint for section 00072
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00072 | repeated table row |
| words | 64 | stable measurement text |

$$
render_72 = parse_72 + highlight_72 + preview_72
$$

```swift
struct PerfSection00072 {
    let title = "Section 00072"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00072","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00072 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00073

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00073.md), and an image reference ![sample](./assets/image-00073.png). The prose also carries inline math like $score_73 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00073
- [x] Reviewed checkpoint for section 00073
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00073 | repeated table row |
| words | 64 | stable measurement text |

$$
render_73 = parse_73 + highlight_73 + preview_73
$$

```swift
struct PerfSection00073 {
    let title = "Section 00073"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00073","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00073 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00074

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00074.md), and an image reference ![sample](./assets/image-00074.png). The prose also carries inline math like $score_74 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00074
- [x] Reviewed checkpoint for section 00074
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00074 | repeated table row |
| words | 64 | stable measurement text |

$$
render_74 = parse_74 + highlight_74 + preview_74
$$

```swift
struct PerfSection00074 {
    let title = "Section 00074"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00074","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00074 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00075

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00075.md), and an image reference ![sample](./assets/image-00075.png). The prose also carries inline math like $score_75 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00075
- [x] Reviewed checkpoint for section 00075
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00075 | repeated table row |
| words | 64 | stable measurement text |

$$
render_75 = parse_75 + highlight_75 + preview_75
$$

```swift
struct PerfSection00075 {
    let title = "Section 00075"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00075","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00075 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00076

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00076.md), and an image reference ![sample](./assets/image-00076.png). The prose also carries inline math like $score_76 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00076
- [x] Reviewed checkpoint for section 00076
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00076 | repeated table row |
| words | 64 | stable measurement text |

$$
render_76 = parse_76 + highlight_76 + preview_76
$$

```swift
struct PerfSection00076 {
    let title = "Section 00076"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00076","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00076 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00077

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00077.md), and an image reference ![sample](./assets/image-00077.png). The prose also carries inline math like $score_77 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00077
- [x] Reviewed checkpoint for section 00077
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00077 | repeated table row |
| words | 64 | stable measurement text |

$$
render_77 = parse_77 + highlight_77 + preview_77
$$

```swift
struct PerfSection00077 {
    let title = "Section 00077"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00077","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00077 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00078

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00078.md), and an image reference ![sample](./assets/image-00078.png). The prose also carries inline math like $score_78 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00078
- [x] Reviewed checkpoint for section 00078
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00078 | repeated table row |
| words | 64 | stable measurement text |

$$
render_78 = parse_78 + highlight_78 + preview_78
$$

```swift
struct PerfSection00078 {
    let title = "Section 00078"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00078","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00078 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00079

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00079.md), and an image reference ![sample](./assets/image-00079.png). The prose also carries inline math like $score_79 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00079
- [x] Reviewed checkpoint for section 00079
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00079 | repeated table row |
| words | 64 | stable measurement text |

$$
render_79 = parse_79 + highlight_79 + preview_79
$$

```swift
struct PerfSection00079 {
    let title = "Section 00079"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00079","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00079 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00080

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00080.md), and an image reference ![sample](./assets/image-00080.png). The prose also carries inline math like $score_80 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00080
- [x] Reviewed checkpoint for section 00080
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00080 | repeated table row |
| words | 64 | stable measurement text |

$$
render_80 = parse_80 + highlight_80 + preview_80
$$

```swift
struct PerfSection00080 {
    let title = "Section 00080"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00080","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00080 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00081

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00081.md), and an image reference ![sample](./assets/image-00081.png). The prose also carries inline math like $score_81 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00081
- [x] Reviewed checkpoint for section 00081
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00081 | repeated table row |
| words | 64 | stable measurement text |

$$
render_81 = parse_81 + highlight_81 + preview_81
$$

```swift
struct PerfSection00081 {
    let title = "Section 00081"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00081","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00081 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00082

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00082.md), and an image reference ![sample](./assets/image-00082.png). The prose also carries inline math like $score_82 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00082
- [x] Reviewed checkpoint for section 00082
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00082 | repeated table row |
| words | 64 | stable measurement text |

$$
render_82 = parse_82 + highlight_82 + preview_82
$$

```swift
struct PerfSection00082 {
    let title = "Section 00082"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00082","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00082 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00083

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00083.md), and an image reference ![sample](./assets/image-00083.png). The prose also carries inline math like $score_83 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00083
- [x] Reviewed checkpoint for section 00083
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00083 | repeated table row |
| words | 64 | stable measurement text |

$$
render_83 = parse_83 + highlight_83 + preview_83
$$

```swift
struct PerfSection00083 {
    let title = "Section 00083"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00083","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00083 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00084

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00084.md), and an image reference ![sample](./assets/image-00084.png). The prose also carries inline math like $score_84 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00084
- [x] Reviewed checkpoint for section 00084
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00084 | repeated table row |
| words | 64 | stable measurement text |

$$
render_84 = parse_84 + highlight_84 + preview_84
$$

```swift
struct PerfSection00084 {
    let title = "Section 00084"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00084","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00084 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00085

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00085.md), and an image reference ![sample](./assets/image-00085.png). The prose also carries inline math like $score_85 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00085
- [x] Reviewed checkpoint for section 00085
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00085 | repeated table row |
| words | 64 | stable measurement text |

$$
render_85 = parse_85 + highlight_85 + preview_85
$$

```swift
struct PerfSection00085 {
    let title = "Section 00085"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00085","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00085 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00086

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00086.md), and an image reference ![sample](./assets/image-00086.png). The prose also carries inline math like $score_86 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00086
- [x] Reviewed checkpoint for section 00086
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00086 | repeated table row |
| words | 64 | stable measurement text |

$$
render_86 = parse_86 + highlight_86 + preview_86
$$

```swift
struct PerfSection00086 {
    let title = "Section 00086"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00086","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00086 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00087

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00087.md), and an image reference ![sample](./assets/image-00087.png). The prose also carries inline math like $score_87 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00087
- [x] Reviewed checkpoint for section 00087
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00087 | repeated table row |
| words | 64 | stable measurement text |

$$
render_87 = parse_87 + highlight_87 + preview_87
$$

```swift
struct PerfSection00087 {
    let title = "Section 00087"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00087","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00087 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00088

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00088.md), and an image reference ![sample](./assets/image-00088.png). The prose also carries inline math like $score_88 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00088
- [x] Reviewed checkpoint for section 00088
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00088 | repeated table row |
| words | 64 | stable measurement text |

$$
render_88 = parse_88 + highlight_88 + preview_88
$$

```swift
struct PerfSection00088 {
    let title = "Section 00088"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00088","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00088 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00089

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00089.md), and an image reference ![sample](./assets/image-00089.png). The prose also carries inline math like $score_89 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00089
- [x] Reviewed checkpoint for section 00089
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00089 | repeated table row |
| words | 64 | stable measurement text |

$$
render_89 = parse_89 + highlight_89 + preview_89
$$

```swift
struct PerfSection00089 {
    let title = "Section 00089"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00089","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00089 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00090

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00090.md), and an image reference ![sample](./assets/image-00090.png). The prose also carries inline math like $score_90 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00090
- [x] Reviewed checkpoint for section 00090
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00090 | repeated table row |
| words | 64 | stable measurement text |

$$
render_90 = parse_90 + highlight_90 + preview_90
$$

```swift
struct PerfSection00090 {
    let title = "Section 00090"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00090","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00090 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00091

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00091.md), and an image reference ![sample](./assets/image-00091.png). The prose also carries inline math like $score_91 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00091
- [x] Reviewed checkpoint for section 00091
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00091 | repeated table row |
| words | 64 | stable measurement text |

$$
render_91 = parse_91 + highlight_91 + preview_91
$$

```swift
struct PerfSection00091 {
    let title = "Section 00091"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00091","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00091 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00092

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00092.md), and an image reference ![sample](./assets/image-00092.png). The prose also carries inline math like $score_92 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00092
- [x] Reviewed checkpoint for section 00092
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00092 | repeated table row |
| words | 64 | stable measurement text |

$$
render_92 = parse_92 + highlight_92 + preview_92
$$

```swift
struct PerfSection00092 {
    let title = "Section 00092"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00092","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00092 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00093

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00093.md), and an image reference ![sample](./assets/image-00093.png). The prose also carries inline math like $score_93 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00093
- [x] Reviewed checkpoint for section 00093
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00093 | repeated table row |
| words | 64 | stable measurement text |

$$
render_93 = parse_93 + highlight_93 + preview_93
$$

```swift
struct PerfSection00093 {
    let title = "Section 00093"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00093","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00093 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00094

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00094.md), and an image reference ![sample](./assets/image-00094.png). The prose also carries inline math like $score_94 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00094
- [x] Reviewed checkpoint for section 00094
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00094 | repeated table row |
| words | 64 | stable measurement text |

$$
render_94 = parse_94 + highlight_94 + preview_94
$$

```swift
struct PerfSection00094 {
    let title = "Section 00094"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00094","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00094 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00095

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00095.md), and an image reference ![sample](./assets/image-00095.png). The prose also carries inline math like $score_95 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00095
- [x] Reviewed checkpoint for section 00095
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00095 | repeated table row |
| words | 64 | stable measurement text |

$$
render_95 = parse_95 + highlight_95 + preview_95
$$

```swift
struct PerfSection00095 {
    let title = "Section 00095"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00095","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00095 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00096

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00096.md), and an image reference ![sample](./assets/image-00096.png). The prose also carries inline math like $score_96 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00096
- [x] Reviewed checkpoint for section 00096
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00096 | repeated table row |
| words | 64 | stable measurement text |

$$
render_96 = parse_96 + highlight_96 + preview_96
$$

```swift
struct PerfSection00096 {
    let title = "Section 00096"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00096","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00096 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00097

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00097.md), and an image reference ![sample](./assets/image-00097.png). The prose also carries inline math like $score_97 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00097
- [x] Reviewed checkpoint for section 00097
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00097 | repeated table row |
| words | 64 | stable measurement text |

$$
render_97 = parse_97 + highlight_97 + preview_97
$$

```swift
struct PerfSection00097 {
    let title = "Section 00097"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00097","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00097 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00098

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00098.md), and an image reference ![sample](./assets/image-00098.png). The prose also carries inline math like $score_98 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00098
- [x] Reviewed checkpoint for section 00098
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00098 | repeated table row |
| words | 64 | stable measurement text |

$$
render_98 = parse_98 + highlight_98 + preview_98
$$

```swift
struct PerfSection00098 {
    let title = "Section 00098"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00098","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00098 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00099

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00099.md), and an image reference ![sample](./assets/image-00099.png). The prose also carries inline math like $score_99 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00099
- [x] Reviewed checkpoint for section 00099
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00099 | repeated table row |
| words | 64 | stable measurement text |

$$
render_99 = parse_99 + highlight_99 + preview_99
$$

```swift
struct PerfSection00099 {
    let title = "Section 00099"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00099","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00099 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00100

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00100.md), and an image reference ![sample](./assets/image-00100.png). The prose also carries inline math like $score_100 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00100
- [x] Reviewed checkpoint for section 00100
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00100 | repeated table row |
| words | 64 | stable measurement text |

$$
render_100 = parse_100 + highlight_100 + preview_100
$$

```swift
struct PerfSection00100 {
    let title = "Section 00100"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00100","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00100 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00101

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00101.md), and an image reference ![sample](./assets/image-00101.png). The prose also carries inline math like $score_101 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00101
- [x] Reviewed checkpoint for section 00101
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00101 | repeated table row |
| words | 64 | stable measurement text |

$$
render_101 = parse_101 + highlight_101 + preview_101
$$

```swift
struct PerfSection00101 {
    let title = "Section 00101"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00101","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00101 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00102

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00102.md), and an image reference ![sample](./assets/image-00102.png). The prose also carries inline math like $score_102 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00102
- [x] Reviewed checkpoint for section 00102
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00102 | repeated table row |
| words | 64 | stable measurement text |

$$
render_102 = parse_102 + highlight_102 + preview_102
$$

```swift
struct PerfSection00102 {
    let title = "Section 00102"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00102","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00102 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00103

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00103.md), and an image reference ![sample](./assets/image-00103.png). The prose also carries inline math like $score_103 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00103
- [x] Reviewed checkpoint for section 00103
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00103 | repeated table row |
| words | 64 | stable measurement text |

$$
render_103 = parse_103 + highlight_103 + preview_103
$$

```swift
struct PerfSection00103 {
    let title = "Section 00103"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00103","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00103 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00104

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00104.md), and an image reference ![sample](./assets/image-00104.png). The prose also carries inline math like $score_104 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00104
- [x] Reviewed checkpoint for section 00104
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00104 | repeated table row |
| words | 64 | stable measurement text |

$$
render_104 = parse_104 + highlight_104 + preview_104
$$

```swift
struct PerfSection00104 {
    let title = "Section 00104"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00104","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00104 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00105

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00105.md), and an image reference ![sample](./assets/image-00105.png). The prose also carries inline math like $score_105 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00105
- [x] Reviewed checkpoint for section 00105
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00105 | repeated table row |
| words | 64 | stable measurement text |

$$
render_105 = parse_105 + highlight_105 + preview_105
$$

```swift
struct PerfSection00105 {
    let title = "Section 00105"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00105","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00105 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00106

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00106.md), and an image reference ![sample](./assets/image-00106.png). The prose also carries inline math like $score_106 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00106
- [x] Reviewed checkpoint for section 00106
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00106 | repeated table row |
| words | 64 | stable measurement text |

$$
render_106 = parse_106 + highlight_106 + preview_106
$$

```swift
struct PerfSection00106 {
    let title = "Section 00106"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00106","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00106 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00107

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00107.md), and an image reference ![sample](./assets/image-00107.png). The prose also carries inline math like $score_107 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00107
- [x] Reviewed checkpoint for section 00107
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00107 | repeated table row |
| words | 64 | stable measurement text |

$$
render_107 = parse_107 + highlight_107 + preview_107
$$

```swift
struct PerfSection00107 {
    let title = "Section 00107"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00107","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00107 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00108

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00108.md), and an image reference ![sample](./assets/image-00108.png). The prose also carries inline math like $score_108 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00108
- [x] Reviewed checkpoint for section 00108
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00108 | repeated table row |
| words | 64 | stable measurement text |

$$
render_108 = parse_108 + highlight_108 + preview_108
$$

```swift
struct PerfSection00108 {
    let title = "Section 00108"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00108","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00108 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00109

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00109.md), and an image reference ![sample](./assets/image-00109.png). The prose also carries inline math like $score_109 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00109
- [x] Reviewed checkpoint for section 00109
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00109 | repeated table row |
| words | 64 | stable measurement text |

$$
render_109 = parse_109 + highlight_109 + preview_109
$$

```swift
struct PerfSection00109 {
    let title = "Section 00109"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00109","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00109 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00110

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00110.md), and an image reference ![sample](./assets/image-00110.png). The prose also carries inline math like $score_110 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00110
- [x] Reviewed checkpoint for section 00110
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00110 | repeated table row |
| words | 64 | stable measurement text |

$$
render_110 = parse_110 + highlight_110 + preview_110
$$

```swift
struct PerfSection00110 {
    let title = "Section 00110"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00110","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00110 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00111

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00111.md), and an image reference ![sample](./assets/image-00111.png). The prose also carries inline math like $score_111 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00111
- [x] Reviewed checkpoint for section 00111
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00111 | repeated table row |
| words | 64 | stable measurement text |

$$
render_111 = parse_111 + highlight_111 + preview_111
$$

```swift
struct PerfSection00111 {
    let title = "Section 00111"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00111","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00111 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00112

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00112.md), and an image reference ![sample](./assets/image-00112.png). The prose also carries inline math like $score_112 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00112
- [x] Reviewed checkpoint for section 00112
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00112 | repeated table row |
| words | 64 | stable measurement text |

$$
render_112 = parse_112 + highlight_112 + preview_112
$$

```swift
struct PerfSection00112 {
    let title = "Section 00112"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00112","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00112 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00113

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00113.md), and an image reference ![sample](./assets/image-00113.png). The prose also carries inline math like $score_113 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00113
- [x] Reviewed checkpoint for section 00113
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00113 | repeated table row |
| words | 64 | stable measurement text |

$$
render_113 = parse_113 + highlight_113 + preview_113
$$

```swift
struct PerfSection00113 {
    let title = "Section 00113"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00113","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00113 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00114

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00114.md), and an image reference ![sample](./assets/image-00114.png). The prose also carries inline math like $score_114 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00114
- [x] Reviewed checkpoint for section 00114
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00114 | repeated table row |
| words | 64 | stable measurement text |

$$
render_114 = parse_114 + highlight_114 + preview_114
$$

```swift
struct PerfSection00114 {
    let title = "Section 00114"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00114","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00114 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00115

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00115.md), and an image reference ![sample](./assets/image-00115.png). The prose also carries inline math like $score_115 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00115
- [x] Reviewed checkpoint for section 00115
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00115 | repeated table row |
| words | 64 | stable measurement text |

$$
render_115 = parse_115 + highlight_115 + preview_115
$$

```swift
struct PerfSection00115 {
    let title = "Section 00115"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00115","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00115 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00116

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00116.md), and an image reference ![sample](./assets/image-00116.png). The prose also carries inline math like $score_116 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00116
- [x] Reviewed checkpoint for section 00116
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00116 | repeated table row |
| words | 64 | stable measurement text |

$$
render_116 = parse_116 + highlight_116 + preview_116
$$

```swift
struct PerfSection00116 {
    let title = "Section 00116"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00116","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00116 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00117

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00117.md), and an image reference ![sample](./assets/image-00117.png). The prose also carries inline math like $score_117 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00117
- [x] Reviewed checkpoint for section 00117
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00117 | repeated table row |
| words | 64 | stable measurement text |

$$
render_117 = parse_117 + highlight_117 + preview_117
$$

```swift
struct PerfSection00117 {
    let title = "Section 00117"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00117","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00117 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00118

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00118.md), and an image reference ![sample](./assets/image-00118.png). The prose also carries inline math like $score_118 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00118
- [x] Reviewed checkpoint for section 00118
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00118 | repeated table row |
| words | 64 | stable measurement text |

$$
render_118 = parse_118 + highlight_118 + preview_118
$$

```swift
struct PerfSection00118 {
    let title = "Section 00118"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00118","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00118 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00119

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00119.md), and an image reference ![sample](./assets/image-00119.png). The prose also carries inline math like $score_119 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00119
- [x] Reviewed checkpoint for section 00119
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00119 | repeated table row |
| words | 64 | stable measurement text |

$$
render_119 = parse_119 + highlight_119 + preview_119
$$

```swift
struct PerfSection00119 {
    let title = "Section 00119"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00119","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00119 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00120

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00120.md), and an image reference ![sample](./assets/image-00120.png). The prose also carries inline math like $score_120 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00120
- [x] Reviewed checkpoint for section 00120
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00120 | repeated table row |
| words | 64 | stable measurement text |

$$
render_120 = parse_120 + highlight_120 + preview_120
$$

```swift
struct PerfSection00120 {
    let title = "Section 00120"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00120","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00120 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00121

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00121.md), and an image reference ![sample](./assets/image-00121.png). The prose also carries inline math like $score_121 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00121
- [x] Reviewed checkpoint for section 00121
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00121 | repeated table row |
| words | 64 | stable measurement text |

$$
render_121 = parse_121 + highlight_121 + preview_121
$$

```swift
struct PerfSection00121 {
    let title = "Section 00121"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00121","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00121 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00122

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00122.md), and an image reference ![sample](./assets/image-00122.png). The prose also carries inline math like $score_122 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00122
- [x] Reviewed checkpoint for section 00122
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00122 | repeated table row |
| words | 64 | stable measurement text |

$$
render_122 = parse_122 + highlight_122 + preview_122
$$

```swift
struct PerfSection00122 {
    let title = "Section 00122"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00122","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00122 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00123

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00123.md), and an image reference ![sample](./assets/image-00123.png). The prose also carries inline math like $score_123 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00123
- [x] Reviewed checkpoint for section 00123
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00123 | repeated table row |
| words | 64 | stable measurement text |

$$
render_123 = parse_123 + highlight_123 + preview_123
$$

```swift
struct PerfSection00123 {
    let title = "Section 00123"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00123","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00123 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00124

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00124.md), and an image reference ![sample](./assets/image-00124.png). The prose also carries inline math like $score_124 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00124
- [x] Reviewed checkpoint for section 00124
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00124 | repeated table row |
| words | 64 | stable measurement text |

$$
render_124 = parse_124 + highlight_124 + preview_124
$$

```swift
struct PerfSection00124 {
    let title = "Section 00124"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00124","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00124 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00125

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00125.md), and an image reference ![sample](./assets/image-00125.png). The prose also carries inline math like $score_125 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00125
- [x] Reviewed checkpoint for section 00125
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00125 | repeated table row |
| words | 64 | stable measurement text |

$$
render_125 = parse_125 + highlight_125 + preview_125
$$

```swift
struct PerfSection00125 {
    let title = "Section 00125"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00125","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00125 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00126

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00126.md), and an image reference ![sample](./assets/image-00126.png). The prose also carries inline math like $score_126 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00126
- [x] Reviewed checkpoint for section 00126
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00126 | repeated table row |
| words | 64 | stable measurement text |

$$
render_126 = parse_126 + highlight_126 + preview_126
$$

```swift
struct PerfSection00126 {
    let title = "Section 00126"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00126","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00126 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00127

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00127.md), and an image reference ![sample](./assets/image-00127.png). The prose also carries inline math like $score_127 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00127
- [x] Reviewed checkpoint for section 00127
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00127 | repeated table row |
| words | 64 | stable measurement text |

$$
render_127 = parse_127 + highlight_127 + preview_127
$$

```swift
struct PerfSection00127 {
    let title = "Section 00127"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00127","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00127 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00128

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00128.md), and an image reference ![sample](./assets/image-00128.png). The prose also carries inline math like $score_128 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00128
- [x] Reviewed checkpoint for section 00128
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00128 | repeated table row |
| words | 64 | stable measurement text |

$$
render_128 = parse_128 + highlight_128 + preview_128
$$

```swift
struct PerfSection00128 {
    let title = "Section 00128"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00128","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00128 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00129

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00129.md), and an image reference ![sample](./assets/image-00129.png). The prose also carries inline math like $score_129 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00129
- [x] Reviewed checkpoint for section 00129
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00129 | repeated table row |
| words | 64 | stable measurement text |

$$
render_129 = parse_129 + highlight_129 + preview_129
$$

```swift
struct PerfSection00129 {
    let title = "Section 00129"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00129","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00129 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00130

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00130.md), and an image reference ![sample](./assets/image-00130.png). The prose also carries inline math like $score_130 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00130
- [x] Reviewed checkpoint for section 00130
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00130 | repeated table row |
| words | 64 | stable measurement text |

$$
render_130 = parse_130 + highlight_130 + preview_130
$$

```swift
struct PerfSection00130 {
    let title = "Section 00130"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00130","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00130 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00131

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00131.md), and an image reference ![sample](./assets/image-00131.png). The prose also carries inline math like $score_131 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00131
- [x] Reviewed checkpoint for section 00131
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00131 | repeated table row |
| words | 64 | stable measurement text |

$$
render_131 = parse_131 + highlight_131 + preview_131
$$

```swift
struct PerfSection00131 {
    let title = "Section 00131"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00131","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00131 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00132

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00132.md), and an image reference ![sample](./assets/image-00132.png). The prose also carries inline math like $score_132 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00132
- [x] Reviewed checkpoint for section 00132
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00132 | repeated table row |
| words | 64 | stable measurement text |

$$
render_132 = parse_132 + highlight_132 + preview_132
$$

```swift
struct PerfSection00132 {
    let title = "Section 00132"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00132","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00132 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00133

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00133.md), and an image reference ![sample](./assets/image-00133.png). The prose also carries inline math like $score_133 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00133
- [x] Reviewed checkpoint for section 00133
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00133 | repeated table row |
| words | 64 | stable measurement text |

$$
render_133 = parse_133 + highlight_133 + preview_133
$$

```swift
struct PerfSection00133 {
    let title = "Section 00133"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00133","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00133 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00134

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00134.md), and an image reference ![sample](./assets/image-00134.png). The prose also carries inline math like $score_134 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00134
- [x] Reviewed checkpoint for section 00134
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00134 | repeated table row |
| words | 64 | stable measurement text |

$$
render_134 = parse_134 + highlight_134 + preview_134
$$

```swift
struct PerfSection00134 {
    let title = "Section 00134"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00134","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00134 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00135

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00135.md), and an image reference ![sample](./assets/image-00135.png). The prose also carries inline math like $score_135 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00135
- [x] Reviewed checkpoint for section 00135
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00135 | repeated table row |
| words | 64 | stable measurement text |

$$
render_135 = parse_135 + highlight_135 + preview_135
$$

```swift
struct PerfSection00135 {
    let title = "Section 00135"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00135","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00135 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00136

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00136.md), and an image reference ![sample](./assets/image-00136.png). The prose also carries inline math like $score_136 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00136
- [x] Reviewed checkpoint for section 00136
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00136 | repeated table row |
| words | 64 | stable measurement text |

$$
render_136 = parse_136 + highlight_136 + preview_136
$$

```swift
struct PerfSection00136 {
    let title = "Section 00136"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00136","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00136 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00137

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00137.md), and an image reference ![sample](./assets/image-00137.png). The prose also carries inline math like $score_137 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00137
- [x] Reviewed checkpoint for section 00137
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00137 | repeated table row |
| words | 64 | stable measurement text |

$$
render_137 = parse_137 + highlight_137 + preview_137
$$

```swift
struct PerfSection00137 {
    let title = "Section 00137"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00137","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00137 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00138

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00138.md), and an image reference ![sample](./assets/image-00138.png). The prose also carries inline math like $score_138 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00138
- [x] Reviewed checkpoint for section 00138
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00138 | repeated table row |
| words | 64 | stable measurement text |

$$
render_138 = parse_138 + highlight_138 + preview_138
$$

```swift
struct PerfSection00138 {
    let title = "Section 00138"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00138","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00138 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00139

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00139.md), and an image reference ![sample](./assets/image-00139.png). The prose also carries inline math like $score_139 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00139
- [x] Reviewed checkpoint for section 00139
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00139 | repeated table row |
| words | 64 | stable measurement text |

$$
render_139 = parse_139 + highlight_139 + preview_139
$$

```swift
struct PerfSection00139 {
    let title = "Section 00139"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00139","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00139 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00140

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00140.md), and an image reference ![sample](./assets/image-00140.png). The prose also carries inline math like $score_140 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00140
- [x] Reviewed checkpoint for section 00140
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00140 | repeated table row |
| words | 64 | stable measurement text |

$$
render_140 = parse_140 + highlight_140 + preview_140
$$

```swift
struct PerfSection00140 {
    let title = "Section 00140"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00140","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00140 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00141

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00141.md), and an image reference ![sample](./assets/image-00141.png). The prose also carries inline math like $score_141 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00141
- [x] Reviewed checkpoint for section 00141
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00141 | repeated table row |
| words | 64 | stable measurement text |

$$
render_141 = parse_141 + highlight_141 + preview_141
$$

```swift
struct PerfSection00141 {
    let title = "Section 00141"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00141","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00141 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00142

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00142.md), and an image reference ![sample](./assets/image-00142.png). The prose also carries inline math like $score_142 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00142
- [x] Reviewed checkpoint for section 00142
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00142 | repeated table row |
| words | 64 | stable measurement text |

$$
render_142 = parse_142 + highlight_142 + preview_142
$$

```swift
struct PerfSection00142 {
    let title = "Section 00142"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00142","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00142 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00143

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00143.md), and an image reference ![sample](./assets/image-00143.png). The prose also carries inline math like $score_143 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00143
- [x] Reviewed checkpoint for section 00143
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00143 | repeated table row |
| words | 64 | stable measurement text |

$$
render_143 = parse_143 + highlight_143 + preview_143
$$

```swift
struct PerfSection00143 {
    let title = "Section 00143"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00143","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00143 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00144

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00144.md), and an image reference ![sample](./assets/image-00144.png). The prose also carries inline math like $score_144 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00144
- [x] Reviewed checkpoint for section 00144
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00144 | repeated table row |
| words | 64 | stable measurement text |

$$
render_144 = parse_144 + highlight_144 + preview_144
$$

```swift
struct PerfSection00144 {
    let title = "Section 00144"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00144","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00144 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00145

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00145.md), and an image reference ![sample](./assets/image-00145.png). The prose also carries inline math like $score_145 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00145
- [x] Reviewed checkpoint for section 00145
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00145 | repeated table row |
| words | 64 | stable measurement text |

$$
render_145 = parse_145 + highlight_145 + preview_145
$$

```swift
struct PerfSection00145 {
    let title = "Section 00145"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00145","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00145 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00146

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00146.md), and an image reference ![sample](./assets/image-00146.png). The prose also carries inline math like $score_146 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00146
- [x] Reviewed checkpoint for section 00146
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00146 | repeated table row |
| words | 64 | stable measurement text |

$$
render_146 = parse_146 + highlight_146 + preview_146
$$

```swift
struct PerfSection00146 {
    let title = "Section 00146"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00146","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00146 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00147

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00147.md), and an image reference ![sample](./assets/image-00147.png). The prose also carries inline math like $score_147 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00147
- [x] Reviewed checkpoint for section 00147
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00147 | repeated table row |
| words | 64 | stable measurement text |

$$
render_147 = parse_147 + highlight_147 + preview_147
$$

```swift
struct PerfSection00147 {
    let title = "Section 00147"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00147","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00147 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00148

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00148.md), and an image reference ![sample](./assets/image-00148.png). The prose also carries inline math like $score_148 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00148
- [x] Reviewed checkpoint for section 00148
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00148 | repeated table row |
| words | 64 | stable measurement text |

$$
render_148 = parse_148 + highlight_148 + preview_148
$$

```swift
struct PerfSection00148 {
    let title = "Section 00148"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00148","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00148 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00149

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00149.md), and an image reference ![sample](./assets/image-00149.png). The prose also carries inline math like $score_149 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00149
- [x] Reviewed checkpoint for section 00149
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00149 | repeated table row |
| words | 64 | stable measurement text |

$$
render_149 = parse_149 + highlight_149 + preview_149
$$

```swift
struct PerfSection00149 {
    let title = "Section 00149"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00149","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00149 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00150

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00150.md), and an image reference ![sample](./assets/image-00150.png). The prose also carries inline math like $score_150 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00150
- [x] Reviewed checkpoint for section 00150
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00150 | repeated table row |
| words | 64 | stable measurement text |

$$
render_150 = parse_150 + highlight_150 + preview_150
$$

```swift
struct PerfSection00150 {
    let title = "Section 00150"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00150","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00150 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00151

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00151.md), and an image reference ![sample](./assets/image-00151.png). The prose also carries inline math like $score_151 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00151
- [x] Reviewed checkpoint for section 00151
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00151 | repeated table row |
| words | 64 | stable measurement text |

$$
render_151 = parse_151 + highlight_151 + preview_151
$$

```swift
struct PerfSection00151 {
    let title = "Section 00151"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00151","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00151 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00152

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00152.md), and an image reference ![sample](./assets/image-00152.png). The prose also carries inline math like $score_152 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00152
- [x] Reviewed checkpoint for section 00152
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00152 | repeated table row |
| words | 64 | stable measurement text |

$$
render_152 = parse_152 + highlight_152 + preview_152
$$

```swift
struct PerfSection00152 {
    let title = "Section 00152"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00152","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00152 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00153

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00153.md), and an image reference ![sample](./assets/image-00153.png). The prose also carries inline math like $score_153 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00153
- [x] Reviewed checkpoint for section 00153
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00153 | repeated table row |
| words | 64 | stable measurement text |

$$
render_153 = parse_153 + highlight_153 + preview_153
$$

```swift
struct PerfSection00153 {
    let title = "Section 00153"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00153","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00153 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00154

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00154.md), and an image reference ![sample](./assets/image-00154.png). The prose also carries inline math like $score_154 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00154
- [x] Reviewed checkpoint for section 00154
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00154 | repeated table row |
| words | 64 | stable measurement text |

$$
render_154 = parse_154 + highlight_154 + preview_154
$$

```swift
struct PerfSection00154 {
    let title = "Section 00154"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00154","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00154 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00155

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00155.md), and an image reference ![sample](./assets/image-00155.png). The prose also carries inline math like $score_155 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00155
- [x] Reviewed checkpoint for section 00155
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00155 | repeated table row |
| words | 64 | stable measurement text |

$$
render_155 = parse_155 + highlight_155 + preview_155
$$

```swift
struct PerfSection00155 {
    let title = "Section 00155"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00155","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00155 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00156

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00156.md), and an image reference ![sample](./assets/image-00156.png). The prose also carries inline math like $score_156 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00156
- [x] Reviewed checkpoint for section 00156
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00156 | repeated table row |
| words | 64 | stable measurement text |

$$
render_156 = parse_156 + highlight_156 + preview_156
$$

```swift
struct PerfSection00156 {
    let title = "Section 00156"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00156","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00156 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00157

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00157.md), and an image reference ![sample](./assets/image-00157.png). The prose also carries inline math like $score_157 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00157
- [x] Reviewed checkpoint for section 00157
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00157 | repeated table row |
| words | 64 | stable measurement text |

$$
render_157 = parse_157 + highlight_157 + preview_157
$$

```swift
struct PerfSection00157 {
    let title = "Section 00157"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00157","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00157 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00158

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00158.md), and an image reference ![sample](./assets/image-00158.png). The prose also carries inline math like $score_158 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00158
- [x] Reviewed checkpoint for section 00158
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00158 | repeated table row |
| words | 64 | stable measurement text |

$$
render_158 = parse_158 + highlight_158 + preview_158
$$

```swift
struct PerfSection00158 {
    let title = "Section 00158"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00158","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00158 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00159

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00159.md), and an image reference ![sample](./assets/image-00159.png). The prose also carries inline math like $score_159 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00159
- [x] Reviewed checkpoint for section 00159
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00159 | repeated table row |
| words | 64 | stable measurement text |

$$
render_159 = parse_159 + highlight_159 + preview_159
$$

```swift
struct PerfSection00159 {
    let title = "Section 00159"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00159","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00159 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00160

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00160.md), and an image reference ![sample](./assets/image-00160.png). The prose also carries inline math like $score_160 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00160
- [x] Reviewed checkpoint for section 00160
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00160 | repeated table row |
| words | 64 | stable measurement text |

$$
render_160 = parse_160 + highlight_160 + preview_160
$$

```swift
struct PerfSection00160 {
    let title = "Section 00160"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00160","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00160 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00161

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00161.md), and an image reference ![sample](./assets/image-00161.png). The prose also carries inline math like $score_161 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00161
- [x] Reviewed checkpoint for section 00161
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00161 | repeated table row |
| words | 64 | stable measurement text |

$$
render_161 = parse_161 + highlight_161 + preview_161
$$

```swift
struct PerfSection00161 {
    let title = "Section 00161"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00161","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00161 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00162

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00162.md), and an image reference ![sample](./assets/image-00162.png). The prose also carries inline math like $score_162 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00162
- [x] Reviewed checkpoint for section 00162
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00162 | repeated table row |
| words | 64 | stable measurement text |

$$
render_162 = parse_162 + highlight_162 + preview_162
$$

```swift
struct PerfSection00162 {
    let title = "Section 00162"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00162","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00162 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00163

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00163.md), and an image reference ![sample](./assets/image-00163.png). The prose also carries inline math like $score_163 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00163
- [x] Reviewed checkpoint for section 00163
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00163 | repeated table row |
| words | 64 | stable measurement text |

$$
render_163 = parse_163 + highlight_163 + preview_163
$$

```swift
struct PerfSection00163 {
    let title = "Section 00163"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00163","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00163 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00164

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00164.md), and an image reference ![sample](./assets/image-00164.png). The prose also carries inline math like $score_164 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00164
- [x] Reviewed checkpoint for section 00164
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00164 | repeated table row |
| words | 64 | stable measurement text |

$$
render_164 = parse_164 + highlight_164 + preview_164
$$

```swift
struct PerfSection00164 {
    let title = "Section 00164"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00164","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00164 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00165

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00165.md), and an image reference ![sample](./assets/image-00165.png). The prose also carries inline math like $score_165 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00165
- [x] Reviewed checkpoint for section 00165
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00165 | repeated table row |
| words | 64 | stable measurement text |

$$
render_165 = parse_165 + highlight_165 + preview_165
$$

```swift
struct PerfSection00165 {
    let title = "Section 00165"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00165","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00165 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00166

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00166.md), and an image reference ![sample](./assets/image-00166.png). The prose also carries inline math like $score_166 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00166
- [x] Reviewed checkpoint for section 00166
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00166 | repeated table row |
| words | 64 | stable measurement text |

$$
render_166 = parse_166 + highlight_166 + preview_166
$$

```swift
struct PerfSection00166 {
    let title = "Section 00166"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00166","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00166 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00167

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00167.md), and an image reference ![sample](./assets/image-00167.png). The prose also carries inline math like $score_167 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00167
- [x] Reviewed checkpoint for section 00167
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00167 | repeated table row |
| words | 64 | stable measurement text |

$$
render_167 = parse_167 + highlight_167 + preview_167
$$

```swift
struct PerfSection00167 {
    let title = "Section 00167"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00167","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00167 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00168

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00168.md), and an image reference ![sample](./assets/image-00168.png). The prose also carries inline math like $score_168 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00168
- [x] Reviewed checkpoint for section 00168
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00168 | repeated table row |
| words | 64 | stable measurement text |

$$
render_168 = parse_168 + highlight_168 + preview_168
$$

```swift
struct PerfSection00168 {
    let title = "Section 00168"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00168","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00168 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00169

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00169.md), and an image reference ![sample](./assets/image-00169.png). The prose also carries inline math like $score_169 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00169
- [x] Reviewed checkpoint for section 00169
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00169 | repeated table row |
| words | 64 | stable measurement text |

$$
render_169 = parse_169 + highlight_169 + preview_169
$$

```swift
struct PerfSection00169 {
    let title = "Section 00169"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00169","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00169 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00170

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00170.md), and an image reference ![sample](./assets/image-00170.png). The prose also carries inline math like $score_170 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00170
- [x] Reviewed checkpoint for section 00170
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00170 | repeated table row |
| words | 64 | stable measurement text |

$$
render_170 = parse_170 + highlight_170 + preview_170
$$

```swift
struct PerfSection00170 {
    let title = "Section 00170"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00170","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00170 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00171

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00171.md), and an image reference ![sample](./assets/image-00171.png). The prose also carries inline math like $score_171 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00171
- [x] Reviewed checkpoint for section 00171
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00171 | repeated table row |
| words | 64 | stable measurement text |

$$
render_171 = parse_171 + highlight_171 + preview_171
$$

```swift
struct PerfSection00171 {
    let title = "Section 00171"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00171","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00171 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00172

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00172.md), and an image reference ![sample](./assets/image-00172.png). The prose also carries inline math like $score_172 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00172
- [x] Reviewed checkpoint for section 00172
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00172 | repeated table row |
| words | 64 | stable measurement text |

$$
render_172 = parse_172 + highlight_172 + preview_172
$$

```swift
struct PerfSection00172 {
    let title = "Section 00172"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00172","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00172 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00173

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00173.md), and an image reference ![sample](./assets/image-00173.png). The prose also carries inline math like $score_173 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00173
- [x] Reviewed checkpoint for section 00173
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00173 | repeated table row |
| words | 64 | stable measurement text |

$$
render_173 = parse_173 + highlight_173 + preview_173
$$

```swift
struct PerfSection00173 {
    let title = "Section 00173"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00173","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00173 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00174

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00174.md), and an image reference ![sample](./assets/image-00174.png). The prose also carries inline math like $score_174 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00174
- [x] Reviewed checkpoint for section 00174
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00174 | repeated table row |
| words | 64 | stable measurement text |

$$
render_174 = parse_174 + highlight_174 + preview_174
$$

```swift
struct PerfSection00174 {
    let title = "Section 00174"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00174","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00174 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00175

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00175.md), and an image reference ![sample](./assets/image-00175.png). The prose also carries inline math like $score_175 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00175
- [x] Reviewed checkpoint for section 00175
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00175 | repeated table row |
| words | 64 | stable measurement text |

$$
render_175 = parse_175 + highlight_175 + preview_175
$$

```swift
struct PerfSection00175 {
    let title = "Section 00175"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00175","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00175 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00176

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00176.md), and an image reference ![sample](./assets/image-00176.png). The prose also carries inline math like $score_176 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00176
- [x] Reviewed checkpoint for section 00176
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00176 | repeated table row |
| words | 64 | stable measurement text |

$$
render_176 = parse_176 + highlight_176 + preview_176
$$

```swift
struct PerfSection00176 {
    let title = "Section 00176"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00176","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00176 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00177

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00177.md), and an image reference ![sample](./assets/image-00177.png). The prose also carries inline math like $score_177 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00177
- [x] Reviewed checkpoint for section 00177
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00177 | repeated table row |
| words | 64 | stable measurement text |

$$
render_177 = parse_177 + highlight_177 + preview_177
$$

```swift
struct PerfSection00177 {
    let title = "Section 00177"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00177","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00177 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00178

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00178.md), and an image reference ![sample](./assets/image-00178.png). The prose also carries inline math like $score_178 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00178
- [x] Reviewed checkpoint for section 00178
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00178 | repeated table row |
| words | 64 | stable measurement text |

$$
render_178 = parse_178 + highlight_178 + preview_178
$$

```swift
struct PerfSection00178 {
    let title = "Section 00178"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00178","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00178 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00179

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00179.md), and an image reference ![sample](./assets/image-00179.png). The prose also carries inline math like $score_179 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00179
- [x] Reviewed checkpoint for section 00179
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00179 | repeated table row |
| words | 64 | stable measurement text |

$$
render_179 = parse_179 + highlight_179 + preview_179
$$

```swift
struct PerfSection00179 {
    let title = "Section 00179"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00179","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00179 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00180

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00180.md), and an image reference ![sample](./assets/image-00180.png). The prose also carries inline math like $score_180 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00180
- [x] Reviewed checkpoint for section 00180
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00180 | repeated table row |
| words | 64 | stable measurement text |

$$
render_180 = parse_180 + highlight_180 + preview_180
$$

```swift
struct PerfSection00180 {
    let title = "Section 00180"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00180","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00180 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00181

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00181.md), and an image reference ![sample](./assets/image-00181.png). The prose also carries inline math like $score_181 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00181
- [x] Reviewed checkpoint for section 00181
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00181 | repeated table row |
| words | 64 | stable measurement text |

$$
render_181 = parse_181 + highlight_181 + preview_181
$$

```swift
struct PerfSection00181 {
    let title = "Section 00181"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00181","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00181 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00182

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00182.md), and an image reference ![sample](./assets/image-00182.png). The prose also carries inline math like $score_182 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00182
- [x] Reviewed checkpoint for section 00182
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00182 | repeated table row |
| words | 64 | stable measurement text |

$$
render_182 = parse_182 + highlight_182 + preview_182
$$

```swift
struct PerfSection00182 {
    let title = "Section 00182"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00182","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00182 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00183

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00183.md), and an image reference ![sample](./assets/image-00183.png). The prose also carries inline math like $score_183 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00183
- [x] Reviewed checkpoint for section 00183
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00183 | repeated table row |
| words | 64 | stable measurement text |

$$
render_183 = parse_183 + highlight_183 + preview_183
$$

```swift
struct PerfSection00183 {
    let title = "Section 00183"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00183","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00183 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00184

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00184.md), and an image reference ![sample](./assets/image-00184.png). The prose also carries inline math like $score_184 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00184
- [x] Reviewed checkpoint for section 00184
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00184 | repeated table row |
| words | 64 | stable measurement text |

$$
render_184 = parse_184 + highlight_184 + preview_184
$$

```swift
struct PerfSection00184 {
    let title = "Section 00184"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00184","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00184 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00185

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00185.md), and an image reference ![sample](./assets/image-00185.png). The prose also carries inline math like $score_185 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00185
- [x] Reviewed checkpoint for section 00185
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00185 | repeated table row |
| words | 64 | stable measurement text |

$$
render_185 = parse_185 + highlight_185 + preview_185
$$

```swift
struct PerfSection00185 {
    let title = "Section 00185"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00185","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00185 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00186

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00186.md), and an image reference ![sample](./assets/image-00186.png). The prose also carries inline math like $score_186 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00186
- [x] Reviewed checkpoint for section 00186
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00186 | repeated table row |
| words | 64 | stable measurement text |

$$
render_186 = parse_186 + highlight_186 + preview_186
$$

```swift
struct PerfSection00186 {
    let title = "Section 00186"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00186","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00186 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00187

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00187.md), and an image reference ![sample](./assets/image-00187.png). The prose also carries inline math like $score_187 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00187
- [x] Reviewed checkpoint for section 00187
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00187 | repeated table row |
| words | 64 | stable measurement text |

$$
render_187 = parse_187 + highlight_187 + preview_187
$$

```swift
struct PerfSection00187 {
    let title = "Section 00187"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00187","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00187 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00188

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00188.md), and an image reference ![sample](./assets/image-00188.png). The prose also carries inline math like $score_188 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00188
- [x] Reviewed checkpoint for section 00188
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00188 | repeated table row |
| words | 64 | stable measurement text |

$$
render_188 = parse_188 + highlight_188 + preview_188
$$

```swift
struct PerfSection00188 {
    let title = "Section 00188"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00188","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00188 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00189

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00189.md), and an image reference ![sample](./assets/image-00189.png). The prose also carries inline math like $score_189 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00189
- [x] Reviewed checkpoint for section 00189
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00189 | repeated table row |
| words | 64 | stable measurement text |

$$
render_189 = parse_189 + highlight_189 + preview_189
$$

```swift
struct PerfSection00189 {
    let title = "Section 00189"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00189","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00189 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00190

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00190.md), and an image reference ![sample](./assets/image-00190.png). The prose also carries inline math like $score_190 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00190
- [x] Reviewed checkpoint for section 00190
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00190 | repeated table row |
| words | 64 | stable measurement text |

$$
render_190 = parse_190 + highlight_190 + preview_190
$$

```swift
struct PerfSection00190 {
    let title = "Section 00190"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00190","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00190 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00191

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00191.md), and an image reference ![sample](./assets/image-00191.png). The prose also carries inline math like $score_191 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00191
- [x] Reviewed checkpoint for section 00191
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00191 | repeated table row |
| words | 64 | stable measurement text |

$$
render_191 = parse_191 + highlight_191 + preview_191
$$

```swift
struct PerfSection00191 {
    let title = "Section 00191"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00191","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00191 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00192

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00192.md), and an image reference ![sample](./assets/image-00192.png). The prose also carries inline math like $score_192 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00192
- [x] Reviewed checkpoint for section 00192
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00192 | repeated table row |
| words | 64 | stable measurement text |

$$
render_192 = parse_192 + highlight_192 + preview_192
$$

```swift
struct PerfSection00192 {
    let title = "Section 00192"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00192","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00192 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00193

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00193.md), and an image reference ![sample](./assets/image-00193.png). The prose also carries inline math like $score_193 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00193
- [x] Reviewed checkpoint for section 00193
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00193 | repeated table row |
| words | 64 | stable measurement text |

$$
render_193 = parse_193 + highlight_193 + preview_193
$$

```swift
struct PerfSection00193 {
    let title = "Section 00193"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00193","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00193 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00194

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00194.md), and an image reference ![sample](./assets/image-00194.png). The prose also carries inline math like $score_194 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00194
- [x] Reviewed checkpoint for section 00194
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00194 | repeated table row |
| words | 64 | stable measurement text |

$$
render_194 = parse_194 + highlight_194 + preview_194
$$

```swift
struct PerfSection00194 {
    let title = "Section 00194"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00194","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00194 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00195

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00195.md), and an image reference ![sample](./assets/image-00195.png). The prose also carries inline math like $score_195 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00195
- [x] Reviewed checkpoint for section 00195
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00195 | repeated table row |
| words | 64 | stable measurement text |

$$
render_195 = parse_195 + highlight_195 + preview_195
$$

```swift
struct PerfSection00195 {
    let title = "Section 00195"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00195","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00195 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00196

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00196.md), and an image reference ![sample](./assets/image-00196.png). The prose also carries inline math like $score_196 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00196
- [x] Reviewed checkpoint for section 00196
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00196 | repeated table row |
| words | 64 | stable measurement text |

$$
render_196 = parse_196 + highlight_196 + preview_196
$$

```swift
struct PerfSection00196 {
    let title = "Section 00196"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00196","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00196 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00197

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00197.md), and an image reference ![sample](./assets/image-00197.png). The prose also carries inline math like $score_197 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00197
- [x] Reviewed checkpoint for section 00197
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00197 | repeated table row |
| words | 64 | stable measurement text |

$$
render_197 = parse_197 + highlight_197 + preview_197
$$

```swift
struct PerfSection00197 {
    let title = "Section 00197"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00197","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00197 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00198

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00198.md), and an image reference ![sample](./assets/image-00198.png). The prose also carries inline math like $score_198 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00198
- [x] Reviewed checkpoint for section 00198
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00198 | repeated table row |
| words | 64 | stable measurement text |

$$
render_198 = parse_198 + highlight_198 + preview_198
$$

```swift
struct PerfSection00198 {
    let title = "Section 00198"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00198","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00198 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00199

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00199.md), and an image reference ![sample](./assets/image-00199.png). The prose also carries inline math like $score_199 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00199
- [x] Reviewed checkpoint for section 00199
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00199 | repeated table row |
| words | 64 | stable measurement text |

$$
render_199 = parse_199 + highlight_199 + preview_199
$$

```swift
struct PerfSection00199 {
    let title = "Section 00199"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00199","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00199 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00200

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00200.md), and an image reference ![sample](./assets/image-00200.png). The prose also carries inline math like $score_200 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00200
- [x] Reviewed checkpoint for section 00200
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00200 | repeated table row |
| words | 64 | stable measurement text |

$$
render_200 = parse_200 + highlight_200 + preview_200
$$

```swift
struct PerfSection00200 {
    let title = "Section 00200"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00200","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00200 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00201

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00201.md), and an image reference ![sample](./assets/image-00201.png). The prose also carries inline math like $score_201 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00201
- [x] Reviewed checkpoint for section 00201
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00201 | repeated table row |
| words | 64 | stable measurement text |

$$
render_201 = parse_201 + highlight_201 + preview_201
$$

```swift
struct PerfSection00201 {
    let title = "Section 00201"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00201","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00201 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00202

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00202.md), and an image reference ![sample](./assets/image-00202.png). The prose also carries inline math like $score_202 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00202
- [x] Reviewed checkpoint for section 00202
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00202 | repeated table row |
| words | 64 | stable measurement text |

$$
render_202 = parse_202 + highlight_202 + preview_202
$$

```swift
struct PerfSection00202 {
    let title = "Section 00202"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00202","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00202 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00203

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00203.md), and an image reference ![sample](./assets/image-00203.png). The prose also carries inline math like $score_203 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00203
- [x] Reviewed checkpoint for section 00203
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00203 | repeated table row |
| words | 64 | stable measurement text |

$$
render_203 = parse_203 + highlight_203 + preview_203
$$

```swift
struct PerfSection00203 {
    let title = "Section 00203"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00203","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00203 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00204

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00204.md), and an image reference ![sample](./assets/image-00204.png). The prose also carries inline math like $score_204 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00204
- [x] Reviewed checkpoint for section 00204
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00204 | repeated table row |
| words | 64 | stable measurement text |

$$
render_204 = parse_204 + highlight_204 + preview_204
$$

```swift
struct PerfSection00204 {
    let title = "Section 00204"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00204","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00204 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00205

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00205.md), and an image reference ![sample](./assets/image-00205.png). The prose also carries inline math like $score_205 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00205
- [x] Reviewed checkpoint for section 00205
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00205 | repeated table row |
| words | 64 | stable measurement text |

$$
render_205 = parse_205 + highlight_205 + preview_205
$$

```swift
struct PerfSection00205 {
    let title = "Section 00205"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00205","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00205 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00206

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00206.md), and an image reference ![sample](./assets/image-00206.png). The prose also carries inline math like $score_206 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00206
- [x] Reviewed checkpoint for section 00206
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00206 | repeated table row |
| words | 64 | stable measurement text |

$$
render_206 = parse_206 + highlight_206 + preview_206
$$

```swift
struct PerfSection00206 {
    let title = "Section 00206"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00206","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00206 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00207

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00207.md), and an image reference ![sample](./assets/image-00207.png). The prose also carries inline math like $score_207 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00207
- [x] Reviewed checkpoint for section 00207
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00207 | repeated table row |
| words | 64 | stable measurement text |

$$
render_207 = parse_207 + highlight_207 + preview_207
$$

```swift
struct PerfSection00207 {
    let title = "Section 00207"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00207","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00207 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00208

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00208.md), and an image reference ![sample](./assets/image-00208.png). The prose also carries inline math like $score_208 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00208
- [x] Reviewed checkpoint for section 00208
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00208 | repeated table row |
| words | 64 | stable measurement text |

$$
render_208 = parse_208 + highlight_208 + preview_208
$$

```swift
struct PerfSection00208 {
    let title = "Section 00208"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00208","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00208 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00209

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00209.md), and an image reference ![sample](./assets/image-00209.png). The prose also carries inline math like $score_209 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00209
- [x] Reviewed checkpoint for section 00209
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00209 | repeated table row |
| words | 64 | stable measurement text |

$$
render_209 = parse_209 + highlight_209 + preview_209
$$

```swift
struct PerfSection00209 {
    let title = "Section 00209"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00209","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00209 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00210

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00210.md), and an image reference ![sample](./assets/image-00210.png). The prose also carries inline math like $score_210 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00210
- [x] Reviewed checkpoint for section 00210
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00210 | repeated table row |
| words | 64 | stable measurement text |

$$
render_210 = parse_210 + highlight_210 + preview_210
$$

```swift
struct PerfSection00210 {
    let title = "Section 00210"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00210","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00210 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00211

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00211.md), and an image reference ![sample](./assets/image-00211.png). The prose also carries inline math like $score_211 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00211
- [x] Reviewed checkpoint for section 00211
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00211 | repeated table row |
| words | 64 | stable measurement text |

$$
render_211 = parse_211 + highlight_211 + preview_211
$$

```swift
struct PerfSection00211 {
    let title = "Section 00211"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00211","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00211 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00212

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00212.md), and an image reference ![sample](./assets/image-00212.png). The prose also carries inline math like $score_212 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00212
- [x] Reviewed checkpoint for section 00212
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00212 | repeated table row |
| words | 64 | stable measurement text |

$$
render_212 = parse_212 + highlight_212 + preview_212
$$

```swift
struct PerfSection00212 {
    let title = "Section 00212"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00212","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00212 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00213

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00213.md), and an image reference ![sample](./assets/image-00213.png). The prose also carries inline math like $score_213 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00213
- [x] Reviewed checkpoint for section 00213
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00213 | repeated table row |
| words | 64 | stable measurement text |

$$
render_213 = parse_213 + highlight_213 + preview_213
$$

```swift
struct PerfSection00213 {
    let title = "Section 00213"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00213","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00213 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00214

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00214.md), and an image reference ![sample](./assets/image-00214.png). The prose also carries inline math like $score_214 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00214
- [x] Reviewed checkpoint for section 00214
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00214 | repeated table row |
| words | 64 | stable measurement text |

$$
render_214 = parse_214 + highlight_214 + preview_214
$$

```swift
struct PerfSection00214 {
    let title = "Section 00214"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00214","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00214 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00215

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00215.md), and an image reference ![sample](./assets/image-00215.png). The prose also carries inline math like $score_215 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00215
- [x] Reviewed checkpoint for section 00215
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00215 | repeated table row |
| words | 64 | stable measurement text |

$$
render_215 = parse_215 + highlight_215 + preview_215
$$

```swift
struct PerfSection00215 {
    let title = "Section 00215"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00215","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00215 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00216

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00216.md), and an image reference ![sample](./assets/image-00216.png). The prose also carries inline math like $score_216 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00216
- [x] Reviewed checkpoint for section 00216
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00216 | repeated table row |
| words | 64 | stable measurement text |

$$
render_216 = parse_216 + highlight_216 + preview_216
$$

```swift
struct PerfSection00216 {
    let title = "Section 00216"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00216","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00216 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00217

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00217.md), and an image reference ![sample](./assets/image-00217.png). The prose also carries inline math like $score_217 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00217
- [x] Reviewed checkpoint for section 00217
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00217 | repeated table row |
| words | 64 | stable measurement text |

$$
render_217 = parse_217 + highlight_217 + preview_217
$$

```swift
struct PerfSection00217 {
    let title = "Section 00217"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00217","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00217 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00218

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00218.md), and an image reference ![sample](./assets/image-00218.png). The prose also carries inline math like $score_218 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00218
- [x] Reviewed checkpoint for section 00218
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00218 | repeated table row |
| words | 64 | stable measurement text |

$$
render_218 = parse_218 + highlight_218 + preview_218
$$

```swift
struct PerfSection00218 {
    let title = "Section 00218"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00218","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00218 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00219

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00219.md), and an image reference ![sample](./assets/image-00219.png). The prose also carries inline math like $score_219 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00219
- [x] Reviewed checkpoint for section 00219
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00219 | repeated table row |
| words | 64 | stable measurement text |

$$
render_219 = parse_219 + highlight_219 + preview_219
$$

```swift
struct PerfSection00219 {
    let title = "Section 00219"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00219","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00219 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00220

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00220.md), and an image reference ![sample](./assets/image-00220.png). The prose also carries inline math like $score_220 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00220
- [x] Reviewed checkpoint for section 00220
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00220 | repeated table row |
| words | 64 | stable measurement text |

$$
render_220 = parse_220 + highlight_220 + preview_220
$$

```swift
struct PerfSection00220 {
    let title = "Section 00220"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00220","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00220 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00221

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00221.md), and an image reference ![sample](./assets/image-00221.png). The prose also carries inline math like $score_221 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00221
- [x] Reviewed checkpoint for section 00221
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00221 | repeated table row |
| words | 64 | stable measurement text |

$$
render_221 = parse_221 + highlight_221 + preview_221
$$

```swift
struct PerfSection00221 {
    let title = "Section 00221"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00221","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00221 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00222

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00222.md), and an image reference ![sample](./assets/image-00222.png). The prose also carries inline math like $score_222 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00222
- [x] Reviewed checkpoint for section 00222
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00222 | repeated table row |
| words | 64 | stable measurement text |

$$
render_222 = parse_222 + highlight_222 + preview_222
$$

```swift
struct PerfSection00222 {
    let title = "Section 00222"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00222","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00222 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00223

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00223.md), and an image reference ![sample](./assets/image-00223.png). The prose also carries inline math like $score_223 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00223
- [x] Reviewed checkpoint for section 00223
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00223 | repeated table row |
| words | 64 | stable measurement text |

$$
render_223 = parse_223 + highlight_223 + preview_223
$$

```swift
struct PerfSection00223 {
    let title = "Section 00223"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00223","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00223 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00224

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00224.md), and an image reference ![sample](./assets/image-00224.png). The prose also carries inline math like $score_224 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00224
- [x] Reviewed checkpoint for section 00224
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00224 | repeated table row |
| words | 64 | stable measurement text |

$$
render_224 = parse_224 + highlight_224 + preview_224
$$

```swift
struct PerfSection00224 {
    let title = "Section 00224"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00224","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00224 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00225

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00225.md), and an image reference ![sample](./assets/image-00225.png). The prose also carries inline math like $score_225 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00225
- [x] Reviewed checkpoint for section 00225
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00225 | repeated table row |
| words | 64 | stable measurement text |

$$
render_225 = parse_225 + highlight_225 + preview_225
$$

```swift
struct PerfSection00225 {
    let title = "Section 00225"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00225","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00225 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00226

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00226.md), and an image reference ![sample](./assets/image-00226.png). The prose also carries inline math like $score_226 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00226
- [x] Reviewed checkpoint for section 00226
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00226 | repeated table row |
| words | 64 | stable measurement text |

$$
render_226 = parse_226 + highlight_226 + preview_226
$$

```swift
struct PerfSection00226 {
    let title = "Section 00226"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00226","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00226 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00227

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00227.md), and an image reference ![sample](./assets/image-00227.png). The prose also carries inline math like $score_227 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00227
- [x] Reviewed checkpoint for section 00227
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00227 | repeated table row |
| words | 64 | stable measurement text |

$$
render_227 = parse_227 + highlight_227 + preview_227
$$

```swift
struct PerfSection00227 {
    let title = "Section 00227"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00227","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00227 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00228

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00228.md), and an image reference ![sample](./assets/image-00228.png). The prose also carries inline math like $score_228 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00228
- [x] Reviewed checkpoint for section 00228
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00228 | repeated table row |
| words | 64 | stable measurement text |

$$
render_228 = parse_228 + highlight_228 + preview_228
$$

```swift
struct PerfSection00228 {
    let title = "Section 00228"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00228","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00228 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00229

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00229.md), and an image reference ![sample](./assets/image-00229.png). The prose also carries inline math like $score_229 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00229
- [x] Reviewed checkpoint for section 00229
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00229 | repeated table row |
| words | 64 | stable measurement text |

$$
render_229 = parse_229 + highlight_229 + preview_229
$$

```swift
struct PerfSection00229 {
    let title = "Section 00229"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00229","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00229 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00230

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00230.md), and an image reference ![sample](./assets/image-00230.png). The prose also carries inline math like $score_230 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00230
- [x] Reviewed checkpoint for section 00230
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00230 | repeated table row |
| words | 64 | stable measurement text |

$$
render_230 = parse_230 + highlight_230 + preview_230
$$

```swift
struct PerfSection00230 {
    let title = "Section 00230"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00230","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00230 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00231

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00231.md), and an image reference ![sample](./assets/image-00231.png). The prose also carries inline math like $score_231 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00231
- [x] Reviewed checkpoint for section 00231
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00231 | repeated table row |
| words | 64 | stable measurement text |

$$
render_231 = parse_231 + highlight_231 + preview_231
$$

```swift
struct PerfSection00231 {
    let title = "Section 00231"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00231","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00231 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00232

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00232.md), and an image reference ![sample](./assets/image-00232.png). The prose also carries inline math like $score_232 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00232
- [x] Reviewed checkpoint for section 00232
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00232 | repeated table row |
| words | 64 | stable measurement text |

$$
render_232 = parse_232 + highlight_232 + preview_232
$$

```swift
struct PerfSection00232 {
    let title = "Section 00232"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00232","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00232 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00233

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00233.md), and an image reference ![sample](./assets/image-00233.png). The prose also carries inline math like $score_233 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00233
- [x] Reviewed checkpoint for section 00233
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00233 | repeated table row |
| words | 64 | stable measurement text |

$$
render_233 = parse_233 + highlight_233 + preview_233
$$

```swift
struct PerfSection00233 {
    let title = "Section 00233"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00233","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00233 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00234

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00234.md), and an image reference ![sample](./assets/image-00234.png). The prose also carries inline math like $score_234 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00234
- [x] Reviewed checkpoint for section 00234
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00234 | repeated table row |
| words | 64 | stable measurement text |

$$
render_234 = parse_234 + highlight_234 + preview_234
$$

```swift
struct PerfSection00234 {
    let title = "Section 00234"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00234","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00234 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00235

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00235.md), and an image reference ![sample](./assets/image-00235.png). The prose also carries inline math like $score_235 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00235
- [x] Reviewed checkpoint for section 00235
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00235 | repeated table row |
| words | 64 | stable measurement text |

$$
render_235 = parse_235 + highlight_235 + preview_235
$$

```swift
struct PerfSection00235 {
    let title = "Section 00235"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00235","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00235 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00236

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00236.md), and an image reference ![sample](./assets/image-00236.png). The prose also carries inline math like $score_236 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00236
- [x] Reviewed checkpoint for section 00236
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00236 | repeated table row |
| words | 64 | stable measurement text |

$$
render_236 = parse_236 + highlight_236 + preview_236
$$

```swift
struct PerfSection00236 {
    let title = "Section 00236"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00236","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00236 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00237

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00237.md), and an image reference ![sample](./assets/image-00237.png). The prose also carries inline math like $score_237 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00237
- [x] Reviewed checkpoint for section 00237
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00237 | repeated table row |
| words | 64 | stable measurement text |

$$
render_237 = parse_237 + highlight_237 + preview_237
$$

```swift
struct PerfSection00237 {
    let title = "Section 00237"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00237","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00237 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00238

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00238.md), and an image reference ![sample](./assets/image-00238.png). The prose also carries inline math like $score_238 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00238
- [x] Reviewed checkpoint for section 00238
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00238 | repeated table row |
| words | 64 | stable measurement text |

$$
render_238 = parse_238 + highlight_238 + preview_238
$$

```swift
struct PerfSection00238 {
    let title = "Section 00238"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00238","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00238 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00239

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00239.md), and an image reference ![sample](./assets/image-00239.png). The prose also carries inline math like $score_239 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00239
- [x] Reviewed checkpoint for section 00239
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00239 | repeated table row |
| words | 64 | stable measurement text |

$$
render_239 = parse_239 + highlight_239 + preview_239
$$

```swift
struct PerfSection00239 {
    let title = "Section 00239"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00239","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00239 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00240

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00240.md), and an image reference ![sample](./assets/image-00240.png). The prose also carries inline math like $score_240 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00240
- [x] Reviewed checkpoint for section 00240
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00240 | repeated table row |
| words | 64 | stable measurement text |

$$
render_240 = parse_240 + highlight_240 + preview_240
$$

```swift
struct PerfSection00240 {
    let title = "Section 00240"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00240","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00240 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00241

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00241.md), and an image reference ![sample](./assets/image-00241.png). The prose also carries inline math like $score_241 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00241
- [x] Reviewed checkpoint for section 00241
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00241 | repeated table row |
| words | 64 | stable measurement text |

$$
render_241 = parse_241 + highlight_241 + preview_241
$$

```swift
struct PerfSection00241 {
    let title = "Section 00241"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00241","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00241 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00242

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00242.md), and an image reference ![sample](./assets/image-00242.png). The prose also carries inline math like $score_242 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00242
- [x] Reviewed checkpoint for section 00242
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00242 | repeated table row |
| words | 64 | stable measurement text |

$$
render_242 = parse_242 + highlight_242 + preview_242
$$

```swift
struct PerfSection00242 {
    let title = "Section 00242"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00242","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00242 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00243

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00243.md), and an image reference ![sample](./assets/image-00243.png). The prose also carries inline math like $score_243 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00243
- [x] Reviewed checkpoint for section 00243
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00243 | repeated table row |
| words | 64 | stable measurement text |

$$
render_243 = parse_243 + highlight_243 + preview_243
$$

```swift
struct PerfSection00243 {
    let title = "Section 00243"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00243","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00243 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00244

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00244.md), and an image reference ![sample](./assets/image-00244.png). The prose also carries inline math like $score_244 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00244
- [x] Reviewed checkpoint for section 00244
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00244 | repeated table row |
| words | 64 | stable measurement text |

$$
render_244 = parse_244 + highlight_244 + preview_244
$$

```swift
struct PerfSection00244 {
    let title = "Section 00244"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00244","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00244 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00245

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00245.md), and an image reference ![sample](./assets/image-00245.png). The prose also carries inline math like $score_245 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00245
- [x] Reviewed checkpoint for section 00245
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00245 | repeated table row |
| words | 64 | stable measurement text |

$$
render_245 = parse_245 + highlight_245 + preview_245
$$

```swift
struct PerfSection00245 {
    let title = "Section 00245"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00245","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00245 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00246

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00246.md), and an image reference ![sample](./assets/image-00246.png). The prose also carries inline math like $score_246 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00246
- [x] Reviewed checkpoint for section 00246
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00246 | repeated table row |
| words | 64 | stable measurement text |

$$
render_246 = parse_246 + highlight_246 + preview_246
$$

```swift
struct PerfSection00246 {
    let title = "Section 00246"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00246","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00246 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00247

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00247.md), and an image reference ![sample](./assets/image-00247.png). The prose also carries inline math like $score_247 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00247
- [x] Reviewed checkpoint for section 00247
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00247 | repeated table row |
| words | 64 | stable measurement text |

$$
render_247 = parse_247 + highlight_247 + preview_247
$$

```swift
struct PerfSection00247 {
    let title = "Section 00247"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00247","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00247 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00248

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00248.md), and an image reference ![sample](./assets/image-00248.png). The prose also carries inline math like $score_248 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00248
- [x] Reviewed checkpoint for section 00248
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00248 | repeated table row |
| words | 64 | stable measurement text |

$$
render_248 = parse_248 + highlight_248 + preview_248
$$

```swift
struct PerfSection00248 {
    let title = "Section 00248"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00248","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00248 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00249

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00249.md), and an image reference ![sample](./assets/image-00249.png). The prose also carries inline math like $score_249 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00249
- [x] Reviewed checkpoint for section 00249
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00249 | repeated table row |
| words | 64 | stable measurement text |

$$
render_249 = parse_249 + highlight_249 + preview_249
$$

```swift
struct PerfSection00249 {
    let title = "Section 00249"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00249","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00249 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00250

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00250.md), and an image reference ![sample](./assets/image-00250.png). The prose also carries inline math like $score_250 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00250
- [x] Reviewed checkpoint for section 00250
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00250 | repeated table row |
| words | 64 | stable measurement text |

$$
render_250 = parse_250 + highlight_250 + preview_250
$$

```swift
struct PerfSection00250 {
    let title = "Section 00250"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00250","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00250 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00251

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00251.md), and an image reference ![sample](./assets/image-00251.png). The prose also carries inline math like $score_251 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00251
- [x] Reviewed checkpoint for section 00251
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00251 | repeated table row |
| words | 64 | stable measurement text |

$$
render_251 = parse_251 + highlight_251 + preview_251
$$

```swift
struct PerfSection00251 {
    let title = "Section 00251"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00251","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00251 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00252

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00252.md), and an image reference ![sample](./assets/image-00252.png). The prose also carries inline math like $score_252 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00252
- [x] Reviewed checkpoint for section 00252
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00252 | repeated table row |
| words | 64 | stable measurement text |

$$
render_252 = parse_252 + highlight_252 + preview_252
$$

```swift
struct PerfSection00252 {
    let title = "Section 00252"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00252","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00252 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00253

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00253.md), and an image reference ![sample](./assets/image-00253.png). The prose also carries inline math like $score_253 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00253
- [x] Reviewed checkpoint for section 00253
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00253 | repeated table row |
| words | 64 | stable measurement text |

$$
render_253 = parse_253 + highlight_253 + preview_253
$$

```swift
struct PerfSection00253 {
    let title = "Section 00253"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00253","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00253 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00254

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00254.md), and an image reference ![sample](./assets/image-00254.png). The prose also carries inline math like $score_254 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00254
- [x] Reviewed checkpoint for section 00254
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00254 | repeated table row |
| words | 64 | stable measurement text |

$$
render_254 = parse_254 + highlight_254 + preview_254
$$

```swift
struct PerfSection00254 {
    let title = "Section 00254"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00254","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00254 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00255

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00255.md), and an image reference ![sample](./assets/image-00255.png). The prose also carries inline math like $score_255 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00255
- [x] Reviewed checkpoint for section 00255
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00255 | repeated table row |
| words | 64 | stable measurement text |

$$
render_255 = parse_255 + highlight_255 + preview_255
$$

```swift
struct PerfSection00255 {
    let title = "Section 00255"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00255","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00255 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00256

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00256.md), and an image reference ![sample](./assets/image-00256.png). The prose also carries inline math like $score_256 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00256
- [x] Reviewed checkpoint for section 00256
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00256 | repeated table row |
| words | 64 | stable measurement text |

$$
render_256 = parse_256 + highlight_256 + preview_256
$$

```swift
struct PerfSection00256 {
    let title = "Section 00256"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00256","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00256 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00257

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00257.md), and an image reference ![sample](./assets/image-00257.png). The prose also carries inline math like $score_257 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00257
- [x] Reviewed checkpoint for section 00257
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00257 | repeated table row |
| words | 64 | stable measurement text |

$$
render_257 = parse_257 + highlight_257 + preview_257
$$

```swift
struct PerfSection00257 {
    let title = "Section 00257"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00257","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00257 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00258

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00258.md), and an image reference ![sample](./assets/image-00258.png). The prose also carries inline math like $score_258 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00258
- [x] Reviewed checkpoint for section 00258
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00258 | repeated table row |
| words | 64 | stable measurement text |

$$
render_258 = parse_258 + highlight_258 + preview_258
$$

```swift
struct PerfSection00258 {
    let title = "Section 00258"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00258","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00258 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00259

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00259.md), and an image reference ![sample](./assets/image-00259.png). The prose also carries inline math like $score_259 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00259
- [x] Reviewed checkpoint for section 00259
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00259 | repeated table row |
| words | 64 | stable measurement text |

$$
render_259 = parse_259 + highlight_259 + preview_259
$$

```swift
struct PerfSection00259 {
    let title = "Section 00259"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00259","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00259 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00260

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00260.md), and an image reference ![sample](./assets/image-00260.png). The prose also carries inline math like $score_260 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00260
- [x] Reviewed checkpoint for section 00260
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00260 | repeated table row |
| words | 64 | stable measurement text |

$$
render_260 = parse_260 + highlight_260 + preview_260
$$

```swift
struct PerfSection00260 {
    let title = "Section 00260"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00260","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00260 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00261

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00261.md), and an image reference ![sample](./assets/image-00261.png). The prose also carries inline math like $score_261 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00261
- [x] Reviewed checkpoint for section 00261
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00261 | repeated table row |
| words | 64 | stable measurement text |

$$
render_261 = parse_261 + highlight_261 + preview_261
$$

```swift
struct PerfSection00261 {
    let title = "Section 00261"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00261","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00261 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00262

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00262.md), and an image reference ![sample](./assets/image-00262.png). The prose also carries inline math like $score_262 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00262
- [x] Reviewed checkpoint for section 00262
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00262 | repeated table row |
| words | 64 | stable measurement text |

$$
render_262 = parse_262 + highlight_262 + preview_262
$$

```swift
struct PerfSection00262 {
    let title = "Section 00262"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00262","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00262 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00263

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00263.md), and an image reference ![sample](./assets/image-00263.png). The prose also carries inline math like $score_263 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00263
- [x] Reviewed checkpoint for section 00263
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00263 | repeated table row |
| words | 64 | stable measurement text |

$$
render_263 = parse_263 + highlight_263 + preview_263
$$

```swift
struct PerfSection00263 {
    let title = "Section 00263"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00263","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00263 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00264

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00264.md), and an image reference ![sample](./assets/image-00264.png). The prose also carries inline math like $score_264 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00264
- [x] Reviewed checkpoint for section 00264
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00264 | repeated table row |
| words | 64 | stable measurement text |

$$
render_264 = parse_264 + highlight_264 + preview_264
$$

```swift
struct PerfSection00264 {
    let title = "Section 00264"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00264","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00264 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00265

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00265.md), and an image reference ![sample](./assets/image-00265.png). The prose also carries inline math like $score_265 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00265
- [x] Reviewed checkpoint for section 00265
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00265 | repeated table row |
| words | 64 | stable measurement text |

$$
render_265 = parse_265 + highlight_265 + preview_265
$$

```swift
struct PerfSection00265 {
    let title = "Section 00265"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00265","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00265 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00266

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00266.md), and an image reference ![sample](./assets/image-00266.png). The prose also carries inline math like $score_266 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00266
- [x] Reviewed checkpoint for section 00266
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00266 | repeated table row |
| words | 64 | stable measurement text |

$$
render_266 = parse_266 + highlight_266 + preview_266
$$

```swift
struct PerfSection00266 {
    let title = "Section 00266"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00266","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00266 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00267

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00267.md), and an image reference ![sample](./assets/image-00267.png). The prose also carries inline math like $score_267 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00267
- [x] Reviewed checkpoint for section 00267
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00267 | repeated table row |
| words | 64 | stable measurement text |

$$
render_267 = parse_267 + highlight_267 + preview_267
$$

```swift
struct PerfSection00267 {
    let title = "Section 00267"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00267","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00267 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00268

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00268.md), and an image reference ![sample](./assets/image-00268.png). The prose also carries inline math like $score_268 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00268
- [x] Reviewed checkpoint for section 00268
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00268 | repeated table row |
| words | 64 | stable measurement text |

$$
render_268 = parse_268 + highlight_268 + preview_268
$$

```swift
struct PerfSection00268 {
    let title = "Section 00268"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00268","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00268 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00269

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00269.md), and an image reference ![sample](./assets/image-00269.png). The prose also carries inline math like $score_269 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00269
- [x] Reviewed checkpoint for section 00269
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00269 | repeated table row |
| words | 64 | stable measurement text |

$$
render_269 = parse_269 + highlight_269 + preview_269
$$

```swift
struct PerfSection00269 {
    let title = "Section 00269"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00269","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00269 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00270

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00270.md), and an image reference ![sample](./assets/image-00270.png). The prose also carries inline math like $score_270 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00270
- [x] Reviewed checkpoint for section 00270
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00270 | repeated table row |
| words | 64 | stable measurement text |

$$
render_270 = parse_270 + highlight_270 + preview_270
$$

```swift
struct PerfSection00270 {
    let title = "Section 00270"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00270","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00270 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00271

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00271.md), and an image reference ![sample](./assets/image-00271.png). The prose also carries inline math like $score_271 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00271
- [x] Reviewed checkpoint for section 00271
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00271 | repeated table row |
| words | 64 | stable measurement text |

$$
render_271 = parse_271 + highlight_271 + preview_271
$$

```swift
struct PerfSection00271 {
    let title = "Section 00271"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00271","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00271 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00272

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00272.md), and an image reference ![sample](./assets/image-00272.png). The prose also carries inline math like $score_272 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00272
- [x] Reviewed checkpoint for section 00272
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00272 | repeated table row |
| words | 64 | stable measurement text |

$$
render_272 = parse_272 + highlight_272 + preview_272
$$

```swift
struct PerfSection00272 {
    let title = "Section 00272"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00272","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00272 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00273

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00273.md), and an image reference ![sample](./assets/image-00273.png). The prose also carries inline math like $score_273 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00273
- [x] Reviewed checkpoint for section 00273
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00273 | repeated table row |
| words | 64 | stable measurement text |

$$
render_273 = parse_273 + highlight_273 + preview_273
$$

```swift
struct PerfSection00273 {
    let title = "Section 00273"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00273","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00273 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00274

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00274.md), and an image reference ![sample](./assets/image-00274.png). The prose also carries inline math like $score_274 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00274
- [x] Reviewed checkpoint for section 00274
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00274 | repeated table row |
| words | 64 | stable measurement text |

$$
render_274 = parse_274 + highlight_274 + preview_274
$$

```swift
struct PerfSection00274 {
    let title = "Section 00274"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00274","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00274 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00275

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00275.md), and an image reference ![sample](./assets/image-00275.png). The prose also carries inline math like $score_275 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00275
- [x] Reviewed checkpoint for section 00275
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00275 | repeated table row |
| words | 64 | stable measurement text |

$$
render_275 = parse_275 + highlight_275 + preview_275
$$

```swift
struct PerfSection00275 {
    let title = "Section 00275"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00275","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00275 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00276

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00276.md), and an image reference ![sample](./assets/image-00276.png). The prose also carries inline math like $score_276 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00276
- [x] Reviewed checkpoint for section 00276
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00276 | repeated table row |
| words | 64 | stable measurement text |

$$
render_276 = parse_276 + highlight_276 + preview_276
$$

```swift
struct PerfSection00276 {
    let title = "Section 00276"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00276","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00276 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00277

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00277.md), and an image reference ![sample](./assets/image-00277.png). The prose also carries inline math like $score_277 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00277
- [x] Reviewed checkpoint for section 00277
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00277 | repeated table row |
| words | 64 | stable measurement text |

$$
render_277 = parse_277 + highlight_277 + preview_277
$$

```swift
struct PerfSection00277 {
    let title = "Section 00277"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00277","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00277 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00278

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00278.md), and an image reference ![sample](./assets/image-00278.png). The prose also carries inline math like $score_278 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00278
- [x] Reviewed checkpoint for section 00278
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00278 | repeated table row |
| words | 64 | stable measurement text |

$$
render_278 = parse_278 + highlight_278 + preview_278
$$

```swift
struct PerfSection00278 {
    let title = "Section 00278"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00278","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00278 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00279

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00279.md), and an image reference ![sample](./assets/image-00279.png). The prose also carries inline math like $score_279 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00279
- [x] Reviewed checkpoint for section 00279
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00279 | repeated table row |
| words | 64 | stable measurement text |

$$
render_279 = parse_279 + highlight_279 + preview_279
$$

```swift
struct PerfSection00279 {
    let title = "Section 00279"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00279","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00279 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00280

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00280.md), and an image reference ![sample](./assets/image-00280.png). The prose also carries inline math like $score_280 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00280
- [x] Reviewed checkpoint for section 00280
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00280 | repeated table row |
| words | 64 | stable measurement text |

$$
render_280 = parse_280 + highlight_280 + preview_280
$$

```swift
struct PerfSection00280 {
    let title = "Section 00280"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00280","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00280 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00281

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00281.md), and an image reference ![sample](./assets/image-00281.png). The prose also carries inline math like $score_281 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00281
- [x] Reviewed checkpoint for section 00281
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00281 | repeated table row |
| words | 64 | stable measurement text |

$$
render_281 = parse_281 + highlight_281 + preview_281
$$

```swift
struct PerfSection00281 {
    let title = "Section 00281"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00281","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00281 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00282

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00282.md), and an image reference ![sample](./assets/image-00282.png). The prose also carries inline math like $score_282 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00282
- [x] Reviewed checkpoint for section 00282
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00282 | repeated table row |
| words | 64 | stable measurement text |

$$
render_282 = parse_282 + highlight_282 + preview_282
$$

```swift
struct PerfSection00282 {
    let title = "Section 00282"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00282","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00282 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00283

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00283.md), and an image reference ![sample](./assets/image-00283.png). The prose also carries inline math like $score_283 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00283
- [x] Reviewed checkpoint for section 00283
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00283 | repeated table row |
| words | 64 | stable measurement text |

$$
render_283 = parse_283 + highlight_283 + preview_283
$$

```swift
struct PerfSection00283 {
    let title = "Section 00283"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00283","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00283 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00284

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00284.md), and an image reference ![sample](./assets/image-00284.png). The prose also carries inline math like $score_284 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00284
- [x] Reviewed checkpoint for section 00284
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00284 | repeated table row |
| words | 64 | stable measurement text |

$$
render_284 = parse_284 + highlight_284 + preview_284
$$

```swift
struct PerfSection00284 {
    let title = "Section 00284"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00284","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00284 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00285

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00285.md), and an image reference ![sample](./assets/image-00285.png). The prose also carries inline math like $score_285 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00285
- [x] Reviewed checkpoint for section 00285
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00285 | repeated table row |
| words | 64 | stable measurement text |

$$
render_285 = parse_285 + highlight_285 + preview_285
$$

```swift
struct PerfSection00285 {
    let title = "Section 00285"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00285","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00285 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00286

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00286.md), and an image reference ![sample](./assets/image-00286.png). The prose also carries inline math like $score_286 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00286
- [x] Reviewed checkpoint for section 00286
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00286 | repeated table row |
| words | 64 | stable measurement text |

$$
render_286 = parse_286 + highlight_286 + preview_286
$$

```swift
struct PerfSection00286 {
    let title = "Section 00286"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00286","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00286 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00287

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00287.md), and an image reference ![sample](./assets/image-00287.png). The prose also carries inline math like $score_287 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00287
- [x] Reviewed checkpoint for section 00287
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00287 | repeated table row |
| words | 64 | stable measurement text |

$$
render_287 = parse_287 + highlight_287 + preview_287
$$

```swift
struct PerfSection00287 {
    let title = "Section 00287"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00287","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00287 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00288

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00288.md), and an image reference ![sample](./assets/image-00288.png). The prose also carries inline math like $score_288 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00288
- [x] Reviewed checkpoint for section 00288
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00288 | repeated table row |
| words | 64 | stable measurement text |

$$
render_288 = parse_288 + highlight_288 + preview_288
$$

```swift
struct PerfSection00288 {
    let title = "Section 00288"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00288","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00288 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00289

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00289.md), and an image reference ![sample](./assets/image-00289.png). The prose also carries inline math like $score_289 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00289
- [x] Reviewed checkpoint for section 00289
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00289 | repeated table row |
| words | 64 | stable measurement text |

$$
render_289 = parse_289 + highlight_289 + preview_289
$$

```swift
struct PerfSection00289 {
    let title = "Section 00289"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00289","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00289 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00290

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00290.md), and an image reference ![sample](./assets/image-00290.png). The prose also carries inline math like $score_290 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00290
- [x] Reviewed checkpoint for section 00290
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00290 | repeated table row |
| words | 64 | stable measurement text |

$$
render_290 = parse_290 + highlight_290 + preview_290
$$

```swift
struct PerfSection00290 {
    let title = "Section 00290"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00290","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00290 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00291

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00291.md), and an image reference ![sample](./assets/image-00291.png). The prose also carries inline math like $score_291 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00291
- [x] Reviewed checkpoint for section 00291
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00291 | repeated table row |
| words | 64 | stable measurement text |

$$
render_291 = parse_291 + highlight_291 + preview_291
$$

```swift
struct PerfSection00291 {
    let title = "Section 00291"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00291","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00291 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00292

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00292.md), and an image reference ![sample](./assets/image-00292.png). The prose also carries inline math like $score_292 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00292
- [x] Reviewed checkpoint for section 00292
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00292 | repeated table row |
| words | 64 | stable measurement text |

$$
render_292 = parse_292 + highlight_292 + preview_292
$$

```swift
struct PerfSection00292 {
    let title = "Section 00292"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00292","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00292 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00293

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00293.md), and an image reference ![sample](./assets/image-00293.png). The prose also carries inline math like $score_293 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00293
- [x] Reviewed checkpoint for section 00293
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00293 | repeated table row |
| words | 64 | stable measurement text |

$$
render_293 = parse_293 + highlight_293 + preview_293
$$

```swift
struct PerfSection00293 {
    let title = "Section 00293"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00293","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00293 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00294

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00294.md), and an image reference ![sample](./assets/image-00294.png). The prose also carries inline math like $score_294 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00294
- [x] Reviewed checkpoint for section 00294
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00294 | repeated table row |
| words | 64 | stable measurement text |

$$
render_294 = parse_294 + highlight_294 + preview_294
$$

```swift
struct PerfSection00294 {
    let title = "Section 00294"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00294","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00294 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00295

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00295.md), and an image reference ![sample](./assets/image-00295.png). The prose also carries inline math like $score_295 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00295
- [x] Reviewed checkpoint for section 00295
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00295 | repeated table row |
| words | 64 | stable measurement text |

$$
render_295 = parse_295 + highlight_295 + preview_295
$$

```swift
struct PerfSection00295 {
    let title = "Section 00295"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00295","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00295 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00296

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00296.md), and an image reference ![sample](./assets/image-00296.png). The prose also carries inline math like $score_296 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00296
- [x] Reviewed checkpoint for section 00296
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00296 | repeated table row |
| words | 64 | stable measurement text |

$$
render_296 = parse_296 + highlight_296 + preview_296
$$

```swift
struct PerfSection00296 {
    let title = "Section 00296"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00296","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00296 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00297

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00297.md), and an image reference ![sample](./assets/image-00297.png). The prose also carries inline math like $score_297 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00297
- [x] Reviewed checkpoint for section 00297
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00297 | repeated table row |
| words | 64 | stable measurement text |

$$
render_297 = parse_297 + highlight_297 + preview_297
$$

```swift
struct PerfSection00297 {
    let title = "Section 00297"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00297","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00297 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00298

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00298.md), and an image reference ![sample](./assets/image-00298.png). The prose also carries inline math like $score_298 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00298
- [x] Reviewed checkpoint for section 00298
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00298 | repeated table row |
| words | 64 | stable measurement text |

$$
render_298 = parse_298 + highlight_298 + preview_298
$$

```swift
struct PerfSection00298 {
    let title = "Section 00298"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00298","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00298 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00299

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00299.md), and an image reference ![sample](./assets/image-00299.png). The prose also carries inline math like $score_299 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00299
- [x] Reviewed checkpoint for section 00299
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00299 | repeated table row |
| words | 64 | stable measurement text |

$$
render_299 = parse_299 + highlight_299 + preview_299
$$

```swift
struct PerfSection00299 {
    let title = "Section 00299"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00299","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00299 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00300

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00300.md), and an image reference ![sample](./assets/image-00300.png). The prose also carries inline math like $score_300 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00300
- [x] Reviewed checkpoint for section 00300
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00300 | repeated table row |
| words | 64 | stable measurement text |

$$
render_300 = parse_300 + highlight_300 + preview_300
$$

```swift
struct PerfSection00300 {
    let title = "Section 00300"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00300","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00300 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00301

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00301.md), and an image reference ![sample](./assets/image-00301.png). The prose also carries inline math like $score_301 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00301
- [x] Reviewed checkpoint for section 00301
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00301 | repeated table row |
| words | 64 | stable measurement text |

$$
render_301 = parse_301 + highlight_301 + preview_301
$$

```swift
struct PerfSection00301 {
    let title = "Section 00301"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00301","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00301 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00302

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00302.md), and an image reference ![sample](./assets/image-00302.png). The prose also carries inline math like $score_302 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00302
- [x] Reviewed checkpoint for section 00302
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00302 | repeated table row |
| words | 64 | stable measurement text |

$$
render_302 = parse_302 + highlight_302 + preview_302
$$

```swift
struct PerfSection00302 {
    let title = "Section 00302"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00302","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00302 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00303

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00303.md), and an image reference ![sample](./assets/image-00303.png). The prose also carries inline math like $score_303 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00303
- [x] Reviewed checkpoint for section 00303
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00303 | repeated table row |
| words | 64 | stable measurement text |

$$
render_303 = parse_303 + highlight_303 + preview_303
$$

```swift
struct PerfSection00303 {
    let title = "Section 00303"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00303","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00303 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00304

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00304.md), and an image reference ![sample](./assets/image-00304.png). The prose also carries inline math like $score_304 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00304
- [x] Reviewed checkpoint for section 00304
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00304 | repeated table row |
| words | 64 | stable measurement text |

$$
render_304 = parse_304 + highlight_304 + preview_304
$$

```swift
struct PerfSection00304 {
    let title = "Section 00304"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00304","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00304 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00305

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00305.md), and an image reference ![sample](./assets/image-00305.png). The prose also carries inline math like $score_305 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00305
- [x] Reviewed checkpoint for section 00305
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00305 | repeated table row |
| words | 64 | stable measurement text |

$$
render_305 = parse_305 + highlight_305 + preview_305
$$

```swift
struct PerfSection00305 {
    let title = "Section 00305"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00305","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00305 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00306

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00306.md), and an image reference ![sample](./assets/image-00306.png). The prose also carries inline math like $score_306 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00306
- [x] Reviewed checkpoint for section 00306
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00306 | repeated table row |
| words | 64 | stable measurement text |

$$
render_306 = parse_306 + highlight_306 + preview_306
$$

```swift
struct PerfSection00306 {
    let title = "Section 00306"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00306","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00306 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00307

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00307.md), and an image reference ![sample](./assets/image-00307.png). The prose also carries inline math like $score_307 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00307
- [x] Reviewed checkpoint for section 00307
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00307 | repeated table row |
| words | 64 | stable measurement text |

$$
render_307 = parse_307 + highlight_307 + preview_307
$$

```swift
struct PerfSection00307 {
    let title = "Section 00307"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00307","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00307 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00308

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00308.md), and an image reference ![sample](./assets/image-00308.png). The prose also carries inline math like $score_308 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00308
- [x] Reviewed checkpoint for section 00308
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00308 | repeated table row |
| words | 64 | stable measurement text |

$$
render_308 = parse_308 + highlight_308 + preview_308
$$

```swift
struct PerfSection00308 {
    let title = "Section 00308"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00308","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00308 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00309

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00309.md), and an image reference ![sample](./assets/image-00309.png). The prose also carries inline math like $score_309 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00309
- [x] Reviewed checkpoint for section 00309
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00309 | repeated table row |
| words | 64 | stable measurement text |

$$
render_309 = parse_309 + highlight_309 + preview_309
$$

```swift
struct PerfSection00309 {
    let title = "Section 00309"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00309","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00309 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00310

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00310.md), and an image reference ![sample](./assets/image-00310.png). The prose also carries inline math like $score_310 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00310
- [x] Reviewed checkpoint for section 00310
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00310 | repeated table row |
| words | 64 | stable measurement text |

$$
render_310 = parse_310 + highlight_310 + preview_310
$$

```swift
struct PerfSection00310 {
    let title = "Section 00310"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00310","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00310 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00311

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00311.md), and an image reference ![sample](./assets/image-00311.png). The prose also carries inline math like $score_311 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00311
- [x] Reviewed checkpoint for section 00311
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00311 | repeated table row |
| words | 64 | stable measurement text |

$$
render_311 = parse_311 + highlight_311 + preview_311
$$

```swift
struct PerfSection00311 {
    let title = "Section 00311"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00311","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00311 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00312

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00312.md), and an image reference ![sample](./assets/image-00312.png). The prose also carries inline math like $score_312 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00312
- [x] Reviewed checkpoint for section 00312
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00312 | repeated table row |
| words | 64 | stable measurement text |

$$
render_312 = parse_312 + highlight_312 + preview_312
$$

```swift
struct PerfSection00312 {
    let title = "Section 00312"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00312","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00312 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00313

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00313.md), and an image reference ![sample](./assets/image-00313.png). The prose also carries inline math like $score_313 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00313
- [x] Reviewed checkpoint for section 00313
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00313 | repeated table row |
| words | 64 | stable measurement text |

$$
render_313 = parse_313 + highlight_313 + preview_313
$$

```swift
struct PerfSection00313 {
    let title = "Section 00313"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00313","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00313 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00314

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00314.md), and an image reference ![sample](./assets/image-00314.png). The prose also carries inline math like $score_314 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00314
- [x] Reviewed checkpoint for section 00314
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00314 | repeated table row |
| words | 64 | stable measurement text |

$$
render_314 = parse_314 + highlight_314 + preview_314
$$

```swift
struct PerfSection00314 {
    let title = "Section 00314"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00314","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00314 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00315

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00315.md), and an image reference ![sample](./assets/image-00315.png). The prose also carries inline math like $score_315 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00315
- [x] Reviewed checkpoint for section 00315
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00315 | repeated table row |
| words | 64 | stable measurement text |

$$
render_315 = parse_315 + highlight_315 + preview_315
$$

```swift
struct PerfSection00315 {
    let title = "Section 00315"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00315","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00315 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00316

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00316.md), and an image reference ![sample](./assets/image-00316.png). The prose also carries inline math like $score_316 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00316
- [x] Reviewed checkpoint for section 00316
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00316 | repeated table row |
| words | 64 | stable measurement text |

$$
render_316 = parse_316 + highlight_316 + preview_316
$$

```swift
struct PerfSection00316 {
    let title = "Section 00316"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00316","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00316 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00317

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00317.md), and an image reference ![sample](./assets/image-00317.png). The prose also carries inline math like $score_317 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00317
- [x] Reviewed checkpoint for section 00317
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00317 | repeated table row |
| words | 64 | stable measurement text |

$$
render_317 = parse_317 + highlight_317 + preview_317
$$

```swift
struct PerfSection00317 {
    let title = "Section 00317"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00317","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00317 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00318

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00318.md), and an image reference ![sample](./assets/image-00318.png). The prose also carries inline math like $score_318 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00318
- [x] Reviewed checkpoint for section 00318
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00318 | repeated table row |
| words | 64 | stable measurement text |

$$
render_318 = parse_318 + highlight_318 + preview_318
$$

```swift
struct PerfSection00318 {
    let title = "Section 00318"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00318","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00318 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00319

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00319.md), and an image reference ![sample](./assets/image-00319.png). The prose also carries inline math like $score_319 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00319
- [x] Reviewed checkpoint for section 00319
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00319 | repeated table row |
| words | 64 | stable measurement text |

$$
render_319 = parse_319 + highlight_319 + preview_319
$$

```swift
struct PerfSection00319 {
    let title = "Section 00319"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00319","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00319 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00320

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00320.md), and an image reference ![sample](./assets/image-00320.png). The prose also carries inline math like $score_320 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00320
- [x] Reviewed checkpoint for section 00320
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00320 | repeated table row |
| words | 64 | stable measurement text |

$$
render_320 = parse_320 + highlight_320 + preview_320
$$

```swift
struct PerfSection00320 {
    let title = "Section 00320"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00320","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00320 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00321

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00321.md), and an image reference ![sample](./assets/image-00321.png). The prose also carries inline math like $score_321 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00321
- [x] Reviewed checkpoint for section 00321
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00321 | repeated table row |
| words | 64 | stable measurement text |

$$
render_321 = parse_321 + highlight_321 + preview_321
$$

```swift
struct PerfSection00321 {
    let title = "Section 00321"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00321","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00321 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00322

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00322.md), and an image reference ![sample](./assets/image-00322.png). The prose also carries inline math like $score_322 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00322
- [x] Reviewed checkpoint for section 00322
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00322 | repeated table row |
| words | 64 | stable measurement text |

$$
render_322 = parse_322 + highlight_322 + preview_322
$$

```swift
struct PerfSection00322 {
    let title = "Section 00322"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00322","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00322 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00323

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00323.md), and an image reference ![sample](./assets/image-00323.png). The prose also carries inline math like $score_323 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00323
- [x] Reviewed checkpoint for section 00323
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00323 | repeated table row |
| words | 64 | stable measurement text |

$$
render_323 = parse_323 + highlight_323 + preview_323
$$

```swift
struct PerfSection00323 {
    let title = "Section 00323"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00323","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00323 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00324

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00324.md), and an image reference ![sample](./assets/image-00324.png). The prose also carries inline math like $score_324 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00324
- [x] Reviewed checkpoint for section 00324
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00324 | repeated table row |
| words | 64 | stable measurement text |

$$
render_324 = parse_324 + highlight_324 + preview_324
$$

```swift
struct PerfSection00324 {
    let title = "Section 00324"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00324","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00324 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00325

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00325.md), and an image reference ![sample](./assets/image-00325.png). The prose also carries inline math like $score_325 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00325
- [x] Reviewed checkpoint for section 00325
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00325 | repeated table row |
| words | 64 | stable measurement text |

$$
render_325 = parse_325 + highlight_325 + preview_325
$$

```swift
struct PerfSection00325 {
    let title = "Section 00325"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00325","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00325 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00326

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00326.md), and an image reference ![sample](./assets/image-00326.png). The prose also carries inline math like $score_326 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00326
- [x] Reviewed checkpoint for section 00326
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00326 | repeated table row |
| words | 64 | stable measurement text |

$$
render_326 = parse_326 + highlight_326 + preview_326
$$

```swift
struct PerfSection00326 {
    let title = "Section 00326"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00326","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00326 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00327

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00327.md), and an image reference ![sample](./assets/image-00327.png). The prose also carries inline math like $score_327 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00327
- [x] Reviewed checkpoint for section 00327
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00327 | repeated table row |
| words | 64 | stable measurement text |

$$
render_327 = parse_327 + highlight_327 + preview_327
$$

```swift
struct PerfSection00327 {
    let title = "Section 00327"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00327","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00327 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00328

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00328.md), and an image reference ![sample](./assets/image-00328.png). The prose also carries inline math like $score_328 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00328
- [x] Reviewed checkpoint for section 00328
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00328 | repeated table row |
| words | 64 | stable measurement text |

$$
render_328 = parse_328 + highlight_328 + preview_328
$$

```swift
struct PerfSection00328 {
    let title = "Section 00328"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00328","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00328 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00329

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00329.md), and an image reference ![sample](./assets/image-00329.png). The prose also carries inline math like $score_329 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00329
- [x] Reviewed checkpoint for section 00329
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00329 | repeated table row |
| words | 64 | stable measurement text |

$$
render_329 = parse_329 + highlight_329 + preview_329
$$

```swift
struct PerfSection00329 {
    let title = "Section 00329"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00329","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00329 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00330

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00330.md), and an image reference ![sample](./assets/image-00330.png). The prose also carries inline math like $score_330 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00330
- [x] Reviewed checkpoint for section 00330
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00330 | repeated table row |
| words | 64 | stable measurement text |

$$
render_330 = parse_330 + highlight_330 + preview_330
$$

```swift
struct PerfSection00330 {
    let title = "Section 00330"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00330","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00330 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00331

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00331.md), and an image reference ![sample](./assets/image-00331.png). The prose also carries inline math like $score_331 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00331
- [x] Reviewed checkpoint for section 00331
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00331 | repeated table row |
| words | 64 | stable measurement text |

$$
render_331 = parse_331 + highlight_331 + preview_331
$$

```swift
struct PerfSection00331 {
    let title = "Section 00331"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00331","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00331 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00332

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00332.md), and an image reference ![sample](./assets/image-00332.png). The prose also carries inline math like $score_332 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00332
- [x] Reviewed checkpoint for section 00332
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00332 | repeated table row |
| words | 64 | stable measurement text |

$$
render_332 = parse_332 + highlight_332 + preview_332
$$

```swift
struct PerfSection00332 {
    let title = "Section 00332"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00332","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00332 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00333

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00333.md), and an image reference ![sample](./assets/image-00333.png). The prose also carries inline math like $score_333 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00333
- [x] Reviewed checkpoint for section 00333
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00333 | repeated table row |
| words | 64 | stable measurement text |

$$
render_333 = parse_333 + highlight_333 + preview_333
$$

```swift
struct PerfSection00333 {
    let title = "Section 00333"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00333","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00333 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00334

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00334.md), and an image reference ![sample](./assets/image-00334.png). The prose also carries inline math like $score_334 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00334
- [x] Reviewed checkpoint for section 00334
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00334 | repeated table row |
| words | 64 | stable measurement text |

$$
render_334 = parse_334 + highlight_334 + preview_334
$$

```swift
struct PerfSection00334 {
    let title = "Section 00334"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00334","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00334 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00335

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00335.md), and an image reference ![sample](./assets/image-00335.png). The prose also carries inline math like $score_335 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00335
- [x] Reviewed checkpoint for section 00335
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00335 | repeated table row |
| words | 64 | stable measurement text |

$$
render_335 = parse_335 + highlight_335 + preview_335
$$

```swift
struct PerfSection00335 {
    let title = "Section 00335"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00335","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00335 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00336

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00336.md), and an image reference ![sample](./assets/image-00336.png). The prose also carries inline math like $score_336 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00336
- [x] Reviewed checkpoint for section 00336
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00336 | repeated table row |
| words | 64 | stable measurement text |

$$
render_336 = parse_336 + highlight_336 + preview_336
$$

```swift
struct PerfSection00336 {
    let title = "Section 00336"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00336","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00336 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00337

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00337.md), and an image reference ![sample](./assets/image-00337.png). The prose also carries inline math like $score_337 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00337
- [x] Reviewed checkpoint for section 00337
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00337 | repeated table row |
| words | 64 | stable measurement text |

$$
render_337 = parse_337 + highlight_337 + preview_337
$$

```swift
struct PerfSection00337 {
    let title = "Section 00337"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00337","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00337 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00338

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00338.md), and an image reference ![sample](./assets/image-00338.png). The prose also carries inline math like $score_338 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00338
- [x] Reviewed checkpoint for section 00338
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00338 | repeated table row |
| words | 64 | stable measurement text |

$$
render_338 = parse_338 + highlight_338 + preview_338
$$

```swift
struct PerfSection00338 {
    let title = "Section 00338"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00338","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00338 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00339

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00339.md), and an image reference ![sample](./assets/image-00339.png). The prose also carries inline math like $score_339 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00339
- [x] Reviewed checkpoint for section 00339
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00339 | repeated table row |
| words | 64 | stable measurement text |

$$
render_339 = parse_339 + highlight_339 + preview_339
$$

```swift
struct PerfSection00339 {
    let title = "Section 00339"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00339","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00339 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00340

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00340.md), and an image reference ![sample](./assets/image-00340.png). The prose also carries inline math like $score_340 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00340
- [x] Reviewed checkpoint for section 00340
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00340 | repeated table row |
| words | 64 | stable measurement text |

$$
render_340 = parse_340 + highlight_340 + preview_340
$$

```swift
struct PerfSection00340 {
    let title = "Section 00340"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00340","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00340 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00341

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00341.md), and an image reference ![sample](./assets/image-00341.png). The prose also carries inline math like $score_341 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00341
- [x] Reviewed checkpoint for section 00341
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00341 | repeated table row |
| words | 64 | stable measurement text |

$$
render_341 = parse_341 + highlight_341 + preview_341
$$

```swift
struct PerfSection00341 {
    let title = "Section 00341"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00341","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00341 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00342

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00342.md), and an image reference ![sample](./assets/image-00342.png). The prose also carries inline math like $score_342 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00342
- [x] Reviewed checkpoint for section 00342
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00342 | repeated table row |
| words | 64 | stable measurement text |

$$
render_342 = parse_342 + highlight_342 + preview_342
$$

```swift
struct PerfSection00342 {
    let title = "Section 00342"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00342","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00342 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00343

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00343.md), and an image reference ![sample](./assets/image-00343.png). The prose also carries inline math like $score_343 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00343
- [x] Reviewed checkpoint for section 00343
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00343 | repeated table row |
| words | 64 | stable measurement text |

$$
render_343 = parse_343 + highlight_343 + preview_343
$$

```swift
struct PerfSection00343 {
    let title = "Section 00343"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00343","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00343 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00344

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00344.md), and an image reference ![sample](./assets/image-00344.png). The prose also carries inline math like $score_344 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00344
- [x] Reviewed checkpoint for section 00344
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00344 | repeated table row |
| words | 64 | stable measurement text |

$$
render_344 = parse_344 + highlight_344 + preview_344
$$

```swift
struct PerfSection00344 {
    let title = "Section 00344"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00344","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00344 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00345

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00345.md), and an image reference ![sample](./assets/image-00345.png). The prose also carries inline math like $score_345 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00345
- [x] Reviewed checkpoint for section 00345
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00345 | repeated table row |
| words | 64 | stable measurement text |

$$
render_345 = parse_345 + highlight_345 + preview_345
$$

```swift
struct PerfSection00345 {
    let title = "Section 00345"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00345","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00345 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00346

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00346.md), and an image reference ![sample](./assets/image-00346.png). The prose also carries inline math like $score_346 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00346
- [x] Reviewed checkpoint for section 00346
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00346 | repeated table row |
| words | 64 | stable measurement text |

$$
render_346 = parse_346 + highlight_346 + preview_346
$$

```swift
struct PerfSection00346 {
    let title = "Section 00346"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00346","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00346 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00347

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00347.md), and an image reference ![sample](./assets/image-00347.png). The prose also carries inline math like $score_347 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00347
- [x] Reviewed checkpoint for section 00347
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00347 | repeated table row |
| words | 64 | stable measurement text |

$$
render_347 = parse_347 + highlight_347 + preview_347
$$

```swift
struct PerfSection00347 {
    let title = "Section 00347"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00347","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00347 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00348

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00348.md), and an image reference ![sample](./assets/image-00348.png). The prose also carries inline math like $score_348 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00348
- [x] Reviewed checkpoint for section 00348
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00348 | repeated table row |
| words | 64 | stable measurement text |

$$
render_348 = parse_348 + highlight_348 + preview_348
$$

```swift
struct PerfSection00348 {
    let title = "Section 00348"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00348","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00348 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00349

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00349.md), and an image reference ![sample](./assets/image-00349.png). The prose also carries inline math like $score_349 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00349
- [x] Reviewed checkpoint for section 00349
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00349 | repeated table row |
| words | 64 | stable measurement text |

$$
render_349 = parse_349 + highlight_349 + preview_349
$$

```swift
struct PerfSection00349 {
    let title = "Section 00349"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00349","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00349 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00350

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00350.md), and an image reference ![sample](./assets/image-00350.png). The prose also carries inline math like $score_350 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00350
- [x] Reviewed checkpoint for section 00350
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00350 | repeated table row |
| words | 64 | stable measurement text |

$$
render_350 = parse_350 + highlight_350 + preview_350
$$

```swift
struct PerfSection00350 {
    let title = "Section 00350"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00350","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00350 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00351

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00351.md), and an image reference ![sample](./assets/image-00351.png). The prose also carries inline math like $score_351 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00351
- [x] Reviewed checkpoint for section 00351
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00351 | repeated table row |
| words | 64 | stable measurement text |

$$
render_351 = parse_351 + highlight_351 + preview_351
$$

```swift
struct PerfSection00351 {
    let title = "Section 00351"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00351","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00351 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00352

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00352.md), and an image reference ![sample](./assets/image-00352.png). The prose also carries inline math like $score_352 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00352
- [x] Reviewed checkpoint for section 00352
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00352 | repeated table row |
| words | 64 | stable measurement text |

$$
render_352 = parse_352 + highlight_352 + preview_352
$$

```swift
struct PerfSection00352 {
    let title = "Section 00352"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00352","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00352 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00353

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00353.md), and an image reference ![sample](./assets/image-00353.png). The prose also carries inline math like $score_353 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00353
- [x] Reviewed checkpoint for section 00353
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00353 | repeated table row |
| words | 64 | stable measurement text |

$$
render_353 = parse_353 + highlight_353 + preview_353
$$

```swift
struct PerfSection00353 {
    let title = "Section 00353"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00353","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00353 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00354

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00354.md), and an image reference ![sample](./assets/image-00354.png). The prose also carries inline math like $score_354 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00354
- [x] Reviewed checkpoint for section 00354
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00354 | repeated table row |
| words | 64 | stable measurement text |

$$
render_354 = parse_354 + highlight_354 + preview_354
$$

```swift
struct PerfSection00354 {
    let title = "Section 00354"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00354","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00354 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00355

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00355.md), and an image reference ![sample](./assets/image-00355.png). The prose also carries inline math like $score_355 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00355
- [x] Reviewed checkpoint for section 00355
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00355 | repeated table row |
| words | 64 | stable measurement text |

$$
render_355 = parse_355 + highlight_355 + preview_355
$$

```swift
struct PerfSection00355 {
    let title = "Section 00355"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00355","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00355 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00356

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00356.md), and an image reference ![sample](./assets/image-00356.png). The prose also carries inline math like $score_356 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00356
- [x] Reviewed checkpoint for section 00356
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00356 | repeated table row |
| words | 64 | stable measurement text |

$$
render_356 = parse_356 + highlight_356 + preview_356
$$

```swift
struct PerfSection00356 {
    let title = "Section 00356"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00356","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00356 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00357

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00357.md), and an image reference ![sample](./assets/image-00357.png). The prose also carries inline math like $score_357 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00357
- [x] Reviewed checkpoint for section 00357
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00357 | repeated table row |
| words | 64 | stable measurement text |

$$
render_357 = parse_357 + highlight_357 + preview_357
$$

```swift
struct PerfSection00357 {
    let title = "Section 00357"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00357","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00357 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00358

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00358.md), and an image reference ![sample](./assets/image-00358.png). The prose also carries inline math like $score_358 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00358
- [x] Reviewed checkpoint for section 00358
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00358 | repeated table row |
| words | 64 | stable measurement text |

$$
render_358 = parse_358 + highlight_358 + preview_358
$$

```swift
struct PerfSection00358 {
    let title = "Section 00358"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00358","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00358 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00359

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00359.md), and an image reference ![sample](./assets/image-00359.png). The prose also carries inline math like $score_359 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00359
- [x] Reviewed checkpoint for section 00359
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00359 | repeated table row |
| words | 64 | stable measurement text |

$$
render_359 = parse_359 + highlight_359 + preview_359
$$

```swift
struct PerfSection00359 {
    let title = "Section 00359"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00359","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00359 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00360

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00360.md), and an image reference ![sample](./assets/image-00360.png). The prose also carries inline math like $score_360 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00360
- [x] Reviewed checkpoint for section 00360
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00360 | repeated table row |
| words | 64 | stable measurement text |

$$
render_360 = parse_360 + highlight_360 + preview_360
$$

```swift
struct PerfSection00360 {
    let title = "Section 00360"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00360","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00360 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00361

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00361.md), and an image reference ![sample](./assets/image-00361.png). The prose also carries inline math like $score_361 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00361
- [x] Reviewed checkpoint for section 00361
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00361 | repeated table row |
| words | 64 | stable measurement text |

$$
render_361 = parse_361 + highlight_361 + preview_361
$$

```swift
struct PerfSection00361 {
    let title = "Section 00361"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00361","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00361 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00362

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00362.md), and an image reference ![sample](./assets/image-00362.png). The prose also carries inline math like $score_362 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00362
- [x] Reviewed checkpoint for section 00362
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00362 | repeated table row |
| words | 64 | stable measurement text |

$$
render_362 = parse_362 + highlight_362 + preview_362
$$

```swift
struct PerfSection00362 {
    let title = "Section 00362"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00362","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00362 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00363

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00363.md), and an image reference ![sample](./assets/image-00363.png). The prose also carries inline math like $score_363 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00363
- [x] Reviewed checkpoint for section 00363
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00363 | repeated table row |
| words | 64 | stable measurement text |

$$
render_363 = parse_363 + highlight_363 + preview_363
$$

```swift
struct PerfSection00363 {
    let title = "Section 00363"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00363","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00363 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00364

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00364.md), and an image reference ![sample](./assets/image-00364.png). The prose also carries inline math like $score_364 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00364
- [x] Reviewed checkpoint for section 00364
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00364 | repeated table row |
| words | 64 | stable measurement text |

$$
render_364 = parse_364 + highlight_364 + preview_364
$$

```swift
struct PerfSection00364 {
    let title = "Section 00364"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00364","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00364 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00365

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00365.md), and an image reference ![sample](./assets/image-00365.png). The prose also carries inline math like $score_365 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00365
- [x] Reviewed checkpoint for section 00365
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00365 | repeated table row |
| words | 64 | stable measurement text |

$$
render_365 = parse_365 + highlight_365 + preview_365
$$

```swift
struct PerfSection00365 {
    let title = "Section 00365"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00365","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00365 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00366

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00366.md), and an image reference ![sample](./assets/image-00366.png). The prose also carries inline math like $score_366 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00366
- [x] Reviewed checkpoint for section 00366
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00366 | repeated table row |
| words | 64 | stable measurement text |

$$
render_366 = parse_366 + highlight_366 + preview_366
$$

```swift
struct PerfSection00366 {
    let title = "Section 00366"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00366","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00366 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00367

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00367.md), and an image reference ![sample](./assets/image-00367.png). The prose also carries inline math like $score_367 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00367
- [x] Reviewed checkpoint for section 00367
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00367 | repeated table row |
| words | 64 | stable measurement text |

$$
render_367 = parse_367 + highlight_367 + preview_367
$$

```swift
struct PerfSection00367 {
    let title = "Section 00367"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00367","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00367 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00368

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00368.md), and an image reference ![sample](./assets/image-00368.png). The prose also carries inline math like $score_368 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00368
- [x] Reviewed checkpoint for section 00368
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00368 | repeated table row |
| words | 64 | stable measurement text |

$$
render_368 = parse_368 + highlight_368 + preview_368
$$

```swift
struct PerfSection00368 {
    let title = "Section 00368"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00368","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00368 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00369

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00369.md), and an image reference ![sample](./assets/image-00369.png). The prose also carries inline math like $score_369 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00369
- [x] Reviewed checkpoint for section 00369
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00369 | repeated table row |
| words | 64 | stable measurement text |

$$
render_369 = parse_369 + highlight_369 + preview_369
$$

```swift
struct PerfSection00369 {
    let title = "Section 00369"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00369","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00369 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00370

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00370.md), and an image reference ![sample](./assets/image-00370.png). The prose also carries inline math like $score_370 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00370
- [x] Reviewed checkpoint for section 00370
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00370 | repeated table row |
| words | 64 | stable measurement text |

$$
render_370 = parse_370 + highlight_370 + preview_370
$$

```swift
struct PerfSection00370 {
    let title = "Section 00370"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00370","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00370 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00371

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00371.md), and an image reference ![sample](./assets/image-00371.png). The prose also carries inline math like $score_371 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00371
- [x] Reviewed checkpoint for section 00371
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00371 | repeated table row |
| words | 64 | stable measurement text |

$$
render_371 = parse_371 + highlight_371 + preview_371
$$

```swift
struct PerfSection00371 {
    let title = "Section 00371"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00371","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00371 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00372

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00372.md), and an image reference ![sample](./assets/image-00372.png). The prose also carries inline math like $score_372 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00372
- [x] Reviewed checkpoint for section 00372
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00372 | repeated table row |
| words | 64 | stable measurement text |

$$
render_372 = parse_372 + highlight_372 + preview_372
$$

```swift
struct PerfSection00372 {
    let title = "Section 00372"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00372","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00372 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00373

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00373.md), and an image reference ![sample](./assets/image-00373.png). The prose also carries inline math like $score_373 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00373
- [x] Reviewed checkpoint for section 00373
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00373 | repeated table row |
| words | 64 | stable measurement text |

$$
render_373 = parse_373 + highlight_373 + preview_373
$$

```swift
struct PerfSection00373 {
    let title = "Section 00373"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00373","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00373 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00374

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00374.md), and an image reference ![sample](./assets/image-00374.png). The prose also carries inline math like $score_374 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00374
- [x] Reviewed checkpoint for section 00374
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00374 | repeated table row |
| words | 64 | stable measurement text |

$$
render_374 = parse_374 + highlight_374 + preview_374
$$

```swift
struct PerfSection00374 {
    let title = "Section 00374"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00374","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00374 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00375

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00375.md), and an image reference ![sample](./assets/image-00375.png). The prose also carries inline math like $score_375 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00375
- [x] Reviewed checkpoint for section 00375
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00375 | repeated table row |
| words | 64 | stable measurement text |

$$
render_375 = parse_375 + highlight_375 + preview_375
$$

```swift
struct PerfSection00375 {
    let title = "Section 00375"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00375","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00375 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00376

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00376.md), and an image reference ![sample](./assets/image-00376.png). The prose also carries inline math like $score_376 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00376
- [x] Reviewed checkpoint for section 00376
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00376 | repeated table row |
| words | 64 | stable measurement text |

$$
render_376 = parse_376 + highlight_376 + preview_376
$$

```swift
struct PerfSection00376 {
    let title = "Section 00376"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00376","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00376 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00377

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00377.md), and an image reference ![sample](./assets/image-00377.png). The prose also carries inline math like $score_377 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00377
- [x] Reviewed checkpoint for section 00377
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00377 | repeated table row |
| words | 64 | stable measurement text |

$$
render_377 = parse_377 + highlight_377 + preview_377
$$

```swift
struct PerfSection00377 {
    let title = "Section 00377"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00377","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00377 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00378

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00378.md), and an image reference ![sample](./assets/image-00378.png). The prose also carries inline math like $score_378 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00378
- [x] Reviewed checkpoint for section 00378
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00378 | repeated table row |
| words | 64 | stable measurement text |

$$
render_378 = parse_378 + highlight_378 + preview_378
$$

```swift
struct PerfSection00378 {
    let title = "Section 00378"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00378","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00378 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00379

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00379.md), and an image reference ![sample](./assets/image-00379.png). The prose also carries inline math like $score_379 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00379
- [x] Reviewed checkpoint for section 00379
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00379 | repeated table row |
| words | 64 | stable measurement text |

$$
render_379 = parse_379 + highlight_379 + preview_379
$$

```swift
struct PerfSection00379 {
    let title = "Section 00379"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00379","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00379 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00380

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00380.md), and an image reference ![sample](./assets/image-00380.png). The prose also carries inline math like $score_380 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00380
- [x] Reviewed checkpoint for section 00380
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00380 | repeated table row |
| words | 64 | stable measurement text |

$$
render_380 = parse_380 + highlight_380 + preview_380
$$

```swift
struct PerfSection00380 {
    let title = "Section 00380"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00380","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00380 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00381

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00381.md), and an image reference ![sample](./assets/image-00381.png). The prose also carries inline math like $score_381 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00381
- [x] Reviewed checkpoint for section 00381
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00381 | repeated table row |
| words | 64 | stable measurement text |

$$
render_381 = parse_381 + highlight_381 + preview_381
$$

```swift
struct PerfSection00381 {
    let title = "Section 00381"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00381","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00381 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00382

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00382.md), and an image reference ![sample](./assets/image-00382.png). The prose also carries inline math like $score_382 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00382
- [x] Reviewed checkpoint for section 00382
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00382 | repeated table row |
| words | 64 | stable measurement text |

$$
render_382 = parse_382 + highlight_382 + preview_382
$$

```swift
struct PerfSection00382 {
    let title = "Section 00382"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00382","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00382 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00383

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00383.md), and an image reference ![sample](./assets/image-00383.png). The prose also carries inline math like $score_383 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00383
- [x] Reviewed checkpoint for section 00383
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00383 | repeated table row |
| words | 64 | stable measurement text |

$$
render_383 = parse_383 + highlight_383 + preview_383
$$

```swift
struct PerfSection00383 {
    let title = "Section 00383"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00383","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00383 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00384

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00384.md), and an image reference ![sample](./assets/image-00384.png). The prose also carries inline math like $score_384 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00384
- [x] Reviewed checkpoint for section 00384
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00384 | repeated table row |
| words | 64 | stable measurement text |

$$
render_384 = parse_384 + highlight_384 + preview_384
$$

```swift
struct PerfSection00384 {
    let title = "Section 00384"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00384","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00384 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00385

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00385.md), and an image reference ![sample](./assets/image-00385.png). The prose also carries inline math like $score_385 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00385
- [x] Reviewed checkpoint for section 00385
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00385 | repeated table row |
| words | 64 | stable measurement text |

$$
render_385 = parse_385 + highlight_385 + preview_385
$$

```swift
struct PerfSection00385 {
    let title = "Section 00385"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00385","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00385 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00386

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00386.md), and an image reference ![sample](./assets/image-00386.png). The prose also carries inline math like $score_386 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00386
- [x] Reviewed checkpoint for section 00386
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00386 | repeated table row |
| words | 64 | stable measurement text |

$$
render_386 = parse_386 + highlight_386 + preview_386
$$

```swift
struct PerfSection00386 {
    let title = "Section 00386"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00386","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00386 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00387

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00387.md), and an image reference ![sample](./assets/image-00387.png). The prose also carries inline math like $score_387 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00387
- [x] Reviewed checkpoint for section 00387
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00387 | repeated table row |
| words | 64 | stable measurement text |

$$
render_387 = parse_387 + highlight_387 + preview_387
$$

```swift
struct PerfSection00387 {
    let title = "Section 00387"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00387","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00387 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00388

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00388.md), and an image reference ![sample](./assets/image-00388.png). The prose also carries inline math like $score_388 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00388
- [x] Reviewed checkpoint for section 00388
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00388 | repeated table row |
| words | 64 | stable measurement text |

$$
render_388 = parse_388 + highlight_388 + preview_388
$$

```swift
struct PerfSection00388 {
    let title = "Section 00388"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00388","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00388 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00389

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00389.md), and an image reference ![sample](./assets/image-00389.png). The prose also carries inline math like $score_389 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00389
- [x] Reviewed checkpoint for section 00389
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00389 | repeated table row |
| words | 64 | stable measurement text |

$$
render_389 = parse_389 + highlight_389 + preview_389
$$

```swift
struct PerfSection00389 {
    let title = "Section 00389"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00389","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00389 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00390

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00390.md), and an image reference ![sample](./assets/image-00390.png). The prose also carries inline math like $score_390 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00390
- [x] Reviewed checkpoint for section 00390
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00390 | repeated table row |
| words | 64 | stable measurement text |

$$
render_390 = parse_390 + highlight_390 + preview_390
$$

```swift
struct PerfSection00390 {
    let title = "Section 00390"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00390","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00390 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00391

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00391.md), and an image reference ![sample](./assets/image-00391.png). The prose also carries inline math like $score_391 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00391
- [x] Reviewed checkpoint for section 00391
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00391 | repeated table row |
| words | 64 | stable measurement text |

$$
render_391 = parse_391 + highlight_391 + preview_391
$$

```swift
struct PerfSection00391 {
    let title = "Section 00391"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00391","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00391 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00392

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00392.md), and an image reference ![sample](./assets/image-00392.png). The prose also carries inline math like $score_392 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00392
- [x] Reviewed checkpoint for section 00392
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00392 | repeated table row |
| words | 64 | stable measurement text |

$$
render_392 = parse_392 + highlight_392 + preview_392
$$

```swift
struct PerfSection00392 {
    let title = "Section 00392"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00392","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00392 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00393

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00393.md), and an image reference ![sample](./assets/image-00393.png). The prose also carries inline math like $score_393 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00393
- [x] Reviewed checkpoint for section 00393
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00393 | repeated table row |
| words | 64 | stable measurement text |

$$
render_393 = parse_393 + highlight_393 + preview_393
$$

```swift
struct PerfSection00393 {
    let title = "Section 00393"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00393","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00393 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00394

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00394.md), and an image reference ![sample](./assets/image-00394.png). The prose also carries inline math like $score_394 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00394
- [x] Reviewed checkpoint for section 00394
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00394 | repeated table row |
| words | 64 | stable measurement text |

$$
render_394 = parse_394 + highlight_394 + preview_394
$$

```swift
struct PerfSection00394 {
    let title = "Section 00394"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00394","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00394 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00395

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00395.md), and an image reference ![sample](./assets/image-00395.png). The prose also carries inline math like $score_395 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00395
- [x] Reviewed checkpoint for section 00395
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00395 | repeated table row |
| words | 64 | stable measurement text |

$$
render_395 = parse_395 + highlight_395 + preview_395
$$

```swift
struct PerfSection00395 {
    let title = "Section 00395"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00395","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00395 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00396

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00396.md), and an image reference ![sample](./assets/image-00396.png). The prose also carries inline math like $score_396 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00396
- [x] Reviewed checkpoint for section 00396
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00396 | repeated table row |
| words | 64 | stable measurement text |

$$
render_396 = parse_396 + highlight_396 + preview_396
$$

```swift
struct PerfSection00396 {
    let title = "Section 00396"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00396","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00396 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00397

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00397.md), and an image reference ![sample](./assets/image-00397.png). The prose also carries inline math like $score_397 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00397
- [x] Reviewed checkpoint for section 00397
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00397 | repeated table row |
| words | 64 | stable measurement text |

$$
render_397 = parse_397 + highlight_397 + preview_397
$$

```swift
struct PerfSection00397 {
    let title = "Section 00397"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00397","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00397 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00398

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00398.md), and an image reference ![sample](./assets/image-00398.png). The prose also carries inline math like $score_398 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00398
- [x] Reviewed checkpoint for section 00398
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00398 | repeated table row |
| words | 64 | stable measurement text |

$$
render_398 = parse_398 + highlight_398 + preview_398
$$

```swift
struct PerfSection00398 {
    let title = "Section 00398"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00398","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00398 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00399

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00399.md), and an image reference ![sample](./assets/image-00399.png). The prose also carries inline math like $score_399 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00399
- [x] Reviewed checkpoint for section 00399
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00399 | repeated table row |
| words | 64 | stable measurement text |

$$
render_399 = parse_399 + highlight_399 + preview_399
$$

```swift
struct PerfSection00399 {
    let title = "Section 00399"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00399","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00399 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00400

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00400.md), and an image reference ![sample](./assets/image-00400.png). The prose also carries inline math like $score_400 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00400
- [x] Reviewed checkpoint for section 00400
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00400 | repeated table row |
| words | 64 | stable measurement text |

$$
render_400 = parse_400 + highlight_400 + preview_400
$$

```swift
struct PerfSection00400 {
    let title = "Section 00400"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00400","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00400 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00401

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00401.md), and an image reference ![sample](./assets/image-00401.png). The prose also carries inline math like $score_401 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00401
- [x] Reviewed checkpoint for section 00401
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00401 | repeated table row |
| words | 64 | stable measurement text |

$$
render_401 = parse_401 + highlight_401 + preview_401
$$

```swift
struct PerfSection00401 {
    let title = "Section 00401"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00401","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00401 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00402

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00402.md), and an image reference ![sample](./assets/image-00402.png). The prose also carries inline math like $score_402 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00402
- [x] Reviewed checkpoint for section 00402
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00402 | repeated table row |
| words | 64 | stable measurement text |

$$
render_402 = parse_402 + highlight_402 + preview_402
$$

```swift
struct PerfSection00402 {
    let title = "Section 00402"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00402","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00402 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00403

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00403.md), and an image reference ![sample](./assets/image-00403.png). The prose also carries inline math like $score_403 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00403
- [x] Reviewed checkpoint for section 00403
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00403 | repeated table row |
| words | 64 | stable measurement text |

$$
render_403 = parse_403 + highlight_403 + preview_403
$$

```swift
struct PerfSection00403 {
    let title = "Section 00403"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00403","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00403 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00404

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00404.md), and an image reference ![sample](./assets/image-00404.png). The prose also carries inline math like $score_404 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00404
- [x] Reviewed checkpoint for section 00404
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00404 | repeated table row |
| words | 64 | stable measurement text |

$$
render_404 = parse_404 + highlight_404 + preview_404
$$

```swift
struct PerfSection00404 {
    let title = "Section 00404"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00404","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00404 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00405

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00405.md), and an image reference ![sample](./assets/image-00405.png). The prose also carries inline math like $score_405 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00405
- [x] Reviewed checkpoint for section 00405
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00405 | repeated table row |
| words | 64 | stable measurement text |

$$
render_405 = parse_405 + highlight_405 + preview_405
$$

```swift
struct PerfSection00405 {
    let title = "Section 00405"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00405","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00405 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00406

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00406.md), and an image reference ![sample](./assets/image-00406.png). The prose also carries inline math like $score_406 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00406
- [x] Reviewed checkpoint for section 00406
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00406 | repeated table row |
| words | 64 | stable measurement text |

$$
render_406 = parse_406 + highlight_406 + preview_406
$$

```swift
struct PerfSection00406 {
    let title = "Section 00406"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00406","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00406 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00407

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00407.md), and an image reference ![sample](./assets/image-00407.png). The prose also carries inline math like $score_407 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00407
- [x] Reviewed checkpoint for section 00407
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00407 | repeated table row |
| words | 64 | stable measurement text |

$$
render_407 = parse_407 + highlight_407 + preview_407
$$

```swift
struct PerfSection00407 {
    let title = "Section 00407"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00407","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00407 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00408

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00408.md), and an image reference ![sample](./assets/image-00408.png). The prose also carries inline math like $score_408 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00408
- [x] Reviewed checkpoint for section 00408
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00408 | repeated table row |
| words | 64 | stable measurement text |

$$
render_408 = parse_408 + highlight_408 + preview_408
$$

```swift
struct PerfSection00408 {
    let title = "Section 00408"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00408","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00408 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00409

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00409.md), and an image reference ![sample](./assets/image-00409.png). The prose also carries inline math like $score_409 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00409
- [x] Reviewed checkpoint for section 00409
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00409 | repeated table row |
| words | 64 | stable measurement text |

$$
render_409 = parse_409 + highlight_409 + preview_409
$$

```swift
struct PerfSection00409 {
    let title = "Section 00409"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00409","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00409 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00410

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00410.md), and an image reference ![sample](./assets/image-00410.png). The prose also carries inline math like $score_410 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00410
- [x] Reviewed checkpoint for section 00410
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00410 | repeated table row |
| words | 64 | stable measurement text |

$$
render_410 = parse_410 + highlight_410 + preview_410
$$

```swift
struct PerfSection00410 {
    let title = "Section 00410"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00410","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00410 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00411

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00411.md), and an image reference ![sample](./assets/image-00411.png). The prose also carries inline math like $score_411 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00411
- [x] Reviewed checkpoint for section 00411
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00411 | repeated table row |
| words | 64 | stable measurement text |

$$
render_411 = parse_411 + highlight_411 + preview_411
$$

```swift
struct PerfSection00411 {
    let title = "Section 00411"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00411","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00411 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00412

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00412.md), and an image reference ![sample](./assets/image-00412.png). The prose also carries inline math like $score_412 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00412
- [x] Reviewed checkpoint for section 00412
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00412 | repeated table row |
| words | 64 | stable measurement text |

$$
render_412 = parse_412 + highlight_412 + preview_412
$$

```swift
struct PerfSection00412 {
    let title = "Section 00412"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00412","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00412 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00413

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00413.md), and an image reference ![sample](./assets/image-00413.png). The prose also carries inline math like $score_413 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00413
- [x] Reviewed checkpoint for section 00413
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00413 | repeated table row |
| words | 64 | stable measurement text |

$$
render_413 = parse_413 + highlight_413 + preview_413
$$

```swift
struct PerfSection00413 {
    let title = "Section 00413"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00413","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00413 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00414

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00414.md), and an image reference ![sample](./assets/image-00414.png). The prose also carries inline math like $score_414 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00414
- [x] Reviewed checkpoint for section 00414
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00414 | repeated table row |
| words | 64 | stable measurement text |

$$
render_414 = parse_414 + highlight_414 + preview_414
$$

```swift
struct PerfSection00414 {
    let title = "Section 00414"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00414","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00414 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00415

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00415.md), and an image reference ![sample](./assets/image-00415.png). The prose also carries inline math like $score_415 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00415
- [x] Reviewed checkpoint for section 00415
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00415 | repeated table row |
| words | 64 | stable measurement text |

$$
render_415 = parse_415 + highlight_415 + preview_415
$$

```swift
struct PerfSection00415 {
    let title = "Section 00415"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00415","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00415 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00416

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00416.md), and an image reference ![sample](./assets/image-00416.png). The prose also carries inline math like $score_416 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00416
- [x] Reviewed checkpoint for section 00416
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00416 | repeated table row |
| words | 64 | stable measurement text |

$$
render_416 = parse_416 + highlight_416 + preview_416
$$

```swift
struct PerfSection00416 {
    let title = "Section 00416"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00416","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00416 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00417

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00417.md), and an image reference ![sample](./assets/image-00417.png). The prose also carries inline math like $score_417 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00417
- [x] Reviewed checkpoint for section 00417
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00417 | repeated table row |
| words | 64 | stable measurement text |

$$
render_417 = parse_417 + highlight_417 + preview_417
$$

```swift
struct PerfSection00417 {
    let title = "Section 00417"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00417","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00417 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00418

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00418.md), and an image reference ![sample](./assets/image-00418.png). The prose also carries inline math like $score_418 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00418
- [x] Reviewed checkpoint for section 00418
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00418 | repeated table row |
| words | 64 | stable measurement text |

$$
render_418 = parse_418 + highlight_418 + preview_418
$$

```swift
struct PerfSection00418 {
    let title = "Section 00418"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00418","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00418 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00419

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00419.md), and an image reference ![sample](./assets/image-00419.png). The prose also carries inline math like $score_419 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00419
- [x] Reviewed checkpoint for section 00419
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00419 | repeated table row |
| words | 64 | stable measurement text |

$$
render_419 = parse_419 + highlight_419 + preview_419
$$

```swift
struct PerfSection00419 {
    let title = "Section 00419"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00419","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00419 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00420

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00420.md), and an image reference ![sample](./assets/image-00420.png). The prose also carries inline math like $score_420 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00420
- [x] Reviewed checkpoint for section 00420
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00420 | repeated table row |
| words | 64 | stable measurement text |

$$
render_420 = parse_420 + highlight_420 + preview_420
$$

```swift
struct PerfSection00420 {
    let title = "Section 00420"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00420","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00420 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00421

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00421.md), and an image reference ![sample](./assets/image-00421.png). The prose also carries inline math like $score_421 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00421
- [x] Reviewed checkpoint for section 00421
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00421 | repeated table row |
| words | 64 | stable measurement text |

$$
render_421 = parse_421 + highlight_421 + preview_421
$$

```swift
struct PerfSection00421 {
    let title = "Section 00421"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00421","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00421 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00422

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00422.md), and an image reference ![sample](./assets/image-00422.png). The prose also carries inline math like $score_422 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00422
- [x] Reviewed checkpoint for section 00422
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00422 | repeated table row |
| words | 64 | stable measurement text |

$$
render_422 = parse_422 + highlight_422 + preview_422
$$

```swift
struct PerfSection00422 {
    let title = "Section 00422"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00422","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00422 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00423

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00423.md), and an image reference ![sample](./assets/image-00423.png). The prose also carries inline math like $score_423 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00423
- [x] Reviewed checkpoint for section 00423
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00423 | repeated table row |
| words | 64 | stable measurement text |

$$
render_423 = parse_423 + highlight_423 + preview_423
$$

```swift
struct PerfSection00423 {
    let title = "Section 00423"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00423","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00423 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00424

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00424.md), and an image reference ![sample](./assets/image-00424.png). The prose also carries inline math like $score_424 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00424
- [x] Reviewed checkpoint for section 00424
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00424 | repeated table row |
| words | 64 | stable measurement text |

$$
render_424 = parse_424 + highlight_424 + preview_424
$$

```swift
struct PerfSection00424 {
    let title = "Section 00424"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00424","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00424 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00425

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00425.md), and an image reference ![sample](./assets/image-00425.png). The prose also carries inline math like $score_425 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00425
- [x] Reviewed checkpoint for section 00425
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00425 | repeated table row |
| words | 64 | stable measurement text |

$$
render_425 = parse_425 + highlight_425 + preview_425
$$

```swift
struct PerfSection00425 {
    let title = "Section 00425"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00425","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00425 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00426

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00426.md), and an image reference ![sample](./assets/image-00426.png). The prose also carries inline math like $score_426 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00426
- [x] Reviewed checkpoint for section 00426
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00426 | repeated table row |
| words | 64 | stable measurement text |

$$
render_426 = parse_426 + highlight_426 + preview_426
$$

```swift
struct PerfSection00426 {
    let title = "Section 00426"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00426","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00426 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00427

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00427.md), and an image reference ![sample](./assets/image-00427.png). The prose also carries inline math like $score_427 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00427
- [x] Reviewed checkpoint for section 00427
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00427 | repeated table row |
| words | 64 | stable measurement text |

$$
render_427 = parse_427 + highlight_427 + preview_427
$$

```swift
struct PerfSection00427 {
    let title = "Section 00427"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00427","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00427 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00428

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00428.md), and an image reference ![sample](./assets/image-00428.png). The prose also carries inline math like $score_428 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00428
- [x] Reviewed checkpoint for section 00428
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00428 | repeated table row |
| words | 64 | stable measurement text |

$$
render_428 = parse_428 + highlight_428 + preview_428
$$

```swift
struct PerfSection00428 {
    let title = "Section 00428"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00428","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00428 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00429

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00429.md), and an image reference ![sample](./assets/image-00429.png). The prose also carries inline math like $score_429 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00429
- [x] Reviewed checkpoint for section 00429
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00429 | repeated table row |
| words | 64 | stable measurement text |

$$
render_429 = parse_429 + highlight_429 + preview_429
$$

```swift
struct PerfSection00429 {
    let title = "Section 00429"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00429","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00429 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00430

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00430.md), and an image reference ![sample](./assets/image-00430.png). The prose also carries inline math like $score_430 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00430
- [x] Reviewed checkpoint for section 00430
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00430 | repeated table row |
| words | 64 | stable measurement text |

$$
render_430 = parse_430 + highlight_430 + preview_430
$$

```swift
struct PerfSection00430 {
    let title = "Section 00430"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00430","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00430 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00431

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00431.md), and an image reference ![sample](./assets/image-00431.png). The prose also carries inline math like $score_431 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00431
- [x] Reviewed checkpoint for section 00431
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00431 | repeated table row |
| words | 64 | stable measurement text |

$$
render_431 = parse_431 + highlight_431 + preview_431
$$

```swift
struct PerfSection00431 {
    let title = "Section 00431"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00431","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00431 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00432

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00432.md), and an image reference ![sample](./assets/image-00432.png). The prose also carries inline math like $score_432 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00432
- [x] Reviewed checkpoint for section 00432
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00432 | repeated table row |
| words | 64 | stable measurement text |

$$
render_432 = parse_432 + highlight_432 + preview_432
$$

```swift
struct PerfSection00432 {
    let title = "Section 00432"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00432","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00432 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00433

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00433.md), and an image reference ![sample](./assets/image-00433.png). The prose also carries inline math like $score_433 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00433
- [x] Reviewed checkpoint for section 00433
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00433 | repeated table row |
| words | 64 | stable measurement text |

$$
render_433 = parse_433 + highlight_433 + preview_433
$$

```swift
struct PerfSection00433 {
    let title = "Section 00433"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00433","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00433 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00434

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00434.md), and an image reference ![sample](./assets/image-00434.png). The prose also carries inline math like $score_434 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00434
- [x] Reviewed checkpoint for section 00434
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00434 | repeated table row |
| words | 64 | stable measurement text |

$$
render_434 = parse_434 + highlight_434 + preview_434
$$

```swift
struct PerfSection00434 {
    let title = "Section 00434"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00434","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00434 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00435

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00435.md), and an image reference ![sample](./assets/image-00435.png). The prose also carries inline math like $score_435 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00435
- [x] Reviewed checkpoint for section 00435
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00435 | repeated table row |
| words | 64 | stable measurement text |

$$
render_435 = parse_435 + highlight_435 + preview_435
$$

```swift
struct PerfSection00435 {
    let title = "Section 00435"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00435","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00435 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00436

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00436.md), and an image reference ![sample](./assets/image-00436.png). The prose also carries inline math like $score_436 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00436
- [x] Reviewed checkpoint for section 00436
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00436 | repeated table row |
| words | 64 | stable measurement text |

$$
render_436 = parse_436 + highlight_436 + preview_436
$$

```swift
struct PerfSection00436 {
    let title = "Section 00436"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00436","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00436 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00437

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00437.md), and an image reference ![sample](./assets/image-00437.png). The prose also carries inline math like $score_437 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00437
- [x] Reviewed checkpoint for section 00437
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00437 | repeated table row |
| words | 64 | stable measurement text |

$$
render_437 = parse_437 + highlight_437 + preview_437
$$

```swift
struct PerfSection00437 {
    let title = "Section 00437"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00437","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00437 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00438

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00438.md), and an image reference ![sample](./assets/image-00438.png). The prose also carries inline math like $score_438 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00438
- [x] Reviewed checkpoint for section 00438
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00438 | repeated table row |
| words | 64 | stable measurement text |

$$
render_438 = parse_438 + highlight_438 + preview_438
$$

```swift
struct PerfSection00438 {
    let title = "Section 00438"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00438","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00438 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00439

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00439.md), and an image reference ![sample](./assets/image-00439.png). The prose also carries inline math like $score_439 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00439
- [x] Reviewed checkpoint for section 00439
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00439 | repeated table row |
| words | 64 | stable measurement text |

$$
render_439 = parse_439 + highlight_439 + preview_439
$$

```swift
struct PerfSection00439 {
    let title = "Section 00439"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00439","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00439 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00440

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00440.md), and an image reference ![sample](./assets/image-00440.png). The prose also carries inline math like $score_440 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00440
- [x] Reviewed checkpoint for section 00440
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00440 | repeated table row |
| words | 64 | stable measurement text |

$$
render_440 = parse_440 + highlight_440 + preview_440
$$

```swift
struct PerfSection00440 {
    let title = "Section 00440"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00440","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00440 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00441

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00441.md), and an image reference ![sample](./assets/image-00441.png). The prose also carries inline math like $score_441 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00441
- [x] Reviewed checkpoint for section 00441
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00441 | repeated table row |
| words | 64 | stable measurement text |

$$
render_441 = parse_441 + highlight_441 + preview_441
$$

```swift
struct PerfSection00441 {
    let title = "Section 00441"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00441","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00441 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00442

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00442.md), and an image reference ![sample](./assets/image-00442.png). The prose also carries inline math like $score_442 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00442
- [x] Reviewed checkpoint for section 00442
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00442 | repeated table row |
| words | 64 | stable measurement text |

$$
render_442 = parse_442 + highlight_442 + preview_442
$$

```swift
struct PerfSection00442 {
    let title = "Section 00442"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00442","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00442 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00443

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00443.md), and an image reference ![sample](./assets/image-00443.png). The prose also carries inline math like $score_443 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00443
- [x] Reviewed checkpoint for section 00443
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00443 | repeated table row |
| words | 64 | stable measurement text |

$$
render_443 = parse_443 + highlight_443 + preview_443
$$

```swift
struct PerfSection00443 {
    let title = "Section 00443"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00443","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00443 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00444

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00444.md), and an image reference ![sample](./assets/image-00444.png). The prose also carries inline math like $score_444 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00444
- [x] Reviewed checkpoint for section 00444
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00444 | repeated table row |
| words | 64 | stable measurement text |

$$
render_444 = parse_444 + highlight_444 + preview_444
$$

```swift
struct PerfSection00444 {
    let title = "Section 00444"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00444","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00444 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00445

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00445.md), and an image reference ![sample](./assets/image-00445.png). The prose also carries inline math like $score_445 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00445
- [x] Reviewed checkpoint for section 00445
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00445 | repeated table row |
| words | 64 | stable measurement text |

$$
render_445 = parse_445 + highlight_445 + preview_445
$$

```swift
struct PerfSection00445 {
    let title = "Section 00445"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00445","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00445 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00446

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00446.md), and an image reference ![sample](./assets/image-00446.png). The prose also carries inline math like $score_446 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00446
- [x] Reviewed checkpoint for section 00446
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00446 | repeated table row |
| words | 64 | stable measurement text |

$$
render_446 = parse_446 + highlight_446 + preview_446
$$

```swift
struct PerfSection00446 {
    let title = "Section 00446"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00446","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00446 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00447

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00447.md), and an image reference ![sample](./assets/image-00447.png). The prose also carries inline math like $score_447 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00447
- [x] Reviewed checkpoint for section 00447
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00447 | repeated table row |
| words | 64 | stable measurement text |

$$
render_447 = parse_447 + highlight_447 + preview_447
$$

```swift
struct PerfSection00447 {
    let title = "Section 00447"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00447","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00447 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00448

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00448.md), and an image reference ![sample](./assets/image-00448.png). The prose also carries inline math like $score_448 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00448
- [x] Reviewed checkpoint for section 00448
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00448 | repeated table row |
| words | 64 | stable measurement text |

$$
render_448 = parse_448 + highlight_448 + preview_448
$$

```swift
struct PerfSection00448 {
    let title = "Section 00448"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00448","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00448 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00449

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00449.md), and an image reference ![sample](./assets/image-00449.png). The prose also carries inline math like $score_449 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00449
- [x] Reviewed checkpoint for section 00449
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00449 | repeated table row |
| words | 64 | stable measurement text |

$$
render_449 = parse_449 + highlight_449 + preview_449
$$

```swift
struct PerfSection00449 {
    let title = "Section 00449"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00449","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00449 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00450

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00450.md), and an image reference ![sample](./assets/image-00450.png). The prose also carries inline math like $score_450 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00450
- [x] Reviewed checkpoint for section 00450
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00450 | repeated table row |
| words | 64 | stable measurement text |

$$
render_450 = parse_450 + highlight_450 + preview_450
$$

```swift
struct PerfSection00450 {
    let title = "Section 00450"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00450","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00450 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00451

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00451.md), and an image reference ![sample](./assets/image-00451.png). The prose also carries inline math like $score_451 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00451
- [x] Reviewed checkpoint for section 00451
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00451 | repeated table row |
| words | 64 | stable measurement text |

$$
render_451 = parse_451 + highlight_451 + preview_451
$$

```swift
struct PerfSection00451 {
    let title = "Section 00451"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00451","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00451 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00452

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00452.md), and an image reference ![sample](./assets/image-00452.png). The prose also carries inline math like $score_452 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00452
- [x] Reviewed checkpoint for section 00452
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00452 | repeated table row |
| words | 64 | stable measurement text |

$$
render_452 = parse_452 + highlight_452 + preview_452
$$

```swift
struct PerfSection00452 {
    let title = "Section 00452"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00452","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00452 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00453

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00453.md), and an image reference ![sample](./assets/image-00453.png). The prose also carries inline math like $score_453 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00453
- [x] Reviewed checkpoint for section 00453
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00453 | repeated table row |
| words | 64 | stable measurement text |

$$
render_453 = parse_453 + highlight_453 + preview_453
$$

```swift
struct PerfSection00453 {
    let title = "Section 00453"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00453","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00453 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00454

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00454.md), and an image reference ![sample](./assets/image-00454.png). The prose also carries inline math like $score_454 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00454
- [x] Reviewed checkpoint for section 00454
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00454 | repeated table row |
| words | 64 | stable measurement text |

$$
render_454 = parse_454 + highlight_454 + preview_454
$$

```swift
struct PerfSection00454 {
    let title = "Section 00454"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00454","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00454 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00455

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00455.md), and an image reference ![sample](./assets/image-00455.png). The prose also carries inline math like $score_455 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00455
- [x] Reviewed checkpoint for section 00455
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00455 | repeated table row |
| words | 64 | stable measurement text |

$$
render_455 = parse_455 + highlight_455 + preview_455
$$

```swift
struct PerfSection00455 {
    let title = "Section 00455"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00455","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00455 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00456

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00456.md), and an image reference ![sample](./assets/image-00456.png). The prose also carries inline math like $score_456 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00456
- [x] Reviewed checkpoint for section 00456
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00456 | repeated table row |
| words | 64 | stable measurement text |

$$
render_456 = parse_456 + highlight_456 + preview_456
$$

```swift
struct PerfSection00456 {
    let title = "Section 00456"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00456","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00456 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00457

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00457.md), and an image reference ![sample](./assets/image-00457.png). The prose also carries inline math like $score_457 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00457
- [x] Reviewed checkpoint for section 00457
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00457 | repeated table row |
| words | 64 | stable measurement text |

$$
render_457 = parse_457 + highlight_457 + preview_457
$$

```swift
struct PerfSection00457 {
    let title = "Section 00457"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00457","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00457 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00458

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00458.md), and an image reference ![sample](./assets/image-00458.png). The prose also carries inline math like $score_458 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00458
- [x] Reviewed checkpoint for section 00458
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00458 | repeated table row |
| words | 64 | stable measurement text |

$$
render_458 = parse_458 + highlight_458 + preview_458
$$

```swift
struct PerfSection00458 {
    let title = "Section 00458"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00458","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00458 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00459

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00459.md), and an image reference ![sample](./assets/image-00459.png). The prose also carries inline math like $score_459 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00459
- [x] Reviewed checkpoint for section 00459
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00459 | repeated table row |
| words | 64 | stable measurement text |

$$
render_459 = parse_459 + highlight_459 + preview_459
$$

```swift
struct PerfSection00459 {
    let title = "Section 00459"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00459","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00459 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00460

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00460.md), and an image reference ![sample](./assets/image-00460.png). The prose also carries inline math like $score_460 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00460
- [x] Reviewed checkpoint for section 00460
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00460 | repeated table row |
| words | 64 | stable measurement text |

$$
render_460 = parse_460 + highlight_460 + preview_460
$$

```swift
struct PerfSection00460 {
    let title = "Section 00460"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00460","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00460 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00461

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00461.md), and an image reference ![sample](./assets/image-00461.png). The prose also carries inline math like $score_461 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00461
- [x] Reviewed checkpoint for section 00461
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00461 | repeated table row |
| words | 64 | stable measurement text |

$$
render_461 = parse_461 + highlight_461 + preview_461
$$

```swift
struct PerfSection00461 {
    let title = "Section 00461"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00461","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00461 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00462

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00462.md), and an image reference ![sample](./assets/image-00462.png). The prose also carries inline math like $score_462 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00462
- [x] Reviewed checkpoint for section 00462
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00462 | repeated table row |
| words | 64 | stable measurement text |

$$
render_462 = parse_462 + highlight_462 + preview_462
$$

```swift
struct PerfSection00462 {
    let title = "Section 00462"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00462","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00462 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00463

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00463.md), and an image reference ![sample](./assets/image-00463.png). The prose also carries inline math like $score_463 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00463
- [x] Reviewed checkpoint for section 00463
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00463 | repeated table row |
| words | 64 | stable measurement text |

$$
render_463 = parse_463 + highlight_463 + preview_463
$$

```swift
struct PerfSection00463 {
    let title = "Section 00463"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00463","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00463 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00464

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00464.md), and an image reference ![sample](./assets/image-00464.png). The prose also carries inline math like $score_464 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00464
- [x] Reviewed checkpoint for section 00464
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00464 | repeated table row |
| words | 64 | stable measurement text |

$$
render_464 = parse_464 + highlight_464 + preview_464
$$

```swift
struct PerfSection00464 {
    let title = "Section 00464"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00464","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00464 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00465

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00465.md), and an image reference ![sample](./assets/image-00465.png). The prose also carries inline math like $score_465 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00465
- [x] Reviewed checkpoint for section 00465
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00465 | repeated table row |
| words | 64 | stable measurement text |

$$
render_465 = parse_465 + highlight_465 + preview_465
$$

```swift
struct PerfSection00465 {
    let title = "Section 00465"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00465","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00465 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00466

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00466.md), and an image reference ![sample](./assets/image-00466.png). The prose also carries inline math like $score_466 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00466
- [x] Reviewed checkpoint for section 00466
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00466 | repeated table row |
| words | 64 | stable measurement text |

$$
render_466 = parse_466 + highlight_466 + preview_466
$$

```swift
struct PerfSection00466 {
    let title = "Section 00466"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00466","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00466 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00467

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00467.md), and an image reference ![sample](./assets/image-00467.png). The prose also carries inline math like $score_467 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00467
- [x] Reviewed checkpoint for section 00467
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00467 | repeated table row |
| words | 64 | stable measurement text |

$$
render_467 = parse_467 + highlight_467 + preview_467
$$

```swift
struct PerfSection00467 {
    let title = "Section 00467"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00467","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00467 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00468

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00468.md), and an image reference ![sample](./assets/image-00468.png). The prose also carries inline math like $score_468 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00468
- [x] Reviewed checkpoint for section 00468
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00468 | repeated table row |
| words | 64 | stable measurement text |

$$
render_468 = parse_468 + highlight_468 + preview_468
$$

```swift
struct PerfSection00468 {
    let title = "Section 00468"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00468","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00468 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00469

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00469.md), and an image reference ![sample](./assets/image-00469.png). The prose also carries inline math like $score_469 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00469
- [x] Reviewed checkpoint for section 00469
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00469 | repeated table row |
| words | 64 | stable measurement text |

$$
render_469 = parse_469 + highlight_469 + preview_469
$$

```swift
struct PerfSection00469 {
    let title = "Section 00469"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00469","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00469 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00470

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00470.md), and an image reference ![sample](./assets/image-00470.png). The prose also carries inline math like $score_470 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00470
- [x] Reviewed checkpoint for section 00470
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00470 | repeated table row |
| words | 64 | stable measurement text |

$$
render_470 = parse_470 + highlight_470 + preview_470
$$

```swift
struct PerfSection00470 {
    let title = "Section 00470"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00470","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00470 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00471

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00471.md), and an image reference ![sample](./assets/image-00471.png). The prose also carries inline math like $score_471 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00471
- [x] Reviewed checkpoint for section 00471
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00471 | repeated table row |
| words | 64 | stable measurement text |

$$
render_471 = parse_471 + highlight_471 + preview_471
$$

```swift
struct PerfSection00471 {
    let title = "Section 00471"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00471","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00471 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00472

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00472.md), and an image reference ![sample](./assets/image-00472.png). The prose also carries inline math like $score_472 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00472
- [x] Reviewed checkpoint for section 00472
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00472 | repeated table row |
| words | 64 | stable measurement text |

$$
render_472 = parse_472 + highlight_472 + preview_472
$$

```swift
struct PerfSection00472 {
    let title = "Section 00472"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00472","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00472 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00473

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00473.md), and an image reference ![sample](./assets/image-00473.png). The prose also carries inline math like $score_473 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00473
- [x] Reviewed checkpoint for section 00473
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00473 | repeated table row |
| words | 64 | stable measurement text |

$$
render_473 = parse_473 + highlight_473 + preview_473
$$

```swift
struct PerfSection00473 {
    let title = "Section 00473"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00473","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00473 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00474

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00474.md), and an image reference ![sample](./assets/image-00474.png). The prose also carries inline math like $score_474 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00474
- [x] Reviewed checkpoint for section 00474
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00474 | repeated table row |
| words | 64 | stable measurement text |

$$
render_474 = parse_474 + highlight_474 + preview_474
$$

```swift
struct PerfSection00474 {
    let title = "Section 00474"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00474","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00474 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00475

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00475.md), and an image reference ![sample](./assets/image-00475.png). The prose also carries inline math like $score_475 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00475
- [x] Reviewed checkpoint for section 00475
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00475 | repeated table row |
| words | 64 | stable measurement text |

$$
render_475 = parse_475 + highlight_475 + preview_475
$$

```swift
struct PerfSection00475 {
    let title = "Section 00475"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00475","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00475 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00476

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00476.md), and an image reference ![sample](./assets/image-00476.png). The prose also carries inline math like $score_476 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00476
- [x] Reviewed checkpoint for section 00476
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00476 | repeated table row |
| words | 64 | stable measurement text |

$$
render_476 = parse_476 + highlight_476 + preview_476
$$

```swift
struct PerfSection00476 {
    let title = "Section 00476"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00476","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00476 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00477

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00477.md), and an image reference ![sample](./assets/image-00477.png). The prose also carries inline math like $score_477 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00477
- [x] Reviewed checkpoint for section 00477
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00477 | repeated table row |
| words | 64 | stable measurement text |

$$
render_477 = parse_477 + highlight_477 + preview_477
$$

```swift
struct PerfSection00477 {
    let title = "Section 00477"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00477","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00477 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00478

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00478.md), and an image reference ![sample](./assets/image-00478.png). The prose also carries inline math like $score_478 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00478
- [x] Reviewed checkpoint for section 00478
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00478 | repeated table row |
| words | 64 | stable measurement text |

$$
render_478 = parse_478 + highlight_478 + preview_478
$$

```swift
struct PerfSection00478 {
    let title = "Section 00478"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00478","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00478 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00479

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00479.md), and an image reference ![sample](./assets/image-00479.png). The prose also carries inline math like $score_479 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00479
- [x] Reviewed checkpoint for section 00479
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00479 | repeated table row |
| words | 64 | stable measurement text |

$$
render_479 = parse_479 + highlight_479 + preview_479
$$

```swift
struct PerfSection00479 {
    let title = "Section 00479"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00479","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00479 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00480

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00480.md), and an image reference ![sample](./assets/image-00480.png). The prose also carries inline math like $score_480 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00480
- [x] Reviewed checkpoint for section 00480
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00480 | repeated table row |
| words | 64 | stable measurement text |

$$
render_480 = parse_480 + highlight_480 + preview_480
$$

```swift
struct PerfSection00480 {
    let title = "Section 00480"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00480","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00480 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00481

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00481.md), and an image reference ![sample](./assets/image-00481.png). The prose also carries inline math like $score_481 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00481
- [x] Reviewed checkpoint for section 00481
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00481 | repeated table row |
| words | 64 | stable measurement text |

$$
render_481 = parse_481 + highlight_481 + preview_481
$$

```swift
struct PerfSection00481 {
    let title = "Section 00481"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00481","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00481 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00482

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00482.md), and an image reference ![sample](./assets/image-00482.png). The prose also carries inline math like $score_482 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00482
- [x] Reviewed checkpoint for section 00482
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00482 | repeated table row |
| words | 64 | stable measurement text |

$$
render_482 = parse_482 + highlight_482 + preview_482
$$

```swift
struct PerfSection00482 {
    let title = "Section 00482"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00482","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00482 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00483

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00483.md), and an image reference ![sample](./assets/image-00483.png). The prose also carries inline math like $score_483 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00483
- [x] Reviewed checkpoint for section 00483
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00483 | repeated table row |
| words | 64 | stable measurement text |

$$
render_483 = parse_483 + highlight_483 + preview_483
$$

```swift
struct PerfSection00483 {
    let title = "Section 00483"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00483","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00483 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00484

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00484.md), and an image reference ![sample](./assets/image-00484.png). The prose also carries inline math like $score_484 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00484
- [x] Reviewed checkpoint for section 00484
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00484 | repeated table row |
| words | 64 | stable measurement text |

$$
render_484 = parse_484 + highlight_484 + preview_484
$$

```swift
struct PerfSection00484 {
    let title = "Section 00484"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00484","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00484 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00485

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00485.md), and an image reference ![sample](./assets/image-00485.png). The prose also carries inline math like $score_485 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00485
- [x] Reviewed checkpoint for section 00485
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00485 | repeated table row |
| words | 64 | stable measurement text |

$$
render_485 = parse_485 + highlight_485 + preview_485
$$

```swift
struct PerfSection00485 {
    let title = "Section 00485"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00485","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00485 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00486

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00486.md), and an image reference ![sample](./assets/image-00486.png). The prose also carries inline math like $score_486 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00486
- [x] Reviewed checkpoint for section 00486
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00486 | repeated table row |
| words | 64 | stable measurement text |

$$
render_486 = parse_486 + highlight_486 + preview_486
$$

```swift
struct PerfSection00486 {
    let title = "Section 00486"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00486","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00486 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00487

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00487.md), and an image reference ![sample](./assets/image-00487.png). The prose also carries inline math like $score_487 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00487
- [x] Reviewed checkpoint for section 00487
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00487 | repeated table row |
| words | 64 | stable measurement text |

$$
render_487 = parse_487 + highlight_487 + preview_487
$$

```swift
struct PerfSection00487 {
    let title = "Section 00487"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00487","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00487 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00488

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00488.md), and an image reference ![sample](./assets/image-00488.png). The prose also carries inline math like $score_488 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00488
- [x] Reviewed checkpoint for section 00488
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00488 | repeated table row |
| words | 64 | stable measurement text |

$$
render_488 = parse_488 + highlight_488 + preview_488
$$

```swift
struct PerfSection00488 {
    let title = "Section 00488"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00488","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00488 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00489

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00489.md), and an image reference ![sample](./assets/image-00489.png). The prose also carries inline math like $score_489 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00489
- [x] Reviewed checkpoint for section 00489
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00489 | repeated table row |
| words | 64 | stable measurement text |

$$
render_489 = parse_489 + highlight_489 + preview_489
$$

```swift
struct PerfSection00489 {
    let title = "Section 00489"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00489","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00489 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00490

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00490.md), and an image reference ![sample](./assets/image-00490.png). The prose also carries inline math like $score_490 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00490
- [x] Reviewed checkpoint for section 00490
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00490 | repeated table row |
| words | 64 | stable measurement text |

$$
render_490 = parse_490 + highlight_490 + preview_490
$$

```swift
struct PerfSection00490 {
    let title = "Section 00490"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00490","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00490 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00491

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00491.md), and an image reference ![sample](./assets/image-00491.png). The prose also carries inline math like $score_491 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00491
- [x] Reviewed checkpoint for section 00491
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00491 | repeated table row |
| words | 64 | stable measurement text |

$$
render_491 = parse_491 + highlight_491 + preview_491
$$

```swift
struct PerfSection00491 {
    let title = "Section 00491"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00491","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00491 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00492

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00492.md), and an image reference ![sample](./assets/image-00492.png). The prose also carries inline math like $score_492 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00492
- [x] Reviewed checkpoint for section 00492
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00492 | repeated table row |
| words | 64 | stable measurement text |

$$
render_492 = parse_492 + highlight_492 + preview_492
$$

```swift
struct PerfSection00492 {
    let title = "Section 00492"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00492","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00492 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00493

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00493.md), and an image reference ![sample](./assets/image-00493.png). The prose also carries inline math like $score_493 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00493
- [x] Reviewed checkpoint for section 00493
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00493 | repeated table row |
| words | 64 | stable measurement text |

$$
render_493 = parse_493 + highlight_493 + preview_493
$$

```swift
struct PerfSection00493 {
    let title = "Section 00493"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00493","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00493 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00494

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00494.md), and an image reference ![sample](./assets/image-00494.png). The prose also carries inline math like $score_494 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00494
- [x] Reviewed checkpoint for section 00494
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00494 | repeated table row |
| words | 64 | stable measurement text |

$$
render_494 = parse_494 + highlight_494 + preview_494
$$

```swift
struct PerfSection00494 {
    let title = "Section 00494"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00494","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00494 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00495

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00495.md), and an image reference ![sample](./assets/image-00495.png). The prose also carries inline math like $score_495 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00495
- [x] Reviewed checkpoint for section 00495
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00495 | repeated table row |
| words | 64 | stable measurement text |

$$
render_495 = parse_495 + highlight_495 + preview_495
$$

```swift
struct PerfSection00495 {
    let title = "Section 00495"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00495","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00495 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00496

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00496.md), and an image reference ![sample](./assets/image-00496.png). The prose also carries inline math like $score_496 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00496
- [x] Reviewed checkpoint for section 00496
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00496 | repeated table row |
| words | 64 | stable measurement text |

$$
render_496 = parse_496 + highlight_496 + preview_496
$$

```swift
struct PerfSection00496 {
    let title = "Section 00496"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00496","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00496 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00497

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00497.md), and an image reference ![sample](./assets/image-00497.png). The prose also carries inline math like $score_497 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00497
- [x] Reviewed checkpoint for section 00497
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00497 | repeated table row |
| words | 64 | stable measurement text |

$$
render_497 = parse_497 + highlight_497 + preview_497
$$

```swift
struct PerfSection00497 {
    let title = "Section 00497"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00497","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00497 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00498

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00498.md), and an image reference ![sample](./assets/image-00498.png). The prose also carries inline math like $score_498 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00498
- [x] Reviewed checkpoint for section 00498
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00498 | repeated table row |
| words | 64 | stable measurement text |

$$
render_498 = parse_498 + highlight_498 + preview_498
$$

```swift
struct PerfSection00498 {
    let title = "Section 00498"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00498","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00498 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00499

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00499.md), and an image reference ![sample](./assets/image-00499.png). The prose also carries inline math like $score_499 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00499
- [x] Reviewed checkpoint for section 00499
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00499 | repeated table row |
| words | 64 | stable measurement text |

$$
render_499 = parse_499 + highlight_499 + preview_499
$$

```swift
struct PerfSection00499 {
    let title = "Section 00499"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00499","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00499 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00500

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00500.md), and an image reference ![sample](./assets/image-00500.png). The prose also carries inline math like $score_500 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00500
- [x] Reviewed checkpoint for section 00500
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00500 | repeated table row |
| words | 64 | stable measurement text |

$$
render_500 = parse_500 + highlight_500 + preview_500
$$

```swift
struct PerfSection00500 {
    let title = "Section 00500"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00500","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00500 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

## Section 00501

This deterministic paragraph gives the editor ordinary prose to wrap, scan, and highlight. It includes **bold text**, *italic text*, `inline code`, [a relative link](./section-00501.md), and an image reference ![sample](./assets/image-00501.png). The prose also carries inline math like $score_501 = baseline + delta$ so preview math paths see realistic text.

- [ ] Draft checkpoint for section 00501
- [x] Reviewed checkpoint for section 00501
- Nested values: alpha, beta, gamma, delta, epsilon

| Column | Value | Notes |
|---|---:|---|
| section | 00501 | repeated table row |
| words | 64 | stable measurement text |

$$
render_501 = parse_501 + highlight_501 + preview_501
$$

```swift
struct PerfSection00501 {
    let title = "Section 00501"
    let tags = ["performance", "markdown", "plainsong"]
}
```

```json
{"section":"00501","fixture":"perf-500kb","stable":true}
```

> Quoted line for section 00501 with enough text to exercise source styling, preview anchors, and paragraph wrapping.

